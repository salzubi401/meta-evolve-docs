{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>Local author path through M10 is shipped</p> <p>Deterministic first run Typed failures Durable local runs User-owned SDKs</p> <p>Start with one deterministic checklist run. It is the maintained entrance for first-time users and contributors.</p> <ul> <li> <p> Learn the loop</p> <p>Four inputs, then one expansion to <code>Experiment</code>. Search, inspection, source, experience, and persistence follow as standalone chapters.</p> <p> Open the learning path</p> </li> <li> <p> Find an example</p> <p>Route by the result you want, not the milestone that introduced it. Every listed command runs from the repository root.</p> <p> Examples by outcome</p> </li> <li> <p> Use the shipped API</p> <p>Task-oriented guides plus the generated reference. The programming model owns the public contract.</p> <p> Guides \u00b7 Reference</p> </li> <li> <p> Hold the invariants</p> <p>Identity, evidence, conformance, authority, failures, budgets, and domains. Architecture owns the domain rules.</p> <p> Concepts \u00b7 Architecture</p> </li> <li> <p> Read the research track</p> <p>Comparison, autoresearch, bounded live agents, and the SealQA labs. Live notebooks stay marked unexecuted until an authorized capture exists.</p> <p> Research</p> </li> <li> <p> See what is next</p> <p>Future search, meta-evolution, scientific and network proposals stay visibly separated from the shipped local-author path.</p> <p> Design \u00b7 Milestones</p> </li> </ul> <p>The Start here learning path begins with four inputs, then expands once to <code>Experiment</code> before search, inspection, source, experience, and persistence. Use the examples index to route by desired outcome.</p> <p>The executed notebooks in this site are the canonical files maintained beside their runnable examples. Their saved outputs are published as recorded evidence; documentation builds do not execute them again. Separately, the SealQA research notebooks are live-only and remain explicitly marked unexecuted until an authorized capture exists. The autoresearch example is a separate deterministic, executed guide to nested improvement with private selection and held-out checks.</p>"},{"location":"#meta-evolve","title":"Meta-Evolve","text":"<p>Meta-Evolve is a declarative Python library for programmable search: propose a successor, evaluate it independently, and keep the best result the budget allows.</p> <p>Start here Browse examples Shipped API</p>"},{"location":"#read-offline","title":"Read offline","text":"<p>From a checkout, build and serve the complete site:</p> <pre><code>uv sync --locked --group docs\nuv run --group docs mkdocs build --strict\nuv run python -m http.server --directory site\n</code></pre> <p>Open http://127.0.0.1:8000. Repository collaborators can also download the <code>meta-evolve-docs-&lt;commit&gt;</code> artifact from a successful <code>Docs</code> workflow run on <code>main</code>. The artifact is private and retained for 14 days.</p>"},{"location":"#measured-local-author-design-target","title":"Measured local-author design target","text":"<p>The M10.0 design scorecard measures a runnable shipped full-protocol search at 15 scaffolding lines and the statically counted proposed functional syntax at 1: a 93% reduction. The historical labels and counts remain unchanged; the M10.3 after-side now executes in deterministic unnumbered local fixtures. This measures authoring friction, not optimization quality or runtime performance.</p>"},{"location":"adding-an-artifact-domain/","title":"Adding an artifact domain","text":"<p>Status: Shipped.</p> <p><code>meta.run</code> optimizes whatever you can encode. A domain is one <code>(kind, representation)</code> pair plus the codec that translates between the Python value your proposer and evaluator handle and the durable payload the kernel commits. Adding one takes four small pieces and no change to the library: the coordinator has no branch per domain, which <code>tests/test_four_domain_kernel.py</code> proves by driving number, text, repository, and skill-set runs through one <code>meta.run</code> call site and asserting the same kernel record for all four.</p> Piece What it is Shipped example Value type what proposers and evaluators see; declares <code>KIND</code> and <code>REPRESENTATION</code> <code>meta.Text</code> Codec <code>encode</code> value \u2192 payload, <code>decode</code> payload \u2192 value <code>TextCodec</code> Registration <code>Registry.register_artifact_type(codec)</code> built-in codecs skip this Declaration <code>Task(artifact=YourType)</code> <p><code>Text</code> is the smallest complete instance and is worth reading first: the value type and its codec are about fifteen lines each. This page builds a second domain \u2014 <code>recipe</code>, an ordered list of instruction steps \u2014 the same way.</p> <p>The contracts themselves are canonical elsewhere and are not restated here. <code>ArtifactCodec</code> owns the codec laws, the programming model owns <code>Task</code>, <code>Registry</code>, and <code>meta.run</code>, and the architecture owns what artifacts, kinds, and representations mean.</p> <p>Every Python block below is one script, run end to end by <code>tests/test_add_a_domain_tutorial.py</code>.</p>"},{"location":"adding-an-artifact-domain/#0-kind-and-representation","title":"0. Kind and representation","text":"<p>The kind names the schema \u2014 what a payload means. The representation names the storage shape that payload is written in. The two are separate because kinds can share a representation: <code>source-tree</code> and <code>skill-set</code> are both <code>utf8-tree-v1</code>, the same portable tree bytes read under different rules. The kind, never the bytes, decides which codec reads a committed payload.</p> <pre><code>import meta_evolve as meta\n\nprint(\"built-in text domain:\", meta.Text.KIND, meta.Text.REPRESENTATION)\n</code></pre>"},{"location":"adding-an-artifact-domain/#1-the-value-type","title":"1. The value type","text":"<p>Declare <code>KIND</code> and <code>REPRESENTATION</code> as strings on the type; <code>Task(artifact=...)</code> reads exactly those two attributes and nothing else, so a plain class works and no base class is imposed. Subclass whatever Python shape the domain already is \u2014 <code>Text</code> is a <code>str</code>, <code>Recipe</code> is a <code>tuple</code>.</p> <p>Rules that decide whether a value is one of these belong here, in the constructor. Rules about whether a value is any good belong to the evaluator.</p> <pre><code>from typing import ClassVar\n\n\nclass Recipe(tuple):\n    \"\"\"An ordered sequence of instruction steps.\"\"\"\n\n    KIND: ClassVar[str] = \"recipe\"\n    REPRESENTATION: ClassVar[str] = \"json-steps-v1\"\n\n    __slots__ = ()\n\n    def __new__(cls, steps=()):\n        steps = tuple(steps)\n        if not all(isinstance(step, str) and step.strip() for step in steps):\n            raise ValueError(\"every recipe step must be non-blank text\")\n        return super().__new__(cls, steps)\n</code></pre>"},{"location":"adding-an-artifact-domain/#2-the-codec","title":"2. The codec","text":"<p><code>encode</code> and <code>decode</code> must be pure, deterministic, and free of I/O, and <code>decode</code> is the only validation entry point \u2014 a codec has no separate <code>validate</code>. Return any payload <code>freeze</code> accepts: <code>None</code>, <code>bool</code>, <code>int</code>, finite <code>float</code>, <code>str</code>, lists and tuples of those, and string-keyed mappings of those. <code>Artifact</code> freezes what you return and derives the content digest from it, so codecs never mint or check identity.</p> <p>Refuse a value that is not yours by raising <code>TypeError</code> or <code>ValueError</code>. Those two are the rejection signal and nothing else is: <code>encode</code> is reached by every proposed candidate as well as by the seed, so a refusal there fails that one trial as a typed <code>InvalidOutput</code> and the run continues, while any other exception is treated as a bug in the codec and propagates.</p> <pre><code>class RecipeCodec:\n    \"\"\"Translates ``Recipe`` values to and from their durable payload.\"\"\"\n\n    kind = Recipe.KIND\n    representation = Recipe.REPRESENTATION\n    interfaces = (\"ordered-steps/v1\",)\n\n    def encode(self, value):\n        if not isinstance(value, Recipe):\n            raise TypeError(\"recipe tasks require a Recipe value\")\n        return tuple(value)\n\n    def decode(self, payload):\n        return Recipe(payload)\n</code></pre> <p><code>interfaces</code> names what a consumer may rely on, independently of the kind, so a later artifact carrying the same structure can declare the same interface and reuse its consumers. Leave it <code>()</code> when nothing consumes the domain structurally yet. <code>kind</code>, <code>representation</code>, and every interface name are identity declarations: each must be non-empty and have no outer whitespace. Registration rejects invalid declarations rather than trimming them.</p>"},{"location":"adding-an-artifact-domain/#3-register-the-codec","title":"3. Register the codec","text":"<pre><code>registry = meta.Registry()\nregistry.register_artifact_type(RecipeCodec())\nprint(\"declared interfaces:\", registry.artifact_interfaces(\"recipe\", \"json-steps-v1\"))\n</code></pre> <p>Registration is keyed by <code>(kind, representation)</code>. The six built-in keys are not overridable \u2014 changing the decode of an old key would change the meaning of every artifact already committed under it \u2014 so a different behavior needs a different key. A new representation of a built-in kind is a different key and registers normally.</p>"},{"location":"adding-an-artifact-domain/#4-declare-it-on-the-task-and-run","title":"4. Declare it on the task and run","text":"<p>Pass the registry to <code>meta.run</code> and name the value type on the task. From here nothing is domain-specific: the proposer receives decoded values and returns one, the evaluator scores one, and search, budgets, lineage, and experience behave exactly as they do for integers.</p> <pre><code>def add_step(parent, context):\n    return Recipe((*parent, f\"step {len(parent) + 1}\"))\n\n\ndef score(candidate):\n    return {\"score\": float(len(candidate))}\n\n\nexperiment = meta.Experiment(\n    task=meta.Task(\n        evaluator=score,\n        artifact=Recipe,\n        budget=meta.Budget(evaluations=4, trials=3),\n    ),\n    seed=Recipe((\"preheat the oven\",)),\n    proposer=add_step,\n    search=meta.Greedy(max_trials=3),\n    registry=registry,\n)\nresult = meta.run(experiment)\n\nbest = result.best()\nprint(\"committed as:\", best.kind, best.representation)\nprint(\"decoded type:\", type(best.value).__name__)\nprint(\"best value:\", tuple(best.value))\nprint(\"primary score:\", result.summary().primary_score)\n</code></pre> <pre><code>built-in text domain: text utf8-text-v1\ndeclared interfaces: ('ordered-steps/v1',)\ncommitted as: recipe json-steps-v1\ndecoded type: Recipe\nbest value: ('preheat the oven', 'step 2', 'step 3', 'step 4')\nprimary score: 4.0\n</code></pre> <p>That is the whole domain. The script imports <code>meta_evolve</code> and the standard library, and touches no library file.</p>"},{"location":"adding-an-artifact-domain/#5-check-the-codec-against-the-published-suite","title":"5. Check the codec against the published suite","text":"<p><code>meta_evolve.conformance.ArtifactCodecTests</code> checks both round trips, repeated-call stability within one process, common file/socket entry points, <code>TypeError</code>/<code>ValueError</code> rejection of foreign values and of malformed payloads (<code>rejected_payloads</code> \u2014 decode is validation), decoding through the kernel's <code>CodecIndex</code>, and \u2014 for values declared in <code>mutable_values</code> \u2014 that a decoded value is independent of the one you encoded. No law compares values with <code>==</code>: observations go through <code>value_state</code>, whose default knows exact canonical and kernel types only. <code>Recipe</code> subclasses <code>tuple</code>, so its suite overrides <code>value_state</code> with <code>tuple(value)</code> \u2014 an observation the codec has no hand in. The I/O patches are tripwires, not proof of arbitrary purity, and repeated calls do not prove cross-process reproducibility. Subclass the suite and override five fixtures (plus <code>value_state</code> for your own value types and <code>mutate</code> if any value is mutable); the published-conformance section of the extensions guide has the snippet and the install extra. <code>RecipeCodec</code> above is checked that way in this tutorial's own test.</p> <p>Every component family a registered domain composes with \u2014 codecs, repository-editing proposers, evaluators, search programs, context policies, workspaces, sandboxes, and typed mutation surfaces \u2014 has a published subclassable suite; see <code>meta_evolve.conformance</code> for what each deliberately leaves unchecked.</p>"},{"location":"adding-an-artifact-domain/#what-a-registered-domain-does-not-get-yet","title":"What a registered domain does not get yet","text":"<ul> <li>Restart-resumable execution. <code>mode=\"durable-local\"</code> fails closed with <code>CapabilityError</code>   for any task whose <code>(kind, representation)</code> is not one of the six built-in   keys. A registered codec is a live object rather than registered source, so   it carries no manifest and a resumed run could not verify that the code   rebuilding a payload is the code that wrote it. Custom domains are <code>local</code>   mode in Milestone 7. They may still use durable storage: after reopening the   storage and reconstructing the registry, inspect one through   <code>meta.Run(run_id, storage, registry=registry)</code>. This is decoding only, not   <code>resume</code> or execution authority; command-line inspection remains built-in-only.</li> </ul>"},{"location":"architecture/","title":"Meta-Evolve Architecture","text":""},{"location":"architecture/#status","title":"Status","text":"<p>Status: Shipped. This owns domain invariants through the complete M10 author experience. The roadmap owns scope; future work lives under Design.</p> <p>Milestone 5 is complete; Milestone 7 established the cross-domain local v1 boundary; and Milestone 10 completes its author and inspection experience. External SDKs remain user-owned callables at the structural proposer seam.</p>"},{"location":"architecture/#the-idea","title":"The idea","text":"<p>Meta-Evolve evaluates proposed successors to immutable, versioned artifacts. The domain meaning does not depend on an artifact kind, model provider, search preset, evaluator implementation, or storage backend.</p> <pre><code>Task -&gt; Seed -&gt; Proposal -&gt; Successor -&gt; Evaluation -&gt; Search decision\n                      \\-&gt; typed failure\n</code></pre> <p>The public callable path hides kernel ceremony. Durable records retain the facts needed for lineage, accounting, inspection, comparison, and replay.</p>"},{"location":"architecture/#architectural-layers","title":"Architectural layers","text":"<pre><code>Public API       improve, Task, artifacts, Session, run/resume, result queries\nApplication      normalization, coordination, comparison, replay\nDomain           immutable identities, outcomes, evidence, budgets\nPorts            proposer, evaluator, experience, workspace, storage\nAdapters         callables, SDK agents, subprocesses, files, SQLite\n</code></pre> <p>Dependencies point inward. Adapters translate external systems at the edge; the coordinator never branches on a provider, artifact type, evaluator, policy, or infrastructure implementation.</p>"},{"location":"architecture/#identities-and-lineage","title":"Identities and lineage","text":"<p>Content identity and occurrence identity are distinct. Equal artifact bytes may appear in different causal positions and therefore retain different occurrence IDs and parent links. Task, run, proposal, trial, evaluation, evidence, decision, and component identities are not collapsed for storage convenience.</p> <p>Every successor names its parents. Every decision-relevant observation is recorded at a logical time. Durable state is a projection of immutable records, not a mutable branch, workspace, cache, or provider conversation.</p>"},{"location":"architecture/#authority-boundaries","title":"Authority boundaries","text":"<p>Candidate-controlled fields contain only proposed artifact content and declared derivation. Experiment-controlled fields include tasks, evaluators, data splits, seeds, environments, and budgets. Governance controls permissions, admission, visibility, and promotion.</p>"},{"location":"architecture/#proposal-and-promotion-are-separate","title":"Proposal and promotion are separate","text":"<p>Search may select a best measured artifact. That selection grants no production authority. A candidate cannot widen its budget, alter hidden evaluation, erase failures, change permissions, or promote itself. Future typed mutation surfaces must preserve this invariant; see the M8 design.</p>"},{"location":"architecture/#evaluation-and-failures","title":"Evaluation and failures","text":"<p>Evaluation is independent of candidate claims and carries measurements, uncertainty, evidence, environment identity, and exact usage. Invalid output, proposer failure, evaluator failure, timeout, exhaustion, policy violation, and infrastructure failure remain distinct. An outcome without a valid measurement is non-rankable.</p>"},{"location":"architecture/#budgets-and-randomness","title":"Budgets and randomness","text":"<p>The coordinator owns accounting. Seed evaluation and successor work charge the declared dimensions exactly; experience access has separate bounds. Named random streams and recorded decisions make deterministic local trajectories replayable. External provider calls are reproducible only to the recorded transcript and declarations, not guaranteed to regenerate byte-identical output.</p>"},{"location":"architecture/#experience","title":"Experience","text":"<p>Experience is explicit and off by default. Pushed context is policy-selected, bounded, and committed before a proposal. Pulled experience is capability- bounded and audited. Declared derivation may cite only authorized, strictly prior observations in their recorded order.</p>"},{"location":"architecture/#workspaces-and-external-execution","title":"Workspaces and external execution","text":"<p>Repository proposers receive a private mutable workspace from an immutable artifact. Successful capture creates a new immutable occurrence; cleanup always happens before commitment. Evaluators materialize candidates in fresh workspaces and own their commands, environments, hidden graders, and failure translation.</p>"},{"location":"architecture/#durability-and-replay","title":"Durability and replay","text":"<p>Artifacts and append-only transitions are durable truth. SQLite and filesystem stores preserve committed facts; caches and worker liveness do not. Reopen is a read-only projection, and a read-only storage handle observes live WAL commits with no write, migration, or execution authority. Resume first verifies component manifests and the recovery frontier, then performs only missing logical work. Replay never reruns a proposer, evaluator, context selector, or provider. <code>Run.inspect()</code> derives one immutable aggregate from one replay. Session fixes authority and budget; each step advances the classified frontier to ready or stopped without asking search twice. Lifecycle means <code>Stop</code>, rankability means successful evaluation, and bootstrap remains a trial.</p> <p>Safe exact-kind presentation reads frozen payloads into bounded inert records; decoded values are never inspected and canonical attachments remain exact. The loopback browser keeps storage on one thread and artifact routes within one snapshot; cached payloads add bounded-LRU memory without a byte copy.</p>"},{"location":"architecture/#replaceability","title":"Replaceability","text":"<p>An independently testable port is introduced only when a current acceptance test needs a second implementation. Every extension seam \u2014 codecs, repository proposers, evaluators, search programs, context policies, workspaces, sandboxes, and typed mutation surfaces \u2014 has a published conformance suite; store adapters keep an internal contract under <code>tests/</code>. Each suite remains explicit about the laws it does not check.</p>"},{"location":"architecture/#compatibility-anchors","title":"Compatibility anchors","text":""},{"location":"architecture/#improve","title":"Improve","text":"<p><code>improve()</code> is declaration sugar with no independent durable identity or execution semantics. It constructs <code>Task</code>, bounded <code>Greedy</code>, and <code>Experiment</code> values and enters the ordinary local <code>run()</code> path. Its <code>storage</code> argument may persist local history, but does not imply durable-local reconstruction or resumability.</p>"},{"location":"architecture/#experiment","title":"Experiment","text":"<p><code>Experiment</code> is an immutable declaration value, not a new durable record or serialization identity. Positional typed declarations and the source-compatible keyword <code>run()</code> form normalize before validation and enter the same execution path. Storage, mode, and the narrowing call-level budget remain execution overlays. Equality is dataclass equality over the eight fields; it does not promise structural equality for live proposer or registry objects.</p>"},{"location":"architecture/#functional-search","title":"Functional search","text":"<p><code>@search</code> is a dependency-free adapter over the ordinary <code>SearchProgram</code> boundary, not a second coordinator path. Its callback chooses one opaque seed or incumbent occurrence handle, or stops with a validated reason. A private shared lowerer applies the same envelope and authorized unit trial construction as <code>Greedy</code>; the two retain distinct truthful policy and run identities. Complete immutable decorator configuration flows into <code>SearchSpec</code> and run identity. Durable-local reconstruction remains explicit registration through the existing policy-factory extension. Multiple parents, archives, and population-shaped actions stay on <code>SearchProgram.next()</code>.</p>"},{"location":"architecture/#task","title":"Task","text":"<p>See the evaluation guide.</p>"},{"location":"architecture/#artifact","title":"Artifact","text":"<p>See runs and results and extensions.</p>"},{"location":"architecture/#proposer-and-agent","title":"Proposer and agent","text":"<p>See runs and results and user-owned SDK integration.</p>"},{"location":"architecture/#proposal","title":"Proposal","text":"<p>Proposal identity and authority are covered above.</p>"},{"location":"architecture/#trial-and-outcome","title":"Trial and outcome","text":"<p>See evaluation and budgets.</p>"},{"location":"architecture/#evaluation-and-evidence","title":"Evaluation and evidence","text":"<p>See evaluation and budgets.</p>"},{"location":"architecture/#population","title":"Population","text":"<p>See shipped search presets.</p>"},{"location":"architecture/#policy-and-searchprogram","title":"Policy and SearchProgram","text":"<p>See extensions.</p>"},{"location":"architecture/#workitem","title":"WorkItem","text":"<p>This remains kernel vocabulary, not part of the simple callable path.</p>"},{"location":"architecture/#event","title":"Event","text":"<p>Events are immutable transition facts projected into run views.</p>"},{"location":"architecture/#experience-graph","title":"Experience graph","text":"<p>See the experience guide.</p>"},{"location":"architecture/#memory-types","title":"Memory types","text":"<p>See the experience guide.</p>"},{"location":"architecture/#temporal-state","title":"Temporal state","text":"<p>See the advanced provenance material.</p>"},{"location":"architecture/#contextpolicy","title":"ContextPolicy","text":"<p>See <code>ContextPolicy</code>. Its <code>select</code> method receives the read-only <code>ExperienceGraph</code> and forms access-checked <code>ExperienceQuery</code> values. The graph, query, node, record, and state types are root-exported as one type-complete policy-author surface; replay construction remains internal to the application namespace.</p>"},{"location":"architecture/#agent-directed-experience-access","title":"Agent-directed experience access","text":"<p>See audited pull.</p>"},{"location":"architecture/#programming-harnesses","title":"Programming harnesses","text":"<p><code>HarnessArtifact</code> is the built-in <code>agent-harness</code> domain using <code>harness-manifest-v1</code>. Its v1 schema contains a planner <code>TextParam</code> and a context-policy <code>PolicyParam</code>; their names, component references, interfaces, and surface types are immutable across a successor. Only text or policy configuration may change.</p> <p>Artifact codecs may structurally expose <code>validate_successor(parents, candidate)</code>. Proposal completion invokes it after candidate encoding and before artifact creation. Codecs without the hook retain their behavior. This generic boundary is core-owned, so an honest wrapper may use <code>MutationSurface.apply()</code> while a bypassing wrapper receives the same independent check.</p> <p>Harness search evaluates development episodes only. Held-out episodes run once for the unchanged baseline and once for the selected harness as seed-only <code>Greedy(max_trials=0)</code> runs. Strict comparison requires equal evaluator, artifact, objective, budget, proposer, search, randomness, context, experience, and immutable harness declarations. It reports promotion eligibility but owns no promotion or deployment authority.</p> <p>Portable repository episodes digest public source, hidden evaluator tests, process controls, and explicit development/held-out splits. The example-owned evaluator launches fresh isolated inner runs and records complete captured attempts. Resume guarantees begin at a committed outer evaluation; uncommitted inner work may repeat.</p>"},{"location":"architecture/#nested-improvement-loops","title":"Nested improvement loops","text":"<p><code>PolicyArtifact</code> is the built-in <code>search-policy</code> domain using <code>policy-manifest-v1</code>. Its only mutable values are parent selection (<code>seed</code> or <code>incumbent</code>) and a non-negative voluntary trial limit. Fixed schema-owned names, component declarations, interfaces, allowed domains, tasks, seeds, proposers, evaluators, environments, budgets, split membership, caching, aggregation, and promotion remain outside candidate authority.</p> <p><code>CandidatePolicy</code> is the trusted <code>candidate-policy@1</code> interpreter registered through the ordinary policy registry. Its canonical artifact payload is the search configuration and therefore part of the existing <code>RunId</code>; durable-local reconstruction and resume require no coordinator branch.</p> <p>Policy episodes are user-owned composition around ordinary runs. Each isolated inner run retains its own events, lineage, evidence, failures, usage, <code>RunId</code>, and evaluator-owned storage location. The existing content-addressed <code>RunId</code> is the sole cache identity. Development may reopen an exact completed run or resume a compatible incomplete durable run. Held-out execution always starts in a new empty namespace. Aggregates are recomputed from outcomes rather than cached separately.</p> <p>Any failed, incomplete, or missing episode makes an evaluation non-rankable while preserving partial outcomes. Inner trial/evaluation counts remain inner; only token and elapsed usage rolls into an enclosing evaluation. Separate seed-only held-out outer runs compare the baseline and selected candidate under equal external controls. Comparison reports promotion eligibility without granting promotion authority. See the policy-evolution guide.</p>"},{"location":"architecture/#scientific-discovery","title":"Scientific discovery","text":"<p>See scientific direction.</p>"},{"location":"architecture/#toward-an-open-agent-network","title":"Toward an open agent network","text":"<p>See network direction.</p>"},{"location":"architecture/#boundaries-with-adjacent-systems","title":"Boundaries with adjacent systems","text":"<p>Research influences are recorded in references; they do not override shipped semantics.</p>"},{"location":"architecture/#invariants","title":"Invariants","text":"<ul> <li>Artifacts are immutable; equal content does not collapse causal occurrences.</li> <li>Parent lineage, component identity, randomness, evidence, and usage are explicit.</li> <li>Candidates cannot control evaluation, budgets, permissions, or promotion.</li> <li>Failures without valid measurements are never ranked as low scores.</li> <li>Replay projects committed facts without external execution.</li> <li>Replaceable components enter through ports, never coordinator special cases.</li> </ul>"},{"location":"comparison/","title":"How Meta-Evolve compares","text":"<p>This page answers a question new readers ask first: AlphaEvolve, GEPA's <code>optimize_anything</code>, and Meta-Evolve all run \"propose, evaluate, keep the better one\" loops \u2014 so what is actually different, and when would you pick each?</p> <p>Claims about external systems belong to those systems and reflect their public materials as of August 2026. Nothing here implies Meta-Evolve has reproduced their results.</p>"},{"location":"comparison/#one-sentence-each","title":"One sentence each","text":"<ul> <li>AlphaEvolve   is Google DeepMind's hosted discovery service: you bring a problem and   an automated evaluator, Google brings the coding agent, the search, and the   compute. Proprietary; generally available on Google Cloud since July 2026.</li> <li>GEPA's <code>optimize_anything</code>   is an open-source (MIT) optimizer: a strong reflective-mutation   algorithm with Pareto selection, packaged behind a deliberately tiny API \u2014   a seed plus an evaluator.</li> <li>Meta-Evolve is an open kernel: a programming model for building your   own evaluated improvement loop out of replaceable parts, where every run   leaves a durable, replayable, budget-exact record.</li> </ul> <p>All three run the same loop shape. They sell different layers of it:</p> <pre><code>                 AlphaEvolve   optimize_anything   Meta-Evolve\nthe results      product       headline            not yet demonstrated\nthe algorithm    internal      product             simple presets\nthe loop itself  internal      internal            product\n</code></pre> <p>AlphaEvolve's product is the results (you rent the whole system). GEPA's product is the algorithm (the loop is fixed; you feed it). Meta-Evolve's product is the loop itself (you program it; the kernel guarantees the record).</p>"},{"location":"comparison/#side-by-side","title":"Side by side","text":"AlphaEvolve <code>optimize_anything</code> Meta-Evolve What it is Managed cloud service Optimizer library Loop kernel/library You provide Problem + evaluator Seed + evaluator Task, seed, proposer, search It provides Agent, search, compute Reflection + Pareto search Records, lineage, budgets, replay, seams Candidate form Code Text (string or named strings) Typed artifacts with codecs LLM proposer Built in (Gemini) Built in (your model choice) Not bundled; user-owned callable Loop internals Closed Fixed inside <code>optimize()</code> Every stage replaceable Open source No Yes (MIT) Yes (this repository)"},{"location":"comparison/#what-control-means-here-concretely","title":"What \"control\" means here, concretely","text":"<p>Many frameworks are \"flexible\" through configuration flags. Meta-Evolve is different in a specific way: the loop is programmed, not configured. Each stage \u2014 proposer, evaluator, search program, context policy, artifact codec, workspace, storage \u2014 is a seam with a published contract and a conformance suite. You swap implementations, not flags.</p> <p>The practical payoff is ablation. Because a component can be replaced without touching anything else, you can run the identical task several ways under equal budgets and compare the records:</p> <pre><code>task = meta.Task(evaluator=score_candidate)\n\nfor context in (meta.NoMemory(), meta.AncestorsOnly(), meta.PullAccess()):\n    experiment = meta.Experiment(\n        task=task,\n        seed=seed,\n        proposer=improve,\n        search=meta.Greedy(max_trials=20),\n        context=context,\n    )\n    result = meta.run(experiment)\n    print(type(context).__name__, result.best(), result.experience_usage())\n</code></pre> <p>That is a controlled experiment on the loop's own memory policy: same task, same proposer, same budget, one variable changed. The experience-reuse example runs exactly this comparison. The same move works for search (<code>Greedy</code> vs <code>BestOfN</code> vs <code>Population</code> \u2014 see the policy comparison) and for proposers.</p> <p>A monolithic optimizer cannot ablate itself this way. In <code>optimize_anything</code>, reflection and Pareto selection live inside the loop; there is no seam at which to remove or replace one of them while holding the rest fixed. That is not a defect \u2014 it is the price of a two-argument API \u2014 but it means questions like \"did the memory help?\" or \"was it the selection rule or the mutation?\" are answerable in Meta-Evolve and not from inside a sealed loop.</p>"},{"location":"comparison/#control-is-half-the-record-is-the-other-half","title":"Control is half; the record is the other half","text":"<p>Swappable parts alone would just be do-it-yourself. The second half is that whatever you plug in, the kernel still enforces the same invariants:</p> <ul> <li>Independent evaluation. The evaluator measures the candidate outside   the proposer's authority. A candidate cannot grade itself, widen its own   budget, erase failures, or promote itself   (architecture: authority boundaries).</li> <li>Exact budgets. Seed evaluation, successor attempts, and experience   access charge declared budget dimensions exactly, so \"equal budget\"   comparisons are equal by construction, not by convention.</li> <li>Lineage. Every successor names its parents; every decision-relevant   observation is recorded at a logical time. <code>run.lineage()</code> is the exact   causal chain, and proposers can declare which prior records they used   through <code>derived_from</code>.</li> <li>Replay. A durable run can be reopened and replayed to a byte-identical   event stream   (crash-recovery example).   Stated precisely: replay reproduces the recorded run exactly; it does not   re-execute a stochastic model. That is what a reviewer needs to check a   claim, and it is the part most optimizer libraries do not attempt.</li> </ul> <p>Text-only optimizers also collapse artifact structure into strings: a candidate is well-formed only if the evaluator happens to reject malformed text at scoring time. Meta-Evolve artifacts are typed \u2014 source trees take exact-file patches through private workspaces, skill sets and harnesses have declared interfaces \u2014 and successors are validated against their parent before commit (add a domain).</p>"},{"location":"comparison/#what-the-others-do-better","title":"What the others do better","text":"<p>Honesty in both directions:</p> <ul> <li>Call-site simplicity. <code>optimize_anything</code> is a seed and an evaluator.   A first Meta-Evolve run asks for five declarations. For \"make this prompt   better this afternoon,\" the smaller API is simply the better tool, and the   determinism ceremony here is friction with no payoff.</li> <li>A strong proposer out of the box. GEPA ships state-of-the-art   reflective mutation; AlphaEvolve ships a frontier coding agent. Meta-Evolve   deliberately bundles no model \u2014 the shipped presets are simple, and the   intelligence is whatever you connect at the proposer seam.</li> <li>Demonstrated results. AlphaEvolve and GEPA lead with headline numbers.   Meta-Evolve's examples are deterministic teaching loops; it has not yet   produced a live optimization result of its own. Its claims are about the   loop's guarantees, not about optimization performance.</li> </ul>"},{"location":"comparison/#choosing-in-practice","title":"Choosing in practice","text":"<ul> <li>Improving one prompt or config quickly, cheap evaluations, nobody will   audit the run \u2192 <code>optimize_anything</code>.</li> <li>Enterprise code optimization with managed compute and no infrastructure   appetite \u2192 AlphaEvolve on Google Cloud.</li> <li>Controlled experiments on the loop itself (memory, search, proposer   ablations), expensive or noisy evaluations where crash recovery and exact   accounting matter, bi-level   autoresearch setups, or any run whose record must   justify a promotion decision \u2192 Meta-Evolve.</li> </ul>"},{"location":"comparison/#using-them-together","title":"Using them together","text":"<p>These tools compose rather than compete. GEPA-style reflective mutation is, from Meta-Evolve's point of view, a proposer: a callable that reads the parent and prior evidence and returns a <code>ProposalResult</code> (SDK integration). Pareto-frontier retention is a search program behind the same contract as <code>Greedy</code> and <code>Population</code> (extensions). Hosted that way, the optimizer keeps its intelligence and the kernel adds lineage, budgets, and replay.</p> <p>The same seams make Meta-Evolve usable as a neutral harness for comparing optimizers under exactly equal budgets on identical records \u2014 a comparison no single optimizer can run on itself from the inside. Adapter compatibility with existing frameworks is the stated preference over duplicating their APIs; see OpenEvolve in the references.</p>"},{"location":"comparison/#related-pages","title":"Related pages","text":"<ul> <li>Research references \u2014 the fuller map of neighboring   systems, including FunSearch, OpenEvolve, DSPy, and Trellis.</li> <li>Architecture \u2014 domain semantics and authority   boundaries in full.</li> <li>Start here \u2014 the five declarations in a runnable   ten-minute loop.</li> </ul>"},{"location":"contributor-code-tour/","title":"Trace one run through the kernel","text":"<p>Run the canonical example once, then keep its two output lines in view:</p> <pre><code>uv run python examples/learn/number_search/main.py\n</code></pre> <pre><code>12\n[0, 1, 2, 7, 12]\n</code></pre> <p>The short script enters one coordinator. The actual order is interleaved:</p> <pre><code>declaration\n  \u2192 encode, evaluate, and atomically commit the seed\n  \u2192 search decision and commit\n  \u2192 proposal, candidate encoding, and commit\n  \u2192 independent evaluation and commit\n  \u2192 repeat until the committed stop decision\n  \u2192 reopen the committed stream as a read-only Run\n</code></pre> <p>No stage branches on \u201cnumber.\u201d The codec handles representation, the proposer handles mutation, the evaluator handles measurement, and search handles the next decision.</p>"},{"location":"contributor-code-tour/#1-declaration","title":"1. Declaration","text":"<p><code>examples/learn/number_search/main.py</code> declares the task, seed, proposer, search preset, budget, and random seed. The root <code>meta.run</code> export reaches <code>application/runtime.py</code>.</p> <p><code>runtime.run</code> validates the public values, chooses the codec index, computes the effective budget, adapts callables, checks capabilities, prepares search, and derives the run and task identities. Durable-local validation finishes before the coordinator can write an event.</p>"},{"location":"contributor-code-tour/#2-encoding","title":"2. Encoding","text":"<p>The seed is encoded immediately through <code>CodecIndex.encode</code>. A successor stays a Python value while the proposer works; the one place it becomes a durable payload is <code>proposal_completion.py</code>.</p> <p>A codec's <code>TypeError</code> or <code>ValueError</code> means invalid candidate output and becomes a typed failure. Another exception is a codec defect and propagates. Codecs do not choose artifact IDs, parents, scores, or promotion.</p>"},{"location":"contributor-code-tour/#3-proposal","title":"3. Proposal","text":"<p><code>execution.py</code> loads only the committed parent artifacts named by the trial. It builds a <code>ProposerContext</code> from recorded randomness, logical step, objectives, bounded context, and audited experience, then calls the replaceable proposer runner.</p> <p><code>proposal_completion.py</code> checks usage and derivation authority before accepting the candidate. Core code creates the proposal, artifact occurrence, stable IDs, parent links, and provenance. The proposer supplies content and evidence; it does not mint durable identity.</p>"},{"location":"contributor-code-tour/#4-evaluation","title":"4. Evaluation","text":"<p><code>evaluation_execution.py</code> decodes the committed artifact and calls the evaluator. Core code adds one evaluation charge, enforces effective budgets, creates stable evidence and evaluation IDs, and records either metrics or a typed failure.</p> <p>The evaluator is outside candidate authority. A candidate can fail a task and receive a real low score. A broken evaluator, exhausted resource, or infrastructure fault carries no metric and cannot be ranked as poor candidate performance.</p>"},{"location":"contributor-code-tour/#5-search","title":"5. Search","text":"<p><code>run_support.py</code> contains the small drive loop. A prepared policy reads only committed <code>SearchState</code>, returns a trial or stop decision, and the coordinator commits the decision before executing a trial.</p> <p>Greedy's parent choice and retention rules live in <code>policies/greedy.py</code>, not in the coordinator. This is why BestOfN, Population, and a conforming custom policy use the same execution path.</p>"},{"location":"contributor-code-tour/#6-commitment","title":"6. Commitment","text":"<p><code>coordinator.py</code> funnels every durable fact through <code>commit_batch</code>. It first folds the proposed events on a local projection and checks the same batch grammar recovery uses. <code>Storage.commit</code> then writes events and artifact occurrences together. Only a successful store commit advances the in-memory projection.</p> <p>The seed follows the same rule through <code>bootstrap.py</code>: its declaration, occurrence, trial outcome, evidence, and evaluation form one genesis batch. A partial genesis is never a run.</p>"},{"location":"contributor-code-tour/#7-projection","title":"7. Projection","text":"<p><code>projections.py</code> rebuilds <code>RunProjection</code> by folding the ordered event stream. Replay consumes records; it never reruns the proposer, evaluator, context selector, or search policy.</p> <p><code>results.py</code> projects read-only <code>best</code>, <code>trials</code>, <code>lineage</code>, summary, and comparison views. The printed lineage follows committed artifact parent IDs. It omits rejected <code>16</code> because that occurrence is not an ancestor of the winning <code>12</code>.</p>"},{"location":"contributor-code-tour/#exercises-on-the-invariants","title":"Exercises on the invariants","text":""},{"location":"contributor-code-tour/#authority","title":"Authority","text":"<p>In <code>proposal_completion.py</code>, list the fields copied from proposer output and the fields minted by core. Explain why moving artifact ID creation into the proposer would let a candidate overwrite history.</p>"},{"location":"contributor-code-tour/#identity","title":"Identity","text":"<p>Change only <code>random_seed</code> in a temporary number-search run. Predict which run identity changes and which task identity stays fixed. Then find both inputs in <code>runtime.py</code> and <code>run_support.py</code>.</p>"},{"location":"contributor-code-tour/#lineage","title":"Lineage","text":"<p>Sketch the parent IDs for attempts <code>1</code>, <code>6</code>, <code>11</code>, <code>16</code>, and <code>12</code>. Explain why two artifact occurrences with equal payload bytes still need distinct IDs.</p>"},{"location":"contributor-code-tour/#budgets","title":"Budgets","text":"<p>Account for the example's six evaluations and five successor trials. Identify where the seed is charged, where proposal usage is checked, and where evaluator usage is checked. No component may widen the effective budget.</p>"},{"location":"contributor-code-tour/#typed-failures","title":"Typed failures","text":"<p>Classify these outcomes: a candidate violates the task, a codec rejects the candidate type, the grader crashes, and a proposer exceeds its resource grant. Only the first can be an ordinary low score.</p>"},{"location":"contributor-code-tour/#replay","title":"Replay","text":"<p>Read <code>commit_batch</code> beside <code>replay_run_projection</code>. Name the shared fold and grammar checks that keep \u201ccommitted\u201d equivalent to \u201crecoverable.\u201d Explain why replay must not ask the proposer for the same candidate again.</p>"},{"location":"contributor-code-tour/#comparison-validity","title":"Comparison validity","text":"<p>Use the operations lab to compare Greedy and BestOfN. Then list the declarations that must match before the winner is meaningful. A displayed winner is not evidence when task, evaluator, initial artifact, budget, proposer, or hidden component configuration changed.</p> <p>When you can answer all seven exercises from source, continue to the M8 design capstone.</p>"},{"location":"examples/","title":"Examples by outcome","text":"<p>Choose the result you want, not the milestone that introduced it. All commands run from the repository root.</p>"},{"location":"examples/#learn","title":"Learn","text":"Example Outcome Prerequisite Status Run Next <code>checklist</code> First bounded improvement None Deterministic <code>uv run python examples/learn/checklist/main.py</code> <code>number_search</code> <code>number_search</code> Named satisfaction and reproducible randomness Checklist Deterministic <code>uv run python examples/learn/number_search/main.py</code> <code>policy_comparison</code> <code>policy_comparison</code> Compare chain, star, and population search Search basics Deterministic <code>uv run python examples/learn/policy_comparison/main.py</code> <code>functional_search</code> <code>experience_reuse</code> Compare omitted, pushed, and pulled experience Result queries Deterministic <code>uv run python examples/learn/experience_reuse/main.py</code> <code>crash_recovery</code> <code>functional_search</code> Express a decision beyond presets Search presets Deterministic <code>uv run pytest -q tests/test_functional_search_fixtures.py</code> <code>harness_evolution</code>"},{"location":"examples/#domains","title":"Domains","text":"Example Outcome Prerequisite Status Run Next <code>python_function</code> Patch source against an independent grader Inspection Deterministic <code>uv run python examples/domains/python_function/main.py</code> <code>text_optimization</code> <code>text_optimization</code> Improve the built-in text domain Experiment Deterministic <code>uv run python examples/domains/text_optimization/main.py</code> <code>skill_evolution</code> <code>skill_evolution</code> Evolve a typed skill set Nested evaluation Deterministic <code>uv run python examples/domains/skill_evolution/main.py</code> <code>sql_migrations</code> <code>sql_migrations</code> Add and run a custom artifact codec Extensions Deterministic <code>uv run python examples/domains/sql_migrations/main.py</code> <code>adversarial_inputs</code> <code>adversarial_inputs</code> Find evaluator-confirmed failures Typed failures Deterministic <code>uv run python examples/domains/adversarial_inputs/main.py</code> <code>agent_test_repair</code>"},{"location":"examples/#operations","title":"Operations","text":"Example Outcome Prerequisite Status Run Next <code>crash_recovery</code> Resume verified durable components Experiment Deterministic <code>uv run python examples/operations/crash_recovery/main.py</code> <code>command_line</code> <code>command_line</code> Run and inspect an experiment factory Persistence Deterministic <code>uv run meta-evolve run examples/operations/command_line/experiment.py --storage /tmp/meta-evolve-cli-example</code> <code>durable_operations</code> <code>durable_operations</code> Operate a crashed run through the CLI CLI Deterministic <code>uv run python examples/operations/durable_operations/main.py</code> <code>user_owned_sdk</code>"},{"location":"examples/#agents","title":"Agents","text":"Example Outcome Prerequisite Status Run Next <code>user_owned_sdk</code> Wrap an SDK-shaped callable without credentials Experiment Deterministic fixture <code>uv run python examples/agents/user_owned_sdk/main.py</code> <code>agent_test_repair</code> <code>agent_test_repair</code> Repair code under an external grader Source domain Deterministic fixture <code>uv run python examples/agents/agent_test_repair/main.py</code> <code>best_of_n_agent</code> <code>best_of_n_agent</code> Sample independent agent attempts Search presets Deterministic fixture <code>uv run python examples/agents/best_of_n_agent/main.py</code> <code>experience_ab</code> <code>experience_ab</code> Compare an agent with and without experience Experience Deterministic fixture <code>uv run python examples/agents/experience_ab/main.py</code> <code>prompt_optimization</code> <code>prompt_optimization</code> Optimize a prompt with held-out checks Nested runs Deterministic fixture <code>uv run python examples/agents/prompt_optimization/main.py</code> <code>model_migration</code> <code>model_migration</code> Compare wrappers across model declarations Comparison Deterministic fixture <code>uv run python examples/agents/model_migration/main.py</code> <code>sealqa_labs</code>"},{"location":"examples/#research","title":"Research","text":"Example Outcome Prerequisite Status Run Next <code>sealqa_labs</code> Study four bring-your-own-wrapper mechanisms Agents Experimental/live-only <code>uv run pytest -q tests/test_sealqa_labs.py</code> <code>autoresearch</code> <code>autoresearch</code> Evaluate an inner research procedure Nested runs Deterministic <code>uv run python examples/research/autoresearch/main.py</code> <code>harness_evolution</code> <code>harness_evolution</code> Improve and externally check a typed harness Domains and agents Deterministic <code>uv run python examples/research/harness_evolution/main.py --output /tmp/meta-evolve-m8</code> <code>policy_evolution</code> <code>policy_evolution</code> Evolve trusted data-only search controls Search and harnesses Deterministic <code>uv run python examples/research/policy_evolution/main.py --output /tmp/meta-evolve-m9</code> Roadmap <p>Credentialed adaptations belong in user-owned wrappers and must stay explicit; the commands above are deterministic repository evidence unless marked Experimental/live-only.</p>"},{"location":"implementation-plan/","title":"Implementation Plan","text":"<p>This document is the technical companion to the canonical milestone roadmap. The roadmap owns implementation order, demonstrations, and acceptance gates. The programming model owns public API signatures. This plan records only the package boundaries, ports, adapters, and technical choices needed to implement each milestone.</p>"},{"location":"implementation-plan/#pre-milestone-api-spike","title":"Pre-milestone API spike","text":"<p>Status: complete. The spike lives under <code>experiments/00_api_spike/</code> and ran both required demonstrations; its ergonomic findings are recorded in <code>real_model_findings.md</code> and reflected in the programming model. The section below is kept as the record of what the spike was scoped to prove.</p> <p>Before creating the durable package, build a disposable one-to-three-file prototype under <code>experiments/00_api_spike/</code>.</p> <p>It must run the same target API against:</p> <ol> <li>deterministic integer improvement with greedy and best-of-N search;</li> <li>a real-model prompt-optimization example with a small callable proposer and evaluator.</li> </ol> <p>The spike may use closures, live objects, and an in-memory list. It does not need events, ports, grants, persistence, or production provider handling. Its purpose is to test whether <code>Task</code>, <code>meta.run</code>, <code>Proposer</code>, <code>Evaluator</code>, <code>SearchProgram</code>, and <code>Run</code> feel coherent in working code.</p> <p>Treat the implementation as disposable. Feed ergonomic findings back into the programming model, then implement only the justified contracts in Milestone 1.</p>"},{"location":"implementation-plan/#staged-package-shape","title":"Staged package shape","text":"<p>The expected package may eventually look like this:</p> <pre><code>src/meta_evolve/\n\u251c\u2500\u2500 domain/\n\u2502   \u251c\u2500\u2500 ids.py\n\u2502   \u251c\u2500\u2500 task.py\n\u2502   \u251c\u2500\u2500 artifact.py\n\u2502   \u251c\u2500\u2500 proposal.py\n\u2502   \u251c\u2500\u2500 trial.py\n\u2502   \u251c\u2500\u2500 evaluation.py\n\u2502   \u251c\u2500\u2500 evidence.py\n\u2502   \u251c\u2500\u2500 policy.py\n\u2502   \u251c\u2500\u2500 budget.py\n\u2502   \u251c\u2500\u2500 work.py\n\u2502   \u2514\u2500\u2500 events.py\n\u251c\u2500\u2500 ports/\n\u2502   \u251c\u2500\u2500 artifact_store.py\n\u2502   \u251c\u2500\u2500 event_store.py\n\u2502   \u251c\u2500\u2500 proposer.py\n\u2502   \u251c\u2500\u2500 evaluator.py\n\u2502   \u251c\u2500\u2500 experience_graph.py\n\u2502   \u251c\u2500\u2500 experience_reader.py\n\u2502   \u251c\u2500\u2500 runtime.py\n\u2502   \u251c\u2500\u2500 workspace.py\n\u2502   \u2514\u2500\u2500 sandbox.py\n\u251c\u2500\u2500 application/\n\u2502   \u251c\u2500\u2500 coordinator.py\n\u2502   \u251c\u2500\u2500 projections.py\n\u2502   \u251c\u2500\u2500 context.py\n\u2502   \u2514\u2500\u2500 experience_access.py\n\u251c\u2500\u2500 adapters/\n\u2502   \u251c\u2500\u2500 memory/\n\u2502   \u251c\u2500\u2500 sqlite/\n\u2502   \u251c\u2500\u2500 filesystem/\n\u2502   \u251c\u2500\u2500 callable/\n\u2502   \u251c\u2500\u2500 fake/\n\u2502   \u2514\u2500\u2500 sdk/\n\u2514\u2500\u2500 policies/\n    \u251c\u2500\u2500 greedy.py\n    \u251c\u2500\u2500 population.py\n    \u2514\u2500\u2500 context.py\n</code></pre> <p>This is a destination map, not a command to create every module upfront. A module or port appears only when its milestone requires an independently testable seam. Domain code contains immutable values and transitions; adapters contain infrastructure; the coordinator depends only on the ports introduced so far.</p>"},{"location":"implementation-plan/#milestone-1-deterministic-in-memory-kernel","title":"Milestone 1: deterministic in-memory kernel","text":"<p>Status: complete (July 21, 2026). The exit gate is covered by the canonical number-search example and the retained-event reconstruction acceptance suite. This is event-to-projection replay and built-in decision reproduction, not process restart or resume.</p>"},{"location":"implementation-plan/#domain-records","title":"Domain records","text":"<p>Implement frozen, serializable models for only the first complete logical loop:</p> <ul> <li><code>Task</code></li> <li><code>Artifact</code></li> <li><code>Proposal</code></li> <li><code>TrialSpec</code>, <code>Trial</code>, and <code>TrialOutcome</code></li> <li><code>Evaluation</code> and <code>Evidence</code></li> <li><code>SearchDecision</code>, <code>PolicyRef</code>, and <code>OperatorSpec</code></li> <li><code>Budget</code> and <code>Usage</code></li> <li>the minimal typed events and projected <code>SearchState</code></li> </ul> <p><code>Trial</code> is the logical experiment. <code>WorkItem</code> is its delivery envelope and stays an internal kernel record: the sequential local coordinator may introduce it when scheduling needs a seam, and it never appears in user-facing types. Do not add leases or retry-attempt identities until distribution requires them.</p> <p>Use opaque typed identifiers and schema versions on durable envelopes. Keep semantic artifact kind separate from storage representation. Simple integers or strings are normalized through a basic artifact codec rather than special-cased by the coordinator.</p> <p>Durable foreign references use explicit <code>_id</code> and <code>_ids</code> suffixes. The common artifact envelope exposes <code>kind</code>, <code>representation</code>, <code>schema_version</code>, and <code>parent_ids</code>; <code>Task.artifact_kind</code> names the same semantic classification as <code>Artifact.kind</code>.</p>"},{"location":"implementation-plan/#minimal-ports","title":"Minimal ports","text":"<pre><code>class ArtifactStore(Protocol):\n    def put(self, artifact: Artifact) -&gt; None: ...\n    def get(self, artifact_id: ArtifactId) -&gt; Artifact: ...\n\n\nclass EventStore(Protocol):\n    def append(\n        self,\n        run_id: RunId,\n        expected_version: int,\n        events: Iterable[EventEnvelope],\n    ) -&gt; int: ...\n\n    def read(\n        self,\n        run_id: RunId,\n        after: int = 0,\n    ) -&gt; tuple[EventEnvelope, ...]: ...\n\n\nclass Proposer(Protocol):\n    def propose(self, parent, context) -&gt; CandidateLike | ProposalResult: ...\n\n\nclass Evaluator(Protocol):\n    def evaluate(self, candidate) -&gt; EvaluationLike: ...\n</code></pre> <p>Start with callable proposer and evaluator adapters plus in-memory stores. Do not introduce <code>ExperienceGraph</code>, SDK runtime, workspace, sandbox, or SQLite ports in this milestone.</p> <p>M1.1 deliberately made <code>Artifact</code> responsible for freezing payloads, validating content digests, and carrying occurrence identity. Storage adapters therefore accept complete, already validated artifacts and preserve them as-is instead of reconstructing artifacts from separate payload and metadata arguments.</p> <p>M1.3 keeps replay as a pure <code>events \u2192 SearchState</code> fold. Durable event facts record the run configuration, policy decisions, completed trials, artifact occurrences, and evaluations; the disposable state derives best-so-far, failures, usage, remaining budgets, and decision-scoped randomness through plain properties. Historical budget overages remain replayable and floor projected remaining resources at zero, while <code>Budget.remaining_after</code> retains its strict rejection semantics for prospective accounting. Named seeds and logical decision ids use versioned SHA-256 derivation rather than Python's process-randomized hash.</p> <p>The M1.3 <code>SearchProgram</code> contract intentionally contains only <code>next(state)</code>. Archive composition remains Milestone 2 work. Proposal submission facts, in-flight trial transitions, authority checks, and idempotent completion remain with the callable-adapter and coordinator slices that can establish their full semantics.</p> <p>M1.4 adds the independently testable callable boundary. <code>Proposer</code> and <code>Evaluator</code> ports operate on decoded Python values, and local callable adapters normalize plain or rich returns into immutable <code>ProposalResult</code> and <code>EvaluationResult</code> records. <code>EvidenceDraft</code> carries validated evidence content before the coordinator assigns identity. Evaluator normalization validates finite metrics, non-negative uncertainty, and coverage of the task objectives; additional diagnostic metrics remain valid, while objective direction stays in the task. Callable-reported tokens and wall time remain attributable to the action, but nonzero trial or evaluation counts are invalid because the coordinator owns those counts. When a returned failure is converted to <code>InvalidOutput</code> \u2014 a role-mismatched failure kind or a count violation \u2014 the original failure's kind, message, and details are preserved in the invalid output's details. These boundary records are not durable types and do not expand the root public API or serialization allowlist.</p> <p>M1.5 implements the sequential coordinator and durable conversion. One rich lifecycle projection is the replay validator and derives the smaller policy-facing <code>SearchState</code>. The coordinator commits a causal bootstrap trial without charging a successor attempt, commits successor proposals and artifacts before evaluation, mints evidence identity, preserves lineage across evaluator failure, and accounts for authoritative proposal attempts and evaluator calls.</p> <p>M1.6 records one durable <code>SearchSpec</code> on <code>RunStarted</code>: the policy reference, frozen declared configuration, and authorized proposer. Internal <code>prepare_search()</code> composes that declaration with the executable program and a deterministic identity key. Presets bind themselves structurally; <code>Greedy.bind</code> owns its <code>mutate</code> operator and unit trial allocation, while already executable custom programs pass through unchanged. This keeps preset-specific binding and identity formatting out of <code>run()</code> and lets retained events reproduce built-in Greedy decisions without adding replay to the root API.</p>"},{"location":"implementation-plan/#sequential-coordinator","title":"Sequential coordinator","text":"<pre><code>while True:\n    state = projections.search_state(run_id)\n    decision = search.next(state)\n\n    if isinstance(decision, Stop):\n        coordinator.stop(decision)\n        break\n\n    coordinator.execute(decision)\n</code></pre> <p>For a <code>TrialSpec</code>, the coordinator:</p> <ol> <li>validates authority and remaining budget;</li> <li>derives and records named deterministic random substreams;</li> <li>commits the stable <code>Trial</code> identity with the decision;</li> <li>allocates a local <code>WorkItem</code>;</li> <li>invokes the proposer and commits its proposal before evaluation;</li> <li>captures the successor artifact occurrence;</li> <li>commits the execution <code>TrialOutcome</code> and usage;</li> <li>invokes the evaluator outside candidate authority and records its separate evaluation and evidence.</li> </ol> <p>The seed follows the same causal record chain through a coordinator-owned bootstrap trial at logical step zero. It consumes one evaluation but zero successor trials, so policy logical step one and <code>Greedy.max_trials</code> begin with the first proposer attempt. Projected counts sum the authoritative usage on outcomes and evaluations rather than counting record collections.</p> <p>Operator policies return typed <code>OperatorSpec</code> values. User-facing string shorthands are normalized before the trial is committed.</p>"},{"location":"implementation-plan/#reference-problem-and-exit","title":"Reference problem and exit","text":"<p>Use integer search. The same task, initial artifact, root random seed, policy, and budget must produce the same decisions and projected state. Rebuilding a fresh in-memory projection from the retained event list must reproduce the result; process restart and filesystem persistence remain Milestone 4 concerns.</p> <p>The kernel exits this milestone when trial identity, lineage, budgets, failure taxonomy, deterministic randomness, duplicate completion, replay, and policy replacement have focused tests.</p>"},{"location":"implementation-plan/#milestone-2-replaceable-search-policies","title":"Milestone 2: replaceable search policies","text":"<p>M2.1 establishes the shared durable boundary before adding another preset. <code>ArchiveUpdate</code> is a record-only <code>SearchDecision</code>: projection validates its evaluation, candidate, admission or rejection, optional eviction, and complete rank-ordered membership. <code>PolicyDecisionExplained</code> immediately links rationale to a decision without changing the frozen M1 decision or trial schemas.</p> <p>The runtime commits trials, archive updates, and stops through one application path. Policies may optionally describe a decision and name the random streams they used; the runtime derives the recorded seeds. Deterministic fallbacks keep a custom policy's required contract at <code>next(state)</code> only. The initial shared conformance harness covers Greedy and such a minimal custom policy. Ranking, tournament selection, and composed archive or stopping policies wait for a second concrete use in BestOfN or Population.</p> <p>M2.2 adds <code>BestOfN(max_trials=20)</code> through the same preset-binding and <code>SearchProgram.next(state)</code> contracts as Greedy. It always expands the seed, uses the derived stable top-1 result view, emits no <code>ArchiveUpdate</code>, and binds a <code>mutate</code> operator with a one-trial, one-evaluation allocation narrowed by remaining budget. Its versioned declaration is <code>best-of-n@1</code> with frozen <code>max_trials</code> configuration. The bootstrap evaluation participates in ranking but consumes neither a successor trial nor a policy step.</p> <p>Keep this small implementation parallel to Greedy in M2.2. M2.3 is the extraction checkpoint: Population supplies the third concrete policy shape needed to judge whether shared selection, allocation, stopping, ranking, or archive seams simplify the code. Put Greedy, BestOfN, Population, and a minimal custom policy behind one conformance suite without adding coordinator cases.</p> <p>M2.3 adds <code>Population(size=3, tournament_size=2, max_trials=20)</code> through the same preset contracts. The extraction checkpoint yielded exactly one shared seam: the private stable-ranking primitive <code>rank_evaluations</code> in the domain, which now backs the incumbent view, the derived result view, and population membership. Selection, allocation, and stopping seams stay inlined; three policies did not justify them. Population processes every evaluation through one record-only <code>ArchiveUpdate</code> before any stop, and draws tournament members from the named <code>selection</code> stream with a self-contained integer draw rather than <code>random.Random</code>, whose sampling algorithm may change across Python versions and would break historical decision reproduction. When no member is admitted it stops with <code>population_empty</code> rather than expanding a non-member seed.</p> <p>M2.4 completes the milestone with frozen, slotted <code>RunSummary</code> and <code>RunComparison</code> application views. <code>Run.summary()</code> retains only immutable identities, declarations, usage, content references, and frozen payload values; it carries no storage or run-token dependency. <code>Run.compare()</code> rejects every task, primary-objective, budget, initial-artifact, evaluator, and proposer declaration mismatch independently. Policy declarations/configurations and root seeds may differ, and exact primary scores tie without secondary tiebreaking. The views remain outside the durable serialization allowlist.</p> <p><code>examples/learn/policy_comparison/</code> owns the deterministic 40-seed benchmark and aggregation. A tiny <code>main.py</code> executes it under its canonical local callable identity, while an executed notebook presents the full score, evaluation, and usage distributions plus representative topology. Notebook verification uses locked <code>nbconvert</code> tooling and always writes execution output to a temporary file. The companion executed <code>milestone_2_tour.ipynb</code> is the guided entry point for the completed milestone and keeps internal projection inspection explicitly separate from the simple callable path.</p> <p>The coordinator must not know which policy is active. <code>Selector</code>, <code>Allocator</code>, <code>OperatorPolicy</code>, <code>ArchivePolicy</code>, and <code>StopPolicy</code> may begin as small protocols or values only when at least two implementations justify each seam.</p> <p>Record each decision's policy version, logical identity, observed state reference, named random substreams, and rationale metadata. Comparisons use equal declared evaluation budgets and deterministic tie-breaking.</p>"},{"location":"implementation-plan/#milestone-3-shared-experience","title":"Milestone 3: shared experience","text":"<p>Add the first experience-specific ports only now:</p> <pre><code>class ExperienceGraph(Protocol):\n    def node(self, ref, query): ...\n    def ancestors(self, ref, query): ...\n    def children(self, ref, query): ...\n    def siblings(self, ref, query): ...\n    def frontier(self, query): ...\n    def trace(self, ref, query): ...\n    def state_at(self, query): ...\n\n\nclass ExperienceReader(Protocol):\n    def search(\n        self, *, record_kinds=(), failure_kinds=(), as_of=None, limit=None\n    ) -&gt; ExperienceObservation: ...\n    def open(self, ref, *, as_of=None) -&gt; ExperienceObservation: ...\n    def ancestors(\n        self, trial_ref, *, as_of=None, limit=None\n    ) -&gt; ExperienceObservation: ...\n    def siblings(\n        self, trial_ref, *, as_of=None, limit=None\n    ) -&gt; ExperienceObservation: ...\n    def compare(\n        self, left_trial_ref, right_trial_ref, *, as_of=None\n    ) -&gt; ExperienceComparison: ...\n</code></pre> <p>M3.1 is the read-only temporal projection only. It adds durable <code>ExperienceRef(run_id, kind, occurrence_id)</code> values based on existing occurrence identities and immutable <code>ExperienceRecord</code>, trial-centered <code>ExperienceNode</code>, <code>ExperienceQuery(task_id, run_id, as_of)</code>, and <code>ExperienceState</code> views. <code>replay_experience_graph()</code> first uses the validated run replay path, then attaches the exact committed decision, explanation, proposal, artifact-creation, outcome, evidence, evaluation, and completed-trial records. It does not change coordinator, event-store, artifact-store, existing identity, or event-byte contracts. The #98 conformance closure later root-exported those four values plus <code>ExperienceGraph</code> as the minimal type-complete, read-only surface needed to implement a third-party <code>ContextPolicy</code>; replay construction stays in <code>meta_evolve.application</code>.</p> <p>Parent edges follow declared parent artifact occurrences to their producing trials. Relations preserve parent order and deduplicate; returned nodes sort by <code>(logical_step, trial_id)</code>. Ancestors exclude self, siblings include self, frontier returns visible causal leaves rather than archive members, and a trace covers the target trial plus its accessible ancestors, with records sorted by event sequence plus a fixed kind order. Historical <code>as_of</code> boundaries are inclusive, including bootstrap step zero.</p> <p>One private access predicate enforces matching task and run, inclusive logical time, local occurrence, visibility, and synthetic retraction. All inaccessible direct targets raise the same content-free <code>ExperienceAccessDenied</code>; collection queries omit inaccessible nodes and do not cross them. The hidden, retracted, and non-local sets exist only to test the filtering contract. They are not a public policy abstraction and establish no Milestone 12 governance contract.</p> <p>M3.2 implements <code>NoMemory</code>, <code>AncestorsOnly</code>, and <code>BestSibling</code> with direct <code>max_records=20</code> and <code>max_chars=8_000</code> defaults. <code>None</code> and <code>NoMemory()</code> add no declaration, wrapper, identity input, fact, or trial policy. A non-default <code>ContextSpec</code> is the next run-identity input after the M1/M2 segments and is declared after bootstrap. A generic search wrapper stamps its <code>PolicyRef</code> into successor <code>TrialSpec</code> values; a separate generic coordinator operation selects or reloads the exact bundle. No coordinator branch depends on a policy or graph relation.</p> <p>Without pull access, selection follows <code>PolicyDecisionMade \u2192 PolicyDecisionExplained \u2192 ContextSelected \u2192 ProposalSubmitted</code>; with pull access, grant issuance and audited operations sit between selection and proposal. Replay validates declaration authority, ordering, uniqueness, observed version, strictly prior visible references, and exact source values, then returns the committed bundle without invoking the selector or renderer. Context record/render usage remains separate from task <code>Usage</code>.</p> <p>M3.3 adds run-level <code>PullAccess</code>, durable <code>ExperienceSpec</code> and per-successor <code>ExperienceGrant</code>, scoped pull operations, exact observations/comparisons, and cumulative <code>ExperienceUsage</code>. The serialized access spec follows any <code>ContextSpec</code> in run identity. <code>None</code> contributes no identity segment, event, grant, reader, or compatibility change.</p> <p>The local grant fixes task, run, trial, and <code>as_of=logical_step - 1</code>, then enforces operation, result, record, and independently rendered-character bounds. Each structurally valid call commits an attempt before validation and then exactly one success, denial, or exhaustion. Attempt 21 under the default 20-operation limit is charged as operation 21 and audited as exhausted. Successes store the exact returned value; replay holds each success to the shared deterministic contract against committed sources and returns the committed value without re-running live retrieval, traversal, or rendering logic. The reader holds only its grant, a view of its own committed operations, a grant-scoped graph built once per grant, and a trial-bound append capability for the permitted audit facts. Identity and visibility remain single-user stubs. Principal authorization, licensing, and federation policy are deliberately deferred; future-information leakage and observation recording are not.</p> <p>Evaluate push-only context, pull-only exploration, combined access, and no-memory baselines under equal budgets.</p> <p>M3.4 adds no new lifecycle event. Durable and callable proposals gain an ordered <code>derived_from</code> tuple whose empty default is canonically omitted. A small application contract reconstructs the trial observation ledger from committed context selections and successful pull results, then serves both live completion and proposal replay. The coordinator applies resource exhaustion, derivation policy, and reported proposer failure in that order.</p> <p><code>RunExperienceUsage</code> is a derived application view. It sums committed bundle usage and the final cumulative pull ledger once per grant. <code>RunSummary</code> carries both memory declarations and this projection; <code>RunComparison</code> allows different memory declarations and reports exact equality flags. The canonical <code>examples/learn/experience_reuse/</code> experiment closes the milestone with all four memory variants under identical non-memory declarations and budgets.</p>"},{"location":"implementation-plan/#milestone-4-durable-local-kernel","title":"Milestone 4: durable local kernel","text":"<p>Status: complete (July 24, 2026; #4, #47). The implementation uses filesystem content-addressed artifacts, an append-only SQLite event stream, explicit component manifests, atomic storage composition, and shared replay/resume validation.</p>"},{"location":"implementation-plan/#sqlite-object-and-transition-store","title":"SQLite object and transition store","text":"<p>Store immutable domain objects as normalized append-only records or content-addressed envelopes. Store lifecycle changes in an append-only event ledger. Creation of an object and its corresponding transition event occurs in one transaction.</p> <p>Add:</p> <ul> <li>optimistic event-stream concurrency;</li> <li>durable run, trial, work-item, usage, population, and experience projections;</li> <li>stable idempotency keys and logical steps;</li> <li>exact context bundles and experience-query results;</li> <li>as-of search-state reconstruction;</li> <li>explicit schema versions and migrations.</li> </ul> <p>Object records are authoritative for object content. Events are authoritative for the fact that transitions occurred. Projections are disposable and rebuildable.</p>"},{"location":"implementation-plan/#filesystem-artifact-store","title":"Filesystem artifact store","text":"<pre><code>.meta-evolve/\n\u251c\u2500\u2500 artifacts/sha256/...\n\u251c\u2500\u2500 runs/...\n\u2514\u2500\u2500 meta-evolve.sqlite\n</code></pre> <p>Identical payload bytes reuse content identity while distinct artifact occurrences retain trial, proposal, contributor, and parent lineage.</p>"},{"location":"implementation-plan/#recovery-scenario","title":"Recovery scenario","text":"<p>Terminate a deterministic run at each important transition, reconstruct a new coordinator from disk, resume from committed state, and compare it with an uninterrupted run. Closures are rejected in <code>durable-local</code> mode or cause an explicit capability error before execution; they never silently create a run advertised as resumable.</p>"},{"location":"implementation-plan/#milestone-5-executable-artifacts","title":"Milestone 5: executable artifacts","text":"<p>Status: complete. Milestone 5 is complete through the M5.4 canonical clamp acceptance. The closure adds integrated assertions, an executed tour, and documentation only; it adds no production port, schema, registry, patch format, or authority claim.</p> <p>M5.1 ships the first synchronous source-workspace vertical:</p> <pre><code>class WorkspaceProvider(Protocol):\n    def materialize(self, artifact: object) -&gt; Workspace: ...\n    def capture(self, workspace: Workspace) -&gt; object: ...\n    def dispose(self, workspace: Workspace) -&gt; None: ...\n\n\nclass ProposerRunner(Protocol):\n    component: ComponentRef\n    def validate_run(\n        self,\n        *,\n        artifact_kind: str,\n        artifact_representation: str,\n        objectives: tuple[Objective, ...],\n        budget: Budget,\n        experience: ExperienceSpec | None,\n    ) -&gt; None: ...\n    def execute(\n        self,\n        parents: tuple[Artifact, ...],\n        context: ProposerContext,\n    ) -&gt; ProposalResult: ...\n</code></pre> <p><code>SourceTree</code> uses the stable <code>utf8-tree-v1</code> representation. <code>PatchProposer</code> accepts one exact-file command, while <code>PatchResult</code> carries the same evidence, usage, metadata, failure, and derivation information as an ordinary proposal. A decoded-value runner preserves the original callable path; the coordinator does not branch on either runner.</p> <p><code>DirectoryWorkspaces</code> copies immutable source into a fresh private directory, captures through the same source validation, and disposes before result commitment. A disposal failure is an infrastructure failure and prevents success commitment because M5.1 has no honest mixed success-plus-infrastructure-warning outcome.</p> <p>M5.2 ships the synchronous <code>SandboxProvider</code>, immutable <code>ProcessSpec</code> and <code>ProcessResult</code>, and the clearly named <code>DevelopmentSubprocessSandbox</code>. The POSIX adapter enforces an absolute executable, workspace-contained normalized cwd, exact declared environment, timeout, independent raw-byte output bounds, whole-process-group termination, and lifecycle cleanup. Policy, timeout, resource, and infrastructure failures remain typed; ordinary nonzero exits are not interpreted. Narrow clock and process-operation seams make precedence and cleanup behavior deterministic under test. It makes no claim to secure arbitrary untrusted code. Add Git worktrees only as an optimization.</p> <p>M5.3 ships <code>CommandEvaluator</code>, <code>EvaluationEnvironment</code>, and root-exported <code>ProcessSpec</code>. The evaluator is a declared local callable component and uses the existing callable-evaluator path. Its digest-backed component version commits the complete command, environment, scoring, adapter, and exit-protocol semantics. Task construction invokes a general optional evaluator <code>validate_task</code> hook after normalization; no concrete evaluator branch enters the coordinator.</p> <p>The adapter materializes only a <code>SourceTree</code>, runs the declared process, and always disposes the workspace. Exit <code>0</code> and <code>1</code> produce passing and failing metrics; other exits become <code>EvaluatorFailure</code>. Existing process failures stay typed and non-rankable. Environment and bounded process-result evidence remain separate from metrics, and evaluator usage receives process wall time without claiming coordinator-owned evaluation counts. Cleanup failure overrides prior completion while retaining its classification, evidence, and usage.</p> <p>The evaluator-owned grader remains outside the candidate mutation surface. The development sandbox still makes no hostile-code filesystem-isolation claim; stronger providers remain replaceable at the same structural port.</p> <p>M5.4 pins deterministic content digests and semantic outcomes, while treating measured wall time as occurrence data. Reopened durable storage supports best-artifact and lineage queries; cold resume of these local live components fails explicitly and without side effects. SDK-backed proposers now remain user-owned local callables; portable reconstruction of patch, command, and live SDK components remains later work.</p> <p>The M5-owned file audit splits patch-proposer core behavior from lifecycle/resume failures and sandbox declarations/contracts from process execution. Canonical design documents remain cohesive source-of-truth exceptions to the 300-line guideline, and unrelated pre-M5 suites are not refactored as part of this closure.</p>"},{"location":"implementation-plan/#milestone-6-sdk-proposer-boundary-proof","title":"Milestone 6: SDK proposer boundary proof","text":"<p>Status: Shipped. M6 completed on July 28, 2026 by proving that native coding SDKs could participate through the ordinary proposer seam without a provider-shaped coordinator. The first-party adapters and extras were superseded on August 5 in favor of user-owned SDK callables; the obsolete provider capture archive was removed on August 6. This section records only the contract that remains part of the shipped architecture.</p>"},{"location":"implementation-plan/#retained-contract","title":"Retained contract","text":"<ul> <li><code>ProposerContext</code> carries exact objectives, randomness, pushed context, and   an optional scoped experience reader without evaluator, budget, permission,   or promotion authority.</li> <li>A structural <code>propose(parent, context)</code> object and a plain callable use the   same runner path, validation, typed failures, usage, evidence, and derivation   records.</li> <li>SDK authentication, sessions, tools, permissions, retries, structured   decoding, and provider telemetry belong to the user-owned wrapper.</li> <li>Meta-Evolve retains candidate validation, external evaluation, effective   budgets, durable records, lineage, audited experience, and promotion   authority.</li> </ul>"},{"location":"implementation-plan/#removed-surface","title":"Removed surface","text":"<p>There are no <code>meta_evolve.claude</code> or <code>meta_evolve.codex</code> modules, provider extras, compatibility shims, provider conformance fixtures, or generic live smoke directory. Current opt-in SDK demonstrations are milestone-owned experiments, such as the M8 harness driver, and add no core dependency.</p> <p>The retired adapter implementations and live measurements remain recoverable from Git history and the closed M6 issues. They are historical development evidence, not current tests, documentation fixtures, or acceptance gates.</p>"},{"location":"implementation-plan/#milestone-7-cross-domain-local-v1","title":"Milestone 7: cross-domain local v1","text":"<p>Add the artifact registry and public extension contracts only after number, text, and code examples have exercised them.</p> <p>The common artifact envelope contains semantic kind, representation, schema, interfaces, payload reference, content digest, parent occurrences, and provenance. Domain names such as <code>TextArtifact</code>, <code>GitTreeArtifact</code>, and <code>SkillSetArtifact</code> are typed constructors or views rather than a required inheritance hierarchy.</p> <p>M7 P1+P2 formalized the <code>SourceTree</code> payload shape into an explicit codec protocol, <code>ArtifactCodec</code> (<code>ports/codecs.py</code>): every kind, including the default <code>python-value</code>, now resolves through a per-run <code>CodecIndex</code> (<code>application/codecs.py</code>) instead of a hard-coded dispatch. Declared interfaces resolve through the registered codec (<code>Registry.artifact_interfaces</code>), not through a new field on the <code>Artifact</code> envelope, so this added no durable schema bump \u2014 <code>CURRENT_SCHEMA_VERSION</code> is unchanged. <code>Registry.register_artifact_type</code> rejects every codec offered for an already-built-in <code>(kind, representation)</code> key \u2014 the built-in object included \u2014 so the then-four built-in keys (<code>python-value</code>/<code>canonical-json</code>, <code>source-tree</code>/<code>utf8-tree-v1</code>, <code>text</code>/<code>utf8-text-v1</code>, <code>skill-set</code>/<code>utf8-tree-v1</code>) remain non-overridable. The unit is the key, not the kind: a new representation of a built-in kind is a distinct key and registers like any other custom type.</p> <p>P3 added <code>skill-set</code> (<code>domain/skill_set.py</code>), the first kind to reuse another kind's representation and the first codec with a non-empty <code>interfaces</code> declaration (<code>skill-documents/v1</code>). <code>SkillSet</code> wraps <code>SourceTree</code> construction and adds one rule, layout: every path matches <code>skills/&lt;name&gt;.md</code>. It carries no manifest \u2014 M8's <code>HarnessArtifact</code> defines those against a real consumer \u2014 and no content validation, since content meaning belongs to whoever reads the skills. Its codec is a fourth built-in rather than a registered custom type: the <code>durable-local</code> gate exists because a registered codec is a live object with no verifiable manifest, and a codec shipped with the library is verifiable exactly like the other three, so skill-set runs stay durable and resumable. That registration works for third parties is proven separately by <code>tests/test_artifact_type_registry.py</code>.</p> <p>Proposed candidates route through the same <code>CodecIndex</code> as the seed, in <code>application/proposal_completion.py</code> \u2014 the original plan scoped only seed encoding \u2014 so a candidate the codec rejects becomes a typed <code>InvalidOutput</code> that fails its trial without raising into the run, and a custom artifact type now works through proposer output as well as input. <code>application/runtime.py</code>'s <code>durable-local</code> validation block fails closed with <code>CapabilityError</code> when a task's <code>(kind, representation)</code> is not one of the built-in keys: a registered codec is a live object rather than a source function, so <code>require_source_function</code> cannot reach it, and a resumed run would otherwise rebuild payloads with unverified code.</p> <p>The codec index is a per-run dependency. <code>application/runtime.py</code> and <code>application/resume.py</code> construct it once and return active results through the private <code>Run._reopen(run_id, storage, codecs)</code> path, preserving the exact codec objects used for that run. The public <code>Run(run_id, storage, *, registry: Registry | None = None)</code> constructor builds a read-only index with <code>CodecIndex.with_extras(registry.artifact_types())</code> when a registry is supplied, merging its custom codecs with all built-ins while the built-ins retain precedence. This intentionally exposes every codec in that registry to decoding; the replayed run projection still selects the artifact kind read by each query, and the constructor grants no execution or mutation authority. Without a registry it remains the built-in reopening path. Codec declaration validation lives once in <code>registry_support.validate_artifact_codec</code>: <code>kind</code>, <code>representation</code>, and every interface name are non-empty and whitespace-exact, while an empty interface tuple remains valid. Both registry registration and the published codec suite call that same validator.</p> <p>P4 demonstrated the milestone's exit gate. <code>tests/test_four_domain_kernel.py</code> drives number, text, repository, and skill-set runs through one <code>meta.run</code> call site; everything M7 permits a use case to vary lives in one <code>Domain</code> record, and no assertion is written for a single domain. The four leave an identical kernel record \u2014 same event sequence, usage, scores, logical steps, and audited experience \u2014 while declaring four distinct kinds over three representations, two of which share <code>utf8-tree-v1</code>. No domain needed a coordinator branch, special case, or workaround, and the phase changed no file under <code>src/</code>: the generality was already there to be measured, which is what makes the result a proof rather than a repair.</p> <p>The purity allowlists are decisions, not accidents. Two properties are pinned by <code>tests/test_application_purity.py</code>, each with an explicit allowlist whose every member carries its justification in the test file itself, and each with a companion test that fails once a member stops being needed \u2014 so an exemption cannot quietly outlive its reason.</p> <ul> <li>Imports. Application modules import no concrete adapter except   <code>storage.py</code> (composes the default memory/filesystem/sqlite stores),   <code>unit_of_work.py</code> (owns the atomic durable commit across both stores),   <code>run_support.py</code> (constructs the callable-adapter runners for run and   resume), and <code>runtime.py</code> (wires the public <code>run()</code> entry point). All four   are wiring: something has to name the concrete implementations a default   <code>meta.run</code> composes, and the allowlist exists to keep that set visible and   small rather than to excuse it.</li> <li>Type dispatch. Nothing calls <code>isinstance</code> against a concrete artifact   type outside <code>application/codecs.py</code> and <code>adapters/</code>. The codec is the   artifact-type dispatch \u2014 <code>encode</code> must refuse a value of the wrong type   before it can become a payload \u2014 and adapters translate at the edge, where an   external system's output first has to become a declared artifact value or a   typed failure. The watched set is derived rather than listed: any class in   <code>meta_evolve.domain</code> declaring string <code>KIND</code> and <code>REPRESENTATION</code> \u2014 the pair   <code>Task(artifact=...)</code> reads \u2014 falls under the gate the moment it exists, and a   guard test pins the derived set so a fifth artifact type is a visible   decision rather than a silent widening.</li> </ul> <p>P5a added the milestone's one new storage surface, the read-only listing <code>Storage.runs() -&gt; tuple[RunId, ...]</code>, so the CLI's <code>list</code> verb (and any caller whose ids have left the terminal scrollback) can reach <code>Run</code>, <code>resume</code>, and <code>compare</code> again. Two decisions are worth recording.</p> <p>Enumeration is an optional capability, not a widened port. <code>EventStore</code> keeps exactly <code>append</code> and <code>read</code>; the listing is a separate one-method protocol, <code>ports.RunIndex</code>, that both bundled stores implement. Both required methods are run-scoped, so an implementation can be addressable without being enumerable \u2014 one stream handed in at construction, a backend exposing only <code>GET /runs/{id}/events</code>, a store sharded so that \"all runs\" spans shards. Requiring enumeration of every implementation would earn complexity from a hypothetical rather than the current gate, and would break the hand-built <code>Storage(artifact_store, event_store)</code> pair that <code>LocalOnlyUnitOfWork</code> exists to serve. <code>Storage.runs()</code> checks <code>isinstance(self.events, RunIndex)</code> and raises <code>CapabilityError</code> otherwise; it never reports a non-enumerable store as an empty one. This follows the <code>is_durable</code> principle \u2014 a capability is declared by the component, never inferred from a concrete type \u2014 but not its mechanism: durability needs a separate boolean because it is invisible in a commit strategy's method set, whereas here the capability is the method, so implementing it is the declaration and a parallel flag could only disagree with it.</p> <p>Ids, sorted. The listing returns <code>RunId</code>s rather than a richer record: <code>Run.summary()</code> already owns per-run detail, and a richer listing would force the SQLite backend to decode every envelope just to enumerate. Ordering is lexicographic by id, not insertion \u2014 in-memory insertion order is a <code>dict</code> accident and SQLite has no insertion column short of <code>rowid</code>, an implementation detail unstable across VACUUM and interleaved appends. Sorting makes the two backends provably agree, which is the property callers depend on. Run ids are <code>run-&lt;sha256&gt;</code>, so the order carries no chronological meaning and is documented not to.</p> <p>P5b added <code>meta_evolve.cli</code> and the <code>meta-evolve</code> entry point: five verbs \u2014 <code>run</code>, <code>list</code>, <code>inspect</code>, <code>resume</code>, <code>compare</code> \u2014 over the same public API, using stdlib <code>argparse</code>, no configuration DSL, and no capability logic of its own. A <code>run</code> takes an experiment module exposing one <code>experiment()</code> factory with no import-time side effects; the command line supplies only <code>--storage</code> and <code>--mode</code>. M10.1 made the factory's return a typed <code>Experiment</code>; dictionary returns remain 1.x compatibility with a <code>DeprecationWarning</code>. <code>list</code> is one verb beyond the milestone's four because run ids are content-derived digests rather than names anyone retypes from memory: without enumeration, every other verb is unreachable once an id leaves the terminal. Exit codes are 0 for success, 1 for a domain failure (<code>RunError</code>), and 2 for a usage or experiment-module error; anything else keeps its traceback rather than being flattened into a message.</p> <p>Closed by the M7 defect-repair pass. <code>compare_runs</code> previously refused two incompatible runs with a bare <code>ValueError</code> from <code>_require_equal</code> (<code>application/comparisons.py</code>), which is not a <code>RunError</code> and so escaped the command line's typed-failure translation, reaching the user as a traceback. <code>_require_equal</code> now raises <code>RunsNotComparable</code>, a <code>RunError</code> carrying the <code>declaration</code> that differed as structured data rather than only in its message. The message text is unchanged, so prose matching still works; the command line now reports it as one error line and exit code 1 like every other typed failure.</p> <p>The other four decisions of that pass. The surfaces themselves are <code>docs/programming-model.md</code>'s; what is recorded here is why each landed the way it did.</p> <ul> <li>One accessor, not a second view type. <code>Run.trials()</code> returns the existing   <code>TrialView</code>, which gained a <code>state</code> field set from the projection records   rather than re-derived from other view fields. <code>failure</code> was deliberately   left alone: splitting it is breaking, and it still could not express   <code>\"unevaluated\"</code>, which is an authorization outcome rather than a fault. The   seed trial is included, because three of the six workarounds it replaced   depended on unpacking it.</li> <li><code>CapabilityError</code> is reserved abort control flow, escaping both the   proposer and the evaluator boundary unwrapped. <code>RunError</code> is deliberately   not in either escape tuple: the <code>skill_evolution</code>, <code>prompt_optimization</code>,   and <code>model_migration</code> examples call <code>summary()</code> on an inner run inside their   evaluator, which raises <code>NoSuccessfulEvaluation</code>, so a broad escape would   crash three shipped examples. Because <code>CapabilityError</code> is   publicly exported, the contract binds user code and is documented, not merely   implemented.</li> <li>The name-only report keys on the <code>run-local</code> version sentinel, never on   <code>origin == \"local\"</code>. <code>CommandEvaluator</code> is local-origin and versions itself   with a configuration digest; keying on origin would condemn exactly the local   comparisons that are controlled. <code>ComponentRef.configured_local</code> refuses that   sentinel for the same reason, and derives <code>origin</code> and the retained adapter   rather than accepting them, since <code>Task</code>'s declaration hook admits a component   only when it is local and holds that exact callable by identity.</li> <li>Declarable turn limits keep their existing defaults exactly. The defaults   emit byte-identical native profiles, so no <code>component.version</code> and no Claude   run id moves. <code>native_profile</code> takes the limit keyword-required instead of   choosing between two constants, so a normalizer and a transport cannot   silently disagree about what was sent. Rejected: <code>max(requested, PULL_MIN)</code> \u2014   silently rewriting a chosen number is the defect class being removed.</li> </ul> <p>P6 published the conformance kit and the tutorial.</p> <p>Published conformance covers all seven component types. <code>meta_evolve.conformance</code> ships subclassable pytest suites, on the pandas <code>ExtensionArray</code> model, for artifact codecs (<code>ArtifactCodecTests</code>), repository-editing proposers (<code>RepositoryProposerTests</code>, <code>PullRepositoryProposerTests</code>), evaluators (<code>EvaluatorTests</code>), search programs (<code>SearchProgramTests</code>), context policies (<code>ContextPolicyTests</code>), workspaces (<code>WorkspaceProviderTests</code>), and sandboxes (<code>SandboxProviderTests</code>), alongside the <code>MutationSurfaceTests</code> that M8 introduced and M9 generalized to four surfaces.</p> <p>P6 shipped only the first two. M9 published <code>SearchProgramTests</code>, and the #98 follow-up closed the rest. Three of them existed as function-based suites under <code>tests/</code> (<code>policy_contract.py</code>, <code>workspace_contract.py</code>, <code>sandbox_contract.py</code>) whose laws were already validated against a second implementation; converting those was exposure, not invention, and the function modules were deleted so a published suite and its retired twin cannot drift. <code>policy_contract.py</code> outlived M9's class suite because that suite took a policy instance, which cannot express a program built around the proposer; <code>SearchProgramTests</code> now takes either an instance or a <code>search_factory</code>, so the function module could go and <code>MinimalPolicy</code> runs through the published suite like everything else.</p> <p>The two proposer suites are exercised by in-repository reference callables (<code>tests/test_proposer_conformance.py</code>) \u2014 no first-party provider adapter ships, and the docs say so rather than implying one. The earlier claim that <code>tests/</code> covered \"both providers\" had been false since the user-owned SDK boundary removed those adapters; the reference callables are what make it true again at the level a test can honestly pin. Evaluators and context policies had no reusable suite. <code>ContextPolicyTests</code> derives its laws from what the kernel already enforces, in <code>bundle_violation</code>. <code>ContextPolicy.select</code> receives the root-exported <code>ExperienceGraph</code>, and the query, node, record, and state values its methods exchange are root-exported as one closed read-only surface. Construction through <code>replay_experience_graph</code> remains an internal application service. <code>EvaluatorTests</code> is only partly of that kind: the objective-coverage and typed-failure laws follow <code>adapters/evaluators.py</code>, but stability, order-independence, and non-mutation are not kernel-enforced \u2014 they are obligations an evaluator can break today, which is why the suite is where they are stated. Only the store suite stays internal in <code>store_contract.py</code>: store adapters are not one of the seven extension families.</p> <p>The suites state their limits. <code>ContextPolicyTests</code> does not apply to <code>NoMemory</code>, which <code>meta.run</code> normalizes away before any policy runs, and asks a policy that selects nothing to declare it through <code>expects_records</code> \u2014 an empty bundle is accepted by the kernel, so refusing one outright would have been a law the shipped contract does not have. <code>EvaluatorTests</code> skips its typed-failure law visibly when a subclass declares no <code>rejected_candidates</code>.</p> <p><code>EvaluatorTests</code> also keeps a candidate-mutation law that was first removed on the argument that it could never fail. That argument was wrong, and the counter-example is now a test: <code>decode</code> is only required to return an equal value, so a registered codec that hands back the instance it encoded gives the evaluator the caller's own object to edit. The root cause is closed at the codec end too, by <code>ArtifactCodecTests.mutable_values</code>. That fixture has no default \u2014 <code>()</code> is a positive claim of immutability, the same shape as <code>rejected_values</code> \u2014 because an opt-in law still certifies a codec whose author never considered aliasing. It cannot simply apply to every value: requiring <code>decode(encode(v)) is not v</code> unconditionally would fail the built-in python-value codec on interned small integers, where sharing is harmless. The subclass declares its mutable values outright; the evaluator's mutation law observes through the kernel content digest and needs no shared helper.</p> <p>The <code>*_teeth</code> test modules point a suite at a deliberately broken component and check its complaint, so a suite that stopped asserting would itself fail. They cover codecs, evaluators, search programs, context policies, workspaces, sandboxes, mutation surfaces, and \u2014 through broken reference callables such as a works-once proposer \u2014 the two proposer suites. They also pin the weaknesses review found: a workspace provider that leaks only when two workspaces are live, an evaluator whose drift hides in its uncertainty or evidence rather than its metrics, a sandbox that snapshots the environment in its constructor, a mutation surface that ignores the value it was asked to apply, a field that forges its own projection through <code>__deepcopy__</code>, a policy identity declared only for the kernel's read, a hidden random stream drawn only by the executed instance, an <code>encode</code> that special-cases the objects being watched, a value type whose <code>__eq__</code> certifies a payload collapse, a sandbox that fabricates process results without executing, sandboxes that ignore timeout or either output limit, a sandbox that claims kernel-owned evaluation/trial/token usage, a random stream declared one decision late, a proposer that only works on its first call, a capture that re-reads only the paths it materialized, and a workspace backed by external symlinks.</p> <p><code>tests/</code> subclasses the published classes for every built-in codec, all four mutation surfaces, every shipped search program, both workspace and sandbox providers, every selecting context policy, and \u2014 via the reference callables recorded above \u2014 both proposer suites, so those suites and the kernel's own conformance are the same code and cannot drift. <code>pytest</code> is imported by that subpackage and nowhere else in the package, pinned by an AST scan plus a subprocess check that importing <code>meta_evolve</code> never loads it, and surfaced as the package's one optional extra, <code>conformance</code>.</p> <p>The codec suite checks round trips, rejection signaling, identity independence, repeated calls within one process, and common <code>open</code>/socket entry points. The last two are deliberately scoped evidence: they do not prove arbitrary purity or cross-process reproducibility. A separate built-in test creates artifacts in fresh interpreters under two fixed <code>PYTHONHASHSEED</code> values and compares their content digests.</p> <p>The tutorial is executed, not illustrated. <code>docs/adding-an-artifact-domain.md</code> is written as one script split across fenced <code>python</code> blocks, followed by a single fenced <code>text</code> block holding the output it claims. <code>tests/test_add_a_domain_tutorial.py</code> joins the code back together, runs it in a fresh interpreter from an empty directory with no <code>PYTHONPATH</code>, and compares stdout byte for byte; it also checks that the script imports nothing but the <code>meta_evolve</code> root and the standard library, that every repository path the page links to exists, and that the page's \"six built-in keys\" claim still matches <code>BUILTIN_CODECS</code>. The tutorial's own codec then runs the published <code>ArtifactCodecTests</code>, which is the step the page tells its reader to take. Extraction beat mirroring the code into a test: a mirror drifts silently in exactly the direction that matters, because nothing forces the copy a reader sees to be the copy that ran.</p>"},{"location":"implementation-plan/#milestone-8-harness-evolution","title":"Milestone 8: harness evolution","text":"<p>Status: Experimental/live-only. M8.1\u2013M8.5 and #124 are implemented; the authorized live capture remains the M8.6 closure gate.</p> <p>The canonical deterministic implementation is <code>examples/research/harness_evolution/</code>. M9 uses the <code>policy_evolution</code> example independently of the still-open M8.6 live proof.</p> <p>The whole constructible family lives under <code>meta_evolve.harness</code>. The built-in codec owns <code>agent-harness</code> / <code>harness-manifest-v1</code>; the schema-v1 artifact has exactly <code>planner: TextParam</code> and <code>context_policy: PolicyParam</code>. Both implement the published <code>MutationSurfaceTests</code> contract.</p> <p><code>ArtifactCodec.validate_successor</code> is optional and discovered structurally by <code>CodecIndex</code>. Proposal completion invokes it after encode and before creating an <code>Artifact</code>. The hook sees parent and candidate frozen payloads and returns a typed failure or <code>None</code>; codecs without it are unchanged. This is the one generic seam, and no coordinator module imports a concrete harness type.</p> <p><code>RepositoryEpisode</code> and <code>EpisodeSuite</code> are portable namespaced values. The example owns their execution composition: fresh inner runs, isolated hidden test workspaces, captured evidence, and only token/elapsed roll-up. Evaluator configuration identity covers suite digest, split, worker/model, inner budget, timeouts, environment, and scoring version.</p> <p>Held-out comparison accepts only two seed-only <code>Greedy(max_trials=0)</code> runs and fails closed on any changed control or immutable harness declaration. Export is harness-specific rather than a speculative capability on every codec: it stages beside the destination, reconstructs and verifies the artifact digest, then atomically swaps only absent or valid prior harness exports.</p> <p>The fixture TFD remains user code. The opt-in driver under <code>experiments/08_harness_evolution_live/</code> imports one official SDK directly and adds no package dependency. Its first authorized result is retained as an unchanged tie after both proposal calls failed strict output-schema validation. That outcome is not improved or discarded, but M8.6 remains open because no native mutation reached the trusted successor boundary.</p>"},{"location":"implementation-plan/#milestone-87-audited-prior-candidate-source-inspection","title":"Milestone 8.7: audited prior-candidate source inspection","text":"<p>Status: Design\u2014not shipped. This independent post-M8 program is tracked by</p>"},{"location":"implementation-plan/#150-and-does-not-widen-the-m86-closure-gate-or-m95-it-first-proves-value","title":"150 and does not widen the M8.6 closure gate or M9.5. It first proves value","text":"<p>through existing durable evidence, then locks a Git-neutral source-access manifest before adding audited path reads, canonical source diffs, and an optional disposable read-only Git projection.</p> <p>The capability remains same-task, same-run, strictly prior, occurrence-based, and bounded by <code>ExperienceGrant</code>. Every returned source observation is audited and replayed from committed facts. Git metadata stays outside artifacts and durable identity; no coordinator branch depends on source artifacts or their presentation adapter.</p>"},{"location":"implementation-plan/#milestone-9-policy-evolution","title":"Milestone 9: policy evolution","text":"<p>Status: Shipped. The dependency is M8.1\u2013M8.5, not #124 or M8.6. The deterministic implementation is <code>examples/research/policy_evolution/</code>.</p> <p><code>meta_evolve.mutation</code> owns only the generic <code>MutationApplication</code> result and <code>MutationSurface</code> protocol. Harness keeps compatibility re-exports and all harness-specific values. <code>meta_evolve.policy</code> owns the parallel policy domain: <code>search-policy</code> / <code>policy-manifest-v1</code>, schema-fixed <code>ParentSelection</code> and <code>TrialLimit</code>, one-surface <code>PolicyMutation</code>, portable <code>PolicyEpisode</code> and <code>PolicySuite</code>, captured <code>EpisodeOutcome</code>, aggregate <code>PolicyEvaluation</code>, and strict <code>PolicyComparison</code>. No generic episode/comparison framework was added.</p> <p>The built-in <code>candidate-policy@1</code> interpreter is registered with <code>Registry.register_policy()</code>. Its exact policy-manifest payload is frozen into the <code>SearchSpec</code> configuration. Seed selection always expands the original artifact; incumbent selection uses <code>SearchState.incumbent_id</code>; the voluntary trial limit never widens the run budget. Durable-local run and resume paths resolve it through the unchanged registry seam.</p> <p>The published <code>SearchProgramTests</code> suite covers all four built-in policies. <code>MutationSurfaceTests</code> takes plural <code>(mutation, expected successor)</code> pairs and <code>invalid_mutations</code>, observes successors through an inert canonical-bytes projection rather than the surface's own equality, and runs unchanged against both harness and both policy surfaces.</p> <p>Nested execution remains user-owned composition around ordinary <code>meta.run()</code> calls. Each outcome retains an inner <code>RunId</code> and storage location. Development may reopen an exact completed run and resume a permitted incomplete durable run; held-out execution creates a fresh empty namespace. Aggregation is recomputed from all outcomes, becomes non-rankable on any failed, incomplete, or missing episode, and rolls up only token and elapsed usage.</p> <p>Separate outer <code>Greedy(max_trials=0)</code> runs capture held-out baseline and candidate evaluations. <code>meta.compare_policies()</code> requires equality of every external control and immutable surface declaration; only the two schema-owned surface values may differ. It returns improvement, tie, regression, or unrankable and reports strict promotion eligibility without promoting.</p>"},{"location":"implementation-plan/#milestone-10-local-author-api-and-inspection-experience","title":"Milestone 10: local author API and inspection experience","text":"<p>Status: complete (August 14, 2026; #149). This milestone does not depend on M8.6 or M9.5. Its nine one-PR stages are #151 (M10.0), #152 (M10.1), #165 (M10.2), #164 (M10.3), #153 (M10.4), #154 (M10.5), #166 (M10.6), #156 (M10.7), and #157 (M10.8).</p> <p>The subordinate M10.0 design record owns the six journeys, vocabulary, measurements, and alternatives. Technical staging is:</p> <ul> <li>Shipped \u2014 M10.1: the frozen declaration value lives in   <code>application/experiment.py</code> without moving storage, mode, or the narrowing   budget out of <code>run()</code>. Keyword calls lower to the same declaration. CLI   parsing stays in <code>cli.py</code>; dictionary factories remain supported through 1.x   with a <code>DeprecationWarning</code> beside the typed alternative.</li> <li>Shipped \u2014 M10.2: <code>application/improvement.py</code> implements <code>improve()</code> in   the application callable boundary. It lowers   exactly through <code>Task(evaluator=...)</code>, <code>Experiment</code>,   <code>Greedy(max_trials=trials)</code>, and local <code>run(..., storage=...)</code>; it adds no   second coordinator path and no durable-local claim.</li> <li>Shipped \u2014 M10.3: <code>policies/functional.py</code> implements the curated   <code>@meta.search</code> adapter and shares one private incumbent decision lowerer with   <code>Greedy</code>. The callback never receives kernel state; immutable serializable   configuration flows into <code>SearchSpec</code> identity, and <code>DecisionDescription</code>   derives from the decorated name and cleaned docstring. Population actions   remain on the full extension protocol.</li> <li>Shipped \u2014 M10.4: <code>application/inspection.py</code> owns the immutable aggregate   read model, while a shared <code>application/views.py</code> builder keeps artifact,   evidence, trial-state, and lineage construction single-sourced. One   <code>Run.inspect()</code> call performs exactly one validated replay.</li> <li>Shipped \u2014 M10.5: <code>Storage.read_only()</code> wires dedicated read-only artifact,   blob, event, and unit-of-work implementations. <code>adapters/sqlite/</code> opens with   <code>mode=ro</code>, enables <code>query_only</code>, validates without DDL or migration, and reads   the maximum committed event sequence in a fresh deferred transaction. It does   not use <code>immutable=1</code>, so one handle observes later WAL commits.</li> <li>Shipped \u2014 M10.6: <code>application/session.py</code> owns the small public lifecycle,   while one preparation boundary reconstructs fresh and resumed coordinators.   <code>run()</code> and <code>resume()</code> exhaust Session; authority and budget are fixed at   start, and recovery completes committed work rather than accepting injected   decisions.</li> <li>Shipped \u2014 M10.7: <code>browser/reader.py</code> owns a single-threaded read-only   handle, exact-version catalog cards, and separate 32-entry inspection and   comparison LRUs. <code>browser/app.py</code> supplies escaped server-rendered routes,   same-origin polling, safe failure translation, and security headers over   root-exported APIs only. <code>cli.py</code> conditionally delegates <code>ui</code> to a fixed   loopback, one-worker Uvicorn lifecycle.</li> <li>Shipped \u2014 M10.8: <code>presentation/</code> owns dependency-free inert records,   exact kind/representation dispatch, deterministic preview bounds, semantic   direct-parent diffs, and <code>canonical-payload-v1</code> bytes. <code>ArtifactView</code> retains   its frozen payload without changing decoded value, equality, or repr   semantics. Browser artifact/diff/raw routes resolve occurrences within one   exact inspection snapshot; templates remain escaped, and the CLI's optional   module contributes only its registry for custom-codec reconstruction.   Notebook HTML is owned by the post-M10 presentation follow-up below.</li> </ul> <p>The dependency order is <code>M10.0 \u2192 (M10.1, M10.3, M10.4, M10.5)</code>, <code>M10.1 \u2192 M10.2</code>, <code>M10.1 + M10.4 \u2192 M10.6</code>, <code>M10.4 + M10.5 \u2192 M10.7</code>, and <code>M10.7 \u2192 M10.8</code>.</p> <p>Core installation remains dependency-free. M10.7\u2013M10.8 form the shipped bounded exception to the general dashboard prohibition, and only after M10.4 and M10.5 stabilize the public inspection, query, read-only storage, and version contracts. Its optional <code>meta_evolve.browser</code> package imports those public surfaces only\u2014never <code>Session</code>, <code>run</code>, <code>resume</code>, proposers, evaluators, internal projections, or persistence implementations. <code>cli.py</code> conditionally imports it for the UI command. It binds to loopback, remains read-only, and exposes no operational, mutable, remote, collaboration, or distributed controls.</p> <p>The <code>browser</code> extra contains Starlette, Jinja, and Uvicorn: Starlette supplies tested routing and lifecycle, Jinja supplies escaped templates, and Uvicorn supplies server operation without bespoke web infrastructure. They do not enter core dependencies. Presentation itself remains dependency-free.</p>"},{"location":"implementation-plan/#post-m10-presentation-follow-up","title":"Post-M10 presentation follow-up","text":"<p>Status: Design\u2014not shipped. This future slice owns notebook HTML representations over M10.8's shipped inert records and browser screenshot or visual-regression automation. The safe presentation boundary is a dependency, not permission to claim notebook rendering today. This work is outside the M10 closure gate and requires a separate implementation and acceptance gate.</p>"},{"location":"implementation-plan/#milestone-11-trusted-distribution","title":"Milestone 11: trusted distribution","text":"<p>Status: Design\u2014not shipped. Tracked by #10.</p> <p>Only after local semantics are stable:</p> <ul> <li>add <code>WorkAttempt</code> and durable lease acquisition, renewal, expiration, cancellation, and completion;</li> <li>retain one logical <code>Trial</code> across delivery retries;</li> <li>introduce trusted worker identities and capability matching;</li> <li>add atomic budget reservation and settlement;</li> <li>make idempotent result submission explicit;</li> <li>define consistency requirements per state type;</li> <li>load-test simulated populations before paid agent fleets.</li> </ul> <p>The canonical reservation is <code>examples/research/trusted_workers/</code>.</p> <p>Worker liveness and queue estimates are ephemeral. Accepted outcomes, evaluations, budgets, and promotion decisions are durable. Out-of-order wall-clock completion must not alter logical random choices or reveal future experience.</p>"},{"location":"implementation-plan/#milestone-12-contributed-and-federated-network","title":"Milestone 12: contributed and federated network","text":"<p>Status: Design\u2014not shipped. Tracked by #11 and dependent on M11.</p> <p>Add signed manifests, scoped contributor identities, task-specific trust policies, visibility, licensing, embargoes, attribution, retraction, disputes, and reputation. Exchange policy-filtered subgraphs rather than assuming universal access.</p> <p>References may be redacted, tombstoned, or inaccessible while preserving enough metadata to maintain causal structure. Untrusted execution, evaluator collusion, poisoning, sybil attacks, reward hacking, and correlated replication remain separate threats with separate controls.</p> <p>The canonical reservation is <code>examples/research/federated_exchange/</code>.</p>"},{"location":"implementation-plan/#interface-staging","title":"Interface staging","text":"Capability First implementation Later implementation Proposer local callable SDK agent, human workflow, composite harness Evaluator pure callable isolated grader, simulator, proof checker Artifact store in-memory payloads content-addressed filesystem or object store Event store in-memory list SQLite, then durable service Search greedy and best-of-N populations, Pareto, islands, learned allocation Experience no memory exact graph traversal, then measured semantic retrieval Workspace absent directory copy, Git tree/worktree, remote volume Sandbox absent development subprocess, container, hardened remote sandbox Identity absent or local stub trusted worker, then signed contributor identity"},{"location":"implementation-plan/#what-not-to-build-first","title":"What not to build first","text":"<ul> <li>every module in the destination package tree;</li> <li>a custom graph or vector database;</li> <li>a peer-to-peer protocol or token economy;</li> <li>a thousand-agent deployment;</li> <li>mutable, operational, or remote dashboards, and any browser before stable   public event and query contracts;</li> <li>universal agent communication;</li> <li>learned scheduling without strong baselines;</li> <li>unrestricted self-modification;</li> <li>a template DSL when local closures and typed durable manifests suffice;</li> <li>scientific-discovery claims without domain evidence and replication.</li> </ul>"},{"location":"implementation-plan/#near-term-definition-of-done","title":"Near-term definition of done","text":"<p>Meta-Evolve has a credible first release when a user can define a task, seed artifact, proposer, and evaluator through the canonical small API; choose a search and context policy; run callable and SDK-backed proposers in appropriate execution profiles; evaluate immutable successors; inspect exact causal experience; resume durable runs after failure; and compare runs under equal budgets.</p> <p>Everything beyond that should extend the same contracts rather than replace the kernel.</p>"},{"location":"live-agent-track/","title":"User-owned live SDK track","text":"<p>Status: Experimental/live-only. Meta-Evolve ships no provider client or credentialed launcher. Use the shipped SDK-integration guide to wrap your own client as a callable or structural proposer.</p> <p>The current examples are credential-free and deterministic. Their injection points can be used by an opt-in maintainer driver:</p> Exercise Injection point Maximum wrapper calls Repository proposal <code>examples/agents/user_owned_sdk</code> 1 Experience A/B <code>examples/agents/experience_ab</code> 4 Held-out skill evolution <code>examples/domains/skill_evolution</code> 6 <p>The ceiling is a declaration for a driver, not permission to spend. The driver must identify its wrapper and behavior-affecting configuration, authenticate outside Meta-Evolve, require explicit opt-in, and preserve SDK-specific tools, permissions, timeouts, and telemetry at the edge.</p> <p>Only shared Meta-Evolve facts are comparable: task, seed, evaluator, effective budget, search declaration, durable usage, failures, lineage, and audited experience. A provider or infrastructure failure, timeout, invalid candidate, missing derivation, or grader failure carries no score and must never be converted to zero.</p> <p>There is no official provider capture pending. A user may perform one authorized run with their own wrapper. Any published capture must name the wrapper declaration and retain ties, failures, and regressions honestly. CI uses deterministic fixtures and performs no credentialed execution.</p>"},{"location":"m8-design-capstone/","title":"M8 design capstone","text":"<p>Status: Experimental/live-only.</p> <p>This stable page is a compatibility route. The decision questions, current blockers, authority boundary, and acceptance evidence now live in the M8 harness-evolution design brief.</p> <p>The deterministic example is implemented at <code>examples/research/harness_evolution/</code>; the retained failed capture and remaining live-validation gate are recorded in the design brief.</p> <p>The execution decision is fixed by the user-owned SDK boundary: the flagship takes an injected callable, CI uses a deterministic fixture-backed SDK client, and an opt-in maintainer driver uses the same boundary for an authorized live run. That one sanitized capture must retain failures and regressions honestly. External evaluation, equal budgets, mutation validation, and promotion remain outside user SDK authority.</p>"},{"location":"milestones/","title":"Meta-Evolve Milestones","text":""},{"location":"milestones/#purpose","title":"Purpose","text":"<p>This roadmap grows Meta-Evolve through small, testable vertical slices. Every milestone must produce a runnable demonstration, preserve the contracts established by earlier milestones, and add only the infrastructure required by its acceptance tests.</p> <p>The first product target is a local, extensible improvement system in which a user can define an artifact, task, evaluator, proposer, search policy, and context policy; run a bounded experiment; let agents inspect prior work; replay or resume the run; and compare results under equal budgets. Harness evolution, meta-evolution, trusted concurrency, and federation build on that local product.</p> <p>The harness-evolution demonstration (milestone 8) is the flagship product demo \u2014 \"point this at your agent and eval set, get a measurably better agent, and inspect exactly why each change was kept.\" Earlier milestones should keep that demonstration in view rather than treating it as a distant capstone.</p> <p>Before the numbered milestones, a disposable API spike validated the user-facing interface against both deterministic code and a real model. The spike is a design probe, not a shortcut into the durable implementation. It is complete; see its status below.</p>"},{"location":"milestones/#development-rules","title":"Development rules","text":"<ol> <li>Every milestone has one canonical example that runs with a documented command.</li> <li>Every new port has an in-memory or deterministic reference adapter and a conformance suite.</li> <li>The coordinator contains no branches for particular artifact types, proposers, evaluators, or search policies.</li> <li>Durable behavior is specified through domain events and observable results, not adapter internals.</li> <li>New infrastructure is introduced only when a milestone's proof requires it.</li> <li>Failures remain typed: poor task performance, invalid output, timeout, resource exhaustion, policy violation, and infrastructure failure are not interchangeable.</li> <li>Tests are deterministic by default. Tests requiring credentials, network access, or hardened infrastructure are explicitly separated.</li> <li>Each milestone updates examples, tests, and user-facing documentation together.</li> <li>The programming model is the canonical public API contract; other documents link to it rather than redefining signatures.</li> <li>Canonical examples are first-class user-facing documentation, not test fixtures: each must be short, copy-pasteable, and runnable from a clean checkout, and the simple callable path never requires or surfaces kernel vocabulary such as <code>Trial</code>, <code>OperatorSpec</code>, or <code>WorkItem</code>.</li> </ol>"},{"location":"milestones/#pre-milestone-api-spike","title":"Pre-milestone API spike","text":"<p>Status: complete (July 16, 2026). Both demonstrations ran through the same small public surface under <code>experiments/00_api_spike/</code>, including a real <code>gpt-5.4-mini</code> Codex SDK prompt-optimization run. The exit gate passed: findings are recorded in <code>real_model_findings.md</code> and folded into the programming model's evidence section. Closed by #23.</p>"},{"location":"milestones/#goal","title":"Goal","text":"<p>Exercise the smallest declarative API before durable domain models and package boundaries are fixed.</p>"},{"location":"milestones/#demonstrations","title":"Demonstrations","text":"<p>Build a disposable one-to-three-file implementation that runs:</p> <ol> <li>deterministic integer improvement with <code>Greedy</code> and <code>BestOfN</code>;</li> <li>one real-model prompt-optimization task whose proposer and evaluator each require only a small user function.</li> </ol> <p>The spike should test the canonical <code>Task</code>, <code>meta.run</code>, proposer, evaluator, search, and <code>Run.best()</code>/<code>lineage()</code> surface from the programming model.</p>"},{"location":"milestones/#questions-it-must-answer","title":"Questions it must answer","text":"<ul> <li>Can a plain callable be used as a proposer and evaluator in local mode?</li> <li>Is the distinction between task, seed, proposer, evaluator, and search obvious in working code?</li> <li>Does changing search policy require no change to task or proposer code?</li> <li>Can failures and multi-metric evaluations be represented without exposing orchestration machinery?</li> <li>Which values must become durable records, and which abstractions are premature?</li> </ul>"},{"location":"milestones/#exit-gate","title":"Exit gate","text":"<ul> <li>Both examples run through the same small public surface.</li> <li>The programming model is revised from observed use rather than aspiration alone.</li> <li>Any deliberately unsupported capability is named explicitly.</li> <li>None of the spike's internal structure becomes a kernel contract without Milestone 1 justification.</li> </ul>"},{"location":"milestones/#out-of-scope","title":"Out of scope","text":"<p>Replay, crash recovery, ports and adapters, stable schemas, experience grants, workspace isolation, provider portability, and production-quality model integration.</p>"},{"location":"milestones/#common-definition-of-done","title":"Common definition of done","text":"<p>A milestone is complete only when:</p> <ul> <li>its canonical example runs from a clean checkout;</li> <li>its acceptance tests pass locally and in CI where the required capabilities are available;</li> <li>events, artifacts, evaluations, and policy decisions retain causal lineage;</li> <li>public contracts and durable schemas are documented and versioned;</li> <li>newly added adapters pass their conformance suites;</li> <li>resource use and failure outcomes are observable;</li> <li>no later-milestone infrastructure was added speculatively;</li> <li>the next milestone can build on the result without replacing the kernel.</li> </ul>"},{"location":"milestones/#release-checkpoints","title":"Release checkpoints","text":"Checkpoint Included milestones Meaning Kernel proof 1\u20133 The abstractions compose, policies are replaceable, and agents can reuse prior experience. Durable local engine 4\u20136 Runs persist, real code can execute, and one SDK agent works end to end. Local v1 7 Several artifact domains use one public kernel without special cases. Evolution system 8\u20139.5 Harnesses and search policies can be evaluated as artifacts under external controls, then use replayable iterative frontier and tree search. Local author experience 10 Experiments are easier to declare, diagnose, and inspect through stable typed and optional read-only surfaces. Distributed v2 11 Trusted workers execute concurrently with correct leases, retries, and budgets. Federated direction 12 Independent installations exchange authorized, verifiable experience. <p>The roadmap issue #12 descriptions of M6 closure and the durable-local-engine checkpoint are historical: first-party SDK adapters were superseded before 1.0 by the user-owned callable boundary. The underlying durability and local engine claims remain current.</p>"},{"location":"milestones/#github-tracking","title":"GitHub tracking","text":"<p>The implementation roadmap issue tracks the release checkpoints. Each milestone issue is an acceptance epic and can be decomposed into smaller implementation issues when work begins.</p> Milestone Issue spike #23 \u2014 Pre-milestone API spike (complete) 1 #1 \u2014 Deterministic in-memory kernel (complete) 2 #2 \u2014 Replaceable search policies (complete) 3 #3 \u2014 Shared experience and agent-directed exploration (complete) 4 #4 \u2014 Durable local runs and crash recovery (complete) 5 #5 \u2014 Executable artifacts and external evaluation (complete) 6 #6 \u2014 Native-first SDK proposer portability (complete) 7 #7 \u2014 Cross-domain generality and local v1 (complete) 8 #8 \u2014 Typed harness evolution 8.7 #150 \u2014 Audited prior-candidate source inspection 9 #9 \u2014 Policy artifacts and meta-evolution 9.5 #144 \u2014 Iterative frontier and tree search policies 10 #149 \u2014 Streamline the local author API and inspection experience (complete) 11 #10 \u2014 Trusted concurrency and distributed workers 12 #11 \u2014 Contributed and federated experience exchange"},{"location":"milestones/#milestone-1-deterministic-kernel","title":"Milestone 1: deterministic kernel","text":"<p>Status: complete (July 21, 2026; #1, #32). The canonical number search, exact budget accounting, retained-event projection rebuild, built-in Greedy decision reproduction, failure replay, and the remaining acceptance tests now pass. Restart and resume were deliberately deferred to, and are now shipped by, Milestone 4.</p>"},{"location":"milestones/#goal_1","title":"Goal","text":"<p>Prove the smallest complete improvement loop without language models, subprocesses, databases, or distributed workers.</p>"},{"location":"milestones/#canonical-demonstration","title":"Canonical demonstration","text":"<p>An integer artifact begins away from a hidden target. Deterministic proposers add or subtract fixed values, a pure evaluator rewards proximity to the target, and a sequential coordinator runs until its evaluation budget is exhausted or the target is reached.</p> <pre><code>select parent\n  \u2192 allocate bounded work\n  \u2192 produce proposal\n  \u2192 create successor artifact\n  \u2192 evaluate with evidence\n  \u2192 append events\n  \u2192 update projected state\n  \u2192 choose the next action\n</code></pre>"},{"location":"milestones/#deliverables","title":"Deliverables","text":"<ul> <li>Frozen, serializable domain models for tasks, artifacts, proposals, trials, trial outcomes, evaluations, evidence, budgets, usage, policies, and typed events; a work-item delivery record stays internal to the kernel and out of user-facing types.</li> <li>Opaque typed identifiers and schema versions on durable envelopes.</li> <li>In-memory artifact and event stores.</li> <li>A deterministic callable proposer adapter and pure-function evaluator adapter.</li> <li>A sequential coordinator that validates transitions, authority, and budgets.</li> <li>A minimal <code>SearchProgram.next(state)</code> decision contract; remaining budget is part of projected state.</li> <li>Typed <code>OperatorSpec</code> values produced by operator policies rather than durable raw strings.</li> <li>Logical ordering, deterministic tie-breaking, and recorded named random substreams.</li> <li><code>examples/learn/number_search/</code> with a documented run command.</li> </ul>"},{"location":"milestones/#expected-behavior","title":"Expected behavior","text":"<ul> <li>The same task, initial artifact, policy, seed, and budget produce the same logical decisions and final projected state.</li> <li>Every artifact and evaluation can be traced to the trial, proposal, and search decision that caused it.</li> <li>Trial identity remains distinct from duplicate delivery or completion attempts.</li> <li>Duplicate completion is idempotent.</li> <li>Exhausted budgets prevent additional work.</li> <li>Invalid proposals and evaluator failures are recorded without being treated as low scores.</li> <li>The run can be rebuilt from its event stream without agent-local state.</li> </ul>"},{"location":"milestones/#acceptance-tests","title":"Acceptance tests","text":"<ul> <li>Reachable and unreachable target scenarios.</li> <li>Exact evaluation and usage accounting.</li> <li>Deterministic replay from an empty projection.</li> <li>Decision reproduction with the same named random substreams and trial seeds.</li> <li>Optimistic append conflict in the in-memory event store.</li> <li>Duplicate work completion.</li> <li>Invalid transition and budget-exhaustion rejection.</li> <li>Failure taxonomy serialization and replay.</li> </ul>"},{"location":"milestones/#out-of-scope_1","title":"Out of scope","text":"<p>SQLite, filesystem persistence, subprocesses, real agents, semantic search, snapshots, concurrency, and user interfaces beyond the example command.</p>"},{"location":"milestones/#milestone-2-replaceable-search-policies","title":"Milestone 2: replaceable search policies","text":"<p>Status: complete. M1 supplies the Greedy baseline and the generic preparation boundary. M2.1 adds durable archive decisions, linked decision explanations, and the first reusable policy-contract gate. M2.2 adds the public <code>BestOfN(max_trials=20)</code> preset, independent seed expansion, and historical decision reproduction through the same contract. M2.3 adds the public <code>Population(size=3, tournament_size=2, max_trials=20)</code> preset with explicit rank-ordered archive membership, deterministic tournament selection, and a corridor test showing retention of a temporarily worse branch. M2.4 adds strict compatible-run comparison and the canonical equal-budget three-policy benchmark.</p>"},{"location":"milestones/#goal_2","title":"Goal","text":"<p>Prove that search behavior is a replaceable policy and that different policies can be compared fairly without changing the coordinator.</p>"},{"location":"milestones/#canonical-demonstration_1","title":"Canonical demonstration","text":"<p>Run independent best-of-N, greedy hill climbing, and a small population policy against the same integer task, initial artifact, seed rules, and evaluation budget. Produce a comparison report containing best score, evaluations, usage, and lineage references.</p>"},{"location":"milestones/#deliverables_1","title":"Deliverables","text":"<ul> <li>Independent best-of-N policy.</li> <li>The M1 Greedy baseline covered by the shared M2 policy conformance suite.</li> <li>Small population retention policy.</li> <li>Deterministic selection and tie-breaking rules.</li> <li>Explicit admission, stopping, and budget-allocation decisions.</li> <li>A run-comparison projection or report.</li> <li>One publishable benchmark example: an honest, reproducible equal-budget comparison (for example, greedy with context reuse versus independent best-of-N) presented as a notebook or report a reader can rerun, serving as the project's first credibility artifact.</li> <li><code>examples/learn/policy_comparison/</code>.</li> </ul>"},{"location":"milestones/#expected-behavior_1","title":"Expected behavior","text":"<ul> <li>Policies consume the same projected state and return typed decisions.</li> <li>Replacing a policy requires no coordinator, runtime, evaluator, or storage changes.</li> <li>Every policy decision records its inputs, policy version, rationale metadata, and observed logical step.</li> <li>Comparisons normalize results by the same declared budget.</li> <li>A policy cannot directly mutate projections or grant itself additional budget.</li> </ul>"},{"location":"milestones/#acceptance-tests_1","title":"Acceptance tests","text":"<ul> <li>All three policies pass one shared policy conformance suite.</li> <li>Equal inputs produce deterministic decisions.</li> <li>Tie cases have stable outcomes.</li> <li>Population admission and eviction replay correctly.</li> <li>A historical policy decision can be reproduced without future evaluations.</li> <li>Adding a minimal test policy requires only implementing the policy contract.</li> </ul>"},{"location":"milestones/#out-of-scope_2","title":"Out of scope","text":"<p>Pareto archives, novelty search, islands, bandits, MCTS, learned allocation, and policy evolution.</p>"},{"location":"milestones/#m24-completion","title":"M2.4 completion","text":"<p><code>Run.summary()</code> and <code>Run.compare()</code> are immutable derived views over committed records. Fair comparison requires identical evaluator/proposer declarations, primary objective, task and effective run budgets, artifact kind, and initial artifact declaration and digest. Policy choice, policy configuration, and root seed may differ; exact primary scores tie without a usage or policy tiebreaker.</p> <p>The canonical <code>examples/learn/policy_comparison/</code> sweep uses target <code>12</code>, moves <code>(-2, 1, 5)</code>, initial value <code>0</code>, nine evaluations, eight successor trials, and root seeds <code>0</code> through <code>39</code>. Its executed notebook reports complete score and usage distributions and representative star, chain, and tree-branch lineages, with an explicit small-illustration evidence disclaimer. The companion executed <code>milestone_2_tour.ipynb</code> introduces M2.1 through M2.4 from the public callable path before showing optional kernel-level inspection.</p>"},{"location":"milestones/#milestone-3-shared-experience-and-agent-directed-exploration","title":"Milestone 3: shared experience and agent-directed exploration","text":"<p>Status: complete (July 24, 2026; #3). M3.1 adds the read-only temporal experience projection, M3.2 adds bounded replayable policy-pushed context, M3.3 adds audited, strictly-prior agent-pulled access, and M3.4 adds declared derivation, run-aggregate experience usage, memory-strategy comparison, and the canonical four-variant demonstration.</p>"},{"location":"milestones/#goal_3","title":"Goal","text":"<p>Make prior trials useful to later agents through both policy-pushed context and agent-pulled investigation.</p>"},{"location":"milestones/#canonical-demonstration_2","title":"Canonical demonstration","text":"<p>Agent A produces several failed trials. Agent B searches those trials, opens a relevant proposal and evaluation, compares siblings, and uses the observed failure to choose a successful transformation. The new proposal records the exact experience B observed and used.</p>"},{"location":"milestones/#deliverables_2","title":"Deliverables","text":"<ul> <li>An in-memory <code>ExperienceGraph</code> projection with occurrence-based node,   ancestor, child, inclusive-sibling, causal-leaf frontier, trace, and inclusive   historical <code>state_at</code> queries.</li> <li>A scoped, read-only <code>ExperienceReader</code> with exact filtering and traversal.</li> <li><code>ExperienceGrant</code> bounds for task, run, logical time, result count, context size, and resource use; identity and visibility remain single-user stubs.</li> <li><code>ContextPolicy</code> implementations for <code>NoMemory</code>, <code>AncestorsOnly</code>, and <code>BestSibling</code>.</li> <li>Events for automatic context selection, experience queries, opened records, and comparisons.</li> <li>Immutable <code>ContextBundle</code> records and exact query-result records, including ordering and truncation.</li> <li>Stable references from observed experience into <code>Proposal.derived_from</code>.</li> <li><code>examples/learn/experience_reuse/</code> covering push-only, pull-only, and combined access.</li> </ul>"},{"location":"milestones/#expected-behavior_2","title":"Expected behavior","text":"<ul> <li>Agents distinguish experience that existed from experience they actually observed.</li> <li>An <code>as_of</code> argument may narrow but never widen a work item's logical-time grant.</li> <li>Cross-task, out-of-run, retracted, or future experience is not returned.</li> <li>M3.2 context selection obeys record and rendering bounds without consuming   task usage; M3.3 pull operations count against their <code>ExperienceGrant</code>.</li> <li>The derivation graph remains acyclic while semantic relationships may be typed cross-links.</li> <li>Content deduplication does not collapse distinct causal occurrences.</li> </ul>"},{"location":"milestones/#acceptance-tests_2","title":"Acceptance tests","text":"<ul> <li>Agent B improves by consulting Agent A's recorded failure.</li> <li>Push-only, pull-only, combined, and no-memory variants run under equal budgets.</li> <li>Ancestor, sibling, and historical-state queries return expected results.</li> <li>Attempts to cross task, run, or logical-time boundaries fail.</li> <li>Query result limits and usage limits are enforced.</li> <li>Replay reconstructs the same observed-context references and derived-from edges.</li> </ul>"},{"location":"milestones/#out-of-scope_3","title":"Out of scope","text":"<p>Embeddings, vector databases, learned retrieval, graph databases, filesystem projections, principal-based authorization, licensing enforcement, and cross-installation experience.</p>"},{"location":"milestones/#m31-completion","title":"M3.1 completion","text":"<p><code>replay_experience_graph()</code> derives one trial-centered node per committed trial occurrence through the validated run replay path. <code>ExperienceRef</code> uses the run, record kind, and existing occurrence identity; identical artifact payloads therefore remain distinct causal occurrences. Exact decision, explanation, proposal, artifact-creation, outcome, evidence, evaluation, and completed-trial records are retained without defining another lifecycle.</p> <p>Queries are scoped by task, run, and an optional inclusive logical step. Ancestors exclude self, siblings include self, frontier means visible causal leaves rather than archive membership, and a trace spans the target trial and its accessible ancestors while preserving committed event order with a fixed intra-event kind order. Missing, wrong-kind, cross-scope, future, hidden, retracted, and synthetic non-local direct targets all receive one opaque denial. Collection reads omit inaccessible occurrences and do not traverse through them.</p> <p>Only <code>ExperienceRef</code> is durable and exported through <code>meta_evolve.domain</code>. Graph and query views remain outside the package-root callable API. M3.1 adds no reader grant, pushed context, retrieval, durable graph store, authentication, licensing, or proposal-derivation contract.</p>"},{"location":"milestones/#m32-completion","title":"M3.2 completion","text":"<p><code>meta.run(context=...)</code> accepts <code>NoMemory</code>, <code>AncestorsOnly</code>, <code>BestSibling</code>, or a compatible declared policy. <code>None</code> and <code>NoMemory()</code> preserve pre-M3.2 identity, stream, trial specs, and proposer behavior except that the internal <code>ProposerContext</code> now exposes a synthesized empty bundle. Non-default configuration contributes one final serialized <code>ContextSpec</code> identity segment.</p> <p><code>ContextPolicyDeclared</code> follows bootstrap evaluation. A transparent search-program wrapper stamps its policy reference only into successor specs, and a generic coordinator operation commits one <code>ContextSelected</code> after the decision explanation and before proposer invocation. Bundles snapshot exact typed records, stable reference order, canonical rendering, reason, truncation, and <code>ContextUsage</code>; record and character bounds are independent from task <code>Usage</code>.</p> <p><code>AncestorsOnly</code> flattens accessible ancestors in graph-node and committed-record order. <code>BestSibling</code> excludes the current node, ranks only successful sibling evaluations through <code>rank_evaluations</code>, and preserves completion-order ties. Replay validates the committed records against their strictly prior visible sources and returns the committed bundle without rerunning selection or rendering. Context declaration and selection facts remain inspectable in the experience graph but cannot themselves become policy candidates.</p>"},{"location":"milestones/#m33-completion","title":"M3.3 completion","text":"<p><code>meta.run(experience=...)</code> accepts the run-level <code>ExperienceAccess</code> protocol; <code>PullAccess(max_operations=20, max_results=20, max_records=100, max_chars=32_000)</code> is the shipped declaration. <code>None</code> normalizes completely away. A non-default access declaration contributes the final serialized <code>ExperienceSpec</code> identity segment after any <code>ContextSpec</code>.</p> <p>Every successor receives a fresh immutable <code>ExperienceGrant</code> bounded to the same task/run and <code>as_of=trial.logical_step - 1</code>. Its <code>ExperienceReader</code> supports exact conjunctive search filters, open, ancestors, inclusive siblings, and right-minus-left numeric comparison. Results preserve committed ordering, exact structured snapshots, canonical prefix-bounded rendering, truncation, and cumulative operation/result/record/character usage. Failed evaluations remain inspectable and never become synthetic losses.</p> <p>The audited lifecycle is <code>grant \u2192 (attempt \u2192 success|denial|exhaustion)* \u2192 proposal</code>. Attempts charge before validation, opaque denials disclose no protected content, and over-ceiling calls receive a separate content-free exception. Replay validates authority, placement, reproducible operation identities, cumulative charging, and terminal matching, and holds every success to the shared deterministic contract: records must equal committed sources inside the effective boundary, references must stay visible, deltas must derive from the paired records, and rendering must prefix the canonical rendering of the committed records. Selection, traversal, filtering, and comparison logic is never re-run, and committed outcomes are returned directly. Context and pull-audit facts remain graph-inspectable but are excluded from both push and pull candidate sets, and naming them in a search filter is a malformed request. Pull usage does not change task budgets, logical steps, or named randomness.</p>"},{"location":"milestones/#m34-completion","title":"M3.4 completion","text":"<p><code>Proposal</code> and callable <code>ProposalResult</code> now carry an explicit ordered <code>derived_from</code> declaration. One shared pure validator reconstructs pushed and pulled observations from committed records and requires a unique, strictly-prior same-run subsequence. Resource exhaustion precedes derivation policy failure; both coordinator-imposed failures clear edges and commit no candidate. Valid typed proposer failures and failed independent evaluations retain their declared lineage.</p> <p>Canonical serialization omits empty derivation and rejects an explicitly encoded empty default, so old proposal, event, context, and replay bytes remain unchanged at schema version 1. <code>RunExperienceUsage</code>, <code>Run.experience_usage()</code>, and expanded summaries report context and pull totals. Comparisons continue to require equal task/run budgets and core declarations while allowing different memory strategies, with exact declaration-match flags.</p> <p>The runnable <code>examples/learn/experience_reuse/</code> experiment gives no-memory, push-only, pull-only, and combined variants the same task, proposer, evaluator, chain search, seed, and task/run budget. It records early evaluator failures, then succeeds only when prior failure evidence is reused and cited. Replay reconstructs the same derivation edges. The companion executed <code>milestone_3_tour.ipynb</code> introduces M3.1 through M3.4 from the public callable path, with <code>push_pull_lab.ipynb</code> and <code>derivation_comparison_lab.ipynb</code> as focused kernel-level labs.</p>"},{"location":"milestones/#milestone-4-durable-local-runs-and-crash-recovery","title":"Milestone 4: durable local runs and crash recovery","text":"<p>Status: complete (July 24, 2026; #4, #47). Durable artifacts and events, atomic commit, verified manifests, corruption detection, migration, recovery-frontier replay, and context/experience-aware <code>resume</code> satisfy the acceptance gate without changing the M1\u2013M3 logical identities or trajectories.</p>"},{"location":"milestones/#goal_4","title":"Goal","text":"<p>Persist the kernel without changing its logical behavior and prove that interrupted runs resume safely.</p>"},{"location":"milestones/#canonical-demonstration_3","title":"Canonical demonstration","text":"<p>Start a deterministic run, complete several trials, terminate the process, rebuild the coordinator and projections from disk, resume from the committed frontier, and compare the outcome with an uninterrupted run.</p>"},{"location":"milestones/#deliverables_3","title":"Deliverables","text":"<ul> <li>SQLite append-only event store with optimistic concurrency.</li> <li>Content-addressed filesystem artifact store.</li> <li>Durable run, trial, population, work-item, usage, and experience projections.</li> <li>Stable idempotency keys and committed logical steps.</li> <li>A resume command or documented API.</li> <li>Migration/versioning strategy for durable schemas.</li> <li><code>examples/operations/crash_recovery/</code>.</li> </ul>"},{"location":"milestones/#expected-behavior_3","title":"Expected behavior","text":"<ul> <li>SQLite and in-memory adapters satisfy the same event-store contract.</li> <li>Identical payload bytes reuse content identity while occurrence provenance remains distinct.</li> <li>Only atomically committed object records and transition events affect reconstructed state.</li> <li>Restarting does not repeat completed logical work or lose remaining budget.</li> <li>Historical state is initially reconstructed from immutable object records and transition events; snapshots are optional future caches.</li> <li>Corrupt or incompatible durable data fails explicitly rather than being silently ignored.</li> </ul>"},{"location":"milestones/#acceptance-tests_3","title":"Acceptance tests","text":"<ul> <li>Interrupted and uninterrupted deterministic runs reach equivalent logical results.</li> <li>Restart before and after each important transition.</li> <li>Duplicate completion across a restart.</li> <li>Optimistic concurrency conflicts across two store clients.</li> <li>Artifact deduplication with distinct causal occurrences.</li> <li>Projection rebuild from empty projection tables using durable object records and transition events.</li> <li>Durable schema-version rejection and supported migration tests.</li> </ul>"},{"location":"milestones/#out-of-scope_4","title":"Out of scope","text":"<p>Graph databases, remote databases, object stores, parallel workers, filesystem snapshots, and backup services.</p>"},{"location":"milestones/#milestone-5-executable-artifacts-and-external-evaluation","title":"Milestone 5: executable artifacts and external evaluation","text":"<p>Status: complete (July 24, 2026). M5.4 closes the canonical script, executed notebook, integrated acceptance, durable-query, and documentation gates without adding a production API or widening the development sandbox.</p>"},{"location":"milestones/#goal_5","title":"Goal","text":"<p>Safely enough for development, execute candidate source artifacts in private workspaces while keeping evaluation outside candidate authority.</p>"},{"location":"milestones/#canonical-demonstration_4","title":"Canonical demonstration","text":"<p>A deterministic patch agent improves a small Python function. Each candidate is materialized into its own workspace and evaluated by external unit tests that the candidate cannot modify.</p>"},{"location":"milestones/#deliverables_4","title":"Deliverables","text":"<ul> <li>[x] Immutable <code>SourceTree</code> with stable <code>utf8-tree-v1</code> digesting.</li> <li>[x] Synchronous directory-copy <code>WorkspaceProvider</code> for materialize, capture, and dispose.</li> <li>[x] Exact-file <code>TextPatch</code>, rich <code>PatchResult</code>, and workspace-backed <code>PatchProposer</code>.</li> <li>[x] Generic <code>ProposerRunner</code> seam preserving the decoded callable path.</li> <li>[x] Clearly named development subprocess <code>SandboxProvider</code> with timeouts and captured output.</li> <li>[x] External evaluator adapter with versioned environment and evidence.</li> <li>[x] Typed process outcomes for success, timeout, resource exhaustion, policy violation, infrastructure failure, and task failure.</li> <li>[x] <code>examples/domains/python_function/</code>.</li> <li>[x] Executed <code>milestone_5_tour.ipynb</code> with deterministic semantic output.</li> </ul>"},{"location":"milestones/#expected-behavior_4","title":"Expected behavior","text":"<ul> <li>Every work item receives a private writable workspace derived from an immutable parent.</li> <li>Capturing changes produces a new immutable artifact without altering its parent.</li> <li>Evaluator code and hidden tests remain outside the candidate mutation boundary.</li> <li>Process execution and task evaluation failures remain distinguishable.</li> <li>Workspace cleanup runs after success and failure.</li> <li>Durable local history remains queryable, while cold resume of local   workspace components fails explicitly with <code>CapabilityError</code>.</li> <li>The subprocess adapter makes no claim to safely execute untrusted community code.</li> </ul>"},{"location":"milestones/#acceptance-tests_4","title":"Acceptance tests","text":"<ul> <li>Passing, failing, invalid, and unchanged patches.</li> <li>Parent immutability after workspace capture.</li> <li>Timeout and process-output capture.</li> <li>Workspace cleanup after evaluator or runtime failure.</li> <li>Candidate attempts to modify external evaluator inputs.</li> <li>Stable source-tree digests and artifact round trips.</li> <li>Workspace and sandbox adapter conformance suites.</li> </ul> <p>The acceptance suite pins the broken and repaired content digests, scores <code>0 \u2192 1</code>, process exits <code>1 \u2192 0</code>, evidence kinds, winning lineage, reopened best/lineage equivalence, clean-checkout script output, and temporary notebook re-execution. It also proves a failed cold resume leaves events, artifacts, and both workspace trees unchanged. The exact development boundary is unchanged: patch traversal is rejected, capture excludes the grader, and the canonical run leaves grader bytes intact; hostile-runtime filesystem isolation is not claimed.</p>"},{"location":"milestones/#out-of-scope_5","title":"Out of scope","text":"<p>Containers, hardened untrusted execution, Git worktree optimization, remote volumes, real LLM agents, multi-repository artifacts, and portable runtime reconstruction.</p>"},{"location":"milestones/#milestone-6-sdk-proposer-boundary-proof","title":"Milestone 6: SDK proposer boundary proof","text":"<p>Status: Shipped. M6 completed on July 28, 2026. It proved that native coding SDKs could produce ordinary candidates through the existing proposer seam without adding provider vocabulary or branches to the coordinator.</p> <p>The first-party Claude and Codex adapters, package extras, and provider-specific capture fixtures were superseded before 1.0 by the smaller user-owned SDK boundary and have now been removed. Their detailed implementation and live measurements remain available in Git history and closed issues #57\u2013#93, but they are not part of the current source tree or acceptance surface.</p>"},{"location":"milestones/#shipped-outcome","title":"Shipped outcome","text":"<ul> <li>Plain callables and structural <code>propose(parent, context)</code> objects share one   runner, preflight, accounting, evidence, and failure path.</li> <li><code>ProposerContext</code> exposes exact candidate-visible context and optional   audited experience without task authority.</li> <li>User code owns SDK authentication, sessions, tools, permissions, retries,   output decoding, and provider telemetry.</li> <li>Meta-Evolve owns candidate validation, independent evaluation, budgets,   durable lineage, experience auditing, and promotion authority.</li> <li>No provider dependency, extra, compatibility shim, generic provider runtime,   or provider-specific coordinator behavior ships.</li> </ul>"},{"location":"milestones/#acceptance-boundary","title":"Acceptance boundary","text":"<p>Current deterministic tests prove the structural callable seam, configured component identity, typed SDK-wrapper failures, usage validation, pushed context, audited pull delegation, and declared derivation. Provider behavior is validated only by an explicitly authorized milestone-owned experiment when a current acceptance gate requires it.</p>"},{"location":"milestones/#out-of-scope_6","title":"Out of scope","text":"<p>A first-party provider adapter, cross-provider option model, generic agent runtime, durable provider session, automatic retry, portable live component reconstruction, or provider parity claim.</p>"},{"location":"milestones/#milestone-7-generality-proof-and-local-v1","title":"Milestone 7: generality proof and local v1","text":"<p>Historical annotation. M7's provider-conformance references describe the then-shipped M6 adapters. They were superseded by the user-owned callable boundary on August 5, 2026; M7's artifact-domain and local-kernel acceptance evidence remains current.</p> <p>Status: complete (August 2, 2026; #7, PRs #94\u2013#97). Four artifact domains \u2014 number, text, repository, and skill set \u2014 reach the coordinator through one <code>meta.run</code> call site and leave an identical kernel record, with four distinct kinds over three representations (<code>tests/test_four_domain_kernel.py</code>). No domain needed a coordinator branch, and the proving phase changed no file under <code>src/</code>. A static gate holds the property open: application modules import no concrete adapter outside a justified wiring allowlist, and nothing dispatches on a concrete artifact type outside the codecs and the adapters (<code>tests/test_application_purity.py</code>). The add-a-domain walkthrough is <code>docs/adding-an-artifact-domain.md</code>; its code blocks are one script, executed in a fresh interpreter from an empty directory and compared against the output the page claims, so the documentation walkthrough cannot drift from the library.</p> <p>Three deliverables landed differently from their wording above, and the difference is not absorbed into it:</p> <ul> <li>Extension contracts. M7 added the <code>ArtifactCodec</code> contract and the   optional <code>RunIndex</code> capability, and changed <code>ProposalResult</code> to carry an   artifact value rather than a frozen payload. The runtime, evaluator,   workspace, and sandbox ports and the search- and context-policy contracts   already existed and gained no members here.</li> <li>Commands. The command line ships five verbs, not four: <code>list</code> joins   <code>run</code>, <code>inspect</code>, <code>resume</code>, and <code>compare</code>. Run ids are content-derived   digests rather than names anyone retypes from memory, so without enumeration   every other verb becomes unreachable once an id leaves the terminal. It rests   on one new read-only storage surface, <code>Storage.runs()</code>.</li> <li>Conformance utilities covered two of the seven component types.   At the M7 gate <code>meta_evolve.conformance</code> published suites for artifact codecs   and for repository-editing proposers only. Search policies, workspaces, and   sandboxes kept reusable function-based suites under <code>tests/</code> that were not   published; evaluators and context policies had concrete tests and no reusable   suite at all. An extension author writing any of those five had nothing to   subclass.</li> </ul> <p>Historical annotation. That gap is closed. The follow-up in   #98 converted the   workspace and sandbox contracts, retired <code>policy_contract.py</code> by widening   M9's <code>SearchProgramTests</code> with a <code>search_factory</code> fixture, and added   <code>EvaluatorTests</code> and <code>ContextPolicyTests</code>, so all seven families now have   published subclassable suites alongside the typed <code>MutationSurfaceTests</code>.   The same closure root-exported the read-only <code>ExperienceGraph</code> plus its   query, node, record, and state values so a third-party context policy can   implement the typed protocol without importing replay internals. The function modules <code>tests/policy_contract.py</code>,   <code>tests/workspace_contract.py</code>, and <code>tests/sandbox_contract.py</code> were removed   in the same change rather than left to drift beside their published   replacements. This annotation does not widen the M7 gate, which was accepted   on the two-family state described above.</p>"},{"location":"milestones/#the-defect-repair-pass-july-30-august-2-2026","title":"The defect-repair pass (July 30 \u2013 August 2, 2026)","text":"<p>Building the shipped domain and agent examples and hardening their reopening path exposed six defects in M7's own deliverables. Each is backed by something that actually broke while measuring, not by review speculation, and three turned real measurements into misleading ones. They are recorded here rather than deferred because a milestone that ships a misleading measurement has not met its own acceptance gate.</p> defect what it cost closed by No public accessor for a losing attempt six examples reached into <code>run._projection()</code> or inferred failure from <code>evaluation_count</code>; two invented \"no candidate\" as a reason for what was really a timeout <code>Run.trials()</code> and <code>TrialView.state</code> A missing extra became a per-candidate score a <code>CapabilityError</code> in an evaluator surfaced as <code>NoSuccessfulEvaluation</code>, blaming the candidate for the machine <code>CapabilityError</code> reserved as abort control flow at both boundaries A hardcoded turn limit measured itself three corrupted measurements, including an A/B that lost 7 of 8 control reps <code>Claude(max_turns=\u2026, pull_max_turns=\u2026)</code> <code>compare</code> could not see a model inside an evaluator closure a winner reported for an uncontrolled comparison <code>name_only_components</code> reports it; <code>ComponentRef.configured_local</code> lets a caller close it The comparison refusal was untyped a bare <code>ValueError</code> escaped the command line's translation and reached the user as a traceback <code>RunsNotComparable</code>, a <code>RunError</code> carrying the <code>declaration</code> that differed A stored custom-domain local run could not be decoded after restart durable storage outlived the live codec index, but <code>Run(id, storage)</code> knew built-ins only keyword-only <code>Run(..., registry=...)</code> reconstructs the read-only codec index without granting execution authority <p>Reopening a closed milestone sets a precedent, so the justification is narrow and stated rather than assumed. These are repairs to M7's own shipped surface, found by M7's own acceptance examples, and they add no milestone scope. The passage below about Pi integration and filesystem-shaped experience is not a general convention authorizing this \u2014 it is specific to those two post-M6 follow-ups. Anything that is genuinely new capability belongs in M8.</p> <p>Two things this pass deliberately did not do. It did not raise either turn limit: the defaults emit byte-identical native profiles, so a run recorded before the pass still replays. That statement is about the library's defaults only. Any run that declares a turn ceiling declares a different component, and its <code>component.version</code> and <code>RunId</code> move accordingly \u2014 which is correct, and is why <code>compare</code> refuses two runs whose ceilings differ. The <code>experience_ab</code>, <code>adversarial_inputs</code>, <code>prompt_optimization</code>, and <code>model_migration</code> examples all declare one, so all four moved; <code>model_migration</code>'s evaluator identity moved for a second, independent reason. And it did not make <code>compare</code> infer a configuration it was never told about \u2014 <code>compare</code> still refuses only on what a run declared, which is why the gap is reported as well as made closeable.</p> <p>One tightening in the same pass is not a defect repair, so it is recorded apart from the table. <code>Registry.register_artifact_type</code> now refuses a <code>kind</code>, <code>representation</code>, or declared interface name that is empty or carries outer whitespace, through the same <code>_require_text</code> the rest of the domain uses. An empty <code>interfaces</code> tuple stays valid and means the codec declares no consumer-facing interface. These fields reach durable records and content-derived ids, so registration refuses a bad declaration rather than normalizing it \u2014 a silently trimmed <code>kind</code> would encode artifacts under a name its own author never wrote. The observable change for an extension author is the exception type: a non-string <code>kind</code> raised <code>TypeError</code> before this pass and raises <code>ValueError</code> now, so declaration validity is one error family instead of two.</p>"},{"location":"milestones/#goal_6","title":"Goal","text":"<p>Demonstrate that the kernel is a reusable framework rather than an implementation specialized for one artifact or task domain.</p>"},{"location":"milestones/#canonical-demonstrations","title":"Canonical demonstrations","text":"<p>Run four use cases through the same coordinator:</p> <ol> <li>deterministic number search;</li> <li>text or prompt optimization;</li> <li>repository/code improvement;</li> <li>skill-set evolution.</li> </ol> <p>Only artifact codecs, task definitions, proposers, evaluators, and policy configuration may differ between the examples.</p>"},{"location":"milestones/#deliverables_5","title":"Deliverables","text":"<ul> <li>Public extension contracts for artifact codecs, runtimes, evaluators, search policies, context policies, workspaces, and sandboxes.</li> <li>A small artifact-type registry with schema validation and declared interfaces.</li> <li>Text/prompt and skill-set artifact implementations.</li> <li>Minimal user-facing API and commands to run, inspect, resume, and compare experiments.</li> <li>Adapter conformance-test utilities for extension authors.</li> <li>A concise tutorial for adding a new artifact domain.</li> <li><code>examples/domains/text_optimization/</code> and <code>examples/domains/skill_evolution/</code>.</li> </ul>"},{"location":"milestones/#expected-behavior_5","title":"Expected behavior","text":"<ul> <li>No coordinator branch checks a concrete artifact type, provider, evaluator, or policy.</li> <li>A new domain is added by registering implementations behind existing contracts.</li> <li>All examples expose comparable lineage, budgets, evaluations, and experience views.</li> <li>Users can inspect why a candidate exists, what an agent observed, and why it was evaluated or admitted.</li> <li>A clean local installation can run deterministic examples without provider credentials.</li> <li>Provider-dependent examples fail with actionable setup guidance.</li> </ul>"},{"location":"milestones/#acceptance-tests_5","title":"Acceptance tests","text":"<ul> <li>Run all four domains through one shared kernel test harness.</li> <li>Static or behavioral check that the coordinator imports no concrete adapters or artifact implementations.</li> <li>Create a minimal custom artifact/evaluator in a test without modifying core packages.</li> <li>Public API serialization and schema-version tests.</li> <li>CLI smoke tests for run, inspect, resume, and compare.</li> <li>Documentation walkthrough from a clean environment.</li> </ul>"},{"location":"milestones/#exit-gate_1","title":"Exit gate","text":"<p>If a use case requires a domain-specific coordinator branch, stop and repair the abstraction before beginning harness evolution. Passing this gate defines the local v1 architecture.</p>"},{"location":"milestones/#out-of-scope_7","title":"Out of scope","text":"<p>Complete harness mutation, policy evolution, concurrency, remote workers, federation, dashboards, and plugin marketplaces.</p> <p>Pi integration and filesystem-shaped experience are post-M6 follow-ups. They do not retroactively widen M6's portability or conformance gate and enter M7 only through separately accepted issues.</p>"},{"location":"milestones/#milestone-8-harness-evolution","title":"Milestone 8: harness evolution","text":"<p>Status: Experimental/live-only. The deterministic implementation through M8.5 and #124 is present. M8.6 remains the final blocking gate: M8 is not marked shipped and #8 is not closed. The first authorized official-SDK capture is retained honestly, but strict schema validation rejected both proposal calls before native mutation decoding; the held-out comparison was a <code>0.0 \u2192 0.0</code> tie. The canonical example is <code>examples/research/harness_evolution/</code>.</p>"},{"location":"milestones/#executable-subissue-program","title":"Executable subissue program","text":"<ul> <li>[x] M8.0: lock development-only search, seed-only held-out evaluation,   the core-side successor hook, immutable surface declarations, evaluator   identity, outer-boundary resume, and the one-fixture evidence limit.</li> <li>[x] M8.1: ship <code>agent-harness</code> / <code>harness-manifest-v1</code>, both mutation   surfaces, their published conformance suite, and the core bypass rejection.</li> <li>[x] M8.2: ship portable repository episodes, explicit splits, stable   digests, hidden evaluator tests, captured attempts, and nested accounting.</li> <li>[x] M8.3: implement user-owned structured-output TFD as an ordinary   callable; no core optimizer, provider adapter, or agent runtime was added.</li> <li>[x] M8.4: add strict seed-only held-out comparison and external   eligibility reporting.</li> <li>[x] #124: add staged, digest-preserving, symlink-refusing harness export.</li> <li>[x] M8.5: run the deterministic durable flagship with development quality   <code>0.0 \u2192 0.5 \u2192 1.0</code>, held-out <code>0.0 \u2192 1.0</code>, and committed-episode recovery.</li> <li>[ ] M8.6: retain the first authorized schema-failure capture without   improving its outcome; a future separately authorized run must reach native   mutation decoding and successor validation before M8 ships and #8 closes.</li> </ul>"},{"location":"milestones/#goal_7","title":"Goal","text":"<p>Treat an executable agent harness as a typed composite artifact whose permitted components can be improved under external evaluation and promotion controls.</p>"},{"location":"milestones/#canonical-demonstration_5","title":"Canonical demonstration","text":"<p>A worker harness performs held-out tasks. An outer agent investigates earlier harness source and traces, then modifies one or two declared surfaces\u2014initially a planner prompt and context policy. External evaluation determines whether the successor is admitted.</p> <p>The outer agent is injected as a user-owned callable. CI uses a deterministic fixture-backed SDK client; an opt-in maintainer driver may call a real SDK through the same boundary. The first authorized live run is captured once, sanitized, and retained with ties, regressions, and failures intact. Evaluation, equal budgets, mutation authority, and promotion stay outside the wrapper. See the boundary decision.</p>"},{"location":"milestones/#deliverables_6","title":"Deliverables","text":"<ul> <li><code>HarnessArtifact</code> manifest containing component references and declared interfaces.</li> <li>Typed parameters for initial mutable surfaces such as <code>TextParam</code> and <code>PolicyParam</code>.</li> <li>Explicit <code>MutationSurface</code> separating candidate-controlled components from hidden evaluation, secrets, permissions, budgets, audit history, and promotion policy.</li> <li>Harness materialization, execution, capture, and validation.</li> <li>Held-out task split and promotion rule.</li> <li>Harness-specific comparison views over quality, cost, and failure behavior.</li> <li><code>examples/research/harness_evolution/</code>.</li> </ul>"},{"location":"milestones/#expected-behavior_6","title":"Expected behavior","text":"<ul> <li>A candidate can modify only manifest-declared mutable surfaces.</li> <li>Component references and the composite harness both retain content identity and causal lineage.</li> <li>Harness evaluation runs fresh executions rather than trusting self-reported improvements.</li> <li>Failed or invalid harnesses remain inspectable experience.</li> <li>Later agents can compare complete harness code and traces, not only summary feedback.</li> <li>Additional mutation surfaces can be registered without changing the coordinator.</li> </ul>"},{"location":"milestones/#acceptance-tests_6","title":"Acceptance tests","text":"<ul> <li>Valid prompt and context-policy mutations.</li> <li>Rejection of hidden-evaluator, permission, budget, and promotion-policy changes.</li> <li>Composite artifact digest and provenance tests.</li> <li>Fresh held-out evaluation and deterministic fixture tests.</li> <li>Comparison against the unchanged baseline harness under equal budgets.</li> <li>Resume and replay of a harness-evolution run.</li> </ul>"},{"location":"milestones/#out-of-scope_8","title":"Out of scope","text":"<p>All mutation surfaces at once, unrestricted self-modification, automatic promotion to production, policy meta-evolution, and claims of general recursive self-improvement.</p>"},{"location":"milestones/#milestone-87-audited-prior-candidate-source-inspection","title":"Milestone 8.7: audited prior-candidate source inspection","text":"<p>Status: Design\u2014not shipped. Tracked by #150. This is an independent post-M8 follow-up, not an M8 closure gate, and it does not change M8.6 or M9.5.</p> <p>The ordered program is <code>M8.7.1 \u2192 M8.7.2 \u2192 M8.7.3 \u2192 M8.7.4 \u2192 M8.7.5</code>:</p> <ul> <li>[ ] #155 \u2014 M8.7.1:   prove the value of exact prior source using existing durable evidence.</li> <li>[ ] #158 \u2014 M8.7.2:   lock the public, accounting, replay, and Git-neutral manifest contracts.</li> <li>[ ] #159 \u2014 M8.7.3:   add bounded audited source path listing and reads.</li> <li>[ ] #160 \u2014 M8.7.4:   add a canonical library-owned source diff.</li> <li>[ ] #161 \u2014 M8.7.5:   project authorized manifests into a disposable read-only Git history view.</li> </ul> <p>Source access remains same-task, same-run, strictly prior, occurrence-based, bounded by the existing experience grant, and reconstructed from committed observations. Git is a presentation adapter, never durable identity or part of the candidate artifact. Cross-run libraries, semantic retrieval, whole-tree reads, and coordinator branches remain out of scope.</p>"},{"location":"milestones/#milestone-9-policy-artifacts-and-meta-evolution","title":"Milestone 9: policy artifacts and meta-evolution","text":"<p>Status: Shipped (August 6, 2026). The canonical deterministic example is <code>examples/research/policy_evolution/</code>. M9 depends on the contracts shipped in M8.1\u2013M8.5; it does not depend on #124 or the still-open M8.6 live proof.</p>"},{"location":"milestones/#executable-subissue-program_1","title":"Executable subissue program","text":"<ul> <li>[x] M9.0: lock dependency, authority, reuse, aggregation, and namespace   decisions before implementation.</li> <li>[x] M9.1: ship <code>search-policy</code> / <code>policy-manifest-v1</code>, two fixed mutation   surfaces, neutral mutation contracts, and four-surface conformance.</li> <li>[x] M9.2: register trusted <code>candidate-policy@1</code>, publish   <code>SearchProgramTests</code>, and verify durable reconstruction.</li> <li>[x] M9.3: ship portable policy episodes, explicit splits, captured inner   run references, exact reuse/resume boundaries, and nested usage separation.</li> <li>[x] M9.4: aggregate all outcomes and strictly compare separate seed-only   held-out outer runs without granting promotion authority.</li> <li>[x] M9.5: run the deterministic durable flagship with development quality   <code>6.0 \u2192 7.0 \u2192 8.0</code> and held-out aggregate <code>13.5 \u2192 15.5</code>.</li> </ul>"},{"location":"milestones/#m90-executable-contract","title":"M9.0 executable contract","text":"<ul> <li><code>meta_evolve.policy</code> owns artifact-domain values;   <code>meta_evolve.policies</code> continues to own executable presets.</li> <li>Only generic <code>MutationApplication[SurfaceT]</code> and   <code>MutationSurface[MutationT, SurfaceT]</code> move to <code>meta_evolve.mutation</code>;   harness-specific mutations and parameters stay namespaced under harness.</li> <li>Policy episodes and comparisons remain domain-specific peers of harness   equivalents. No generic episode/comparison framework exists without a third   domain demonstrating it.</li> <li>An episode's existing content-addressed <code>RunId</code> is its sole cache identity.   Aggregates are recomputed; evaluator identity covers the exact suite, split,   scoring, aggregation, budgets, and environment.</li> <li>Fresh development forbids non-identical reuse, while an exact completed run   may reopen. Held-out execution always uses a new empty namespace and bypasses   reuse.</li> <li>Any failed, incomplete, or missing episode makes the aggregate non-rankable   while retaining partial results and typed failures.</li> <li>Inner trials and evaluations remain inner usage. Only tokens and elapsed time   roll into the enclosing evaluation's <code>Usage</code>.</li> <li>Candidates cannot control tasks, seeds, proposers, evaluators, environments,   budgets, split membership, caching, aggregation, promotion, or executable   policy source.</li> </ul>"},{"location":"milestones/#goal_8","title":"Goal","text":"<p>Evaluate search or update policies as artifacts by measuring the improvement episodes they produce on held-out task distributions.</p>"},{"location":"milestones/#canonical-demonstration_6","title":"Canonical demonstration","text":"<p>Two versioned <code>PolicyArtifact</code>s each control fresh inner-loop optimization episodes across training and held-out tasks. An outer policy compares their quality, cost, robustness, and failure rates under equal nested budgets.</p>"},{"location":"milestones/#deliverables_7","title":"Deliverables","text":"<ul> <li>Serializable <code>PolicyArtifact</code> with declared interfaces and mutation boundaries.</li> <li>Nested run/episode relationships and separate inner/outer budgets.</li> <li>Task-distribution artifacts and held-out evaluation splits.</li> <li>Existing <code>RunId</code> cache identity and explicit leakage boundaries.</li> <li>Aggregation of quality, cost, uncertainty, and failure metrics across episodes.</li> <li>External promotion decision for policy candidates.</li> <li><code>examples/research/policy_evolution/</code>.</li> </ul>"},{"location":"milestones/#expected-behavior_7","title":"Expected behavior","text":"<ul> <li>A policy candidate cannot inspect held-out results before its evaluation episode permits them.</li> <li>Inner-loop events retain their own lineage while linking to the outer policy evaluation.</li> <li>Cache hits are valid only when task, artifact, policy, evaluator, environment, seed, and relevant budget semantics match.</li> <li>Promotion uses fresh evidence across multiple tasks or seeds rather than a policy's self-report.</li> <li>Budget exhaustion in an inner episode does not consume undeclared outer authority.</li> </ul>"},{"location":"milestones/#acceptance-tests_7","title":"Acceptance tests","text":"<ul> <li>Compare two deterministic policy artifacts across several task fixtures.</li> <li>Nested budget accounting and exhaustion.</li> <li>Held-out leakage rejection.</li> <li>Cache-key correctness and invalidation.</li> <li>Episode aggregation with failed and incomplete runs.</li> <li>Replay from an outer decision to every supporting inner evaluation.</li> </ul>"},{"location":"milestones/#out-of-scope_9","title":"Out of scope","text":"<p>Learned schedulers, model-weight training inside the kernel, autonomous authority changes, production deployment, and claims that improvement generalizes beyond measured tasks.</p>"},{"location":"milestones/#milestone-95-advanced-search","title":"Milestone 9.5: advanced search","text":"<p>Status: Design\u2014not shipped. M9.5 begins only after the current M8 closure gate passes. It adds a bounded advanced-search slice after shipped M9. The local-author M10 does not depend on it; trusted distribution and federation move to M11 and M12. Tracked by #144. This interstitial milestone is distinct from M9's historical <code>M9.5</code> executable substep, which assembled the already-shipped policy-evolution flagship.</p>"},{"location":"milestones/#ordered-subissue-program","title":"Ordered subissue program","text":"<ul> <li>[ ] #146 \u2014 M9.5.1:   lock primary-objective defaults, frontier metric coverage, multi-member   archive transitions, deterministic ties, named randomness, and   history-derived policy statistics.</li> <li>[ ] #147 \u2014 M9.5.2:   add replayable per-key frontier evolution with deterministic winners and   ownership-weighted parent selection.</li> <li>[ ] #145 \u2014 M9.5.3:   add replayable value-directed tree search with fixed progressive widening   and best-descendant backup.</li> <li>[ ] #148 \u2014 M9.5.4:   publish the deterministic equal-budget five-policy comparison after both   concrete policies land.</li> </ul> <p>Implementation order is <code>M9.5.1 \u2192 (M9.5.2 and M9.5.3) \u2192 M9.5.4</code>.</p>"},{"location":"milestones/#goal_9","title":"Goal","text":"<p>Demonstrate genuine multi-generation evolution through two concrete, replayable search policies: GEPA-style per-key frontier evolution and UCT-style tree allocation with best-descendant backup. Both must pass the existing search-program conformance suite and outperform the appropriate baseline on deterministic, equal-budget fixtures.</p> <p>The proposed public presets are:</p> <pre><code>meta.PerKeyFrontier(\n    metrics=(\"case:0\", \"case:1\", \"case:2\"),\n    max_trials=20,\n)\n\nmeta.TreeSearch(\n    max_trials=100,\n    exploration=2**0.5,\n)\n</code></pre> <p><code>BestOfN</code> remains the one-generation equal-budget sampling control. M9.5 does not reopen the closed novelty, surrogate, or plateau-escalation proposals in #16, #17, or #18.</p>"},{"location":"milestones/#contract-boundary","title":"Contract boundary","text":"<ul> <li>Shipped default ranking, stopping, and incumbent selection use   <code>Task.objectives[0]</code>; successful evaluations may retain additional metrics.</li> <li><code>PerKeyFrontier</code> binds a fixed ordered metric-key tuple. Every successful   candidate must contain each configured key; extra metrics are ignored and a   missing key is invalid search history.</li> <li>Archive membership remains authoritative. Existing records keep replaying;   membership deltas may remove several members without a new schema version or   <code>removed_member_ids</code> field. <code>evicted_artifact_id</code> is <code>None</code> for zero or   multiple removals and identifies the member for exactly one removal.</li> <li>Per-key winners, ownership weights, tree visits, subtree values, and   normalization inputs reconstruct from durable history. M9.5 deliberately   accepts derived-state recomputation cost instead of adding hidden caches.</li> <li>Stable tie rules and named decision randomness make replay and   cross-process reconstruction deterministic.</li> <li>Failed evaluations remain typed outcomes: they never become frontier members   or synthetic low tree rewards, though failed tree attempts still count as   visits.</li> </ul>"},{"location":"milestones/#canonical-demonstration_7","title":"Canonical demonstration","text":"<p>Extend the deterministic policy comparison to run <code>BestOfN</code>, <code>Greedy</code>, <code>Population</code>, <code>PerKeyFrontier</code>, and <code>TreeSearch</code> under identical task, seed, evaluator, proposer, randomness, and budget records. A frontier fixture retains specialists long enough to discover a later-generation aggregate winner that equal-budget <code>BestOfN</code> misses. A delayed-reward fixture shows greedy becoming trapped while tree search explores the better branch. Report score, lineage depth, evaluations, failures, and exact resource usage without credentials or paid providers.</p> <p>Replay equivalence is a conformance invariant. The milestone's behavioral evidence is improvement on these controlled equal-budget fixtures.</p>"},{"location":"milestones/#acceptance-tests_8","title":"Acceptance tests","text":"<ul> <li>Existing archive records replay, valid multi-removal transitions validate,   and malformed membership deltas fail deterministically.</li> <li>Frontier specialists remain selectable, one candidate may displace several   obsolete members, missing dimensions fail, and extra dimensions are valid.</li> <li>Frontier replay, resume, and cross-process reconstruction select identical   parents.</li> <li>Tree tests pin node selection, visit accounting, progressive widening,   deterministic normalization, direction handling, and Q-value backup.</li> <li>Failure-heavy tree branches remain representable without fabricated scores.</li> <li>Both presets pass conformance, replay, and resume suites.</li> <li>Both deterministic fixtures demonstrate the intended later-generation or   delayed-reward advantage under equal budgets.</li> <li>Examples, API references, guides, architecture, programming model, and   implementation plan agree before the status changes to Shipped.</li> </ul>"},{"location":"milestones/#out-of-scope_10","title":"Out of scope","text":"<p>Pareto archives, novelty search, islands, learned allocation, live-provider evidence, and extracting <code>ParentSelector</code>, <code>Archive</code>, <code>BackupRule</code>, or similar protocols. Revisit a shared protocol only after both concrete policies expose a repeated independently testable seam.</p>"},{"location":"milestones/#milestone-10-local-author-api-and-inspection-experience","title":"Milestone 10: local author API and inspection experience","text":"<p>Status: complete (August 14, 2026; #149). This milestone does not depend on M8.6 or M9.5.</p>"},{"location":"milestones/#goal_10","title":"Goal","text":"<p>Make local experiments easier to declare and pace, search algorithms easier to author, failures easier to diagnose, and artifact evolution easier to inspect. The public narrative is: proposers create candidates, evaluators judge them, search decides what to try next, and Meta-Evolve records the entire process. The subordinate M10.0 design record owns the six journey definitions, vocabulary decisions, and measured scorecard. The core remains dependency-free. An optional browser remains read-only and loopback-only, with no execution, resume, cancellation, promotion, deletion, remote collaboration, or distributed-operations authority.</p>"},{"location":"milestones/#ordered-subissue-program_1","title":"Ordered subissue program","text":"<ul> <li>[x] #151 \u2014 M10.0:   lock the local-author journeys, vocabulary, friction budget, compatibility   promises, and bounded exception to the historical dashboard warning.</li> <li>[x] #152 \u2014 M10.1: Shipped. An immutable typed <code>Experiment</code> declaration preserves the   existing keyword-based <code>run</code> call and dictionary CLI compatibility through   1.x with a <code>DeprecationWarning</code>.</li> <li>[x] #165 \u2014 M10.2: Shipped. The minimal <code>improve()</code> callable path lowers exactly through   <code>Task</code>, <code>Experiment</code>, bounded <code>Greedy</code>, and local <code>run()</code>.</li> <li>[x] #164 \u2014 M10.3: Shipped. Functional incumbent refinement is available through a curated   <code>@meta.search</code> fa\u00e7ade over the full <code>SearchProgram.next()</code> protocol, with   immutable identity-visible configuration and shared Greedy lowering.</li> <li>[x] #153 \u2014 M10.4: Shipped. One immutable aggregate <code>Run.inspect()</code> read model covers   complete, incomplete, rankable, and unrankable histories through exactly one   authoritative validated replay.</li> <li>[x] #154 \u2014 M10.5: Shipped. Genuinely read-only durable storage supports immutable inspection   and exact per-run committed stream versions while observing live WAL commits.</li> <li>[x] #166 \u2014 M10.6: Shipped. Paced and resumable <code>Session</code> execution is the sole synchronous   dispatcher; <code>run()</code> and <code>resume()</code> exhaust it without changing durable   history.</li> <li>[x] #156 \u2014 M10.7: Shipped. An optional server-rendered local run browser uses only public   inspection, comparison, read-only storage, and version APIs. One dedicated   thread owns storage access; exact-version bounded caches avoid unnecessary   replay; loopback-only routes expose no execution or mutation authority.</li> <li>[x] #157 \u2014 M10.8: Shipped. Frozen-payload presentation, bounded direct-parent diffs, exact   canonical attachments, occurrence-aware browser pages, optional custom-codec   reconstruction, and the accessible visual system complete the read-only   artifact history. Notebook HTML and screenshot automation belong to the   explicit post-M10 presentation follow-up below.</li> </ul> <p>The dependency order is:</p> <pre><code>M10.0 \u2500\u252c\u2192 M10.1 \u2192 M10.2\n       \u251c\u2192 M10.3\n       \u251c\u2192 M10.4 \u2500\u252c\u2192 M10.6 \u2190 M10.1\n       \u2502          \u2514\u2192 M10.7 \u2192 M10.8\n       \u2514\u2192 M10.5 \u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>Each child shipped through one focused PR.</p>"},{"location":"milestones/#acceptance-gate","title":"Acceptance gate","text":"<ul> <li>M10.0 publishes the six measured journeys and decisions in the   design record, meets both functional-search   reduction thresholds, adds no proposed API, and preserves the uncreated   <code>examples/research/trusted_workers/</code> reservation.</li> <li>Dictionary CLI factories remain supported through 1.x; M10.1 owns introducing   the typed alternative and its <code>DeprecationWarning</code>.</li> <li>Equivalent beginner, typed, keyword, and functional-search declarations   retain the required honest identities and identical durable facts,   decisions, stop reasons, and effective budgets where equivalence is claimed.</li> <li>Functional search exposes only opaque seed/incumbent handles, immutable   declared configuration, <code>expand()</code>, and <code>stop()</code>; captured configuration may   not remain invisible to identity. Decision narration follows the #164 child   contract.</li> <li>One stable inspection view handles successful, failed, incomplete, and   unrankable runs without winner-only assumptions. It exposes resolved defaults   and immutable decision views reusable by paced execution.</li> <li>Read-only storage performs no migration or filesystem mutation and exposes   the maximum committed event sequence when supported.</li> <li><code>Session</code> fixes authority and budgets at start, commits and completes one   decision per step, returns the committed terminal decision idempotently, and   preserves byte-identical history across uninterrupted and resumed execution.</li> <li>The optional browser imports only public inspection and storage APIs, polls   versions before replay, binds only to loopback, and exposes no mutable or   operational controls or imports of execution surfaces.</li> <li>Occurrence identity remains distinct from content digest; artifact views and   diffs never execute stored active content. Notebook HTML representations and   screenshot automation belong to the post-M10 presentation follow-up.</li> <li>Examples, public exports, tests, and canonical documentation match each   surface before its status changes to Shipped.</li> <li>The documentation entrance labels the scorecard as an authoring-friction   measurement of proposed syntax, never optimization or runtime evidence.</li> </ul>"},{"location":"milestones/#out-of-scope_11","title":"Out of scope","text":"<p>Provider transcripts that were not durably recorded; remote or distributed dashboards; execution or governance controls; authentication claims; and frontier or tree presentation before M9.5 policies ship. Outer/inner loops, generations, and episodes remain relative explanatory terms rather than new core types. Deferred and rejected author surfaces are recorded once in the M10.0 design record.</p>"},{"location":"milestones/#post-m10-presentation-follow-up","title":"Post-M10 presentation follow-up","text":"<p>Status: Design\u2014not shipped. This follow-up owns notebook HTML representations built on the shipped inert presentation contract, plus browser screenshot automation or visual-regression evidence. It depends on the safe presentation boundary shipped in M10.8, is not part of the M10 closure gate, and adds no current notebook-rendering claim. Implementation requires its own future acceptance gate before either capability may be described as shipped.</p>"},{"location":"milestones/#milestone-11-trusted-concurrency-and-distribution","title":"Milestone 11: trusted concurrency and distribution","text":"<p>Status: Design\u2014not shipped. Tracked by #10.</p>"},{"location":"milestones/#goal_11","title":"Goal","text":"<p>Execute work concurrently on trusted workers without corrupting history, budgets, population state, or promotion decisions.</p>"},{"location":"milestones/#canonical-demonstration_8","title":"Canonical demonstration","text":"<p>Several worker processes claim bounded work through leases. Workers finish out of wall-clock order, one lease expires, and one result is submitted twice. The reconstructed logical run preserves one trial across delivery retries, contains one accepted outcome, and has correct budget accounting.</p>"},{"location":"milestones/#deliverables_8","title":"Deliverables","text":"<ul> <li>Durable work states and atomic lease acquisition, renewal, expiration, cancellation, and completion.</li> <li><code>WorkAttempt</code> identities that remain distinct from their logical <code>Trial</code> and <code>WorkItem</code>.</li> <li>Worker capability and resource declarations.</li> <li>Idempotent result submission and retry policy.</li> <li>Atomic budget reservation and settlement.</li> <li>Scheduler matching work requirements to trusted workers.</li> <li>Explicit consistency rules for durable facts, leases, heuristic frontier statistics, and promotion.</li> <li>Multi-process load and fault-injection test harness.</li> <li><code>examples/research/trusted_workers/</code>.</li> </ul>"},{"location":"milestones/#expected-behavior_8","title":"Expected behavior","text":"<ul> <li>At most one completion has logical effect for an idempotency key.</li> <li>Expired workers cannot overwrite an accepted result.</li> <li>Out-of-order completion does not create future-information leakage.</li> <li>Durable events and evaluations are strongly inserted; heuristic statistics may tolerate documented staleness.</li> <li>Promotion and budget accounting use authoritative state.</li> <li>Local and distributed runs preserve the same task, artifact, trial, work, evaluation, and event semantics.</li> </ul>"},{"location":"milestones/#acceptance-tests_9","title":"Acceptance tests","text":"<ul> <li>Competing lease claims.</li> <li>Lease renewal and expiration races.</li> <li>Duplicate and late result submission.</li> <li>Worker termination during execution and evaluation.</li> <li>Atomic budget reservation under concurrency.</li> <li>Projection rebuild after out-of-order events.</li> <li>Load test with simulated trusted workers before paid agent fleets.</li> </ul>"},{"location":"milestones/#out-of-scope_12","title":"Out of scope","text":"<p>Arbitrary untrusted community code, Byzantine workers, public federation, reputation economies, and universal strong consistency.</p>"},{"location":"milestones/#milestone-12-contributed-and-federated-network","title":"Milestone 12: contributed and federated network","text":"<p>Status: Design\u2014not shipped. Tracked by #11 and dependent on M11's trusted-worker boundary.</p>"},{"location":"milestones/#goal_12","title":"Goal","text":"<p>Allow independent installations to contribute and exchange authorized agents, evaluators, artifacts, evidence, and experience subgraphs with verifiable provenance.</p>"},{"location":"milestones/#canonical-demonstration_9","title":"Canonical demonstration","text":"<p>Two Meta-Evolve installations exchange a signed, policy-filtered experience bundle. The receiving installation verifies identity and content, imports only authorized records, preserves source provenance and restrictions, independently evaluates a promoted candidate, and processes a later retraction.</p>"},{"location":"milestones/#deliverables_9","title":"Deliverables","text":"<ul> <li>Signed manifests for contributors, agents, evaluators, tasks, environments, and artifact schemas.</li> <li>Scoped identities and task-specific trust policies.</li> <li>Canonical export/import format for policy-filtered experience subgraphs.</li> <li>Verification of signatures, content digests, schema compatibility, causal closure, and access labels.</li> <li>Provenance, licensing, embargo, attribution, and retraction propagation through derivation edges.</li> <li>Independent or redundant evaluation for promotion-sensitive results.</li> <li>Minimal dispute and reputation projections based on recorded evidence.</li> <li><code>examples/research/federated_exchange/</code> using two local installations.</li> </ul>"},{"location":"milestones/#expected-behavior_9","title":"Expected behavior","text":"<ul> <li>Import never grants broader access than the source export policy and receiving trust policy allow.</li> <li>Missing private ancestors can be represented by verifiable redacted references without fabricating lineage.</li> <li>Invalid signatures, digests, schemas, or causal references are rejected.</li> <li>Imported evaluations are evidence, not automatic local promotion.</li> <li>Retractions and permission changes identify dependent artifacts, datasets, skills, and checkpoints.</li> <li>Operators may keep private events while publishing permitted claims and artifacts.</li> </ul>"},{"location":"milestones/#acceptance-tests_10","title":"Acceptance tests","text":"<ul> <li>Successful signed export/import between two installations.</li> <li>Tampered payload, signature, digest, and causal-edge rejection.</li> <li>Private, embargoed, and redacted source handling.</li> <li>Conflicting evaluator results and independent verification.</li> <li>Retraction propagation through derived records.</li> <li>Duplicate import idempotency and schema-version negotiation.</li> <li>Threat-model review covering code execution, evaluator collusion, poisoning, sybil behavior, reward hacking, and correlated replication.</li> </ul>"},{"location":"milestones/#out-of-scope_13","title":"Out of scope","text":"<p>Token economies, unrestricted peer-to-peer execution, global consensus, claims that containers alone secure untrusted code, and scientific validity without domain-governed evidence and replication.</p>"},{"location":"milestones/#architecture-gate-across-all-milestones","title":"Architecture gate across all milestones","text":"<p>The following test should remain true throughout development:</p> <pre><code>new use case\n  = artifact codec and schema\n  + task and evaluator\n  + agent or mutator\n  + search/context configuration\n  + optional infrastructure adapters\n\nnew use case\n  \u2260 coordinator branch\n  \u2260 new durable semantics\n  \u2260 bypassed authority boundary\n</code></pre> <p>When a new domain does not fit, first determine whether it needs a general protocol capability or whether it lies outside evaluated artifact-improvement loops. The kernel should grow only for the former.</p>"},{"location":"operations-and-extensions/","title":"Operations and extensions","text":"<p>Status: Shipped.</p> <p>Start with two runs in one durable store:</p> <pre><code>uv run meta-evolve run examples/operations/command_line/experiment.py --storage ./store\nuv run meta-evolve run examples/operations/command_line/best_of_n.py --storage ./store\nuv run meta-evolve list --storage ./store\n</code></pre> <p><code>run</code> imports one side-effect-free <code>experiment()</code> factory returning <code>meta.Experiment</code> and prints the new run's summary. <code>list</code> returns the stored run IDs. Use those IDs next:</p> <pre><code>uv run meta-evolve inspect &lt;greedy-run-id&gt; --storage ./store\nuv run meta-evolve compare &lt;greedy-run-id&gt; &lt;best-of-n-run-id&gt; --storage ./store\n</code></pre> <p><code>inspect</code> reports one summary, lineage, and best artifact. <code>compare</code> first checks that the task, initial artifact, budget, and proposer declarations are compatible. It then reports both runs and their winner. The BestOfN module reuses the first module's declarations so only search behavior changes.</p> <p>For an optional server-rendered view over the same read-only public queries:</p> <pre><code>uv sync --extra browser\nuv run meta-evolve ui --storage ./store --port 8000\n</code></pre> <p>The local browser guide documents its fixed loopback scope, exact-version caches, rendering exclusions, and absence of authentication.</p> <p>The canonical <code>examples/operations/command_line/</code> walkthrough pins this entire transcript through subprocess tests.</p>"},{"location":"operations-and-extensions/#resume-is-a-declared-capability","title":"Resume is a declared capability","text":"<p>Create a reconstructable run, then resume it:</p> <pre><code>uv run meta-evolve run examples/operations/command_line/resumable.py \\\n    --storage ./store --mode durable-local\nuv run meta-evolve resume &lt;run-id&gt; \\\n    --module examples/operations/command_line/resumable.py --storage ./store\n</code></pre> <p>The module registers source-digestible proposer and evaluator functions. On resume, its factory rebuilds the registry and the library verifies the recorded component identities. A <code>local</code> run may use durable storage and remains listable and inspectable, but it is not resumable.</p> <p>The command line adds no capability rules. It passes <code>--mode</code> to <code>meta.run</code> and lets <code>meta.resume</code> accept or refuse the recorded run. The programming model owns the five report-verb mappings, factory convention, modes, and exit codes.</p> <p>For a real interrupted-process workflow, continue with the <code>examples/operations/durable_operations/</code> walkthrough.</p>"},{"location":"operations-and-extensions/#add-a-local-artifact-domain","title":"Add a local artifact domain","text":"<p>The add-a-domain tutorial builds and executes a <code>Recipe</code> value, codec, registration, task, proposer, and evaluator. Its custom codec runs through the published codec suite.</p> <p>That codec is a live Python object. It has no durable component manifest. In M9, <code>mode=\"durable-local\"</code> therefore accepts only the six built-in <code>(kind, representation)</code> keys and rejects a registered custom codec before writing events. There is no silent fallback to local execution.</p> <p>A custom-domain <code>local</code> run may still write to <code>Storage.durable(...)</code>. After a restart, rebuild the same registry and open it through <code>meta.Run(run_id, storage, registry=registry)</code>. This grants decoding authority only. It does not make the run resumable.</p> <p>The CLI cannot perform that reopening. <code>inspect</code> and <code>compare</code> accept no registry argument, so they can decode built-in domains only. Use Python for a stored custom domain. This is a reconstruction limit, not a codec or storage failure.</p>"},{"location":"operations-and-extensions/#conformance-every-extension-component-family","title":"Conformance: every extension component family","text":"<p>Install the opt-in test extra before importing the published suites:</p> <pre><code>uv sync --extra conformance\n</code></pre> <p><code>meta_evolve.conformance</code> publishes a reusable suite for each of the seven extension component families, plus the typed mutation-surface contract that M8 and M9 grew:</p> Component family Reusable coverage today Artifact codecs Published <code>ArtifactCodecTests</code> Repository-editing proposers Published <code>RepositoryProposerTests</code> and <code>PullRepositoryProposerTests</code>; exercised by in-repo reference callables Evaluators Published <code>EvaluatorTests</code> Search programs Published <code>SearchProgramTests</code>; Greedy, BestOfN, Population, CandidatePolicy, and a <code>next</code>-only custom program Context policies Published <code>ContextPolicyTests</code> Workspaces Published <code>WorkspaceProviderTests</code> Sandboxes Published <code>SandboxProviderTests</code>; cwd/environment/argv/streams, timeout, independent byte bounds, partial output, and wall-only usage Typed mutation surfaces Published <code>MutationSurfaceTests</code>; four built-in implementations <p>Store adapters keep an internal <code>tests/store_contract.py</code>. It is not one of those extension families and is not published from <code>meta_evolve.conformance</code>.</p> <p>Published means an extension author can subclass the suite from an installed package. Internal means repository tests reuse assertions that are not part of the installed extension surface. Every published suite is subclassed inside <code>tests/</code> \u2014 the built-ins where they exist, and in-repository reference callables for the two proposer suites, whose first-party provider adapters were replaced by the user-owned SDK boundary (no provider adapter ships). The published-conformance section of the extensions guide owns suite contents and limitations. Its migration section shows the required pre-1.0 codec and mutation-surface fixture updates.</p> <p>A published suite states what it does not check as carefully as what it does. <code>EvaluatorTests</code> skips its typed-failure law, visibly, when a subclass declares no <code>rejected_candidates</code>; <code>ArtifactCodecTests</code> does the same for its aliasing law when <code>mutable_values</code> is <code>()</code>; <code>ContextPolicyTests</code> excludes <code>NoMemory</code>, which <code>meta.run</code> normalizes away before any policy runs, and asks a policy that selects nothing to declare it through <code>expects_records</code> rather than pass in silence.</p> <p>One law is easy to think redundant and is not. <code>EvaluatorTests</code> checks that scoring does not edit the candidate. With the built-in codecs an evaluator only ever sees a frozen value decoded from the stored payload, so it cannot reach the caller's object \u2014 but a codec is user-supplied, and one that returns the instance it was handed gives the evaluator the real thing.</p> <p><code>ArtifactCodecTests.mutable_values</code> is how you say which of your values can be modified in place; the codec suite then checks \u2014 through your <code>value_state</code> and <code>mutate</code> methods, never through the codec's own <code>encode</code> \u2014 that <code>decode</code> hands back an independent object. Every value comparison in the suite goes through <code>value_state</code> rather than the value type's <code>__eq__</code>, and <code>rejected_payloads</code> demands the decode-side refusals that make \"decode is validation\" true. It has no default, like <code>rejected_values</code> \u2014 <code>()</code> is a positive claim that your values are immutable, not a way to skip the question. The law cannot simply apply to everything, because the built-in python-value codec decodes a small integer to the very same interned object and that is harmless. So the two suites cover the same failure from both ends: the codec law where the leak starts, the evaluator law where it does damage.</p>"},{"location":"operations-and-extensions/#lab-questions","title":"Lab questions","text":"<ol> <li>Why can a local custom-domain run survive on disk but still require the live    registry to decode after restart?</li> <li>Which CLI verb first makes an interrupted run reachable when its ID never    reached terminal output?</li> <li>If you implement a search program that has to be built around the proposer,    which of <code>SearchProgramTests</code>' two fixtures do you override, and why can the    other one not express it?</li> </ol> <p>Continue to the optional, paid bounded live-agent track only when you can authenticate both providers and accept up to 12 calls.</p>"},{"location":"programming-model/","title":"Meta-Evolve Programming Model","text":""},{"location":"programming-model/#status","title":"Status","text":"<p>Status: Shipped. This is the canonical contract for the public API available from the current package. The generated API reference owns signatures and parameter details; the guides below own task-oriented usage. Milestone 9 is shipped. The deterministic M8.1\u2013M8.5 implementation is in the package; final M8 closure remains Experimental/live-only. The first authorized official-SDK capture is retained, but it failed schema validation before returning a native mutation, so it did not satisfy M8.6.</p> <p>M10.1's immutable <code>Experiment</code> declaration and M10.2's minimal <code>improve()</code> callable are Shipped. M10.3's bounded <code>@search</code> author fa\u00e7ade and M10.4's winner-independent <code>Run.inspect()</code> aggregate are also Shipped. M10.5's read-only durable storage and exact stream versions are Shipped. M10.6's paced and resumable <code>Session</code> execution is Shipped. M10.7's optional browser and M10.8's artifact presentation, diffs, and canonical attachments are Shipped. The parent M10 milestone is complete as of August 14, 2026.</p> <p>Milestone 5 is complete: immutable source trees, exact patches, bounded local processes, and independent command evaluation compose through the same callable path. Milestone 7 completes the cross-domain extension boundary. User-owned SDK wrappers use that same callable path; no first-party provider adapter is shipped.</p>"},{"location":"programming-model/#the-smallest-useful-mental-model","title":"The smallest useful mental model","text":"<p>The smallest complete run names four inputs and a bound:</p> <pre><code>import meta_evolve as meta\n\nitems = (\"outline\", \"draft\", \"review\")\n\ndef complete_one(completed):\n    return (*completed, items[len(completed)])\n\nresult = meta.improve(\n    seed=(),\n    proposer=complete_one,\n    evaluator=len,\n    trials=3,\n)\n\nprint(result.best().value)\n</code></pre> <p><code>improve()</code> is exactly declaration sugar for the expanded path:</p> <pre><code>task = meta.Task(evaluator=len)\nexperiment = meta.Experiment(\n    task=task,\n    seed=(),\n    proposer=complete_one,\n    search=meta.Greedy(max_trials=3),\n)\nresult = meta.run(experiment)\n</code></pre> <p>Use the expansion when you need custom artifacts, objectives, satisfaction thresholds, task budgets, search, context, experience, execution mode, or a registry. It adds one declaration level:</p> <pre><code>Task       what better means, including its independent evaluator\nSeed       the initial immutable artifact\nProposer   how a successor is suggested\nSearch     which parent receives the next unit of work\n</code></pre> <p>For 1.x compatibility, the source-compatible declaration-keyword spelling lowers through the same <code>Experiment</code>; new author material uses the typed form:</p> <pre><code>result = meta.run(\n    task=task,\n    seed=(),\n    proposer=complete_one,\n    search=meta.Greedy(max_trials=3),\n)\n\nprint(result.best().value)\n</code></pre> <p><code>Experiment</code> owns the eight declaration fields and defaults. Storage, mode, and a narrowing call-level budget are execution overlays on <code>run()</code>, never experiment fields.</p> <p>The returned <code>Run</code> is both the result and the query surface for aggregate inspection, trials, lineage, evidence, usage, failures, summaries, and comparisons.</p> <p>Safe frozen-payload presentation, direct-parent diff rules, and exact current canonical-payload-v1 bytes are specified by the browser guide.</p> <p><code>run()</code> exhausts the same synchronous Session available for paced execution. Session fixes the experiment, effective budget, components, and authority at start. Each <code>step()</code> commits one decision or completes one owed by recovery, then returns its immutable <code>DecisionView</code>; it accepts no injected decision. Bootstrap is complete before <code>Session.start()</code> returns and is not a policy step.</p> <p>Durable storage has two independent axes: <code>is_durable</code> means committed facts live on durable media, while <code>is_read_only</code> means this handle has no commit or execution authority. <code>Storage.durable(path)</code> is durable and writable; <code>Storage.read_only(path)</code> is durable and read-only. <code>Storage.run_version(id)</code> returns the maximum committed event sequence, or <code>0</code> when that well-formed id has no events. The latter is a polling result, not authoritative run loading: opening a required missing run still raises <code>RunNotFound</code>.</p>"},{"location":"programming-model/#canonical-public-surface","title":"Canonical public surface","text":"<p>The curated reference partitions shipped root exports into four tiers. Root imports are public; kernel records remain internal unless a guide explicitly describes an extension protocol.</p> <p>The constructible harness family stays under <code>meta_evolve.harness</code>: <code>HarnessArtifact</code>, <code>HarnessMutation</code>, <code>MutationSurface</code>, <code>TextParam</code>, <code>PolicyParam</code>, <code>MutationApplication</code>, portable repository episodes, comparison views, and export views. Only <code>compare_harnesses</code>, <code>export_harness</code>, and <code>load_harness</code> are root conveniences.</p> <p>The neutral <code>MutationApplication</code> and <code>MutationSurface</code> contracts live under <code>meta_evolve.mutation</code>; harness keeps compatibility re-exports. The data-only M9 family lives under <code>meta_evolve.policy</code>: <code>PolicyArtifact</code>, its two fixed surfaces and mutation, <code>CandidatePolicy</code>, portable episodes/suites, captured outcomes/evaluations, and comparison views. <code>compare_policies</code> is the single root convenience.</p>"},{"location":"programming-model/#shipped-guides","title":"Shipped guides","text":"<ul> <li>Runs and results \u2014 the callable story and query views.</li> <li>Evaluation and budgets \u2014 objectives, evidence, usage, and typed failures.</li> <li>Experience \u2014 no memory, pushed context, audited pull, and derivation.</li> <li>Durability \u2014 storage, reopen, recovery, and resume.</li> <li>SDK integration \u2014 user-owned callable wrappers and audited tools.</li> <li>Command line \u2014 run, list, inspect, resume, compare, and browse.</li> <li>Local run browser \u2014 optional server-rendered read-only inspection.</li> <li>Extensions \u2014 codecs, registries, component identity, and conformance.</li> <li>Policy evolution \u2014 trusted policy artifacts, nested capture, and held-out comparison.</li> </ul>"},{"location":"programming-model/#execution-profiles","title":"Execution profiles","text":"<p><code>local</code> accepts live Python components and defaults to isolated in-memory storage. <code>durable-local</code> requires durable storage plus registered, reconstructible components and records enough identity to resume honestly through either <code>Session.resume()</code> or <code>resume()</code>. The proposed distributed profile is design material, not a shipped execution mode.</p>"},{"location":"programming-model/#authority-and-failure-boundary","title":"Authority and failure boundary","text":"<p>Candidates may propose artifact content. They cannot change the evaluator, effective budget, permissions, hidden data, audit history, or promotion rule. Invalid output, proposer/evaluator failure, timeout, exhaustion, policy violation, and infrastructure failure remain typed, inspectable outcomes. A failure with no valid measurement is never ranked as a low score.</p> <p>Harness candidates receive a second, trusted check after codec encoding and before artifact creation. The built-in harness codec compares the encoded successor with its one parent and rejects changed component references, interfaces, parameter names or types as <code>PolicyViolation</code>; malformed and no-op transitions are <code>InvalidOutput</code>. Wrapper-side mutation helpers are convenience, not authority.</p>"},{"location":"programming-model/#compatibility-anchors","title":"Compatibility anchors","text":"<p>These headings preserve important links from the former long-form contract. Each now routes to its single current owner.</p>"},{"location":"programming-model/#evidence-from-the-pre-milestone-spike","title":"Evidence from the pre-milestone spike","text":"<p>Historical spike evidence remains linked from the milestone roadmap.</p>"},{"location":"programming-model/#the-command-line","title":"The command line","text":"<p>See the CLI guide.</p>"},{"location":"programming-model/#conformance-suites-for-extension-authors","title":"Conformance suites for extension authors","text":"<p>See the extensions guide.</p>"},{"location":"programming-model/#one-evaluated-trial","title":"One evaluated trial","text":"<p>See evaluation and budgets.</p>"},{"location":"programming-model/#the-durable-search-contract","title":"The durable search contract","text":"<p>See runs and results and durability.</p>"},{"location":"programming-model/#randomness-and-replay","title":"Randomness and replay","text":"<p>Named random streams and replay guarantees are covered by durability.</p>"},{"location":"programming-model/#composable-policy-pieces","title":"Composable policy pieces","text":"<p>The shipped presets are documented in runs and results.</p> <p>For bounded single-parent refinement, an author may declare the parent choice without receiving kernel state or constructing trials:</p> <pre><code>@meta.search(max_trials=20)\ndef incumbent_refinement(search):\n    return search.expand(search.incumbent or search.seed)\n</code></pre> <p>The callback sees only opaque seed and optional incumbent occurrence handles, immutable declared configuration, <code>expand()</code>, and <code>stop()</code>. The adapter owns proposer binding, the unit budget, the objective and exhaustion envelope, named <code>trial</code> randomness, and durable explanation. Behavior-affecting configuration belongs on the decorator; closure capture is rejected because it would be invisible to run identity. Population-shaped actions and archive decisions remain on the full <code>SearchProgram.next()</code> extension protocol.</p>"},{"location":"programming-model/#independent-best-of-n","title":"Independent best-of-N","text":"<p>See <code>BestOfN</code> and the search guide.</p>"},{"location":"programming-model/#greedy-improvement","title":"Greedy improvement","text":"<p>See <code>Greedy</code> and the search guide.</p>"},{"location":"programming-model/#population-and-evolutionary-search","title":"Population and evolutionary search","text":"<p>See <code>Population</code> and the search guide.</p>"},{"location":"programming-model/#pareto-and-quality-diversity-archives","title":"Pareto and quality-diversity archives","text":"<p>See future search designs.</p>"},{"location":"programming-model/#islands-and-migration","title":"Islands and migration","text":"<p>See future search designs.</p>"},{"location":"programming-model/#bandit-allocation-and-tree-search","title":"Bandit allocation and tree search","text":"<p>See future search designs.</p>"},{"location":"programming-model/#experience-aware-agents","title":"Experience-aware agents","text":"<p>See the experience guide.</p>"},{"location":"programming-model/#inner-and-outer-loops","title":"Inner and outer loops","text":"<p>See meta-evolution design.</p>"},{"location":"programming-model/#policy-meta-evolution","title":"Policy meta-evolution","text":"<p>See the shipped policy-evolution guide.</p>"},{"location":"programming-model/#budget-nesting","title":"Budget nesting","text":"<p>Single-run accounting is in evaluation and budgets. M9's captured nested accounting is in the policy-evolution guide: inner trials and evaluations remain inner usage, while only tokens and elapsed time roll up.</p>"},{"location":"programming-model/#other-workflows-through-the-same-loop","title":"Other workflows through the same loop","text":"<p>Shipped text, source, skill-set, and custom domains use the same <code>meta.run()</code> boundary. Scientific and network directions are explicitly separated under Design.</p>"},{"location":"programming-model/#the-simplicity-test","title":"The simplicity test","text":"<p>A common first run should remain <code>improve(seed, proposer, evaluator, trials)</code>. A common expansion should remain one Task, one Experiment, and one <code>meta.run(experiment)</code> call. New internal protocols must not make that public story larger.</p>"},{"location":"references/","title":"Research and Systems References","text":"<p>This is a working map of neighboring ideas. Meta-Evolve should implement, compose, and compare these approaches rather than rename one of them as a universal solution. For a plain-language comparison with AlphaEvolve and GEPA's <code>optimize_anything</code>, see How Meta-Evolve compares.</p> <p>Claims about capabilities in these papers belong to the cited systems. Their inclusion here does not mean Meta-Evolve has reproduced their results.</p>"},{"location":"references/#experience-and-cumulative-improvement","title":"Experience and cumulative improvement","text":""},{"location":"references/#experience-graphs-trellis","title":"Experience Graphs / Trellis","text":"<p>Experience Graphs: The Data Foundation for Self-Improving Agents treats the causal state produced by long-horizon search\u2014attempts, executable artifacts, prompts, tool outputs, rewards, siblings, search statistics, and lineage\u2014as governed database state.</p> <p>Important implications for Meta-Evolve:</p> <ul> <li>stateless agents can recover and collaborate through durable shared experience;</li> <li>online search, causal traversal, semantic reuse, temporal reconstruction, observability, and training views operate over one logical model;</li> <li>the system must preserve the state observed when a decision was made, not only final mutated search statistics;</li> <li>experience retrieval is a policy with an exploration-versus-anchoring tradeoff;</li> <li>derived skills and training views inherit provenance and access constraints.</li> </ul> <p>The paper reports preliminary domain-specific results for cross-session reuse in kernel optimization, including faster early convergence and fewer buggy nodes, while aggressive reuse reduced strategy diversity and missed the best final candidate. This motivates controlled context-policy experiments rather than a general claim that more memory always improves search.</p> <p>Trellis overlaps with Meta-Evolve's data plane, not its entire product scope. Meta-Evolve focuses additionally on programmable harness and search abstractions, execution adapters, contributed agents/evaluators/compute, verification, and federation.</p>"},{"location":"references/#programming-and-optimizing-lm-systems","title":"Programming and optimizing LM systems","text":""},{"location":"references/#dspy","title":"DSPy","text":"<p>DSPy provides a declarative programming model for modular LM programs and optimizers that compile prompts and demonstrations against metrics.</p> <p>Meta-Evolve should integrate DSPy in both directions:</p> <ul> <li>wrap a DSPy program as a versioned harness component;</li> <li>use a DSPy optimizer for an inner prompt/program compilation loop;</li> <li>expose Meta-Evolve examples and evaluators as DSPy-compatible data and metrics;</li> <li>capture compiled programs as immutable artifacts;</li> <li>let an outer Meta-Evolve policy compare DSPy with other optimizers.</li> </ul> <p>The boundary is: DSPy programs LM pipelines; Meta-Evolve programs and evolves populations of executable systems, including DSPy pipelines.</p>"},{"location":"references/#gepa","title":"GEPA","text":"<p>GEPA uses reflective textual mutation and Pareto selection to optimize LM components from task feedback. It is a reference for <code>TextArtifact</code> mutation, evidence-aware reflection, and Pareto retention.</p>"},{"location":"references/#feedback-descent","title":"Feedback Descent","text":"<p>Feedback Descent motivates structured comparison feedback followed by feedback-derived prompt edits. The research lab isolates that mechanism without claiming to reproduce its results.</p>"},{"location":"references/#autodata","title":"AutoData","text":"<p>AutoData motivates evolving data-generation recipes with solver-based quality signals. The local lab's same-model/different-context restricted/informed setup is explicitly a teaching simplification and confounder.</p>"},{"location":"references/#evoskill","title":"EvoSkill","text":"<p>EvoSkill motivates failure-driven discovery, consolidation, and held-out selection of reusable skills. Meta-Evolve should express this as evolution of <code>SkillArtifact</code> and <code>SkillSetArtifact</code> objects rather than a special memory subsystem.</p>"},{"location":"references/#meta-harness","title":"Meta-Harness","text":"<p>Meta-Harness moves optimization beyond prompts to the source code and control logic surrounding a fixed model. It motivates <code>HarnessArtifact</code>, typed mutation surfaces, full execution history, and external authority boundaries.</p> <p>Its agentic proposer can selectively inspect the source, scores, and execution traces of prior candidates through a filesystem instead of receiving only compressed feedback. Meta-Evolve should preserve this agent-directed exploration as a scoped <code>ExperienceReader</code> capability alongside policy-pushed context. A read-only filesystem projection can provide compatibility for coding agents, while structured graph tools add causal traversal, logical-time queries, provenance, and governance.</p>"},{"location":"references/#shepherd","title":"Shepherd","text":"<p>Shepherd is a runtime substrate that records an agent's model actions, tool calls, and environment changes as a reversible, forkable execution trace. Its unit of control is an execution in progress; Meta-Evolve's unit of evolution is an immutable artifact evaluated between trials. The two models are therefore adjacent rather than interchangeable.</p> <p>A useful composition would keep Meta-Evolve in charge of tasks, search, budgets, independent evaluation, lineage, and promotion while a user-owned Shepherd-backed proposer supplies isolated execution, retained workspace output, and trace-level evidence. Any such integration should begin as an external adapter experiment: the captured artifact remains durable truth, trace references are supporting evidence, and restored or forked execution must not bypass trial or usage accounting. Shepherd's visual-artifact notebooks are also a useful teaching reference for deterministic-first examples, optional live runs, and progressive disclosure of execution internals.</p>"},{"location":"references/#asari-inference-stack-optimization","title":"Asari inference-stack optimization","text":"<p>Asari's inference-optimization report describes many agents improving a pinned vLLM serving stack through a discover/build, evaluate, verify, and self-improve loop. It reports changes across kernels, schedulers, load balancing, and configuration, using hierarchical performance benchmarks, measurement-noise controls, and token-distribution correctness checks.</p> <p>A bounded Meta-Evolve version maps cleanly onto current seams:</p> <ul> <li>a small <code>SourceTree</code> overlay holds candidate kernel and configuration edits;</li> <li>a user-owned SDK proposer investigates and changes that overlay;</li> <li>a custom evaluator builds it against an evaluator-owned base, verifies model   behavior, and returns performance metrics, uncertainty, evidence, and usage;</li> <li>context or audited experience exposes earlier profiler and failure evidence;</li> <li>search retains alternatives while lineage records which evidence informed   each change; and</li> <li>an external decision, never the candidate, owns production promotion.</li> </ul> <p>The shipped <code>CommandEvaluator</code> is only a binary, single-metric exit-code adapter, so a trustworthy experiment needs a domain evaluator that parses repeated benchmark samples. A local sequential precursor is feasible, but a whole-stack implementation also needs typed mutation surfaces and portable episodes from M8 and M9, plus trusted parallel GPU workers from M11. Cross-run knowledge transfer must be introduced explicitly rather than silently widening M3's same-run experience boundary.</p> <p>Meta-Evolve does not reproduce Asari's results. The scoped future work is tracked in issue #118: start with one kernel on one trusted GPU, preserve null and negative results, and defer many-agent whole-stack optimization until its dependencies ship.</p>"},{"location":"references/#evolutionary-and-meta-level-search","title":"Evolutionary and meta-level search","text":""},{"location":"references/#aide2","title":"AIDE\u00b2","text":"<p>Weco's AIDE\u00b2 report describes a bi-level experiment in which an outer autoresearch loop rewrites an inner research agent, evaluates each rewrite across heterogeneous tasks under a fixed cost, and selects using private scores. Its reported empirical claims belong to that system. Meta-Evolve's autoresearch teaching example demonstrates only the small nested-run, budget, evidence, and split boundaries; it is not a reproduction or evidence of recursive self-improvement.</p>"},{"location":"references/#alphaevolve","title":"AlphaEvolve","text":"<p>AlphaEvolve demonstrates coding-agent-driven evolutionary search for algorithmic and scientific problems. It is a reference for program artifacts, evaluator-guided evolution, and populations of executable candidates.</p>"},{"location":"references/#funsearch","title":"FunSearch","text":"<p>FunSearch combines LLM-generated programs with automated evaluation and evolutionary selection for mathematical discovery. It illustrates why executable artifacts and objective evaluators are powerful while also showing that the evaluator and representation bound the discovery process.</p>"},{"location":"references/#openevolve","title":"OpenEvolve","text":"<p>OpenEvolve is an open evolutionary coding-agent framework and useful implementation reference for program databases, evaluation, and search loops. Meta-Evolve should seek adapter compatibility where practical rather than duplicate APIs without benefit.</p>"},{"location":"references/#evox","title":"EvoX","text":"<p>EvoX motivates treating both solutions and the search strategy as evolving artifacts. This corresponds to nested task evaluation of <code>PolicyArtifact</code> and <code>SearchProgram</code> candidates.</p>"},{"location":"references/#group-evolving-agents","title":"Group-Evolving Agents","text":"<p>Group-Evolving Agents studies evolution at the level of composite agent groups and shared experience. It is relevant to <code>TopologyArtifact</code>, performance-novelty selection, and group-level mutation.</p>"},{"location":"references/#self-generated-curricula-and-model-updates","title":"Self-generated curricula and model updates","text":""},{"location":"references/#agent0-and-dr-zero","title":"Agent0 and Dr. Zero","text":"<p>Agent0 and Dr. Zero explore proposer-solver co-evolution and self-generated curricula under verifiable rewards. They motivate explicit curriculum, checkpoint, training-job, and evaluator artifacts with separate validation boundaries.</p>"},{"location":"references/#aero","title":"AERO","text":"<p>AERO couples self-questioning, answering, criticism, and iterative model updates. It is relevant to synthetic-experience artifacts and external training loops consuming experience-graph views.</p>"},{"location":"references/#multi-agent-code-and-workflow-optimization","title":"Multi-agent code and workflow optimization","text":""},{"location":"references/#coral","title":"CORAL","text":"<p>CORAL is relevant to parallel coding agents, evaluated repository successors, shared knowledge, islands, and migration. In Meta-Evolve this becomes one <code>GitTreeArtifact</code> task configuration rather than the universal runtime model.</p>"},{"location":"references/#quality-diversity-and-island-models","title":"Quality diversity and island models","text":"<p>Quality-diversity methods, novelty search, MAP-Elites, Pareto archives, and island models are important baselines for maintaining heterogeneous strategies and avoiding premature convergence. They should be implemented as replaceable population and migration policies.</p>"},{"location":"references/#memory-provenance-and-infrastructure","title":"Memory, provenance, and infrastructure","text":""},{"location":"references/#memgpt","title":"MemGPT","text":"<p>MemGPT frames agent memory as a hierarchy that moves information between limited context and external storage. Meta-Evolve's <code>ContextPolicy</code> operates at this transport boundary, while the experience graph defines durable causal search state beneath it.</p>"},{"location":"references/#graphiti-and-structured-memory","title":"Graphiti and structured memory","text":"<p>Graphiti is relevant to temporal knowledge graphs and dynamic synthesis of agent memory. Meta-Evolve additionally needs executable artifacts, objective rewards, search-policy state, training views, and causal sibling/path queries.</p>"},{"location":"references/#event-sourcing-and-content-addressing","title":"Event sourcing and content addressing","text":"<p>Event sourcing, temporal databases, MVCC/change-data capture, content-addressed storage, reproducible build systems, and scientific provenance systems provide established foundations for immutable facts, current projections, time travel, lineage, deduplication, and audit.</p> <p>Meta-Evolve should reuse conventional database and storage systems until its measured workload demonstrates a need for specialized physical design.</p>"},{"location":"references/#research-questions-for-meta-evolve","title":"Research questions for Meta-Evolve","text":"<ul> <li>When does heterogeneous agent diversity improve compute-normalized progress?</li> <li>Which context policies accelerate reuse without suppressing novelty?</li> <li>Which topologies and migration policies avoid premature convergence?</li> <li>How should compute be allocated among exploration, exploitation, repair, synthesis, and replication?</li> <li>Which graph features predict the value of expanding or abandoning a branch?</li> <li>How do skills and discoveries transfer across models, harnesses, tasks, and domains?</li> <li>Which consistency guarantees are sufficient for concurrent search?</li> <li>How should access changes and retractions propagate into derived skills, datasets, and checkpoints?</li> <li>Can learned or evolved search policies generalize to held-out task distributions?</li> <li>When do nested improvement loops plateau, destabilize, or exploit evaluators?</li> <li>What evidence and replication policies predict real scientific validity?</li> </ul>"},{"location":"sealqa-research-labs/","title":"SealQA research labs","text":"<p>Status: Experimental/live-only. These are small teaching mechanisms, not paper reproductions or an official SealQA evaluation. The complete curriculum contains at most 24 sessions.</p>"},{"location":"sealqa-research-labs/#where-the-notebooks-are","title":"Where the notebooks are","text":"<p>The four unexecuted notebooks live under <code>examples/research/sealqa_labs/</code> and are published byte-for-byte under <code>docs/notebooks/18_sealqa_research_labs/</code>. Standalone chapters explain Feedback Descent, AutoData, the pre-M8 harness precursor, and EvoSkill.</p>"},{"location":"sealqa-research-labs/#the-common-experimental-substrate","title":"The common experimental substrate","text":"<p>All labs consume a structural <code>SessionRunner</code>. The repository supplies orchestration and deterministic injected-runner tests; users supply any live SDK wrapper. The wrapper owns authentication, structured decoding, tools, permissions, model configuration, and provider telemetry.</p>"},{"location":"sealqa-research-labs/#what-is-frozen-and-what-may-change","title":"What is frozen and what may change","text":"<p>The fixture questions, documents, evaluator-only answers, schemas, session arithmetic, and mutation allowlists are frozen. Candidate prompts, recipes, permitted harness files, and skills may change. The wrapper declaration and all behavior-affecting configuration must be recorded before a live run.</p>"},{"location":"sealqa-research-labs/#leakage-boundary","title":"Leakage boundary","text":"<p>Solver views contain only public document ids, titles, dates, and text. Gold answers, source URLs, split roles, evaluation rules, permissions, budgets, and promotion decisions remain outside candidate authority.</p>"},{"location":"sealqa-research-labs/#failure-and-scoring-semantics","title":"Failure and scoring semantics","text":"<p>Malformed structured output, missing candidates, timeout, exhaustion, SDK or infrastructure failure, and protected-control edits are typed and unrankable. They are never converted into a low score. Deterministic graders alone produce rankable measurements.</p>"},{"location":"sealqa-research-labs/#evidence-lineage-and-identity","title":"Evidence, lineage, and identity","text":"<p>Each session retains its label, decoded value or typed failure, wrapper evidence, token and elapsed usage, and experience counters. Derived artifacts name their source evidence. A wrapper configuration change changes declared identity and prevents uncontrolled comparison.</p>"},{"location":"sealqa-research-labs/#how-the-shared-components-actually-work","title":"How the shared components actually work","text":"<p><code>UserSdkSessionRunner</code> invokes a user-provided proposer inside an ordinary one-trial <code>meta.run()</code>. A successful wrapper returns <code>ProposalResult</code>, puts the schema-decoded value in <code>sdk.structured-output</code> evidence, and reports only shared token and elapsed usage. The lab validates that value again before using it.</p>"},{"location":"sealqa-research-labs/#how-to-run-the-curriculum","title":"How to run the curriculum","text":"<p>Create an opt-in driver outside the shipped examples. Implement <code>build_agent(request)</code>, identify its declaration, and pass it to <code>UserSdkSessionRunner</code>; then call <code>run_selected</code>. Review the maximum call count, SDK permissions, and spending limits before setting <code>META_EVOLVE_LIVE_SDK=1</code>.</p> <p>No official provider capture is pending. Users may perform one authorized run with their own wrapper. Any published capture must identify the wrapper declaration and preserve ties, failures, null results, and regressions honestly. The notebooks contain illustrative code and no fabricated live output.</p>"},{"location":"start-here/","title":"Start here: improve a checklist","text":"<p>Status: Shipped.</p> <p>Meta-Evolve can try small changes, evaluate each result independently, and return the best evaluated result it found. Start with a three-item checklist.</p> <pre><code>uv run python examples/learn/checklist/main.py\n</code></pre> <pre><code>['outline', 'draft', 'review']\n[[], ['outline'], ['outline', 'draft'], ['outline', 'draft', 'review']]\n</code></pre>"},{"location":"start-here/#the-complete-program","title":"The complete program","text":"<pre><code>import meta_evolve as meta\n\n\nITEMS = (\"outline\", \"draft\", \"review\")\n\n\ndef complete_one(completed):\n    return (*completed, ITEMS[len(completed)])\n\n\nresult = meta.improve(\n    seed=(),\n    proposer=complete_one,\n    evaluator=len,\n    trials=3,\n)\n\nprint(list(result.best().value))\nprint([list(trial.artifact.value) for trial in result.lineage()])\n</code></pre> <p>Read its four inputs in order:</p> <ul> <li><code>seed=()</code> is the initial empty checklist;</li> <li><code>proposer=complete_one</code> creates one successor from a parent;</li> <li><code>evaluator=len</code> says that longer checklists rank higher;</li> <li><code>trials=3</code> permits at most three successor attempts.</li> </ul> <p>The seed is evaluated before successor attempts begin. <code>result.best().value</code> returns the payload of the best evaluated artifact. <code>result.lineage()</code> returns the evaluated chain from the seed to that artifact.</p> <p>This run stops because it reaches its three-attempt limit. Three items happen to complete this example, but Meta-Evolve has not been told that this state is framework-declared success.</p>"},{"location":"start-here/#change-and-predict","title":"Change and predict","text":"<p>Add <code>\"publish\"</code> to <code>ITEMS</code> and predict the output before running again. Three successor attempts can still append only three items, so the best result ends at <code>review</code>. Restore the file afterward because its output is a documentation fixture.</p> <p>Next, define what better means with a <code>Task</code> and the canonical <code>Experiment</code> expansion.</p>"},{"location":"concepts/authority/","title":"Keep authority where it belongs","text":"<p>Status: Shipped.</p> <p>A proposer can claim anything \u2014 a confident claim is not a measurement. Meta-Evolve survives unreliable proposers because every field in a run has exactly one owner. Start here demonstrates the separation; this page names it.</p>"},{"location":"concepts/authority/#three-fields-three-owners","title":"Three fields, three owners","text":"<p>A trial's record splits into three kinds of fields:</p> <ul> <li>Candidate-controlled \u2014 the proposed artifact content and its declared   citations. Everything a proposer decides, and nothing else.</li> <li>Experiment-controlled \u2014 the task, evaluator, hidden data, environment,   seeds, and budgets. The evaluator scores independently; a proposer's claim   about its own quality never moves a metric.</li> <li>Governance-controlled \u2014 permissions, admission, visibility, and   promotion. These live outside the run entirely.</li> </ul> <pre><code>candidate            experiment             governance\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500            \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500             \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nartifact content     evaluator + data       permissions\ndeclared citations   budgets + seeds        visibility\n                     environments           promotion\n</code></pre> <p>A candidate cannot read or edit the grader, enlarge its budget, erase a failure, or grant itself permissions. Those controls stay with the experiment and its operator.</p>"},{"location":"concepts/authority/#proposal-is-not-promotion","title":"Proposal is not promotion","text":"<p>Search may select a best measured artifact. That selection grants no production authority: promotion \u2014 actually deploying the selected artifact \u2014 is an external governance act that no candidate and no search policy can perform. A run ends with a recommendation and its evidence, never with a deployment. The harness- and policy-evolution examples print this boundary explicitly (<code>promotion=external</code>); see the harness evolution guide.</p> <p>The authority invariants are canonical in the architecture and are not restated here.</p>"},{"location":"concepts/authority/#change-and-predict","title":"Change and predict","text":"<p>Make a proposer attach a glowing self-assessment to its proposal metadata. Predict metrics identical to the same candidate without the self-praise: metadata is candidate-controlled, measurement is not.</p> <p>Previous: Trust a shipped contract \u00b7 Next: Fail with a type, not a zero</p>"},{"location":"concepts/budgets/","title":"Count the work exactly","text":"<p>Status: Shipped.</p> <p>An improvement claim means nothing without knowing what it cost. Every run keeps one exact ledger, and no component can quietly widen its own share of it.</p> <p><pre><code>uv run python examples/learn/checklist/_stories/search_story.py\n</code></pre> Output<pre><code>usage: Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\n</code></pre></p>"},{"location":"concepts/budgets/#four-dimensions-one-ledger","title":"Four dimensions, one ledger","text":"<p><code>Usage</code> counts four dimensions: evaluations (each independent scoring), trials (each attempted successor), tokens (model usage a component reports), and wall_seconds (recorded elapsed work). The transcript's arithmetic is exact: the seed costs one evaluation and no trial; each of three successors costs one trial and, when valid, one evaluation \u2014 four evaluations, three trials.</p>"},{"location":"concepts/budgets/#nobody-widens-their-own-budget","title":"Nobody widens their own budget","text":"<p><code>Budget</code> is checked before more work starts, and exhaustion is a recorded stop reason, not an error. A candidate or component never grants itself additional work; the run budget can narrow what a search preset would attempt, never expand it.</p>"},{"location":"concepts/budgets/#experience-has-its-own-meter","title":"Experience has its own meter","text":"<p><pre><code>uv run python examples/learn/experience_reuse/learning_story.py\n</code></pre> Output<pre><code>pushed context: (10, 3)\naudited pull: (10, 7, 20)\n</code></pre></p> <p>Memory reads are charged outside the task ledger, on their own separate bounds. The transcript pairs each score with its memory bill: pushed context reports <code>(score, records pushed)</code> and audited pull reports <code>(score, operations, records returned)</code>. A proposer reading history spends experience budget, never evaluation budget, so memory can neither starve nor subsidize the search itself. <code>run.experience_usage()</code> exposes the meter on any result.</p> <p>Accounting rules are canonical in the architecture and the evaluation and budgets guide, and are not restated here.</p>"},{"location":"concepts/budgets/#change-and-predict","title":"Change and predict","text":"<p>Change <code>max_trials=3</code> to <code>max_trials=2</code> in the first transcript's Greedy run. Predict <code>Usage(evaluations=3, trials=2, ...)</code>: one fewer attempted successor, one fewer independent scoring, and nothing else moves.</p> <p>Previous: Fail with a type, not a zero \u00b7 Next: Encode any artifact</p>"},{"location":"concepts/conformance/","title":"Trust a shipped contract","text":"<p>Status: Shipped.</p> <p>You wrote a custom evaluator, codec, or search program. How do you know it obeys the same rules the built-in components obey? You run the same published tests the library runs on itself.</p>"},{"location":"concepts/conformance/#what-a-conformance-suite-is","title":"What a conformance suite is","text":"<p><code>meta_evolve.conformance</code> publishes one pytest-native suite per extension component family. Each suite is a plain class of laws whose collaborators arrive as fixtures: subclass it, name the subclass <code>Test...</code> so pytest collects it, override the fixtures with your component, and the laws run against your implementation.</p> <pre><code>published suite               your test module\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500               \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nArtifactCodecTests      &lt;--   class TestMyCodec(ArtifactCodecTests):\n  law: decode(encode(v))          fixture: my codec\n  law: decode is validation       fixture: rejected payloads\n  law: no aliasing                fixture: mutable values\n</code></pre> <p>The suites install through an explicit extra, so using the library never pulls in a test runner:</p> <pre><code>uv sync --extra conformance\n</code></pre>"},{"location":"concepts/conformance/#what-the-library-proves-about-itself","title":"What the library proves about itself","text":"<p>Every published suite is subclassed inside the repository's own tests, so the built-in codecs, evaluators, policies, and search programs pass exactly the laws an extension author receives. A suite also states what it does not check as carefully as what it does: <code>EvaluatorTests</code> skips its typed-failure law, visibly, when a subclass declares no rejected candidates, and <code>ArtifactCodecTests</code> does the same for its aliasing law. Silence is never counted as passing.</p> <p>One law is easy to think redundant and is not: <code>EvaluatorTests</code> checks that scoring does not edit the candidate. With built-in codecs an evaluator only ever sees a frozen decoded value \u2014 but a custom codec that returns the instance it was handed gives the evaluator the real thing, so the law waits where the damage would land.</p>"},{"location":"concepts/conformance/#when-you-reach-for-one","title":"When you reach for one","text":"<ul> <li>You registered an artifact domain \u2014 run your codec through   <code>ArtifactCodecTests</code>, as   Add an artifact domain does in its   final step.</li> <li>You wrote an evaluator, context policy, workspace, sandbox, search   program, or repository proposer \u2014 the   operations lab lists every family and   its published suite.</li> </ul> <p>Suite contents and deliberate limitations are canonical in the extensions guide and are not restated here.</p>"},{"location":"concepts/conformance/#change-and-predict","title":"Change and predict","text":"<p>Subclass <code>ArtifactCodecTests</code> around a codec whose <code>decode</code> returns the exact instance it was handed, and declare one mutable value. Predict a named aliasing-law failure, not a vague error: the suite tells you which contract your codec broke.</p> <p>Previous: Attach evidence to results \u00b7 Next: Keep authority where it belongs</p>"},{"location":"concepts/domains/","title":"Encode any artifact","text":"<p>Status: Shipped.</p> <p>The loop never inspects what it improves. A number, a prompt, a repository, a harness \u2014 every artifact travels as a typed payload that one registered codec translates, so no coordinator code branches on what you evolve.</p>"},{"location":"concepts/domains/#six-built-in-kinds","title":"Six built-in kinds","text":"<p>Every artifact is stored under a <code>(kind, representation)</code> key. Six keys ship built in:</p> Kind Representation What it holds <code>python-value</code> <code>canonical-json</code> any canonically serializable value \u2014 the default <code>text</code> <code>utf8-text-v1</code> one UTF-8 document: a prompt, a config, an essay <code>source-tree</code> <code>utf8-tree-v1</code> an immutable directory of UTF-8 files <code>skill-set</code> <code>utf8-tree-v1</code> named skill documents <code>agent-harness</code> <code>harness-manifest-v1</code> a portable agent-harness bundle <code>search-policy</code> <code>policy-manifest-v1</code> declarative search-policy values <p>Text optimization and skill evolution run two of these through the identical <code>meta.run</code> call site; SQL migrations registers a seventh, custom domain and is the runnable tour of everything on this page.</p>"},{"location":"concepts/domains/#kind-versus-representation","title":"Kind versus representation","text":"<p>The kind, never the bytes, decides which codec reads a committed payload. <code>source-tree</code> and <code>skill-set</code> share the same tree bytes (<code>utf8-tree-v1</code>) and still decode differently, because a payload's meaning is a declared contract rather than an inference from its shape.</p>"},{"location":"concepts/domains/#why-custom-domains-stay-durable-local","title":"Why custom domains stay durable-local","text":"<p>A registered custom codec is a live Python object with no verifiable manifest. A durable run must be reopenable by a reader who can decode every payload from the record alone \u2014 a payload another machine cannot decode is not shareable evidence. So <code>durable-local</code> mode accepts only the six built-in keys and rejects a custom codec before writing any event, with no silent fallback. A <code>local</code> run with a custom domain still works and may still write durable storage; reopening it later means rebuilding the same registry in Python, which grants decoding, not resumption.</p> <p>To build a domain end to end, follow Add an artifact domain. Codec laws are canonical in the programming model and the published conformance suite, and are not restated here.</p>"},{"location":"concepts/domains/#change-and-predict","title":"Change and predict","text":"<p>Seed a <code>text</code> task with a tuple instead of a string. Predict a typed refusal at ingress \u2014 <code>utf8-text-v1 tasks require a str value</code> \u2014 before any trial runs: decode and encode are validation, not best-effort coercion.</p> <p>Previous: Count the work exactly \u00b7 Build one: Add an artifact domain</p>"},{"location":"concepts/evidence/","title":"Attach evidence to results","text":"<p>Status: Shipped.</p> <p>A score says how good a candidate measured. Evidence says what was actually observed on the way to that measurement \u2014 and who observed it. Three different authorities can attach evidence to one run, and they never blur.</p> <p><pre><code>uv run python examples/learn/experience_reuse/evidence_story.py\n</code></pre> Output<pre><code>seed evidence: ('grader-transcript',)\nsuccessor evidence: ('hypothesis-note', 'grader-transcript')\nlast proposal derived from: 3 prior records\naudited pull: operations=6 records=8\n</code></pre></p>"},{"location":"concepts/evidence/#1-evidence-on-an-evaluation","title":"1. Evidence on an evaluation","text":"<p>The evaluator returns an <code>EvaluationResult</code> that carries an <code>EvidenceDraft</code> beside its metrics. The seed trial shows only <code>grader-transcript</code>: evaluator-authored evidence of what the grader observed. Evidence records what was observed; it is not a candidate-controlled score.</p>"},{"location":"concepts/evidence/#2-evidence-on-a-proposal","title":"2. Evidence on a proposal","text":"<p>The proposer returns a <code>ProposalResult</code> with a draft of its own. The successor trial adds <code>hypothesis-note</code>: candidate-authored evidence stating the claim behind the change. A proposer can write anything here, and its score does not move \u2014 the metrics stay with the independent evaluator.</p>"},{"location":"concepts/evidence/#3-evidence-a-later-candidate-can-read","title":"3. Evidence a later candidate can read","text":"<p>With <code>PullAccess</code>, a later proposal searches prior <code>evidence</code> records and opens the ones it uses. The last proposal cited three: the seed's grader transcript plus both records from the first successor. Every read is audited \u2014 six operations and eight returned records are charged and inspectable \u2014 and the citations land in the trial's <code>derived_from</code> edges.</p> <p>This page is about evidence records inside a run. The Evidence notebooks navigation section is different: it holds the captured notebook companions to the Learn chapters.</p> <p>Evidence fields and authority are canonical in the programming model and architecture and are not restated here.</p>"},{"location":"concepts/evidence/#change-and-predict","title":"Change and predict","text":"<p>Remove the <code>hypothesis-note</code> draft from the proposer. Predict the successor line shrinking to <code>('grader-transcript',)</code> while <code>derived_from</code> still cites two prior records: the proposal's citations never depended on the proposal publishing evidence of its own.</p> <p>Previous: Tell equal bytes apart \u00b7 Next: Trust a shipped contract</p>"},{"location":"concepts/failures/","title":"Fail with a type, not a zero","text":"<p>Status: Shipped.</p> <p>Most optimization loops turn every mishap into a bad score. That poisons ranking: a crashed grader is not a zero, and a malformed proposal is not a poor candidate. Meta-Evolve keeps failures typed and scores real.</p> <p><pre><code>uv run python examples/learn/checklist/_stories/search_story.py\n</code></pre> Output<pre><code>failure: ('trial_failed', 'InvalidOutput')\n</code></pre></p> <p>A proposer that returns an unusable object produces a failed trial with a named type. The run records what went wrong instead of inventing a number.</p>"},{"location":"concepts/failures/#the-failure-kinds","title":"The failure kinds","text":"<p>Invalid candidate shape, proposer failure, evaluator failure, timeout, resource exhaustion, policy violation, and infrastructure failure all retain distinct types and details. The distinctions matter because the correct responses differ: retry a flaky environment, fix a broken proposer, and never penalize a candidate for the grader's crash.</p>"},{"location":"concepts/failures/#rankable-or-non-rankable-never-fake","title":"Rankable or non-rankable, never fake","text":"<p>A trial with a valid independent evaluation is rankable: search may select it. A trial without one is non-rankable: it never becomes zero, negative infinity, or a fabricated metric, so no failure can outrank or sandbag an honestly measured candidate.</p>"},{"location":"concepts/failures/#two-failures-that-look-alike-but-are-not","title":"Two failures that look alike but are not","text":"<p><pre><code>uv run python examples/domains/python_function/learning_story.py\n</code></pre> Output<pre><code>invalid proposal: InvalidOutput\nevaluator failure: EvaluatorFailure\n</code></pre></p> <p>Improve source runs three negative cases: a poor candidate is valid, evaluated, and rankable at <code>0.0</code>; a patch whose expected bytes do not match is <code>InvalidOutput</code> with no successor; a broken grader is <code>EvaluatorFailure</code> with no score at all. The agent test-repair and adversarial-inputs examples push the same boundary further: three outcomes, not two.</p> <p>Failure semantics are canonical in the architecture and are not restated here.</p>"},{"location":"concepts/failures/#change-and-predict","title":"Change and predict","text":"<p>Make the failing proposer in the first transcript return a well-formed tuple instead. Predict the <code>trial_failed</code> line becoming an evaluated trial: the type disappears because the cause disappeared, not because a score was substituted.</p> <p>Previous: Keep authority where it belongs \u00b7 Next: Count the work exactly</p>"},{"location":"concepts/identity/","title":"Tell equal bytes apart","text":"<p>Status: Shipped.</p> <p>A proposer can hand back a value byte-identical to one the run has already seen. Are they the same artifact? Meta-Evolve answers with two identities: one for the bytes, one for the causal event that produced them.</p> <p><pre><code>uv run python examples/learn/checklist/_stories/identity_story.py\n</code></pre> Output<pre><code>seed: id=f953e1f8 digest=6ae048f0 parents=()\nsuccessor: id=227a6a34 digest=6ae048f0 parents=('f953e1f8',)\nsuccessor: id=99877b49 digest=6ae048f0 parents=('f953e1f8',)\ncontent digests equal: True\ndistinct occurrence ids: 3\n</code></pre></p> <p>The proposer restates its parent unchanged, twice, under <code>BestOfN</code>. Three artifacts hold the same five bytes. The run still records three distinct things.</p>"},{"location":"concepts/identity/#content-identity-is-the-bytes","title":"Content identity is the bytes","text":"<p>The digest is a stable hash of the encoded artifact value. All three rows share <code>6ae048f0</code> because all three values are <code>\"draft\"</code>. Equal bytes always produce an equal digest, so storage deduplicates payloads and a transcript of digests stays stable across reruns. The <code>lineage: ('659bfe4a', '08abe248')</code> line in Improve source prints exactly this: a digest prefix for each lineage step.</p>"},{"location":"concepts/identity/#occurrence-identity-is-the-causal-event","title":"Occurrence identity is the causal event","text":"<p>The occurrence id names one appearance of those bytes at one position in the run: proposed from which parents, at which logical step. The seed has no parents. Each successor names the seed as its parent and receives its own id. Equal artifact bytes in different causal positions remain different occurrences.</p>"},{"location":"concepts/identity/#why-the-run-needs-both","title":"Why the run needs both","text":"<p>Two trials that produce byte-identical artifacts remain different causal events: they may have different parents, observations, usage, and failures. Collapsing them would erase which attempt actually earned its evaluation. Replay and audited experience address records by causal position, never by content, so history stays exact even when values repeat. The experience lesson shows the same rule inside the temporal graph.</p> <p>Both identities are canonical in the architecture and are not restated here.</p>"},{"location":"concepts/identity/#change-and-predict","title":"Change and predict","text":"<p>Change <code>restate</code> to return <code>parent + \"!\"</code>. Predict digests that stop being equal while the parent links stay exactly the same: content identity follows the bytes; occurrence identity never depended on them.</p> <p>Back to Learn \u00b7 Next: Attach evidence to results</p>"},{"location":"design/","title":"Design material","text":"<p>Status: Design\u2014not shipped.</p> <p>This directory contains proposals for behavior beyond the current milestone. Design pages are subordinate to the canonical shipped public API contract, shipped architecture contract, and the acceptance boundary in the milestone roadmap. They may motivate later work, but they do not make a name importable, a behavior available, or an invariant implemented.</p> <p>Use these status labels throughout the documentation:</p> Status Meaning Shipped Available in the current package and covered by its contracts. Experimental/live-only Runnable only under the page's explicit live capabilities; never captured or run in CI. Design\u2014not shipped A proposal outside the current implementation boundary. <p>The manual/reference/design split is complete. The destination map records where material moved, while the concise canonical contracts retain compatibility anchors.</p> <ul> <li>Future search</li> <li>Meta-evolution</li> <li>Scientific direction</li> <li>Distributed and network direction</li> <li>M8 harness evolution</li> <li>M10 local-author design</li> <li>User-owned SDK boundary</li> </ul>"},{"location":"design/destination-map/","title":"Manual, reference, and design destination map","text":"<p>Status: Design\u2014not shipped.</p> <p>This is the completed move ledger for the documentation split tracked by #121. A3 made these moves, preserved useful old anchors, and updated links without changing runtime API or semantics.</p> Current owner or material A3 destination Regime <code>programming-model.md</code> callable path and results <code>guides/runs-and-results.md</code> Shipped Evaluation, objectives, failures, evidence, and budgets <code>guides/evaluation-and-budgets.md</code> Shipped Pushed context, audited pull, and derivation <code>guides/experience.md</code> Shipped Storage, reopen, recovery, and resume <code>guides/durability.md</code> Shipped User-owned SDK callable boundary <code>guides/providers.md</code> Shipped Command-line contract <code>guides/cli.md</code> Shipped Extension seams and conformance <code>guides/extensions.md</code> Shipped Shipped root exports <code>reference/api.md</code> plus generated entries Shipped Current layers, identities, authority, and invariants concise <code>architecture.md</code> Shipped Unshipped search-policy sketches <code>design/search.md</code> Design\u2014not shipped Inner/outer loops and policy meta-evolution <code>design/meta-evolution.md</code> Design\u2014not shipped Scientific discovery direction <code>design/scientific-direction.md</code> Design\u2014not shipped Distributed and network direction <code>design/network-direction.md</code> Design\u2014not shipped M8 capstone <code>design/m8-harness-evolution.md</code> Design\u2014not shipped <p>The learning pages remain unchanged in A3. B1 through B3 rebuild that sequence after this ownership split.</p>"},{"location":"design/m10-local-author/","title":"M10 local-author design","text":"<p>Status: Design\u2014not shipped.</p> <p>This page is the subordinate design record for #151 (M10.0). The milestone roadmap owns status, order, gates, and exclusions. The implementation plan owns technical staging. Journey 2's <code>Experiment</code> declaration and dual-form <code>run()</code> call are shipped by M10.1, Journey 1's <code>improve()</code> call is shipped by M10.2, Journey 3's functional search is shipped by M10.3, and the aggregate <code>Run.inspect()</code> read model is shipped by M10.4. Journey 4's <code>Session</code> is shipped by M10.6. The read-only local browser shipped in M10.7; frozen-payload artifact presentation, previews, diffs, and canonical attachments shipped in M10.8. At the time of this record the parent M10 remained open. Its current status is owned by the milestone roadmap.</p> <p>The live examples were later reorganized by user journey under <code>examples/learn/</code>, <code>domains/</code>, <code>operations/</code>, <code>agents/</code>, and <code>research/</code>. Numbered paths below are frozen historical measurement evidence; the live functional-search fixtures now reside at <code>examples/learn/functional_search/</code>, and the live future reservation is <code>examples/research/trusted_workers/</code>.</p> <p>The public story is: proposers create candidates, evaluators judge them, search decides what to try next, and Meta-Evolve records the entire process.</p>"},{"location":"design/m10-local-author/#vocabulary-tiers","title":"Vocabulary tiers","text":"<p>The beginner tier names the seed, proposer, evaluator, and bounded <code>trials=</code>. The declarative tier adds <code>Task</code>, <code>Experiment</code>, search, context, experience, random seed, and registry. Storage, execution mode, and a narrowing budget are execution overlays. The extension tier retains <code>SearchProgram.next()</code> and the kernel records needed by advanced policy authors.</p> <p><code>trials=</code> is an author-facing cost and control limit, not exposure of kernel <code>Trial</code> records. Search state, trial specifications, operators, work items, events, and raw randomness stay off the simple path.</p>"},{"location":"design/m10-local-author/#measurement-rules","title":"Measurement rules","text":"<p>A physical line is every nonblank, non-comment code line. Imports and lines containing only delimiters count. Every counted line is classified once: algorithm expresses the limit, selection, stopping, or domain declaration; scaffolding adapts protocols, identities, execution, or syntax around those choices. Proposer, evaluator, and artifact-domain logic is held outside every search slice.</p> <p>The shipped before-side for functional search is this runnable full-protocol shape, reduced from the repository's custom-search test without changing its protocol work:</p> <pre><code>from meta_evolve import domain\nclass IncumbentRefinement:\n    policy = domain.PolicyRef(\"example:incumbent\", \"1\", origin=\"local\")\n    def __init__(self, proposer):\n        self.proposer = domain.ComponentRef.local_proposer(proposer)\n    def next(self, state):\n        if state.usage.trials &gt;= 20:\n            return domain.Stop(reason=\"trial limit reached\")\n        parent = state.incumbent_id or state.start.seed_artifact_id\n        return domain.TrialSpec(\n            task_id=state.start.task_id,\n            parent_ids=(parent,),\n            proposer=self.proposer,\n            operator=domain.OperatorSpec(kind=\"rewrite\"),\n            budget=domain.Budget(trials=1, evaluations=1),\n            random_seed=state.randomness.seed(\"trial\"),\n        )\nsearch = IncumbentRefinement(propose)\n</code></pre> <p>It has 18 total lines: 3 algorithm and 15 scaffolding. It has one kernel import and constructs a trial manually. Changing only the name and limit for the two composition fixtures leaves those counts unchanged.</p>"},{"location":"design/m10-local-author/#journey-1-beginner-improve","title":"Journey 1: beginner <code>improve()</code>","text":"<p>The smallest shipped declaration is bounded and can choose durable storage without claiming durable-local reconstruction:</p> <pre><code>run = meta.improve(\n    seed=(),\n    proposer=complete_one,\n    evaluator=len,\n    trials=3,\n    storage=storage,\n)\n</code></pre>"},{"location":"design/m10-local-author/#journey-2-explicit-expansion","title":"Journey 2: explicit expansion","text":"<p>The shipped beginner call lowers exactly to this declaration and execution:</p> <pre><code>experiment = meta.Experiment(\n    task=meta.Task(evaluator=len),\n    seed=(),\n    proposer=complete_one,\n    search=meta.Greedy(max_trials=3),\n)\nrun = meta.run(experiment, storage=storage)\n</code></pre>"},{"location":"design/m10-local-author/#journey-3-functional-incumbent-refinement","title":"Journey 3: functional incumbent refinement","text":"<p>The shipped author-owned equivalent of the full-protocol before-side is:</p> <pre><code>@meta.search(max_trials=20)\ndef incumbent_refinement(search):\n    return search.expand(search.incumbent or search.seed)\n</code></pre> <p>The callback sees opaque occurrence-preserving seed and incumbent handles, immutable declared configuration, <code>expand()</code>, and <code>stop()</code>. The adapter owns budgets, proposer and operator binding, replayed stopping, and named randomness.</p>"},{"location":"design/m10-local-author/#journey-4-uninterrupted-paced-and-resumed-session","title":"Journey 4: uninterrupted, paced, and resumed <code>Session</code>","text":"<p>Uninterrupted execution remains one call:</p> <pre><code>run = meta.run(experiment, storage=storage)\n</code></pre> <p>Paced execution completes one committed decision per step:</p> <pre><code>with meta.Session.start(\n    experiment,\n    storage=storage,\n    budget=budget,\n    mode=\"local\",\n) as session:\n    for decision in session:\n        observe(decision)\nrun = session.run\n</code></pre> <p>Recovery continues the same authority and budget rather than accepting a new decision:</p> <pre><code>with meta.Session.resume(\n    run_id,\n    storage=storage,\n    registry=registry,\n) as session:\n    for decision in session:\n        observe(decision)\nrun = session.run\n</code></pre> <p>Uninterrupted and resumed execution must produce byte-identical durable histories. Human choices require a future declared component with recorded inputs; <code>Session.step(decision=...)</code> is prohibited.</p>"},{"location":"design/m10-local-author/#journey-5-deterministic-textual-feedback-descent","title":"Journey 5: deterministic textual-feedback descent","text":"<p>This shipped functional-search fixture is derived from the deterministic development loop in <code>examples/20_harness_evolution/</code>; its proposer, evaluator, artifact domain, and held-out promotion check remain outside the slice:</p> <pre><code>@meta.search(max_trials=2)\ndef textual_feedback_descent(search):\n    \"\"\"Expand the incumbent after deterministic development feedback.\"\"\"\n    return search.expand(search.incumbent or search.seed)\n</code></pre> <p>It is a deterministic, local, unpaid, unnumbered fixture under <code>examples/functional_search/</code>, not a new numbered example.</p>"},{"location":"design/m10-local-author/#journey-6-deterministic-autoresearch-composition","title":"Journey 6: deterministic AutoResearch composition","text":"<p>This shipped outer-search fixture is derived from <code>examples/19_autoresearch/</code>; private selection, public evidence, inner runs, and held-out evaluation remain evaluator- or user-owned:</p> <pre><code>@meta.search(max_trials=2)\ndef autoresearch_recipe(search):\n    \"\"\"Expand the selected recipe for the next bounded outer evaluation.\"\"\"\n    return search.expand(search.incumbent or search.seed)\n</code></pre> <p>It is also deterministic, local, unpaid, unnumbered under <code>examples/functional_search/</code>, and outside the numbered sequence. Neither fixture creates or consumes the reserved <code>examples/22_trusted_workers/</code>.</p>"},{"location":"design/m10-local-author/#friction-scorecard","title":"Friction scorecard","text":"<p>This is the historical M10.0 measurement, so its original <code>proposed</code> slice labels and line counts remain unchanged as later journeys ship.</p> <p>Public names count distinct public API names in the slice; kernel imports count import statements from internal extension namespaces. A durability rewrite is an author change required to move from local execution to durable-local reconstruction. Reduction is the drop in scaffolding from the 15-line shipped functional-search baseline; it is published only where that comparison applies. The now-runnable Journey 5 and Journey 6 fixtures are statically recounted by the same rules at 3 algorithm lines and 1 scaffolding line each. Both therefore retain the published 93% reduction, well above the required 50%; the historical scorecard labels and measurements below remain unchanged.</p> Journey Slice Public names User-visible concepts Total Algorithm Scaffolding Kernel imports Manual trials First failure Durability rewrites Reduction 1 Beginner proposed 1 seed, proposer, evaluator, trials, storage, run 7 4 3 0 No invalid declarations fail before work; proposal and evaluation failures are typed outcomes graduate to <code>Experiment</code> and register components \u2014 2 Expansion proposed 4 task, experiment, search, execution overlay 7 4 3 0 No duplicate or invalid declarations fail before work register reconstructible components and select durable-local mode \u2014 3 Full protocol before 6 policy identity, state, trial, operator, budget, randomness 18 3 15 1 Yes malformed decisions fail at the protocol boundary provide a registered policy factory \u2014 3 Functional proposed 1 limit, incumbent, seed, expand 3 2 1 0 No unsupported returns name <code>expand()</code> and <code>stop()</code> register the decorated policy for reconstruction 93% 4 Three execution forms proposed 2 experiment, overlays, decision, session, recovery 18 2 16 0 No start/resume validation fails before external work; later failures remain typed resume adds run id, storage, and registry, not new authority \u2014 5 Textual feedback before 6 full policy and trial protocol 18 3 15 1 Yes malformed decisions fail at the protocol boundary provide a registered policy factory \u2014 5 Textual feedback proposed 1 limit, incumbent, feedback-paced expansion 4 3 1 0 No unsupported returns fail with fa\u00e7ade guidance register the decorated policy for reconstruction 93% 6 AutoResearch before 6 full policy and trial protocol 18 3 15 1 Yes malformed decisions fail at the protocol boundary provide a registered policy factory \u2014 6 AutoResearch proposed 1 outer limit, selected recipe, expansion 4 3 1 0 No unsupported returns fail with fa\u00e7ade guidance register the decorated policy for reconstruction 93%"},{"location":"design/m10-local-author/#decisions","title":"Decisions","text":"<ul> <li><code>SearchProgram.next()</code> remains the full 1.x extension protocol. Renaming it   to <code>decide()</code> adds churn while <code>@meta.search</code> removes routine exposure.</li> <li>Decorator configuration is immutable, serializable, and identity-visible.   Hidden closure configuration is rejected. Lowering is exact and introduces   no coordinator branch or special case.</li> <li>Outer and inner loops, generations, and episodes remain relative explanatory   terms, not new core types.</li> <li>Dictionary-returning CLI factories remain supported through 1.x and now emit   the M10.1 <code>DeprecationWarning</code> beside the typed alternative; removal is no   earlier than 2.0.</li> <li>M10.3 #164 shipped the   docstring-derived <code>DecisionDescription</code> contract.</li> </ul>"},{"location":"design/m10-local-author/#deferred-and-rejected-alternatives","title":"Deferred and rejected alternatives","text":"<p>After M10, class-based harness surfaces may remove repeated field names and named search/context presets may improve discovery only when they lower exactly through existing validation, identity, authority, and durability contracts. Reflective proposer recipes remain user-owned.</p> <p>Rejected alternatives are ambient configuration, execution-owning <code>forward()</code>, optimizer-owned <code>compile()</code>, hidden captured configuration, injected human decisions, fused proposer/evaluator/search layers, and any sugar that requires a coordinator, kernel, artifact, provider, or infrastructure special case.</p>"},{"location":"design/m8-harness-evolution/","title":"M8 harness-evolution design brief","text":"<p>Status: Design\u2014not shipped. This retained decision brief records the design that preceded M8 implementation. Current executable semantics live in the shipped programming model and architecture; official-SDK validation remains the M8.6 blocking gate.</p> <p>Design one bounded experiment: start from a worker harness containing a planner prompt and context policy; let an outer proposer change only those surfaces; score each candidate on fresh held-out repository tasks under equal budgets; compare it with the unchanged harness; leave promotion external.</p> <p>The deterministic demonstration is <code>examples/research/harness_evolution/</code>.</p> <p>Its execution boundary is decision-complete: the example accepts a user-owned proposer callable, CI drives it with a deterministic fixture-backed SDK client, and an opt-in maintainer driver may invoke a real SDK through the same callable shape. See the user-owned SDK boundary. The first authorized live run is captured once and sanitized without dropping regressions, ties, or failures. Evaluation, equal budgets, mutation authority, and promotion remain outside the wrapper.</p>"},{"location":"design/m8-harness-evolution/#map-onto-current-seams","title":"Map onto current seams","text":"M8 concept Current seam <code>HarnessArtifact</code> Artifact codecs, registries, component identity, and lineage <code>MutationSurface</code> Proposal validation, policy violations, and workspace capture Held-out evaluation Evaluators launching bounded fresh inner runs Harness comparison Run summaries, usage, failures, and comparison validity External promotion A governance decision after search selection SDK execution A user-owned callable returning a candidate or <code>ProposalResult</code>"},{"location":"design/m8-harness-evolution/#locked-executable-decisions","title":"Locked executable decisions","text":"<ul> <li>Search evaluates development episodes. Held-out episodes evaluate only the   unchanged baseline and selected candidate, each with <code>Greedy(max_trials=0)</code>.</li> <li>Candidate encoding is followed by generic core-side successor validation   before artifact commit. <code>MutationSurface.apply()</code> is convenience only.</li> <li>The parent declares mutable surfaces. Successors cannot change parameter   names/types, component references, interfaces, or non-surface state.</li> <li>Planner text and context policy are independent implementations governed by   one published conformance suite.</li> <li>Evaluator identity includes exact suite content, worker/model declaration,   budgets, timeouts, environment, and scoring/aggregation rule.</li> <li>Resume guarantees begin at a committed outer evaluation. Uncommitted inner   work may repeat.</li> <li>One held-out fixture demonstrates the boundary; it is not statistically   strong promotion evidence.</li> </ul>"},{"location":"design/m8-harness-evolution/#implemented-artifact-and-identity","title":"Implemented artifact and identity","text":"<p>The built-in artifact is <code>agent-harness</code> / <code>harness-manifest-v1</code>, with exactly one <code>TextParam</code> planner and one <code>PolicyParam</code> context policy. Existing artifact occurrence identity, content digest, lineage, and component references apply.</p>"},{"location":"design/m8-harness-evolution/#mutation-authority","title":"Mutation authority","text":"<p>Only text and policy configuration change. The codec returns <code>InvalidOutput</code> for malformed/no-op transitions and <code>PolicyViolation</code> for authority expansion; the proposal, evidence, and typed failure commit without a successor artifact.</p>"},{"location":"design/m8-harness-evolution/#held-out-evaluation","title":"Held-out evaluation","text":"<p>Portable episode values and fresh inner runs are implemented. Hidden tests and process controls remain evaluator-only; captured attempts, diagnostics, diffs, failures, and counts become outer evidence.</p>"},{"location":"design/m8-harness-evolution/#comparison-and-promotion","title":"Comparison and promotion","text":"<p><code>compare_harnesses</code> enforces the full equality gate and reports quality, actual usage, failures, verdict, and eligibility. <code>best()</code> never deploys anything.</p>"},{"location":"design/m8-harness-evolution/#replay-and-extensions","title":"Replay and extensions","text":"<p>Outer episode capture is ordinary durable evidence. Fault injection proves a committed outer evaluation is not repeated. Both built-in surfaces pass the same published suite without a coordinator branch.</p>"},{"location":"design/m8-harness-evolution/#remaining-blocker","title":"Remaining blocker","text":"<p>The first authorized official-SDK result is committed without dropping its failures or tie: strict output-schema validation rejected both proposal calls, so no native mutation reached decoding or successor validation. M8 remains Experimental/live-only until a future separately authorized capture exercises that boundary; the failed result remains part of the record.</p>"},{"location":"design/m8-harness-evolution/#acceptance-evidence","title":"Acceptance evidence","text":"<ul> <li>Valid prompt and context-policy mutations use one ordinary coordinator path.</li> <li>Authority violations are typed rejections with no successor artifact.</li> <li>Composite identity, lineage, component provenance, nested usage, and failures replay.</li> <li>Held-out inner runs are fresh and comparison fails closed on mismatched controls.</li> <li>Resume performs no committed episode twice.</li> <li>Promotion remains an explicit external outcome.</li> </ul> <p>Final review must make candidate authority, distinct causal identity, parent lineage (the parent lineage invariant), effective budget, every typed failure, replay, comparison validity, and promotion ownership explicit.</p>"},{"location":"design/meta-evolution/","title":"Meta-evolution","text":"<p>Status: Design\u2014not shipped.</p> <p>M9's deliberately small trusted policy-artifact slice is shipped and owned by the policy-evolution guide and canonical architecture. This page now records only future generalization beyond that slice; it defines no additional shipped classes.</p>"},{"location":"design/meta-evolution/#inner-and-outer-loops","title":"Inner and outer loops","text":"<p>Future outer candidates may describe broader harnesses or policies. Their evaluators would run fresh, bounded inner <code>meta.run()</code> episodes against an experiment-controlled suite. Inner artifacts, trials, evaluations, failures, evidence, and usage would retain their own identities while linking to the outer evaluation.</p> <p>The candidate could change only a declared mutation surface. It could not change the suite, hidden splits, evaluator, enclosing budget, permissions, cache semantics, or promotion rule.</p>"},{"location":"design/meta-evolution/#broader-policy-meta-evolution","title":"Broader policy meta-evolution","text":"<p>M9 already compares its two-field policy artifacts under exact controls. Any broader policy space must remain compatible across task distribution, model, evaluator, environment, seeds, and budgets. Aggregation must preserve uncertainty and typed failures rather than average missing measurements into low scores. Held-out results remain unavailable until the relevant evaluation permits them.</p>"},{"location":"design/meta-evolution/#nested-budgets","title":"Nested budgets","text":"<p>Outer and inner evaluation, trial, token, and wall limits require explicit reservation and settlement. Inner exhaustion cannot consume undeclared outer authority. Cache reuse is valid only when every behavior-affecting declaration matches.</p> <p>M8 and M9 used decision-complete designs and the <code>harness_evolution</code> and <code>policy_evolution</code> examples. See the M8 design and milestone roadmap.</p>"},{"location":"design/network-direction/","title":"Distributed and network direction","text":"<p>Status: Design\u2014not shipped.</p> <p>The shipped execution profiles are local and durable-local. Distribution begins only after local semantics remain stable under concurrency and failure.</p>"},{"location":"design/network-direction/#trusted-distribution","title":"Trusted distribution","text":"<p>A trusted worker design needs durable attempts, leases, renewal, expiry, cancellation, idempotent result submission, capability matching, and atomic budget reservation/settlement. Delivery retries retain one logical trial. Out-of-order wall-clock completion cannot change logical random choices or reveal future experience.</p>"},{"location":"design/network-direction/#federation","title":"Federation","text":"<p>Cross-institution exchange additionally needs scoped identities, signed manifests, visibility, licensing, attribution, embargo, retraction, disputes, and task-specific trust. Policy-filtered subgraphs may preserve causal shape while redacting content.</p> <p>Worker liveness, service health, queue estimates, and caches are ephemeral. Accepted artifacts, evaluations, budgets, authority changes, and promotion decisions remain durable. No network design may weaken candidate authority or failure honesty.</p>"},{"location":"design/scientific-direction/","title":"Scientific direction","text":"<p>Status: Design\u2014not shipped.</p> <p>Meta-Evolve may eventually represent hypotheses, protocols, datasets, simulations, analyses, claims, replications, and retractions as typed artifacts. That direction does not make software evaluation equivalent to validated science.</p> <p>Scientific use requires explicit provenance, preregistered or otherwise governed evaluation, uncertainty, leakage controls, independent replication, negative results, and human/institutional authority. Model-generated novelty or an improved benchmark score is evidence only within the declared experiment.</p> <p>Training views, semantic retrieval, learned search, and model-weight updates must be derived from immutable authorized records. They cannot become an unrecorded source of experience or retroactively alter a historical decision.</p> <p>Current shipped semantics provide useful ingredients\u2014immutable artifacts, typed failures, evidence, budgets, causal lineage, and replay\u2014but do not ship a scientific artifact domain or make generalization claims.</p>"},{"location":"design/search/","title":"Future search policies","text":"<p>Status: Design\u2014not shipped.</p> <p>Meta-Evolve currently ships deterministic Greedy, BestOfN, and Population presets. The ideas below are roadmap direction, not importable names or accepted semantics.</p>"},{"location":"design/search/#pareto-and-quality-diversity-archives","title":"Pareto and quality-diversity archives","text":"<p>Multi-objective archives would need explicit dominance, constraint, tie, capacity, and admission decisions. Quality-diversity additionally needs a versioned behavior descriptor and deterministic binning. Archive membership must be replayable from recorded evaluations rather than recomputed under a new definition.</p>"},{"location":"design/search/#islands-and-migration","title":"Islands and migration","text":"<p>An island design must distinguish artifact identity from island membership. Migration is a recorded search decision, not artifact copying or Git history. Timing, topology, migrant selection, admission, and random streams must remain deterministic at the logical level.</p>"},{"location":"design/search/#bandits-and-tree-search","title":"Bandits and tree search","text":"<p>Allocation policies may use uncertainty, cost, or upper-confidence estimates only when their observations, update rule, and random choices are explicit. Retry delivery must never become an extra logical trial.</p> <p>No future policy may erase failed outcomes, widen budgets, inspect hidden evaluation, or require a coordinator special case.</p>"},{"location":"design/user-owned-sdk-boundary/","title":"User-owned SDK boundary","text":"<p>Status: Design\u2014not shipped. This retained decision record preceded the deterministic M8 flagship and opt-in driver; the authorized live capture is still pending. The callable boundary itself and the hard-removal migration it gates are shipped; the migration guide is their canonical record. This page adds no agent runtime, provider adapter, or M8 domain type.</p>"},{"location":"design/user-owned-sdk-boundary/#decision","title":"Decision","text":"<p>Meta-Evolve accepts an ordinary callable or an object with a structural <code>propose()</code> method. The user invokes and configures any model SDK inside that boundary. The SDK owns authentication, sessions, tools, permissions, structured decoding, and provider telemetry. Meta-Evolve owns candidate validation, independent evaluation, budgets, durable records, lineage, experience auditing, and promotion authority.</p> <p>There is no provider ABC and no generic agent runtime. A direct candidate return remains the smallest path; <code>ProposalResult</code> is the typed path when a wrapper has usage, evidence, metadata, or derivation to report.</p> SDK wrapper output Meta-Evolve destination Decoded artifact value <code>ProposalResult.candidate</code> Total tokens and elapsed time <code>Usage.tokens</code> and <code>Usage.wall_seconds</code> Raw response, monetary cost, model, granular tokens, provider telemetry <code>EvidenceDraft</code> or <code>metadata</code> Audited observations actually used <code>derived_from</code> <p>Trial and evaluation counts are coordinator-owned. A wrapper that reports either count is normalized to typed <code>InvalidOutput</code>, and completion overwrites both counts independently as defense in depth. Monetary cost and granular token categories remain provider evidence, not shared budget fields.</p> <p>Ordinary SDK exceptions become <code>ProposerFailure</code>. Framework failures that state a fact about durable state, accounting, or run capability escape unchanged: <code>EventStreamConflict</code>, <code>DurableCorruption</code>, <code>InvalidSearchHistory</code>, <code>ExperienceStoreUnavailable</code>, <code>ExecutionAccountingUnknown</code>, and <code>CapabilityError</code>.</p>"},{"location":"design/user-owned-sdk-boundary/#audited-agent-pull","title":"Audited agent pull","text":"<p>A user wrapper may register an SDK custom tool whose handler delegates to the scoped <code>context.experience</code> reader. The handler returns only the authorized observation and retains the observation references that the SDK actually used; the wrapper returns those references in <code>ProposalResult.derived_from</code>.</p> <pre><code>SDK tool call\n  -&gt; user handler\n  -&gt; context.experience.search/open/ancestors/siblings/compare\n  -&gt; audited, bounded observation\n  -&gt; SDK\n  -&gt; ProposalResult(derived_from=used_references)\n</code></pre> <p>The callback can be in-process or forwarded through a user-owned RPC bridge. Meta-Evolve adds no process or tool protocol for that bridge. Access denial, exhaustion, audit-store failure, and unknown accounting keep their existing semantics because the handler delegates to the scoped reader rather than copying or bypassing it. SDKs without custom-tool support may receive pushed <code>context.bundle</code> content, but that is not agent-directed pull.</p>"},{"location":"design/user-owned-sdk-boundary/#m8-flagship-execution","title":"M8 flagship execution","text":"<p>The <code>examples/research/harness_evolution/</code> demonstration accepts a user-owned proposer callable. CI uses a deterministic fixture-backed SDK client. The opt-in maintainer driver invokes a real SDK through the same callable boundary, without a package dependency or a shipped provider example.</p> <p>The first authorized live M8 run must be captured once, sanitized, and retained honestly. The capture must preserve ties, regressions, typed failures, and unsuccessful attempts rather than rewriting them into a success narrative. It must identify the wrapper declaration and the controls that affect behavior.</p> <p>The wrapper may propose only manifest-declared planner and context-policy changes. External evaluators run fresh held-out episodes under equal effective budgets. Candidate code cannot change evaluation, grants, hidden inputs, budgets, promotion rules, or mutation authority. <code>best()</code> selects a recorded result; promotion remains a separate governed action.</p> <p>The provider removal was gated on this boundary and the audited experience bridge being complete, and shipped in the same change as both. No The M8 types are implemented elsewhere; this decision still adds no provider ABC, agent runtime, or core SDK adapter.</p>"},{"location":"design/user-owned-sdk-boundary/#removal-and-evidence-policy","title":"Removal and evidence policy","text":"<p>The pre-1.0 migration is a hard removal: first-party Claude and Codex imports, extras, adapters, fixtures, and live tests receive no compatibility shim. Historical experiments and milestone records remain accessible and are labeled superseded. Their original claims are evidence of what was tested at the time; they are not current package instructions or shipped fixtures.</p> <p>The repository boundary applies to current <code>src/</code>, examples, and shipped guides. Provider names remain legitimate in historical experiments, milestone records, design decisions, and migration material.</p>"},{"location":"guides/browser/","title":"Local run browser","text":"<p>Status: Shipped. M10.7\u2013M10.8 provide an optional, read-only presentation over shipped <code>Storage</code>, inspection, artifact presentation, diff, comparison, and exact-version contracts.</p>"},{"location":"guides/browser/#start-it","title":"Start it","text":"<p>Install the optional dependencies, then point the command at an existing durable store:</p> <pre><code>uv sync --extra browser\nmeta-evolve ui --storage ./store --port 8000\n</code></pre> <p>Runs using a custom artifact codec can reconstruct it through the ordinary experiment-module convention:</p> <pre><code>meta-evolve ui --storage ./store --module experiments.my_run\n</code></pre> <p>The optional module is loaded only after browser dependencies are available. Only its <code>experiment().registry</code> enters the browser reader; proposers, evaluators, policies, sessions, and execution are never invoked. Without the module or codec, the run is an ordinary actionable codec-unavailable <code>422</code> state rather than corruption.</p> <p>Open http://127.0.0.1:8000. The host is fixed to loopback and the server uses one worker. The browser has no write, run, resume, cancellation, promotion, or deletion controls. It has no authentication. Loopback binding limits its intended scope; it is not a claim that the application is safe to proxy or publish remotely.</p> <p>The pages are complete server-rendered HTML. Search, detail, comparison, lineage tables, and navigation work with JavaScript disabled. A small same-origin script only polls committed stream versions and reloads after facts change. A complete run page with a stable version carries no polling attributes at all; only the catalog and incomplete or unstable pages poll.</p>"},{"location":"guides/browser/#what-appears","title":"What appears","text":"<p>The catalog is ordered lexicographically by content-derived run ID; it makes no chronological claim. Cards show task identity, lifecycle, rankability, policy, the primary objective and available score, artifact kind, and usage. Search filters those recorded fields.</p> <p>A detail page follows the recorded story: overview, declaration, lineage, trials, evidence and failures, recorded artifact history, and decision timeline. The SVG lineage diagram has a semantic table containing the same parent edges. The history table preserves recorded occurrence order without calling it time, keeps occurrence identity separate from content digest, and offers one explicit diff choice for every parent.</p> <p>Artifact pages show identity, parents, bounded specialized presentation, exact canonical attachment, and every available parent diff. Text, source trees, skill sets, canonical Python values, harnesses, and policy artifacts have fixed views. A custom kind receives deterministic canonical-payload text and an explicit no-specialized-view notice. All content originates in the frozen payload; decoded <code>ArtifactView.value</code> is never read, stringified, or passed to templates. Evidence URIs remain inert text, never links.</p> <p>Direct-parent diffs support Text, SourceTree, and SkillSet. They identify added, removed, modified, and unchanged files and label added, removed, and unchanged lines textually as well as visually. A file whose content is identical on both sides reports its unchanged status without lines and consumes none of the shared diff budgets. Foreign-run views, non-parent edges, and incompatible families are refused with <code>422</code>. Multi-parent occurrences never acquire an arbitrary default parent.</p> <p>Bounds apply in this order: lexicographic path sorting; first 100 entries; 64 KiB UTF-8 per retained preview document; 256 KiB aggregate preview input; 64 KiB per diff side and 256 KiB aggregate diff input before invoking the diff algorithm; then 4,000 output lines. Final partial and omitted states remain visible. Canonical downloads are not truncated.</p> <p>Comparison always receives an explicit ordered pair and invokes the public <code>Run.compare()</code> contract. It reports score, usage, winner or tie, and the three declaration flags. Incompatible or unrankable pairs remain visible as typed refusals; the browser does not pre-filter candidates or turn failure into a score.</p>"},{"location":"guides/browser/#replay-and-caches","title":"Replay and caches","text":"<p>One dedicated thread creates <code>Storage.read_only()</code>, performs every listing, version query, inspection, and comparison, and closes the handle. Route handlers and Starlette's general threadpool never receive it. This preserves SQLite connection affinity while concurrent HTTP requests queue safely.</p> <p>The first catalog request performs one validated replay for every discovered run. It keeps one lightweight card per run, stamped with the exact <code>run_version</code> it observed and replaced when a newer version appears, plus a 32-entry LRU of full inspections. Comparisons use a separate 32-entry LRU keyed by the ordered IDs and both exact versions. Eviction discards only derived views, never durable facts. Inspections retain each frozen payload beside the decoded object, without eagerly storing an additional serialized byte copy; payload-heavy runs therefore consume proportionally more memory inside the bounded LRU. A comparison cache miss pays the public API's full multi-replay cost.</p> <p>Before replay, the reader polls exact versions. Exact hits avoid replay; completed cached runs are not polled again. Catalog polling still calls <code>Storage.runs()</code> to discover additions and checks versions of incomplete runs.</p> <p>Inspection reads a version before and after replay. A mismatch is retried once. If commits continue, the valid inspection snapshot is served without a version label or cache entry. Comparison is stricter: both versions must remain stable across the complete public comparison. A second mismatch returns retryable <code>503</code> rather than presenting a mixed observation.</p>"},{"location":"guides/browser/#failures-and-safety","title":"Failures and safety","text":"<p>Unknown or concurrently missing runs and artifact occurrences return <code>404</code>. Comparison and diff refusals and codec capability gaps return escaped <code>422</code> pages. Transient store failures and unstable comparisons return <code>503</code>. Corruption and unexpected defects return a safe <code>500</code> without a traceback or durable detail. A corrupt or codec-unavailable run becomes an isolated catalog error card so healthy runs remain reachable.</p> <p>Store-open and schema failures happen before the server binds and use the ordinary CLI domain-failure path. Responses include a restrictive content security policy, <code>nosniff</code>, no-referrer, and frame-denial headers. These defenses do not expand the browser beyond its local, unauthenticated scope.</p> <p>Raw artifact responses use <code>application/json</code>, <code>nosniff</code>, and <code>Content-Disposition: attachment</code> with a digest-derived filename. The bytes are the current digest encoding, canonical-payload-v1, not a native artifact export and not an unconditional promise for every future durable format.</p>"},{"location":"guides/browser/#manual-visual-review-checklist","title":"Manual visual review checklist","text":"<p>For changes to browser templates or CSS, review the catalog, run, artifact, diff, refusal, truncation, and codec-unavailable fixtures with JavaScript disabled at both fixed viewports:</p> <ul> <li>desktop: 1440 \u00d7 900;</li> <li>mobile: 390 \u00d7 844.</li> </ul> <p>Record completion in the PR. Check heading order, landmarks, skip link, visible keyboard focus, native controls, textual status labels, contained wide tables and preformatted text, single-column mobile layout, and reduced-motion mode. Screenshot automation remains out of scope for this milestone.</p>"},{"location":"guides/cli/","title":"Command line","text":"<p>Status: Shipped. The CLI is a thin translation over the public run and durability contracts.</p> <pre><code>meta-evolve run MODULE --storage STORAGE [--mode local|durable-local]\nmeta-evolve list --storage STORAGE\nmeta-evolve inspect RUN_ID --storage STORAGE\nmeta-evolve resume RUN_ID --module MODULE --storage STORAGE\nmeta-evolve compare LEFT_RUN_ID RIGHT_RUN_ID --storage STORAGE\nmeta-evolve ui --storage STORAGE [--port PORT] [--module MODULE]\n</code></pre> <p>Each verb makes one public call:</p> Verb Public call Result <code>run</code> <code>meta.run(...)</code> Run ID and summary <code>list</code> <code>storage.runs()</code> Reachable content-derived run IDs <code>inspect</code> <code>run.inspect()</code> Overview, declaration, trials, decisions <code>resume</code> <code>meta.resume(...)</code> Verified missing durable-local work <code>compare</code> <code>run.compare(other)</code> Compatible comparison and winner <p>The module's side-effect-free <code>experiment()</code> factory returns <code>Experiment</code>. Dictionary factories remain functional through 1.x for compatibility, but each load emits <code>DeprecationWarning</code> naming the module and factory and asking for <code>meta.Experiment(...)</code>. Unknown keys and the execution-owned <code>storage</code>, <code>mode</code>, and call-level <code>budget</code> remain usage errors; those overlays belong to the command line and <code>run()</code>.</p> <p>Failures in the <code>RunError</code> hierarchy become concise CLI errors without tracebacks. Unknown arguments, invalid factories, missing runs, capability gaps, and incompatible comparisons fail closed.</p> <p>The CLI does not add an artifact history, registry, evaluator, or promotion authority of its own. Git may be ingress or egress for source, but durable Meta-Evolve artifacts and transitions remain the experiment record. Inspection includes failures, artifact occurrence identity and digest, evidence, decision rationale, and named randomness. Measured wall-clock values remain omitted so equivalent deterministic runs produce byte-identical output.</p>"},{"location":"guides/cli/#read-only-local-browser","title":"Read-only local browser","text":"<p><code>ui</code> is the one long-running lifecycle exception to the five deterministic report verbs. Install it explicitly and serve an existing durable store:</p> <pre><code>uv sync --extra browser\nmeta-evolve ui --storage ./store --port 8000\n</code></pre> <p>The host is fixed to <code>127.0.0.1</code>, the worker count is fixed to one, and valid ports are <code>1..65535</code>. There is no host or worker override. The command owns no domain query or HTML logic: it conditionally imports and delegates to the browser server. Uvicorn retains its operational logging. Missing browser dependencies give installation guidance and exit <code>2</code>; store-open and schema failures remain typed CLI domain failures and exit <code>1</code> before a socket binds.</p> <p>The browser is read-only, local, and unauthenticated. Loopback scope is not a remote-safety or authentication claim. See the browser guide for the replay, cache, polling, and rendering boundaries.</p> <p>For a run using a custom artifact codec, <code>ui --module MODULE</code> loads the same side-effect-free <code>experiment()</code> convention and passes only its registry to the read-only browser. Browser dependencies are imported first. No proposer, evaluator, policy, session, execution, or mutation surface is invoked. Missing custom codecs remain actionable <code>422</code> capability states.</p>"},{"location":"guides/durability/","title":"Durability","text":"<p>Status: Shipped.</p> <p><code>Storage</code> composes artifact and event storage. In-memory storage is isolated to the process. Durable storage uses a verified filesystem artifact store plus an append-only SQLite event stream.</p> <pre><code>import meta_evolve as meta\n\nstorage = meta.Storage.durable(\".meta-evolve/example\")\n</code></pre>"},{"location":"guides/durability/#store","title":"Store","text":"<p>Pass durable storage to <code>meta.run()</code> to preserve committed artifacts, evidence, decisions, lineage, usage, and failures. <code>mode=\"durable-local\"</code> additionally requires registered reconstructible components and their verified manifests. Validation happens before the first event is written.</p>"},{"location":"guides/durability/#reopen","title":"Reopen","text":"<p><code>Run</code> reconstructs a read-only view from committed facts:</p> <pre><code>reopened = meta.Run(run_id, storage)\nprint(reopened.summary())\n</code></pre> <p>Reopen performs no proposal or evaluation. Local callable and SDK histories can be durable and inspectable even when their live components cannot be rebuilt.</p> <p>For an inspection-only process, open the same directory without commit or execution authority:</p> <pre><code>with meta.Storage.read_only(\".meta-evolve/example\") as storage:\n    version = storage.run_version(run_id)\n    inspection = meta.Run(run_id, storage).inspect()\n</code></pre> <p><code>is_durable</code> and <code>is_read_only</code> answer different questions. The first says committed facts live on durable media; the second says this handle cannot commit. <code>Storage.durable(...)</code> reports <code>True, False</code>, while <code>Storage.read_only(...)</code> reports <code>True, True</code>. Artifact, blob, event, and unit-of-work writes all raise <code>CapabilityError</code>. Passing the handle to <code>run()</code>, <code>improve()</code>, or <code>resume()</code> is rejected before a codec, registry component, proposer, evaluator, provider, or subprocess runs.</p> <p><code>run_version(run_id)</code> is the maximum committed event sequence. It returns <code>0</code> for a well-formed id with no events and uses a new deferred transaction on each call, so a long-lived reader observes successive atomic commits from a WAL writer. It is deliberately a cheap polling query, not a full integrity check. <code>Run(run_id, storage).inspect()</code> performs authoritative loading and still raises <code>RunNotFound</code> when that run is required but absent.</p> <p>The optional local run browser owns one long-lived read-only handle on one dedicated thread. It calls <code>runs()</code> to discover additions, polls exact versions before replay, and never polls a cached completed run again. Its bounded caches are disposable observations; append-only storage and validated replay remain authoritative.</p> <p>Read-only open requires an existing current store. It uses SQLite <code>mode=ro</code> plus <code>query_only</code>, and never creates a directory, changes journal settings, executes DDL, stamps a schema, or migrates. For an older or unstamped store, close readers and reopen it once with writable <code>Storage.durable(...)</code>, then use <code>Storage.read_only(...)</code> again. Future schemas raise <code>SchemaVersionRejected</code>, malformed current schemas raise <code>DurableCorruption</code>, and missing or inaccessible database/filesystem/WAL state raises <code>DurableStoreError</code> with the underlying cause.</p>"},{"location":"guides/durability/#resume","title":"Resume","text":"<p><code>resume</code> verifies manifests, declarations, store integrity, and the recovery frontier before doing missing work. It retains the same run ID and never re-executes a committed operation. A non-resumable local history raises <code>CapabilityError</code>.</p> <p>Use <code>Session.resume</code> to pace that same recovery:</p> <pre><code>with meta.Session.resume(run_id, storage=storage, registry=registry) as session:\n    for decision in session:\n        observe(decision)\nresult = session.run\n</code></pre> <p>Preparation validates and classifies the committed frontier but does not finish pending work. The first <code>step()</code> either completes the already committed trial or evaluation, or records and completes one new decision. Resuming an already stopped run iterates zero times; an explicit terminal <code>step()</code> returns the existing stop view without changing events or artifacts.</p>"},{"location":"guides/durability/#compare","title":"Compare","text":"<p>Reopened completed runs use the same comparison contract as live results. Declaration mismatches raise <code>RunsNotComparable</code> before ranking.</p>"},{"location":"guides/durability/#replay-and-reproducibility","title":"Replay and reproducibility","text":"<p>Replay is a pure fold over immutable transitions and artifacts. It reconstructs logical decisions, projections, context selection, access audits, and usage without calling external systems. Named random streams reproduce deterministic local choices. Provider output is not promised to regenerate byte-for-byte; the durable guarantee is the exact committed transcript and declarations.</p> <p>Corrupt bytes, unknown schemas, gaps, mismatched IDs, and incomplete atomic batches fail explicitly rather than falling back to a partial interpretation.</p>"},{"location":"guides/evaluation-and-budgets/","title":"Evaluation and budgets","text":"<p>Status: Shipped.</p> <p>A <code>Task</code> declares the evaluator, artifact representation, objectives, and optional budget ceiling. The evaluator\u2014not the candidate\u2014owns what better means.</p> <pre><code>import meta_evolve as meta\n\ntask = meta.Task(\n    evaluator=lambda text: {\"quality\": len(set(text.split()))},\n    objectives=(meta.Maximize(\"quality\"),),\n    budget=meta.Budget(evaluations=6, trials=5),\n)\n</code></pre> <p><code>Maximize</code> and <code>Minimize</code> identify ordered objectives. A run-level budget may narrow the task ceiling but cannot widen it.</p> <p><code>Task.environment</code> is an immutable evaluator-owned declaration. Its empty default preserves ordinary runs; when supplied it contributes to task and run identity, so changing a nested episode environment invalidates its <code>RunId</code>.</p>"},{"location":"guides/evaluation-and-budgets/#exact-accounting","title":"Exact accounting","text":"<p>Seed evaluation consumes one evaluation and no successor trial. Each attempted successor consumes one trial; a valid evaluated successor consumes one evaluation. Token and wall usage come from the component action that actually performed the work. <code>Usage</code> is immutable and aggregated in the run summary.</p> <p>Search checks the effective budget before starting more work. Exhaustion is a recorded stop reason. Components never grant themselves additional work.</p>"},{"location":"guides/evaluation-and-budgets/#evidence-and-uncertainty","title":"Evidence and uncertainty","text":"<p>An evaluation can return scalar or objective-keyed metrics, uncertainty, immutable evidence, and action usage. Evidence records what was observed; it is not a candidate-controlled score. External evaluators also record environment and bounded process evidence where available.</p>"},{"location":"guides/evaluation-and-budgets/#typed-failures","title":"Typed failures","text":"<p>Invalid candidate shape, proposer failure, evaluator failure, timeout, resource exhaustion, policy violation, and infrastructure failure retain their distinct types and details. A trial with no valid evaluation is non-rankable. It never becomes zero, negative infinity, or a fabricated metric.</p> <p>Run-level capability and durability errors use the shipped error hierarchy, including <code>CapabilityError</code>, <code>BudgetUnavailable</code>, and <code>InvalidDecision</code>.</p>"},{"location":"guides/evaluation-and-budgets/#authority","title":"Authority","text":"<p>The candidate may propose artifact content and declare causal observations. The task/evaluator owns hidden data, environment, metrics, and failure translation; the coordinator owns effective budgets and commitment; governance owns permissions and promotion.</p>"},{"location":"guides/experience/","title":"Experience","text":"<p>Status: Shipped. Experience is explicit and off by default.</p>"},{"location":"guides/experience/#no-memory","title":"No memory","text":"<p>With no <code>context=</code> or <code>experience=</code> declaration, a proposer receives no prior records. <code>NoMemory</code> states that policy explicitly when a named declaration is useful.</p>"},{"location":"guides/experience/#pushed-context","title":"Pushed context","text":"<p>A <code>ContextPolicy</code> selects a bounded bundle before each proposal. <code>AncestorsOnly</code> follows the current parent lineage; <code>BestSibling</code> exposes a bounded sibling view. The selected structured records, rendered text, reason, logical time, and usage are committed before the proposer runs.</p> <pre><code>import meta_evolve as meta\n\nexperiment = meta.Experiment(\n    task=task,\n    seed=seed,\n    proposer=proposer,\n    search=meta.Greedy(max_trials=3),\n    context=meta.AncestorsOnly(max_records=4, max_chars=4000),\n)\nresult = meta.run(experiment)\n</code></pre>"},{"location":"guides/experience/#audited-pull","title":"Audited pull","text":"<p><code>PullAccess</code> grants bounded operations, result counts, records, and characters. The proposer uses an <code>ExperienceReader</code> only inside that grant. Each request, result, denial, and usage charge is retained; hidden or future records fail closed through typed access errors.</p>"},{"location":"guides/experience/#declared-derivation","title":"Declared derivation","text":"<p>A rich proposal may declare which authorized observations influenced it. Those references are validated against the experience it actually received. Empty derivation remains compatible with ordinary callables.</p>"},{"location":"guides/experience/#causal-provenance","title":"Causal provenance","text":"<p>Everyday users can rely on <code>Run.lineage()</code>, <code>Run.trials()</code>, and <code>RunExperienceUsage</code>. Strict ledger ordering, logical-time validation, and replay are isolated in the advanced provenance reference.</p> <p>Experience changes what a proposer may observe, never the evaluator, budget, permission, or promotion authority.</p>"},{"location":"guides/extensions/","title":"Extensions","text":"<p>Status: Shipped. Add replaceable components at declared seams; never branch the coordinator on a concrete domain, provider, evaluator, policy, or backend.</p>"},{"location":"guides/extensions/#artifact-domains","title":"Artifact domains","text":"<p>An artifact codec declares a stable kind, representation, interfaces, canonical encode/decode behavior, and rejection boundary. Register it with <code>Registry</code>. The same codec encodes the seed and every successor; rejected values become typed invalid-output trials.</p> <p>The complete artifact-domain tutorial is extracted, executed in a clean interpreter, and checked against the published codec conformance suite.</p>"},{"location":"guides/extensions/#components-and-identity","title":"Components and identity","text":"<p>Local callables are convenient but not cold-resumable. Registered durable-local proposers, evaluators, search policies, and context policies use explicit component references plus behavior-affecting configuration. Resume rebuilds the recorded implementation from that registry and refuses mismatches.</p>"},{"location":"guides/extensions/#published-conformance","title":"Published conformance","text":"<p>Every extension component family has a published reusable suite: codecs, repository-editing proposers, evaluators, search programs, context policies, workspaces, sandboxes, and typed mutation surfaces. Subclass one, name the subclass <code>Test...</code>, and override its fixtures and hooks. Each runs unchanged here against the built-in implementations (or, for the proposer suites, reference callables) plus at least one written the way an outside author would write it \u2014 <code>MutationSurfaceTests</code> against <code>TextParam</code>, <code>PolicyParam</code>, <code>ParentSelection</code>, and <code>TrialLimit</code>; <code>SearchProgramTests</code> against Greedy, BestOfN, Population, CandidatePolicy, and a <code>next</code>-only custom program.</p> <p>The two proposer suites are exercised by in-repository reference callables (<code>tests/test_proposer_conformance.py</code>); no first-party provider adapter ships, so those callables are the reference subjects.</p> <p><code>SandboxProviderTests</code> checks the declared cwd and exact environment, verbatim argv, separated output streams, ordinary nonzero exits, enforced timeout, independent stdout/stderr byte ceilings, bounded partial output, and wall-only usage. Its timeout and probe-sleep fixtures are overridable for container and remote startup costs. Passing it does not prove filesystem isolation or that a provider executed rather than simulated every probe; CPU, memory, network, syscall, and broader execution isolation remain outside the suite.</p> <p><code>ContextPolicy.select</code> receives a typed, read-only <code>ExperienceGraph</code>. Import <code>ExperienceGraph</code> and <code>ExperienceQuery</code> from <code>meta_evolve</code>; the returned <code>ExperienceNode</code>, <code>ExperienceRecord</code>, and <code>ExperienceState</code> values are root exports too. Replay construction remains an application service, so helpers such as <code>replay_experience_graph</code> stay under <code>meta_evolve.application</code>.</p> <p>State the limits rather than implying coverage that does not exist. A suite checks the laws it names and no others, and it makes an unexercised law visible: <code>EvaluatorTests</code> skips its typed-failure check when a subclass declares no <code>rejected_candidates</code> and its semantic oracle when it declares no <code>scored_candidates</code>, <code>ArtifactCodecTests</code> skips its aliasing law when <code>mutable_values</code> is <code>()</code> and its decode-validation law when <code>rejected_payloads</code> is <code>()</code>, and <code>ContextPolicyTests</code> makes a policy that selects nothing say so through <code>expects_records</code>. <code>NoMemory</code> is outside that suite, since <code>meta.run</code> normalizes it away before any policy runs.</p> <p>The subclass contract follows one rule set, stated in the package docstring: required hooks raise <code>NotImplementedError</code>; optional laws skip visibly or cross-check a declaration; a law needing an uncontaminated second subject asks for a factory (<code>make_evaluator</code>, <code>search_factory</code>) and refuses the same object twice; laws observe subjects through kernel records or author-supplied projections, never the subject's own equality or self-report.</p> <p>One trap is worth knowing when you write a codec: <code>decode</code> returning an equal value is not the same as returning an independent one. A codec that hands back the instance it was given lets an evaluator reach and edit the caller's own object. <code>ArtifactCodecTests.mutable_values</code> has no default for that reason \u2014 declaring <code>()</code> is a positive claim that your values are immutable, not a way to skip the question. Every value law observes through <code>value_state</code>, an observation the codec has no hand in \u2014 never through the value type's <code>__eq__</code> (which usually ships with the codec and could certify a collapse) and never through the codec's own <code>encode</code> (which could special-case the watched objects); the default projection knows exact canonical and kernel types and refuses anything else. Mutable values additionally require <code>mutate</code>, the edit the aliasing law watches. <code>rejected_payloads</code> is the mirror of <code>rejected_values</code> on the decode side \u2014 decode is validation, and <code>()</code> is a positive claim that every payload decodes. <code>EvaluatorTests.scored_candidates</code> is the analogous semantic oracle: every other evaluator law holds for <code>lambda c: 0.0</code>, so state expected metrics wherever your scores are predictable. <code>EvaluatorTests</code> keeps a matching candidate-mutation law, so the failure is caught where it starts and where it does damage.</p> <p>Store adapters keep an internal contract under <code>tests/</code> and are not one of these families. The operations lab holds the coverage table.</p>"},{"location":"guides/extensions/#migrating-pre-10-conformance-subclasses","title":"Migrating pre-1.0 conformance subclasses","text":"<p>The strengthened codec and mutation-surface laws intentionally changed their subclass fixtures. There are no compatibility aliases: update subclasses so the new laws run instead of silently retaining the weaker contract.</p> <p>For codecs, <code>rejected_payloads</code> and <code>mutable_values</code> are now required. <code>value_state</code> must project custom values the default inert projection cannot read, and <code>mutate</code> is required only when <code>mutable_values</code> is non-empty.</p> <p>Before:</p> <pre><code>class TestRecipeCodec(ArtifactCodecTests):\n    @pytest.fixture\n    def codec(self): return RecipeCodec()\n\n    @pytest.fixture\n    def values(self): return (Recipe([\"mix\"]),)\n\n    @pytest.fixture\n    def rejected_values(self): return (7,)\n</code></pre> <p>After:</p> <pre><code>class TestRecipeCodec(ArtifactCodecTests):\n    # codec, values, and rejected_values stay as above\n    @pytest.fixture\n    def rejected_payloads(self): return (7, None)\n\n    @pytest.fixture\n    def mutable_values(self): return (Recipe([\"mix\"]),)\n\n    def value_state(self, value): return tuple(value.steps)\n\n    def mutate(self, value): value.steps.append(\"bake\")\n</code></pre> <p>Return <code>()</code> from <code>mutable_values</code> for an immutable value type and omit <code>mutate</code>. Return <code>()</code> from <code>rejected_payloads</code> only when every canonical payload is valid input to <code>decode</code>; both empty tuples are positive claims.</p> <p>For mutation surfaces, replace the singular <code>mutation</code> plus <code>expected_surface</code> fixtures with plural <code>(mutation, expected_successor)</code> pairs, and replace <code>invalid_mutation</code> with <code>invalid_mutations</code>.</p> <p>Before:</p> <pre><code>@pytest.fixture\ndef mutation(self): return Rename(\"new\")\n\n@pytest.fixture\ndef expected_surface(self): return NameParam(\"new\")\n\n@pytest.fixture\ndef invalid_mutation(self): return Rename(7)\n</code></pre> <p>After:</p> <pre><code>@pytest.fixture\ndef mutations(self):\n    return ((Rename(\"first\"), NameParam(\"first\")),\n            (Rename(\"second\"), NameParam(\"second\")))\n\n@pytest.fixture\ndef invalid_mutations(self):\n    return (Rename(7),)\n</code></pre> <p>Keep <code>surface</code>, <code>no_op_mutation</code>, and <code>foreign_mutation</code> unchanged. Supply more than one valid pair wherever the domain allows, so an implementation that accepts one fixture value but substitutes its own value for others cannot pass.</p>"},{"location":"guides/extensions/#design-rule","title":"Design rule","text":"<p>Introduce a protocol only when a current acceptance test needs an independently testable second implementation. Keep the public call smaller than the internal port, preserve immutable inputs and typed failures, and make authority and accounting explicit.</p> <p>The generated API reference lists every shipped root name and the namespaced harness and policy families. See the harness evolution and policy evolution guides.</p>"},{"location":"guides/harness-evolution/","title":"Harness evolution","text":"<p>Status: Shipped. The deterministic library and flagship are implemented. The separate official-SDK validation remains Experimental/live-only. Its first authorized capture is retained honestly as a schema-validation failure and tie; M8 closes only after a capture reaches native mutation decoding and core-side successor validation.</p> <p>An agent harness is a typed composite artifact with two v1 mutation surfaces:</p> <pre><code>import meta_evolve as meta\nfrom meta_evolve import harness\nfrom meta_evolve.domain import ComponentRef\n\nbaseline = harness.HarnessArtifact(\n    planner=harness.TextParam(\n        \"planner\",\n        ComponentRef(role=\"planner\", name=\"worker\", version=\"1\"),\n        \"planner-text/v1\",\n        \"Inspect the task and make a conservative repair.\",\n    ),\n    context_policy=harness.PolicyParam(\n        \"context_policy\",\n        ComponentRef(role=\"context-policy\", name=\"context\", version=\"1\"),\n        \"context-policy/v1\",\n        {\"diagnostics\": False},\n    ),\n)\n</code></pre> <p>A user-owned TFD callable asks its SDK for native structured output matching <code>HarnessMutation</code>, uses <code>baseline.apply(mutation)</code> to construct the intended candidate, and returns an ordinary <code>ProposalResult</code>. The core independently compares every encoded successor with its parent before commit. Constructing a <code>HarnessArtifact</code> directly cannot bypass that check.</p> <p>Search uses development episodes. Held-out evaluation is separate:</p> <pre><code>baseline_experiment = meta.Experiment(\n    task=held_out_task,\n    seed=baseline,\n    proposer=proposer,\n    search=meta.Greedy(max_trials=0),\n)\ncandidate_experiment = meta.Experiment(\n    task=held_out_task,\n    seed=selected.value,\n    proposer=proposer,\n    search=meta.Greedy(max_trials=0),\n)\nbaseline_result = meta.run(baseline_experiment)\ncandidate_result = meta.run(candidate_experiment)\ncomparison = meta.compare_harnesses(baseline_result, candidate_result)\n\nif comparison.promotion_eligible:\n    meta.export_harness(selected, destination)\n</code></pre> <p><code>compare_harnesses</code> fails closed unless evaluator identity and its full configuration, artifact schema, objectives, effective budgets, proposer/search declarations, random seed, context, experience, and immutable harness declarations agree. The verdict is <code>improvement</code>, <code>tie</code>, <code>regression</code>, or <code>unrankable</code>. Eligibility never deploys or promotes anything.</p> <p><code>RepositoryEpisode</code> keeps its public seed separate from evaluator-only tests and bounded <code>ProcessSpec</code>. <code>EpisodeSuite</code> requires non-empty development and held-out splits with globally unique names, and both values expose stable content digests. Evaluator identity must cover the suite, worker/model, budgets, timeouts, environment, and scoring implementation.</p> <p>Run the complete deterministic example:</p> <pre><code>uv run python examples/research/harness_evolution/main.py \\\n  --output /tmp/meta-evolve-m8\n</code></pre> <p>It records development quality <code>0.0 \u2192 0.5 \u2192 1.0</code>, evaluates one untouched held-out repository only after selection, and exports the eligible selection. That single held-out fixture demonstrates the execution and authority boundary; it is not statistically strong promotion evidence.</p>"},{"location":"guides/migration-user-owned-sdk/","title":"Migrate to a user-owned SDK wrapper","text":"<p>Status: Shipped. This is a deliberate pre-1.0 breaking change. There are no compatibility shims.</p> <p>The following imports and extras were removed:</p> <pre><code>from meta_evolve.claude import Claude\nfrom meta_evolve.codex import Codex\nmeta-evolve[claude]\nmeta-evolve[codex]\n</code></pre> <p>Install and authenticate your chosen SDK in your application, then call it from an ordinary proposer:</p> <pre><code>import meta_evolve as meta\n\ndef propose(parent, context):\n    response = sdk.generate_structured(parent=parent)\n    return meta.ProposalResult(\n        candidate=response.candidate,\n        usage=meta.Usage(\n            tokens=response.input_tokens + response.output_tokens,\n            wall_seconds=response.elapsed_seconds,\n        ),\n        metadata={\"model\": response.model},\n        evidence=(\n            meta.EvidenceDraft(kind=\"sdk-response\", data=response.telemetry),\n        ),\n    )\n\nexperiment = meta.Experiment(task=task, seed=seed, proposer=propose)\nresult = meta.run(experiment)\n</code></pre> <p>The wrapper now owns authentication, sessions, tools, permissions, output decoding, retry behavior, and provider telemetry. Meta-Evolve continues to own candidate validation, external evaluation, budgets, durable records, lineage, experience auditing, and promotion authority.</p> <p>For agent-directed pull, register an SDK custom tool whose handler delegates to <code>context.experience</code>, and return used observation references in <code>ProposalResult.derived_from</code>. SDKs without custom tools may receive pushed <code>context.bundle</code> data only.</p> <p>The obsolete first-party adapter archive is no longer part of the source tree. Historical measurements remain recoverable from Git history and the closed M6 issues; they are not current fixtures or acceptance evidence.</p> <p>See <code>ProposalResult</code> and <code>ProposerContext</code> for the promoted callable kit.</p>"},{"location":"guides/policy-evolution/","title":"Policy evolution","text":"<p>Status: Shipped. Milestone 9 evaluates tiny declarative search-policy artifacts. Candidates never supply executable policy source.</p>"},{"location":"guides/policy-evolution/#declare-the-trusted-artifact","title":"Declare the trusted artifact","text":"<pre><code>from meta_evolve import policy\n\ncandidate = policy.PolicyArtifact(\n    parent_selection=policy.ParentSelection(\"incumbent\"),\n    trial_limit=policy.TrialLimit(2),\n)\n</code></pre> <p>The schema fixes both surface names, component declarations, interfaces, and allowed domains. <code>PolicyMutation</code> changes exactly one value. Wrapper-side <code>apply()</code> is convenient, while the built-in codec independently validates the encoded successor before artifact commit.</p>"},{"location":"guides/policy-evolution/#interpret-it-through-an-ordinary-run","title":"Interpret it through an ordinary run","text":"<pre><code>import meta_evolve as meta\nfrom meta_evolve import policy\n\nexperiment = meta.Experiment(\n    task=episode.task,\n    seed=episode.seed,\n    proposer=episode.proposer,\n    search=policy.CandidatePolicy(candidate),\n    random_seed=episode.random_seed,\n)\nresult = meta.run(experiment, budget=episode.budget)\n</code></pre> <p><code>seed</code> parent selection behaves like independent best-of-N. <code>incumbent</code> behaves like greedy expansion. The artifact's trial limit is voluntary: the evaluator- owned inner budget remains authoritative. The canonical artifact payload is the registered <code>candidate-policy@1</code> configuration, so durable reconstruction uses the existing policy registry and resume path.</p>"},{"location":"guides/policy-evolution/#capture-episodes-without-adding-a-runtime","title":"Capture episodes without adding a runtime","text":"<p><code>PolicyEpisode</code> declares a task, canonically encoded seed, proposer, budget, random seed, and evaluator-owned environment. <code>PolicySuite</code> requires non-empty, unique development and held-out splits. Registered references are portable; local closures remain valid only for local composition.</p> <p>User code executes ordinary <code>meta.run()</code> calls and records <code>EpisodeOutcome</code> values. Each outcome retains its inner <code>RunId</code>, evaluator-owned storage location, policy digest, quality, uncertainty, exact inner usage, typed failure or incomplete state, and exact-completed-run reuse flag. Development may reopen an exact completed run. Held-out execution uses a new empty namespace and never consults development results.</p> <p><code>PolicyEvaluation</code> recomputes aggregation from every outcome. A failed, incomplete, or missing episode makes the aggregate non-rankable without discarding partial evidence. Inner trials and evaluations stay inner usage; only actual tokens and elapsed time appear in <code>outer_usage</code>.</p>"},{"location":"guides/policy-evolution/#compare-untouched-held-out-evidence","title":"Compare untouched held-out evidence","text":"<p>Evaluate the unchanged baseline and selected candidate in separate seed-only outer runs using <code>Greedy(max_trials=0)</code>, then compare them:</p> <pre><code>comparison = meta.compare_policies(baseline_run, candidate_run)\n</code></pre> <p>Comparison fails closed on changed suites, splits, evaluator/aggregation identity, schemas, objectives, proposers, <code>candidate-policy@1</code>, effective budgets, environments, randomness, context, experience, or immutable surface declarations. Verdicts are <code>improvement</code>, <code>tie</code>, <code>regression</code>, or <code>unrankable</code>. <code>compare_policies</code> returns this report. <code>promotion_eligible</code> requires a strict, fully rankable improvement under equal controls. It reports evidence and never promotes or deploys.</p> <p>Run the deterministic multi-episode flagship:</p> <pre><code>uv run python examples/research/policy_evolution/main.py --output /tmp/meta-evolve-m9\n</code></pre>"},{"location":"guides/providers/","title":"User-owned SDK integration","text":"<p>Status: Shipped. Any SDK integrates through the same callable boundary as deterministic Python. Meta-Evolve does not install, authenticate, configure, or wrap a model SDK.</p>"},{"location":"guides/providers/#start-with-the-ordinary-callable","title":"Start with the ordinary callable","text":"<pre><code>import meta_evolve as meta\n\ndef propose(parent: meta.Text, context: meta.ProposerContext) -&gt; meta.Text:\n    response = my_sdk.generate_structured(\n        input=str(parent),\n        context=context.bundle.rendered,\n    )\n    return meta.Text(response.candidate)\n\nexperiment = meta.Experiment(\n    task=meta.Task(evaluator=lambda text: len(text), artifact=meta.Text),\n    seed=meta.Text(\"draft\"),\n    proposer=propose,\n)\nresult = meta.run(experiment)\n</code></pre> <p>The wrapper owns SDK authentication, session lifecycle, tools, permissions, structured decoding, retry policy, and provider-specific telemetry. Return the decoded artifact value directly when that is all the run needs.</p>"},{"location":"guides/providers/#return-a-typed-result","title":"Return a typed result","text":"<p>Use <code>ProposalResult</code> when the wrapper has more to report:</p> <pre><code>def propose(parent, context):\n    response = my_sdk.generate_structured(input=parent)\n    return meta.ProposalResult(\n        candidate=response.artifact,\n        usage=meta.Usage(\n            tokens=response.input_tokens + response.output_tokens,\n            wall_seconds=response.elapsed_seconds,\n        ),\n        metadata={\"model\": response.model},\n        evidence=(\n            meta.EvidenceDraft(\n                kind=\"sdk-response\",\n                data={\"cost\": response.cost, \"raw\": response.raw},\n            ),\n        ),\n    )\n</code></pre> <p>Only total tokens and elapsed time enter shared <code>Usage</code>. Cost, model identity, granular token categories, and raw provider telemetry belong in evidence or metadata. Trial and evaluation counts are coordinator-owned; reporting either produces typed <code>InvalidOutput</code>.</p> <p>Ordinary SDK exceptions become <code>ProposerFailure</code>. Framework failures about durable state, accounting, or run capability escape unchanged: <code>EventStreamConflict</code>, <code>DurableCorruption</code>, <code>InvalidSearchHistory</code>, <code>ExperienceStoreUnavailable</code>, <code>ExecutionAccountingUnknown</code>, and <code>CapabilityError</code>.</p>"},{"location":"guides/providers/#give-an-sdk-audited-pull-tools","title":"Give an SDK audited pull tools","text":"<p>When an SDK supports custom tools, register a user-owned handler that delegates to <code>context.experience</code>:</p> <pre><code>used = []\n\ndef search_experience(arguments):\n    if context.experience is None:\n        raise meta.ExperienceAccessDenied(\"pull access was not granted\")\n    observation = context.experience.search(**arguments)\n    used.extend(record.ref for record in observation.records)\n    return observation\n\nresponse = my_sdk.run(tools={\"search_experience\": search_experience})\nreturn meta.ProposalResult(\n    candidate=response.artifact,\n    derived_from=tuple(dict.fromkeys(used)),\n)\n</code></pre> <p>The exact observation API is defined by <code>ExperienceReader</code>. The callback may run in process, or a user-owned RPC service may forward each request to this scoped handler and return its result. The RPC service must not cache or broaden the reader capability. Meta-Evolve adds no generic tool server or agent process.</p> <p>Access denial, exhaustion, audit-store failure, and unknown accounting retain their normal semantics because the SDK tool calls the authoritative reader. SDKs without custom-tool support can receive pushed <code>context.bundle</code> data, but that is context injection rather than agent-directed pull.</p> <p>The credential-free <code>06_user_owned_sdk</code> example contains a complete deterministic fake SDK and audited tool callback. An acceptance-gated milestone may own an opt-in real-client experiment, as M8 does for harness validation. There is no generic provider smoke directory: a live script must serve a current gate, add no package dependency, and retain its single authorized outcome honestly.</p>"},{"location":"guides/providers/#authority-and-reproducibility","title":"Authority and reproducibility","text":"<p>The wrapper may propose candidate content. It cannot choose the evaluator, change the run budget, grant itself experience, edit audit history, or promote a result. External evaluation and durable storage remain the sources of score, accounting, and lineage truth.</p> <p>Committed requests, evidence, usage, failures, and artifacts support inspection and replay. Meta-Evolve does not claim that a later external SDK call will regenerate byte-identical output.</p> <p>API details: <code>ProposalResult</code>, <code>ProposerContext</code>, and <code>EvidenceDraft</code>.</p>"},{"location":"guides/runs-and-results/","title":"Runs and results","text":"<p>Status: Shipped.</p> <p><code>meta.improve()</code> is the smallest bounded local path. It evaluates the seed, asks the proposer for successors, evaluates each valid successor independently, and uses greedy search to decide where work goes next.</p> <pre><code>import meta_evolve as meta\n\nitems = (\"outline\", \"draft\", \"review\")\n\ndef complete_one(completed):\n    return (*completed, items[len(completed)])\n\nresult = meta.improve(\n    seed=(),\n    proposer=complete_one,\n    evaluator=len,\n    trials=3,\n)\n</code></pre> <p>That call lowers exactly to:</p> <pre><code>experiment = meta.Experiment(\n    task=meta.Task(evaluator=len),\n    seed=(),\n    proposer=complete_one,\n    search=meta.Greedy(max_trials=3),\n)\nresult = meta.run(experiment)\n</code></pre> <p>Use the expansion for custom artifacts, objectives, satisfaction thresholds, task budgets, search, context, experience, execution mode, or registry components. <code>Experiment</code> owns declaration fields; <code>storage=</code>, <code>mode=</code>, and a narrowing <code>budget=</code> remain call-level execution overlays. The declaration-keyword <code>run()</code> form remains supported through 1.x and is not the taught path.</p> <p><code>improve(storage=Storage.durable(...))</code> persists local history, but the fa\u00e7ade does not claim durable-local reconstruction or resumability. Those require an explicit <code>Experiment</code>, registered reconstructible components, and <code>run(..., mode=\"durable-local\")</code>.</p>"},{"location":"guides/runs-and-results/#pace-one-decision-at-a-time","title":"Pace one decision at a time","text":"<p><code>Session</code> is the execution form beneath <code>run()</code>. Start it when a caller needs to observe each completed policy decision:</p> <pre><code>with meta.Session.start(experiment, storage=storage) as session:\n    first = session.step()\n    for decision in session:\n        observe(decision)\nresult = session.run\n</code></pre> <p>The seed is already bootstrapped when <code>start()</code> returns. A successful <code>step()</code> returns one immutable trial, archive, or stop decision only after its authorized work is complete. Manual steps and later iteration continue without repeating a decision. A newly committed stop is yielded once; repeated terminal <code>step()</code> calls return the same cached stop view without writes.</p> <p>Leaving the context closes only that handle's execution scope. Its stable <code>session.run</code> and <code>session.stopped</code> remain queryable, and caller-owned storage stays open. Exit does not invent a stop for an incomplete run.</p> <p>The seed is an evaluated baseline, not trial zero. Every successor proposal has its own trial and either an artifact plus evaluation or a typed failure.</p>"},{"location":"guides/runs-and-results/#search-presets","title":"Search presets","text":"<p>Search changes allocation, not proposer or evaluator semantics:</p> <ul> <li><code>Greedy</code> expands the current best successful artifact.</li> <li><code>BestOfN</code> samples independent successors from the seed.</li> <li><code>Population</code> retains a bounded population and uses   deterministic tournament selection.</li> </ul> <p>All presets consume the same declared <code>Budget</code> and expose the same <code>Run</code> queries.</p>"},{"location":"guides/runs-and-results/#functional-incumbent-refinement","title":"Functional incumbent refinement","text":"<p>Use <code>@search</code> when a bounded custom policy only needs to choose between the seed and current incumbent:</p> <pre><code>@meta.search(max_trials=20)\ndef incumbent_refinement(search):\n    \"\"\"Expand the best measured artifact.\"\"\"\n    return search.expand(search.incumbent or search.seed)\n</code></pre> <p>The decorated value is an ordinary bindable search program and can be placed directly on <code>Experiment.search</code>. The callback receives opaque occurrence handles, immutable <code>search.configuration</code>, <code>search.expand(parent)</code>, and <code>search.stop(reason)</code>\u2014not <code>SearchState</code>, budgets, proposer references, trial records, or randomness. Additional behavior-affecting values are declared on the decorator, for example <code>@meta.search(max_trials=20, patience=3)</code>, and are persisted in <code>SearchSpec</code> and included in run identity. Captured closure values are rejected with guidance to declare configuration and keep helpers at module scope.</p> <p>The callback name is humanized in recorded decisions, and the first cleaned docstring line becomes its summary. Envelope stops such as objective satisfaction, the trial limit, and budget exhaustion use the same narration. Unsupported returns fail before proposer work and name the supported <code>expand()</code> and <code>stop()</code> actions.</p> <p>Memory and durable storage both work in ordinary local mode. Reconstructible <code>durable-local</code> execution is the existing full extension workflow: register a module-level policy factory under the decorated policy reference, alongside the proposer and evaluator. The fa\u00e7ade performs no ambient or automatic registration. Use <code>SearchProgram.next()</code> when a policy needs multiple parents, archive updates, populations, or other kernel decisions.</p>"},{"location":"guides/runs-and-results/#query-the-result","title":"Query the result","text":"<ul> <li><code>best()</code> decodes the selected artifact value.</li> <li><code>best_trial()</code> returns its <code>TrialView</code>.</li> <li><code>trials()</code> returns every seed and successor outcome in logical order.</li> <li><code>inspect()</code> returns one winner-independent   <code>RunInspection</code> from one validated replay.</li> <li><code>lineage()</code> follows the selected artifact's causal parents.</li> <li><code>summary()</code> returns score, usage, experience use, and winning lineage.</li> <li><code>compare(other)</code> checks declaration compatibility before naming a winner.</li> </ul> <p><code>Run</code> is inspectable even when no successor succeeds. <code>best()</code> then raises the typed <code>NoSuccessfulEvaluation</code> rather than inventing a low score.</p> <p><code>inspect()</code> is the complete read boundary for authors and presentation code. Its <code>overview</code> separates lifecycle (<code>complete</code> or <code>incomplete</code>) from rankability (<code>rankable</code> or <code>unrankable</code>), so failed evaluations and recoverable prefixes do not become synthetic run states or scores. <code>declaration</code> retains the resolved search (including the default <code>max_trials=20</code>), component roles, budgets, capabilities, context, experience, and root random seed. Occurrence-ordered <code>trials</code>, typed <code>decisions</code>, <code>evidence</code>, and <code>artifacts</code> expose the complete history without exposing events or replay projections. The bootstrap seed is a trial at logical step zero, never a search decision.</p> <p>Decision inspection is a real sum type: <code>TrialDecisionView</code> names selected parents and an optional completed trial, <code>ArchiveDecisionView</code> reports admission, eviction, and resulting membership, and <code>StopDecisionView</code> reports the terminal reason. Every variant carries its durable identity, logical step, non-optional rationale, and named randomness.</p>"},{"location":"guides/runs-and-results/#artifact-values","title":"Artifact values","text":"<p>Plain Python values use the default codec. <code>Text</code>, <code>SourceTree</code>, and <code>SkillSet</code> provide shipped typed domains. Every accepted successor is frozen before commitment and records distinct occurrence identity plus parent lineage.</p>"},{"location":"guides/runs-and-results/#comparison","title":"Comparison","text":"<p>Comparison refuses incompatible task, evaluator, artifact, model/environment, or relevant declaration identity through <code>RunsNotComparable</code>. Random seed, context, and experience differences remain visible compatibility flags when the comparison contract permits them. A comparison is measured evidence, never a promotion.</p>"},{"location":"learn/01-define-better/","title":"Define better","text":"<p>Status: Shipped.</p> <p><code>improve()</code> uses a default objective: maximize the evaluator's scalar result. Expand to a <code>Task</code> when direction, a metric name, or satisfaction matters:</p> <pre><code>task = meta.Task(\n    evaluator=evaluate,\n    objectives=(meta.Maximize(\"score\", satisfy=0),),\n)\nexperiment = meta.Experiment(\n    task=task,\n    seed=0,\n    proposer=mutate,\n    search=meta.Greedy(max_trials=8),\n    random_seed=25,\n)\nresult = meta.run(experiment)\n</code></pre> <p><code>Task</code> owns evaluator semantics; it is not a declaration parallel to an evaluator. The seed is evaluated first. Here <code>evaluate()</code> returns one number, which is assigned to the named <code>score</code> objective. A mapping becomes useful only when an evaluator reports multiple or additional metrics.</p> <p>Run the complete expansion:</p> <pre><code>uv run python examples/learn/number_search/main.py\n</code></pre> Output<pre><code>12\n[0, 1, 2, 7, 12]\n</code></pre> <p>The <code>satisfy=0</code> threshold stops the run when it reaches the target. The search policy, not a duplicate task budget, bounds the eight successor attempts.</p>"},{"location":"learn/01-define-better/#change-and-predict","title":"Change and predict","text":"<p>Remove <code>satisfy=0</code>. The same result can still rank best, but reaching it no longer declares the objective satisfied; the run continues until search stops.</p> <p>Back to Start here \u00b7 Next: Change search</p>"},{"location":"learn/02-change-search/","title":"Change search","text":"<p>Status: Shipped.</p> <p>Keep <code>Task</code>, seed, and proposer fixed. Change only the <code>search=</code> field on the same <code>Experiment</code> declaration:</p> <pre><code>greedy = meta.Experiment(\n    task=task, seed=(), proposer=propose, search=meta.Greedy(max_trials=3)\n)\nindependent = meta.Experiment(\n    task=task, seed=(), proposer=propose, search=meta.BestOfN(max_trials=3)\n)\ngreedy_result = meta.run(greedy)\nindependent_result = meta.run(independent)\n</code></pre> <p><code>Greedy</code> expands the best evaluated result so far, producing a chain. <code>BestOfN</code> always expands the seed, producing a star. Generation and evaluation do not change.</p> <pre><code>uv run python examples/learn/checklist/_stories/search_story.py\n</code></pre> Output<pre><code>Greedy best: ('outline', 'draft', 'review')\nBestOfN best: ('outline',)\nsame proposer: propose\nsame evaluator: evaluate\n</code></pre> <p>Use <code>Population</code> when retaining several ranked branches matters. It adds a ranked archive and tournament selection after the chain-versus-star distinction is clear. Use <code>@meta.search</code> only when none of these presets can express the decision.</p>"},{"location":"learn/02-change-search/#change-and-predict","title":"Change and predict","text":"<p>Swap only <code>Greedy</code> for <code>BestOfN</code> in number search. Predict how its winning lineage changes when every proposal starts from <code>0</code>.</p> <p>Previous: Define better \u00b7 Next: Inspect results</p>"},{"location":"learn/03-inspect-results/","title":"Inspect results","text":"<p>Status: Shipped.</p> <p>A <code>Run</code> answers both \u201cwhat ranked best?\u201d and \u201cwhat happened?\u201d</p> <ul> <li><code>best()</code> returns the selected artifact; <code>best().value</code> is its payload.</li> <li><code>best_trial()</code> includes the winning evaluation and evidence.</li> <li><code>trials()</code> includes the seed, losers, and typed failures in logical order.</li> <li><code>lineage()</code> keeps the causal path to the selected artifact.</li> <li><code>compare()</code> compares compatible declarations.</li> <li><code>summary()</code> is compact report and comparison data.</li> <li><code>summary().usage</code> reports the exact work consumed.</li> <li><code>inspect()</code> works even when a run is failed, incomplete, or unrankable, and   supplies the same aggregate used by the CLI and browser.</li> </ul> <pre><code>uv run python examples/learn/checklist/_stories/search_story.py\n</code></pre> Output<pre><code>best(): ('outline', 'draft', 'review')\nbest_trial(): (3, 3)\ntrials(): (((), 'evaluated'), (('outline',), 'evaluated'), (('outline', 'draft'), 'evaluated'), (('outline', 'draft', 'review'), 'evaluated'))\nlineage(): ((), ('outline',), ('outline', 'draft'), ('outline', 'draft', 'review'))\nsummary(): (3, Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0))\ncompare(): Greedy\nfailure: ('trial_failed', 'InvalidOutput')\ninspect(): rankable\n</code></pre> <p>To diagnose a run with no winner, do not substitute a low score:</p> <pre><code>for trial in result.trials():\n    if trial.failure is not None:\n        print(trial.state, trial.failure.kind, trial.failure.message)\n\nreport = result.inspect()\nprint(report.overview.lifecycle, report.overview.rankability)\n</code></pre> <p><code>best()</code> then raises <code>NoSuccessfulEvaluation</code> with the same direction: inspect <code>trials()</code> or <code>inspect()</code> for typed failures.</p>"},{"location":"learn/03-inspect-results/#change-and-predict","title":"Change and predict","text":"<p>Make the proposer return <code>object()</code>. Predict <code>trial_failed</code> with <code>InvalidOutput</code>, while the successfully evaluated seed remains rankable.</p> <p>Previous: Change search \u00b7 Next: Improve source</p>"},{"location":"learn/04-improve-source/","title":"Improve source against an independent grader","text":"<p>Status: Shipped.</p> <p>The same loop can improve a directory. <code>load_source_tree</code> reads UTF-8 files into an immutable <code>SourceTree</code>. The seed contains a deliberately poor <code>clamp()</code> implementation.</p> <p>The proposal path is deliberately narrow:</p> <ol> <li>Immutable source enters the run.</li> <li><code>PatchProposer</code> creates a private mutable workspace.</li> <li><code>TextPatch</code> names one path, the exact expected bytes,    and their replacement.</li> <li>The workspace is captured as an immutable successor and disposed.</li> <li><code>CommandEvaluator</code> independently materializes    that successor and runs its own hidden grader.</li> <li>Search selects only from measured results; deployment remains external.</li> </ol> <p><pre><code>uv run python examples/domains/python_function/learning_story.py\n</code></pre> Output<pre><code>poor candidate: (0.0, 'evaluated')\nselected result: (1.0, True)\nevidence: ('evaluation-environment', 'process-result')\nusage: evaluations=2 trials=1 tokens=0 wall_recorded=True\nlineage: ('659bfe4a', '08abe248')\nparent unchanged: True\nwinning source:\ndef clamp(value, low, high):\n    return max(low, min(value, high))\ninvalid proposal: InvalidOutput\nevaluator failure: EvaluatorFailure\n</code></pre></p> <p>The poor seed is valid and scores <code>0.0</code>. The exact patch produces a separately identified successor that scores <code>1.0</code>; its evidence records the evaluation environment and process result. The ingress tree remains unchanged. The <code>lineage:</code> line prints a content-digest prefix for each step; Tell equal bytes apart separates those digests from occurrence ids.</p> <p>These three negative cases mean different things:</p> <ul> <li>a poor candidate is valid, evaluated, and rankable;</li> <li>an exact patch whose expected bytes do not match is <code>InvalidOutput</code> with no   successor artifact;</li> <li>a broken grader is <code>EvaluatorFailure</code> with no score.</li> </ul> <p>The candidate cannot read or edit the grader, enlarge the budget, grant process permissions, or control promotion. Those controls stay with the experiment.</p>"},{"location":"learn/04-improve-source/#change-and-predict","title":"Change and predict","text":"<p>Change the replacement to <code>return value</code>. Predict a valid successor with score <code>0.0</code>: the patch can apply cleanly while still failing independent evaluation.</p> <p>Previous: Inspect results \u00b7 Next: Use experience</p> <p>After this deterministic path, the optional live-agent track shows provider-backed patch proposals under the same authority boundary.</p>"},{"location":"learn/05-use-experience/","title":"Use experience after repetition creates the need","text":"<p>Status: Shipped.</p> <p>Without experience, the proposer repeats the same three traps. That repeated mistake creates a concrete reason to add memory:</p> <p><pre><code>uv run python examples/learn/experience_reuse/learning_story.py\n</code></pre> Output<pre><code>repeated mistake: (0, 'evaluator_failure', 'evaluator_failure', 'evaluator_failure')\nno experience: (0, 'off')\npushed context: (10, 3)\naudited pull: (10, 7, 20)\ndeclared derivation: ((), ('evaluation',), ('evaluation', 'evaluation', 'trial', 'trial'))\n</code></pre></p>"},{"location":"learn/05-use-experience/#1-no-experience","title":"1. No experience","text":"<p>Experience is off by default. The proposer observes no prior failures, repeats them, and keeps the seed score <code>0</code>. This is a valid control arm, not a degraded fallback.</p>"},{"location":"learn/05-use-experience/#2-pushed-context","title":"2. Pushed context","text":"<p><code>AncestorsOnly</code> or another declared context policy can push a bounded selection before a proposal. The example selects three failure records and reaches <code>10</code>. Selection reason, records, rendering, logical time, and usage are committed. The experience guide contrasts the shipped push presets, including <code>BestSibling</code>.</p>"},{"location":"learn/05-use-experience/#3-audited-pull","title":"3. Audited pull","text":"<p><code>PullAccess</code> lets the proposer search and open prior records inside explicit bounds. The transcript shows seven operations and twenty returned records. Requests, results, denials, and charges remain inspectable; future or out-of-scope data stays unavailable. Count the work exactly decodes this separate meter.</p>"},{"location":"learn/05-use-experience/#4-optional-declared-derivation","title":"4. Optional declared derivation","text":"<p>A rich proposal may cite the authorized observations that influenced it. The last tuple shows those edge kinds. Empty derivation remains valid, so ordinary callables need no provenance ceremony.</p> <p>Strict observation ordering, logical-time validation, and replay belong in the advanced provenance guide, after this outcome-first path.</p>"},{"location":"learn/05-use-experience/#change-and-predict","title":"Change and predict","text":"<p>Set pushed context to one record. Predict one more repeated trap: the proposer needs evidence of two distinct failures before it jumps to <code>10</code>.</p> <p>Previous: Improve source \u00b7 Next: Persist a useful run</p>"},{"location":"learn/06-persist-run/","title":"Persist a useful run","text":"<p>Status: Shipped.</p> <p>Persist only after the run is worth keeping. The durable story has four steps: <code>Storage.durable()</code> can preserve an ordinary local run for later inspection; that alone does not make it resumable. Reconstructible <code>mode=\"durable-local\"</code> adds a registry of verified components and is the form <code>resume()</code> can continue.</p> <p><pre><code>uv run python examples/operations/crash_recovery/learning_story.py\n</code></pre> Output<pre><code>store: (2, 6)\nreopen: (6, True)\nresume: (True, True)\ncompare: (True, False)\n</code></pre></p>"},{"location":"learn/06-persist-run/#1-store","title":"1. Store","text":"<p><code>Storage.durable()</code> keeps artifacts and append-only transitions. The transcript stores two runs and selects <code>6</code>.</p>"},{"location":"learn/06-persist-run/#2-reopen","title":"2. Reopen","text":"<p><code>Run</code> reconstructs a read-only result. The <code>True</code> confirms that asking for <code>best()</code> wrote no new event.</p>"},{"location":"learn/06-persist-run/#3-resume","title":"3. Resume","text":"<p><code>resume</code> verifies registered components and the recovery frontier, retains the same run ID, and performs no already committed work. A crashed partial run uses the same operation; the detailed Evidence notebook demonstrates every interior crash boundary.</p>"},{"location":"learn/06-persist-run/#4-compare","title":"4. Compare","text":"<p>Two equal outcomes with different random-seed declarations compare as a tie; the <code>False</code> keeps that independent-variable difference visible. Comparison reports evidence and never promotes a result.</p>"},{"location":"learn/06-persist-run/#change-and-predict","title":"Change and predict","text":"<p>Replace one registered function after storing the run. Predict that resume fails before doing work: the recorded manifest no longer describes the live component.</p> <p>Previous: Use experience \u00b7 Next: Where to go</p>"},{"location":"learn/07-where-next/","title":"Where to go next","text":"<p>Status: Shipped.</p> <p>You now have the complete written path: define better, change search, inspect results, improve source, add experience only when needed, and persist useful runs. Notebooks remain optional Evidence companions.</p> <p>Choose the next route by outcome:</p> <ul> <li>Concepts: deepen any single idea from the path \u2014   identity, evidence,   conformance,   authority, failures,   budgets, and domains.</li> <li>Coding agents: use the bounded live-agent track   after configuring one provider extra and credentials.</li> <li>Research labs: study the SealQA mechanisms,   which are explicitly Experimental/live-only.</li> <li>Autoresearch: run the deterministic   nested research procedure with private selection and held-out checks.</li> <li>Harness evolution: run the deterministic   M8 guide and the   <code>harness_evolution</code> example. The official-SDK   capture remains Experimental/live-only.</li> <li>Policy evolution: run the deterministic   M9 guide and the   <code>policy_evolution</code> example to inspect trusted policy   artifacts, nested usage, durable inner traces, and held-out comparison.</li> <li>Extensions: add a domain through the   extension guide and its published conformance gates.</li> <li>Internals: trace declaration through projection in the   contributor code tour, then read the concise   architecture.</li> </ul> <p>The generated API reference answers exact signature and root-export questions. The programming model remains the shipped public contract.</p> <p>Previous: Persist a useful run \u00b7 Back to Start here</p>"},{"location":"learn/autoresearch/","title":"Build an autoresearch loop with Meta-Evolve","text":"<p>Status: Shipped.</p> <p>Suppose you already have an agent that can improve a solution. How would you test changes to the research procedure itself without letting that procedure change its own benchmark, budget, or promotion rule?</p> <p>This chapter builds the smallest complete answer. It runs offline in a fraction of a second, and you do not need to open the optional notebook to understand it.</p> <p>The design is inspired by Weco's AIDE\u00b2 report. It borrows the report's inner-task/outer-researcher experimental shape with fixed cost and private selection. It does not reproduce AIDE\u00b2 or provide evidence of recursive self-improvement.</p>"},{"location":"learn/autoresearch/#the-result-first","title":"The result first","text":"<p>Run this from the repository root:</p> <pre><code>uv run python examples/research/autoresearch/main.py\n</code></pre> Output<pre><code>autoresearch teaching run: outer=(evaluations=3,trials=2) inner=(evaluations=5,trials=4)\nrecipe=(1,) public={'public-large': 0.14285714285714285, 'public-small': 0.3333333333333333} private_quality=0.180556\nrecipe=(1, 2) public={'public-large': 0.2, 'public-small': 1.0} private_quality=0.321429\nrecipe=(1, 2, 4) public={'public-large': 0.3333333333333333, 'public-small': 0.5} private_quality=0.600000\nselected=(1, 2, 4)\nheld_out_baseline={'held-out-small': 0.16666666666666666, 'held-out-large': 0.09090909090909091}\nheld_out_selected={'held-out-small': 0.5, 'held-out-large': 0.14285714285714285}\npromotion=none\n</code></pre> <p>The selected recipe does better in this constructed example. That shows the wiring works; it is not a scientific result.</p> <p>Each trial retains only its public report as candidate-visible evidence:</p> <pre><code>[\n    (str(trial.artifact.value), tuple(item.kind for item in trial.evidence))\n    for trial in result.trials()\n    if trial.artifact is not None\n]\n</code></pre> Output<pre><code>[('{\"moves\":[1]}', ('autoresearch-public-report',)),\n ('{\"moves\":[1,2]}', ('autoresearch-public-report',)),\n ('{\"moves\":[1,2,4]}', ('autoresearch-public-report',))]\n</code></pre>"},{"location":"learn/autoresearch/#the-two-levels","title":"The two levels","text":"<p>\u201cInner\u201d and \u201couter\u201d describe which thing is being improved:</p> <pre><code>outer Meta-Evolve run\n\u2502\n\u251c\u2500 artifact: a recipe for conducting inner research\n\u251c\u2500 proposer: change that recipe once\n\u251c\u2500 evaluator: measure the changed researcher under a fixed protocol\n\u2514\u2500 search: retain a recipe only when private quality improves\n       \u2502\n       \u251c\u2500 inner run on public task A\n       \u251c\u2500 inner run on public task B\n       \u251c\u2500 inner run on private task A\n       \u2514\u2500 inner run on private task B\n\nafter outer selection\n\u251c\u2500 seed researcher on held-out tasks\n\u251c\u2500 selected researcher on the same held-out tasks\n\u2514\u2500 report the comparison; promote nothing\n</code></pre> <p>An inner run improves an integer on a small black-box landscape. The candidate research recipe says which move sizes it may try. <code>(1,)</code> makes slow local progress. <code>(1, 2, 4)</code> can cover more distance under the same four-trial limit.</p> <p>The integer task is intentionally unimportant. In a real project, replace it with model training, a solver, a coding agent, or another measured research task. The outer contract stays the same.</p>"},{"location":"learn/autoresearch/#the-five-meta-evolve-declarations","title":"The five Meta-Evolve declarations","text":"<p>The outer loop uses the ordinary public API:</p> <pre><code>experiment = meta.Experiment(\n    task=meta.Task(\n        evaluator=evaluate_researcher,\n        artifact=meta.Text,\n        objectives=(meta.Maximize(\"private_quality\"),),\n        budget=OUTER_BUDGET,\n    ),\n    seed=SEED,\n    proposer=propose_researcher,\n    search=meta.Greedy(max_trials=OUTER_TRIALS),\n    context=meta.AncestorsOnly(max_records=CONTEXT_RECORDS),\n    random_seed=0,\n)\nreturn meta.run(experiment)\n</code></pre> <p>Read it as a sentence: <code>Task</code> fixes evaluation and limits; <code>SEED</code> is the starting recipe; <code>propose_researcher</code> proposes a successor; <code>Greedy</code> retains improvements; and <code>AncestorsOnly</code> supplies public reports from parent lineage.</p> <p>The coordinator records every candidate, score, proposal hypothesis, evidence reference, decision, usage total, and causal parent.</p>"},{"location":"learn/autoresearch/#the-candidate-controlled-surface","title":"The candidate-controlled surface","text":"<p>The complete mutable artifact is tiny JSON:</p> <pre><code>{\"moves\":[1,2,4]}\n</code></pre> <p><code>decode</code> permits only the <code>moves</code> key, one to four integer moves, and values between 1 and 8. An invalid recipe produces a typed <code>InvalidOutput</code> with no metric, so malformed output can never outrank a valid researcher.</p> <pre><code>import meta_evolve as meta\n\noutcome = lab[\"evaluate_researcher\"](meta.Text('{\"moves\":[0]}'))\nprint(\"failure:\", type(outcome.failure).__name__)\nprint(\"metrics:\", dict(outcome.metrics))\n</code></pre> Output<pre><code>failure: InvalidOutput\nmetrics: {}\n</code></pre> <p>Everything else is experiment-controlled:</p> Control Owner Move recipe Candidate Public, private, and held-out tasks Evaluator Inner and outer budgets Experiment <code>private_quality</code> calculation Evaluator Greedy admission Search policy Whether a winner is deployed Governance / user <p>This separation is the safety and validity mechanism. \u201cSelf-improvement\u201d does not mean self-authorized evaluation or deployment.</p>"},{"location":"learn/autoresearch/#one-candidate-evaluation","title":"One candidate evaluation","text":"<p><code>evaluate_researcher</code> first validates the recipe. It then performs four inner runs under byte-identical declarations:</p> <pre><code>public = score(researcher, PUBLIC_TASKS)\nprivate = score(researcher, PRIVATE_TASKS)\nreturn EvaluationResult(\n    metrics={\"private_quality\": fmean(private.values())},\n    evidence=(\n        EvidenceDraft(\n            kind=PUBLIC_REPORT,\n            data={\"scores\": public, \"inner_trials_per_task\": INNER_TRIALS},\n        ),\n    ),\n)\n</code></pre> <p>Public per-task outcomes become inspectable evidence. Private task names, targets, and per-task outcomes are not placed in evidence. The outer search policy receives only their aggregate metric for selection.</p> <p>The deterministic proposer extracts only <code>autoresearch-public-report</code> records. Its <code>ProposalResult.derived_from</code> points to the report it used. This preserves the causal claim \u201cthis change was based on that observation.\u201d</p>"},{"location":"learn/autoresearch/#exact-budget-arithmetic","title":"Exact budget arithmetic","text":"<p>The seed and two successors give the outer run:</p> <pre><code>3 outer evaluations = 1 seed + 2 proposals\n2 outer trials      = 2 proposals\n</code></pre> <p>Each outer evaluation runs four benchmark tasks. Each task permits the seed plus four inner proposals:</p> <pre><code>5 inner evaluations per task\n\u00d7 4 tasks per outer evaluation\n\u00d7 3 outer evaluations\n= 60 inner evaluations used for selection at most\n</code></pre> <p>After selection, the seed and winner each run on two held-out tasks:</p> <pre><code>5 \u00d7 2 policies \u00d7 2 held-out tasks = 20 post-selection evaluations at most\n</code></pre> <p>The nested runs own their own hard budgets. The outer budget counts outer candidate evaluations; it does not pretend that a nested benchmark call is one unit of physical work. A production setup should additionally aggregate token, GPU-hour, dollar, and wall-clock usage at the evaluator boundary.</p>"},{"location":"learn/autoresearch/#why-three-task-splits","title":"Why three task splits?","text":"<p>The public tasks provide feedback for proposing the next researcher. The private tasks decide whether the proposal survives. Held-out tasks are touched only after the search has selected a winner.</p> Split Candidate model may see details? Affects selection? Public Yes Indirectly, through proposals Private No Yes, through aggregate quality Held-out No during search No <p>If the outer model sees private targets or per-task results, \u201cprivate\u201d is only a label. If held-out results influence another proposal, those tasks have become part of development and a new untouched suite is required.</p>"},{"location":"learn/autoresearch/#replacing-the-deterministic-proposer-with-an-agent","title":"Replacing the deterministic proposer with an agent","text":"<p>The deterministic proposer keeps CI credential-free. A model-backed wrapper should preserve this boundary:</p> <pre><code>ProposerContext \u2192 allowlisted public evidence \u2192 private workspace\n  \u2192 one bounded agent session \u2192 validated ProposalResult\n  \u2192 derived_from references and usage\n</code></pre> <p>Do not give the model raw outer context because it contains the aggregate private metric needed by trusted search. Build an allowlisted workspace. The prompt-optimization example shows how a callable outer proposer can run a bounded source-tree agent one level down and lift the result back into <code>meta.Text</code>.</p> <p>For a source-code researcher, change the artifact to <code>meta.SourceTree</code>, use an exact mutation allowlist, and evaluate each candidate in a fresh workspace. Keep benchmark source, credentials, permissions, and promotion controls outside that tree.</p>"},{"location":"learn/autoresearch/#what-the-comparison-may-claim","title":"What the comparison may claim","text":"<p>This tiny example supports one narrow statement: under the declared toy tasks and equal inner budgets, the selected recipe has higher private and held-out quality than the seed.</p> <p>It does not establish real-domain autonomy, superiority to experts, statistical robustness, out-of-distribution generalization, accelerating improvement, reward-hacking safety, or readiness for automatic deployment.</p> <p>Those claims require larger protocols, repeated runs, fixed physical costs, external baselines, untouched tasks, and adversarial checks. Negative results, ties, typed failures, and rejected proposals must remain in the record.</p>"},{"location":"learn/autoresearch/#relationship-to-m8","title":"Relationship to M8","text":"<p>This remains a deliberately smaller, historically pre-M8 composition of shipped M7 pieces: <code>meta.Text</code>, nested <code>meta.run</code>, typed evidence and failures, lineage, budgets, and an ordinary search policy. It introduces no production harness type or runtime behavior.</p> <p>M8 now supplies <code>HarnessArtifact</code>, <code>MutationSurface</code>, portable episode suites, strict held-out comparison, and safe export. This chapter stays as the explicitly small recipe-evolution composition; the canonical harness story is the M8 guide.</p>"},{"location":"learn/autoresearch/#leakage-and-validity-checklist","title":"Leakage and validity checklist","text":"<p>Before adapting the pattern, answer these questions:</p> <ol> <li>Can candidate code read private task files, environment variables, grader    output, or prior held-out results?</li> <li>Are baseline and candidate evaluated on exactly the same tasks, seeds,    model identity, tool permissions, and physical budget?</li> <li>Can a candidate increase concurrency, retries, context, or model calls and    thereby buy a better score?</li> <li>Are malformed output, timeout, policy violation, and infrastructure failure    unrankable typed outcomes rather than zeros?</li> <li>Does every proposal identify the public evidence it actually used?</li> <li>Is held-out evaluation run once after selection and excluded from promotion    unless promotion policy explicitly owns it?</li> </ol>"},{"location":"learn/autoresearch/#change-and-predict","title":"Change and predict","text":"<p>Change <code>INNER_TRIALS</code> from <code>4</code> to <code>2</code>, update <code>INNER_BUDGET</code> consistently, and predict which recipe wins before rerunning the example. Then ask whether the winner changed because its research strategy is generally better or because the experiment changed the resource regime.</p> <p>The optional notebook runs the same canonical module; this page contains the complete explanation.</p>"},{"location":"learn/notebooks/01-policy-tour/","title":"Understand search policies","text":"<p>Status: Shipped.</p> <p>The first run showed the core loop without randomness. This chapter keeps that simple checklist long enough to explain what search actually controls. It adds uncertain number proposals only after the deterministic case is clear.</p> <p>Open the canonical executed notebook if you want to rerun its cells. The notebook begins at the number-search part; this standalone chapter adds the gentler checklist introduction first.</p>"},{"location":"learn/notebooks/01-policy-tour/#what-it-teaches","title":"What it teaches","text":"<p>One improvement attempt contains two decisions that are easy to confuse:</p> <ol> <li>The search policy chooses a parent artifact.</li> <li>The proposer turns that parent into a new candidate.</li> </ol> <p>The evaluator measures the candidate. Search uses the retained evidence to decide what receives the next attempt.</p> <pre><code>Search chooses parent \u2192 Proposer creates candidate \u2192 Evaluator scores it\n         \u25b2                                                   \u2502\n         \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 coordinator records the outcome \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>Search does not rewrite artifacts or select the proposer's future parent.</p>"},{"location":"learn/notebooks/01-policy-tour/#stage-1-the-deterministic-checklist","title":"Stage 1: the deterministic checklist","text":"<p>The artifact is the number of completed checklist items. The seed is <code>0</code>, one proposal completes one item, and score is the resulting count.</p> <pre><code>ITEMS = 3\n\ndef propose(parent, _context):\n    return parent + 1\n\ndef evaluate(candidate):\n    return candidate\n</code></pre> <p>There is only one possible candidate from any parent:</p> Parent Candidate Score <code>0</code> <code>1</code> <code>1</code> <code>1</code> <code>2</code> <code>2</code> <code>2</code> <code>3</code> <code>3</code> <p>The common task declares what success and permitted work mean:</p> <pre><code>task = meta.Task(\n    evaluator=evaluate,\n    objectives=(meta.Maximize(\"completed\", satisfy=ITEMS),),\n    budget=meta.Budget(evaluations=4, trials=3),\n)\n</code></pre> <p>The seed costs one evaluation. Three more fund the successor trials. No policy can exceed this ceiling.</p>"},{"location":"learn/notebooks/01-policy-tour/#greedy-builds-a-chain","title":"Greedy builds a chain","text":"<pre><code>experiment = meta.Experiment(\n    task=task,\n    seed=0,\n    proposer=propose,\n    search=meta.Greedy(max_trials=3),\n)\nresult = meta.run(experiment)\n</code></pre> <p>Greedy maintains one best-so-far artifact, called the incumbent. Because every candidate improves the checklist, each one becomes the next parent:</p> <pre><code>0 \u2192 1 \u2192 2 \u2192 3\n</code></pre> <p>This is a chain. Greedy is useful when small improvements compose: fixing one issue makes a useful base for fixing the next.</p>"},{"location":"learn/notebooks/01-policy-tour/#bestofn-makes-independent-attempts","title":"BestOfN makes independent attempts","text":"<p>Now change only the search declaration:</p> <pre><code>search=meta.BestOfN(max_trials=3)\n</code></pre> <p>BestOfN always gives the proposer the original seed. The proposer therefore receives <code>0</code> three times and returns <code>1</code> three times:</p> <pre><code>       \u250c\u2500 1\n0 \u2500\u2500\u2500\u2500\u2500\u253c\u2500 1\n       \u2514\u2500 1\n</code></pre> <p>This is a star. The best artifact is <code>1</code>, with lineage <code>[0, 1]</code>. Separate candidate occurrences remain separate recorded attempts even when their content is equal.</p> <p>BestOfN is useful when attempts should not build on one another\u2014for example, sampling several independent answers or prompt drafts. It buys breadth, not multi-step refinement.</p> <p>The checklist exposes the key difference without any luck:</p> Policy Parent for attempt 2 Can compose steps? Best here Greedy <code>1</code> Yes <code>3</code> BestOfN <code>0</code> No <code>1</code> <p>Neither result means one policy is universally better. This particular problem requires composition, so its structure favors Greedy.</p>"},{"location":"learn/notebooks/01-policy-tour/#population-keeps-several-possibilities","title":"Population keeps several possibilities","text":"<p><code>Population</code> retains a bounded ranked archive instead of one incumbent:</p> <pre><code>seed\n\u251c\u2500 candidate A \u2500\u2500 successor\n\u251c\u2500 candidate B\n\u2514\u2500 candidate C \u2500\u2500 successor\n</code></pre> <p>That tree matters when branches may lead somewhere different. The checklist cannot produce useful diversity, so Population only adds machinery there. Its <code>size</code> limits retained candidates; <code>tournament_size</code> controls how strongly parent selection favors higher-ranked members.</p> <p>Practical rule: if attempts should be independent, start with <code>BestOfN</code>. If improvements compose, start with <code>Greedy</code>; use <code>Population</code> when losing alternatives may matter later. Confirm that choice with equal-condition evaluation evidence.</p>"},{"location":"learn/notebooks/01-policy-tour/#stage-2-add-uncertain-proposals","title":"Stage 2: add uncertain proposals","text":"<p>Only now introduce the notebook's benchmark. The seed is <code>0</code>, the target is <code>12</code>, and a proposal adds one of <code>-2</code>, <code>1</code>, or <code>5</code>:</p> <pre><code>TARGET = 12\nMOVES = (-2, 1, 5)\n\ndef propose(parent, context):\n    move = context.rng.choice(MOVES)\n    return parent + move\n\ndef evaluate(candidate):\n    return -abs(candidate - TARGET)\n</code></pre> <p>The score is negative distance because the objective maximizes:</p> Candidate Distance from 12 Score <code>0</code> <code>12</code> <code>-12</code> <code>5</code> <code>7</code> <code>-7</code> <code>11</code> <code>1</code> <code>-1</code> <code>12</code> <code>0</code> <code>0</code> <p>Higher is better: <code>-1</code> is better than <code>-7</code>, and <code>0</code> means the target was reached. This scoring trick belongs to this benchmark, not to Meta-Evolve itself; evaluators can return ordinary positive metrics too.</p>"},{"location":"learn/notebooks/01-policy-tour/#what-the-rng-is-doing","title":"What the RNG is doing","text":"<p><code>context.rng</code> is a recorded random stream supplied for that logical decision. The proposer uses it because this benchmark intentionally allows multiple moves from one parent.</p> <pre><code>experiment = meta.Experiment(\n    task=task,\n    seed=0,\n    proposer=propose,\n    search=meta.Greedy(max_trials=8),\n    random_seed=1,\n)\nresult = meta.run(experiment)\n</code></pre> <p>The root <code>random_seed</code> makes local choices repeatable. It does not:</p> <ul> <li>determine what score means;</li> <li>choose a parent on the proposer's behalf;</li> <li>make a hosted language model deterministic;</li> <li>turn replay into a fresh rerun.</li> </ul> <p>The checklist needed no RNG because its proposer had exactly one action. Here, randomness represents uncertainty in which candidate gets proposed.</p>"},{"location":"learn/notebooks/01-policy-tour/#read-the-three-search-shapes","title":"Read the three search shapes","text":"<p>With eight successor trials:</p> <ul> <li>Greedy can accumulate moves along one improving chain.</li> <li>BestOfN can reach only <code>-2</code>, <code>1</code>, or <code>5</code>, because every attempt starts at   seed <code>0</code>.</li> <li>Population can accumulate moves along several retained branches.</li> </ul> <pre><code>for name, result in runs.items():\n    summary = result.summary()\n    print(\n        name,\n        f\"score={summary.primary_score}\",\n        f\"usage={summary.usage}\",\n        f\"lineage={list(summary.winning_lineage)}\",\n    )\n</code></pre> Output<pre><code>Greedy score=0 usage=Usage(evaluations=6, trials=5, tokens=0, wall_seconds=0.0) lineage=[0, 5, 10, 11, 12]\nBestOfN score=-7 usage=Usage(evaluations=9, trials=8, tokens=0, wall_seconds=0.0) lineage=[0, 5]\nPopulation score=-2 usage=Usage(evaluations=9, trials=8, tokens=0, wall_seconds=0.0) lineage=[0, 1, 2, 7, 8, 9, 14]\n</code></pre> <p>Greedy stopped early after satisfying score <code>0</code>. The other policies used all nine evaluations. This single run demonstrates mechanism; it does not establish that Greedy is generally superior.</p> <p>The public result object lets us inspect the winner, score, causal path, and the seed evaluation without reading coordinator internals:</p> <pre><code>result = runs[\"Greedy\"]\nsummary = result.summary()\nprint(\"best:\", result.best())\nprint(\"primary score:\", summary.primary_score)\nprint(\"winning lineage:\", list(summary.winning_lineage))\nprint(\"seed metrics:\", dict(result.trials()[0].metrics))\n</code></pre> Output<pre><code>best: 12\nprimary score: 0\nwinning lineage: [0, 5, 10, 11, 12]\nseed metrics: {'score': -12}\n</code></pre> <p><code>trials()</code> includes the seed and every attempted successor. <code>lineage()</code> keeps only the causal ancestors of the selected artifact. Rejected candidates and typed failures remain recorded even when they are absent from that winning path.</p>"},{"location":"learn/notebooks/01-policy-tour/#what-to-notice","title":"What to notice","text":"<ul> <li>Parent selection and candidate generation are different responsibilities.</li> <li>Greedy produces a chain; BestOfN produces a star; Population produces a tree.</li> <li>The best policy depends on whether improvements compose and diversity matters.</li> <li>Randomness belongs to a component that needs it; it is not mandatory setup.</li> <li>The seed is evaluated before successor trials and therefore costs evaluation   budget.</li> <li>Search selects a best artifact inside a run; it does not externally deploy or   promote that artifact.</li> </ul>"},{"location":"learn/notebooks/01-policy-tour/#interpretation-questions","title":"Interpretation questions","text":"<ol> <li>Why does deterministic BestOfN stop at one completed checklist item?</li> <li>Why is Population unnecessary for that checklist proposer?</li> <li>Why can number-search BestOfN never reach <code>12</code>?</li> <li>What is the difference between replay and rerunning with the same root seed?</li> </ol> Answers  1. Every attempt expands seed `0`, and the proposer adds exactly one. 2. It cannot create different alternatives from the same parent, so there is no    useful diversity to retain. 3. Every proposal is one move from `0`; the only reachable values are `-2`,    `1`, and `5`. 4. Replay reads committed decisions and outcomes. Rerunning invokes components    again and can diverge when external systems are nondeterministic."},{"location":"learn/notebooks/01-policy-tour/#change-and-predict","title":"Change and predict","text":"<p>First stay with the checklist. Change its proposer from <code>parent + 1</code> to <code>parent + 2</code> and set the target to <code>4</code>.</p> <p>Predict before running:</p> <ul> <li>Greedy reaches <code>4</code> through <code>[0, 2, 4]</code> and stops early.</li> <li>BestOfN repeatedly proposes <code>2</code> from seed <code>0</code> and never reaches <code>4</code>.</li> <li>The distinction still comes from parent selection, not randomness.</li> </ul> <p>Then return to the unchanged number benchmark. The next chapter repeats it over many root seeds and explains what a fair policy comparison may claim.</p> <p>Next: compare uncertain search fairly.</p>"},{"location":"learn/notebooks/02-policy-comparison/","title":"Compare search policies fairly","text":"<p>Status: Shipped.</p> <p>The previous chapter explained how policies behave on one run. This chapter shows how to compare them without accidentally changing the task, budget, or measurement at the same time.</p> <p>Open the canonical executed notebook if you want the cells. The benchmark, output, and interpretation are complete below.</p>"},{"location":"learn/notebooks/02-policy-comparison/#what-it-teaches","title":"What it teaches","text":"<p>A policy comparison is valid only when the things that define performance are held fixed. In this benchmark, all three policies share:</p> <ul> <li>target <code>12</code> and score <code>-abs(candidate - 12)</code>;</li> <li>seed artifact <code>0</code>;</li> <li>proposer moves <code>(-2, 1, 5)</code>;</li> <li>nine evaluations and eight successor trials;</li> <li>root seeds <code>0</code> through <code>39</code>;</li> <li>objective direction and satisfaction threshold.</li> </ul> <p>Only the search policy and its own configuration differ.</p>"},{"location":"learn/notebooks/02-policy-comparison/#why-one-run-is-not-enough","title":"Why one run is not enough","text":"<p>The proposer samples a move from a recorded random stream. One root seed can favor one policy by chance. A sweep repeats the same declared comparison over 40 root seeds and aggregates outcomes:</p> <pre><code>for root_seed in range(40):\n    runs = [\n        meta.run(meta.Experiment(\n            task=TASK,\n            seed=0,\n            proposer=propose,\n            search=make_policy(),\n            random_seed=root_seed,\n        ))\n        for make_policy in POLICY_FACTORIES\n    ]\n</code></pre> <p>This is still a small deterministic illustration, not a claim about all search problems. Multiple seeds reveal variability inside this benchmark; they do not create task diversity.</p>"},{"location":"learn/notebooks/02-policy-comparison/#what-must-stay-equal","title":"What must stay equal","text":"<p>Use this table when designing a comparison:</p> Declaration Why changing it confounds the result Task/evaluator Changes what \u201cgood\u201d means Objective order Changes ranking and stopping Artifact kind Changes valid candidate representation Seed artifact Changes the starting difficulty Proposer identity Changes which candidates can be produced Effective budget Lets one arm buy more attempts or time Evaluation environment Changes the conditions being measured <p>Policy choice, policy configuration, and root random seed may differ because they are the independent variables this benchmark is studying.</p> <p><code>Run.compare()</code> checks these declarations before returning a comparison. It does not infer equality from similar output or function names.</p>"},{"location":"learn/notebooks/02-policy-comparison/#the-observed-distributions","title":"The observed distributions","text":"<p>The committed benchmark reports:</p> Policy Solved Best-score behavior Why BestOfN 0/40 Always <code>-7</code> Only one-step values <code>-2</code>, <code>1</code>, <code>5</code> are reachable Greedy 15/40 Scores range <code>-9</code> to <code>0</code> Improvements accumulate along one chain Population 14/40 Scores range <code>-10</code> to <code>0</code> Several branches compete for a three-member archive <p>\u201cSolved\u201d means the best score equals <code>0</code>, not merely that the run completed. BestOfN's invariant <code>-7</code> is not bad luck: it is a proof from its topology and the move set. Its best reachable candidate is always <code>5</code>.</p> <p>Greedy and Population sometimes stop early after reaching the satisfaction threshold. Their evaluation-count distributions therefore include values below nine. BestOfN never solves and always consumes nine evaluations.</p>"},{"location":"learn/notebooks/02-policy-comparison/#read-a-distribution-not-just-a-mean","title":"Read a distribution, not just a mean","text":"<p>Ask the report object for the exact solve count and score distribution:</p> <pre><code>greedy = next(report for report in reports if report.policy == \"Greedy\")\nprint(f\"Greedy solved: {greedy.solve_count}/{greedy.run_count}\")\nprint(\"Greedy scores:\", dict(greedy.score_distribution))\n</code></pre> Output<pre><code>Greedy solved: 15/40\nGreedy scores: {-9: 1, -8: 1, -5: 3, -4: 2, -3: 3, -2: 3, -1: 12, 0: 15}\n</code></pre> <p>That tells more than an average:</p> <ul> <li><code>15</code> runs solved exactly;</li> <li><code>12</code> finished one unit away;</li> <li>a small tail performed much worse;</li> <li>success is not guaranteed by the policy.</li> </ul> <p>Population solves almost as often but has a different tail. If one application cares about worst-case quality, solve rate alone is insufficient. A serious study would predeclare the summary statistics and uncertainty treatment rather than selecting whichever view looks best afterward.</p>"},{"location":"learn/notebooks/02-policy-comparison/#lineage-explains-the-mechanism","title":"Lineage explains the mechanism","text":"<p>The report prints one representative winning lineage per policy:</p> <pre><code>topologies = {\n    \"BestOfN\": \"star spoke\",\n    \"Greedy\": \"chain\",\n    \"Population\": \"winning tree branch\",\n}\nfor report in reports:\n    print(\n        f\"{report.policy}: {topologies[report.policy]} \"\n        f\"{list(report.representative_lineage)} \"\n        f\"(seed {report.representative_seed})\"\n    )\n</code></pre> Output<pre><code>BestOfN: star spoke [0, 5] (seed 0)\nGreedy: chain [0, 5, 10, 11, 12] (seed 1)\nPopulation: winning tree branch [0, 5, 10, 11, 12] (seed 0)\n</code></pre> <p>Identical winning values do not imply identical search. Population may have spent trials maintaining sibling branches that are absent from this one path. Use <code>result.trials()</code> for all attempts and <code>result.lineage()</code> for causal ancestry of one artifact.</p>"},{"location":"learn/notebooks/02-policy-comparison/#how-runcompare-protects-the-claim","title":"How <code>Run.compare()</code> protects the claim","text":"<pre><code>for other_name in (\"BestOfN\", \"Population\"):\n    comparison = runs[\"Greedy\"].compare(runs[other_name])\n    winner = comparison.winner.policy.name if comparison.winner else \"tie\"\n    print(\n        f\"Greedy vs {other_name}: winner={winner}, \"\n        f\"same_random_seed={comparison.same_random_seed}\",\n    )\n</code></pre> Output<pre><code>Greedy vs BestOfN: winner=greedy, same_random_seed=True\nGreedy vs Population: winner=greedy, same_random_seed=True\n</code></pre> <p>The comparison first verifies compatibility. If evaluator, proposer, objective, budget, artifact kind, or initial artifact differs, it raises rather than presenting a misleading winner.</p> <p>Exact primary-score equality is a tie. Lower token use or fewer evaluations does not silently break a score tie because that would introduce an undeclared secondary objective. Declare cost as an objective or constraint if it should affect ranking.</p>"},{"location":"learn/notebooks/02-policy-comparison/#identity-is-a-declaration-not-omniscience","title":"Identity is a declaration, not omniscience","text":"<p>Local callable identity uses a declaration based on module and qualified name. Two callables with matching names are not mathematically proven equivalent. Configured adapters should hash behavior-affecting fields into their component version, and durable registered components should declare configuration.</p> <p>For example, changing a model ID, timeout, hidden case suite, or grader threshold while preserving the same component identity makes a comparison look controlled when it is not. Identity must cover everything that can change behavior but is not already represented by task and run declarations.</p>"},{"location":"learn/notebooks/02-policy-comparison/#claims-this-benchmark-supports","title":"Claims this benchmark supports","text":"<p>It supports:</p> <ul> <li>the three shipped policies produce different topology and distributions on   this exact number-search task;</li> <li>BestOfN cannot compose moves and therefore cannot solve this task;</li> <li>Greedy and Population sometimes solve under the equal declared budget;</li> <li>committed summaries and lineages reproduce the recorded outcomes.</li> </ul> <p>It does not support:</p> <ul> <li>Greedy is generally superior to population search;</li> <li>40 deterministic root seeds provide broad statistical evidence;</li> <li>the result transfers to language models, source code, or noisy evaluators;</li> <li>local callable names prove behavioral equivalence across code changes.</li> </ul>"},{"location":"learn/notebooks/02-policy-comparison/#what-to-notice","title":"What to notice","text":"<ul> <li>Fairness is a property of declarations and evaluation conditions, not equal   row counts in a report.</li> <li>A root-seed sweep studies stochastic variation, not task generalization.</li> <li>Early satisfaction changes actual usage while the maximum budget stays equal.</li> <li>Lineage gives a mechanistic explanation for observed score distributions.</li> <li>A comparison reports ties honestly and never uses usage as a hidden   tiebreaker.</li> </ul>"},{"location":"learn/notebooks/02-policy-comparison/#interpretation-questions","title":"Interpretation questions","text":"<ol> <li>Why is BestOfN's <code>0/40</code> result deterministic despite random moves?</li> <li>Why may Greedy use fewer evaluations than BestOfN under an equal budget?</li> <li>Can we conclude Population is worse because it solved one fewer seed?</li> <li>Why is changing a timeout a comparison change even if no timeout occurs?</li> </ol> Answers  1. Every independent proposal is one move from zero, and the best reachable    value is always `5`. 2. Greedy sometimes reaches the declared satisfaction threshold and stops;    equal budgets are ceilings, not mandatory spending. 3. No. This benchmark is small, task-specific, and does not quantify whether    the one-run difference is stable or meaningful elsewhere. 4. Timeout changes the authorized execution regime and could affect which    results are observable; identity and compatibility must describe potential    behavior, not only what happened once."},{"location":"learn/notebooks/02-policy-comparison/#change-and-predict","title":"Change and predict","text":"<p>Add <code>12</code> to <code>MOVES</code>.</p> <p>Before running:</p> <ul> <li>BestOfN can now solve whenever at least one of eight independent samples   chooses <code>12</code>; its formerly fixed <code>-7</code> distribution must change.</li> <li>Greedy and Population can also solve in one step, but they already had   multi-step paths to the target, so the mechanism changes less radically.</li> <li>The proposer identity or version must change because its behavior changed.</li> </ul> <p>Run the sweep in a temporary copy. Do not compare the changed run to the original as though only policy differed\u2014the proposer declaration is now a new experimental condition.</p> <p>Next: reuse prior experience while preserving temporal and authority boundaries.</p>"},{"location":"learn/notebooks/03-experience-tour/","title":"Reuse experience without losing causality","text":"<p>Status: Shipped.</p> <p>An improvement loop becomes more useful when a later proposal can learn from earlier failures. It becomes much harder to trust if \u201cmemory\u201d is an invisible global variable. This chapter shows how Meta-Evolve exposes prior work as bounded, time-scoped, audited experience.</p> <p>Open the canonical executed notebook only to run the cells. The entire mechanism and result are explained here.</p>"},{"location":"learn/notebooks/03-experience-tour/#what-it-teaches","title":"What it teaches","text":"<p>Meta-Evolve splits experience reuse into four ideas:</p> <pre><code>Graph       what committed records existed at a logical time\nPush        what a policy selected before a proposal began\nPull        what the proposer chose to inspect during its work\nDerivation  which observed records the proposal says it actually used\n</code></pre> <p>Evaluation remains independent. Reading a previous failure may influence a proposal, but it cannot change the grader, grant more budget, or turn a failed evaluation into a success.</p>"},{"location":"learn/notebooks/03-experience-tour/#the-running-problem","title":"The running problem","text":"<p>The seed is <code>0</code>. Ordinary proposals add one. Candidates <code>1</code>, <code>2</code>, and <code>3</code> are known traps: the evaluator returns a typed <code>EvaluatorFailure</code>, not a score. After observing two distinct failures, the proposer can jump to <code>10</code>.</p> <pre><code>def evaluate(candidate):\n    if candidate in {1, 2, 3}:\n        return EvaluatorFailure(message=f\"known trap at {candidate}\")\n    return candidate\n\ndef propose(parent, context):\n    failures = observed_failures(context)\n    candidate = 10 if len(failures) &gt;= 2 else parent + 1\n    return ProposalResult(candidate=candidate, derived_from=used_refs)\n</code></pre> <p>The budget is always four evaluations and three successor trials. A custom chain search makes each successful candidate the next parent so the experiment isolates experience access rather than parent-selection behavior.</p>"},{"location":"learn/notebooks/03-experience-tour/#four-controlled-variants","title":"Four controlled variants","text":"<p>The task, seed, proposer, evaluator, search, root seed, and budget are fixed. Only experience declarations change:</p> Variant Pushed context Pull capability <code>no-memory</code> No No <code>push-only</code> Failed ancestors No <code>pull-only</code> No Bounded reader <code>combined</code> Failed ancestors Bounded reader <p>This is a valid A/B-style design because memory strategy is the declared independent variable. <code>Run.compare()</code> reports whether context and pull declarations match instead of pretending the arms are identical.</p>"},{"location":"learn/notebooks/03-experience-tour/#the-observed-result","title":"The observed result","text":"<pre><code>for name, (result, _) in results.items():\n    summary = result.summary()\n    print(\n        name,\n        f\"score={summary.primary_score}\",\n        f\"task_usage={summary.usage}\",\n        f\"memory_usage={summary.experience_usage}\",\n    )\n</code></pre> Output<pre><code>no-memory score=0 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=0, chars=0), pull=ExperienceUsage(operations=0, results=0, records=0, chars=0))\npush-only score=10 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=3, chars=3295), pull=ExperienceUsage(operations=0, results=0, records=0, chars=0))\npull-only score=10 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=0, chars=0), pull=ExperienceUsage(operations=7, results=8, records=20, chars=20559))\ncombined score=10 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=3, chars=3295), pull=ExperienceUsage(operations=7, results=8, records=20, chars=20855))\n</code></pre> <p>All arms spend <code>evaluations=4, trials=3</code>. Only memory-aware variants can observe both earlier traps and produce <code>10</code>. The final no-memory proposal tries <code>3</code> and fails.</p> <p>This demonstrates the mechanism on a constructed task. It does not prove memory generally helps; experience can also distract a proposer, suppress novelty, or consume model context.</p>"},{"location":"learn/notebooks/03-experience-tour/#the-temporal-experience-graph","title":"The temporal experience graph","text":"<p>The graph is a read model over committed occurrences:</p> <pre><code>step 0: seed proposal \u2192 artifact 0 \u2192 evaluation score 0\nstep 1: decision \u2192 proposal 1 \u2192 artifact 1 \u2192 evaluator failure\nstep 2: decision \u2192 proposal 2 \u2192 artifact 2 \u2192 evaluator failure\nstep 3: decision \u2192 proposal 10 \u2192 artifact 10 \u2192 evaluation score 10\n</code></pre> <p>Occurrences matter. Two trials that produce byte-identical artifacts remain different causal events because they may have different parents, observations, usage, and failures.</p> <p>An <code>ExperienceQuery(as_of=2)</code> may see committed records through logical step <code>2</code>; it cannot see step <code>3</code>. This prevents future leakage during replay and live proposal.</p> <pre><code>historical = graph.state_at(ExperienceQuery(\n    task_id=graph.task_id,\n    run_id=combined.id,\n    as_of=2,\n))\nprint(\"visible steps as_of=2:\", [node.logical_step for node in historical.nodes])\nprint(\"frontier steps:\", [node.logical_step for node in state.nodes])\n</code></pre> Output<pre><code>visible steps as_of=2: [0, 1, 2]\nfrontier steps: [0, 1, 2, 3]\n</code></pre>"},{"location":"learn/notebooks/03-experience-tour/#push-policy-selected-context","title":"Push: policy-selected context","text":"<p>A context policy runs before the proposer. The example's <code>FailedAncestors</code> policy walks the current parent lineage and selects only failed evaluations:</p> <pre><code>projection = combined._projection()\nfor selected in projection.context_selections:\n    refs = tuple(record.ref.kind for record in selected.bundle.records)\n    print(\n        f\"step={selected.bundle.records[-1].logical_step if refs else '-'}\",\n        f\"reason={selected.bundle.reason!r}\",\n        f\"refs={refs}\",\n        f\"usage={selected.bundle.usage}\",\n    )\nprint(\"M3.2 declaration:\", projection.context_declaration.spec)\n</code></pre> Output<pre><code>step=- reason='failed ancestor evaluations' refs=() usage=ContextUsage(records=0, chars=0)\nstep=1 reason='failed ancestor evaluations' refs=('evaluation',) usage=ContextUsage(records=1, chars=1098)\nstep=2 reason='failed ancestor evaluations' refs=('evaluation', 'evaluation') usage=ContextUsage(records=2, chars=2197)\nM3.2 declaration: ContextSpec(policy=PolicyRef(name='failed-ancestors', version='1', origin='local'), max_records=20, max_chars=8000)\n</code></pre> <p>The committed <code>ContextBundle</code> contains:</p> <ul> <li>exact structured records and references;</li> <li>a canonical rendered form for a text consumer;</li> <li>the selection reason;</li> <li>truncation status;</li> <li>record and character usage.</li> </ul> <p>It is a snapshot, not a live database handle. Replay checks the recorded bundle instead of calling the selector again.</p>"},{"location":"learn/notebooks/03-experience-tour/#pull-proposer-directed-inspection","title":"Pull: proposer-directed inspection","text":"<p><code>PullAccess</code> grants a trial-limited reader. The proposer can search, open, traverse, or compare prior records within declared caps:</p> <pre><code>meta.PullAccess(\n    max_operations=20,\n    max_results=20,\n    max_records=100,\n    max_chars=32_000,\n)\n</code></pre> <p>Every operation is recorded as an attempt followed by success, opaque denial, or exhaustion. On the final trial, the example searches two failed evaluations, opens both, and compares their trial occurrences.</p> <p>The grant's <code>as_of</code> boundary is fixed before proposal execution. A tool call cannot move it forward and inspect the trial currently being produced.</p>"},{"location":"learn/notebooks/03-experience-tour/#observation-is-not-use","title":"Observation is not use","text":"<p>A proposer may receive ten records and rely on only two. <code>derived_from</code> names the ordered references it claims caused the proposal:</p> <pre><code>return ProposalResult(\n    candidate=10,\n    derived_from=(failure_one_ref, failure_two_ref),\n)\n</code></pre> <p>The coordinator validates that each reference was actually visible through the committed push-then-pull observation ledger. Invented, future, duplicate, or out-of-order references become a typed policy failure; no candidate is committed.</p>"},{"location":"learn/notebooks/03-experience-tour/#memory-usage-is-separate-from-task-usage","title":"Memory usage is separate from task usage","text":"<p>The result output above reports task and memory usage independently. Experience reads do not consume evaluation/trial budget, but they are still measured. A production experiment must also count how retrieved characters affect provider tokens and cost.</p>"},{"location":"learn/notebooks/03-experience-tour/#authority-map","title":"Authority map","text":"Component May do May not do Context policy Select prior records under caps See future records or mutate history Pull grant Authorize bounded read operations Expand its own limits Proposer Observe granted records and propose Change evaluator or award a score Coordinator Validate visibility, usage, derivation Invent proposal intent Evaluator Return metric or typed failure Rewrite the candidate"},{"location":"learn/notebooks/03-experience-tour/#what-to-notice","title":"What to notice","text":"<ul> <li>The first successor has no prior failure to reuse.</li> <li>Typed failures can be valuable evidence without becoming low scores.</li> <li>Push is chosen by policy; pull is chosen by the proposer under a grant.</li> <li>Both channels are bounded and recorded at a logical time.</li> <li><code>derived_from</code> records claimed use, not everything that was visible.</li> <li>Experience improves this toy result without changing task budget.</li> </ul>"},{"location":"learn/notebooks/03-experience-tour/#interpretation-questions","title":"Interpretation questions","text":"<ol> <li>Why can the first proposal not cite an evaluator failure?</li> <li>Why is a failed evaluation useful but unrankable?</li> <li>What prevents a pull-enabled proposer from reading its future result?</li> <li>Why record derivation separately from the context bundle?</li> </ol> Answers  1. Only the seed evaluation exists before the first proposal, and it succeeds. 2. It contains diagnostic information, but no valid metric exists for search    ranking. 3. The grant has a committed strictly-prior `as_of` boundary. 4. Visibility does not prove causal use; derivation records the proposer's    narrower explicit claim."},{"location":"learn/notebooks/03-experience-tour/#change-and-predict","title":"Change and predict","text":"<p>Change the task so only one observed failure is required before jumping to <code>10</code>.</p> <p>Predict: each memory-aware variant should succeed one trial earlier; the no-memory arm still cannot. The task budget ceiling stays four evaluations, but actual usage may shrink if the objective has a satisfaction threshold.</p> <p>Next: inspect push and pull records in detail.</p>"},{"location":"learn/notebooks/04-push-pull/","title":"Inspect pushed context and pulled experience","text":"<p>Status: Shipped.</p> <p>The previous chapter introduced two routes to prior work. This chapter follows the exact records so you can decide which route belongs in a real agent setup.</p> <p>Open the canonical executed notebook only if you want to manipulate the saved experiment.</p>"},{"location":"learn/notebooks/04-push-pull/#what-it-teaches","title":"What it teaches","text":"<p>Push and pull answer different questions:</p> <pre><code>Push: \u201cWhat should every proposer receive before it starts?\u201d\nPull: \u201cWhat should this proposer be allowed to investigate while working?\u201d\n</code></pre> <p>They can expose the same underlying evaluation, but the decision maker, timing, audit record, and cost are different.</p>"},{"location":"learn/notebooks/04-push-pull/#start-from-one-trial","title":"Start from one trial","text":"<p>Before a successor proposal runs, the coordinator prepares two independent inputs:</p> <pre><code>projected history\n   \u2502\n   \u251c\u2500 context policy.select(...) \u2500\u2500\u25ba immutable ContextBundle\n   \u2502\n   \u2514\u2500 experience declaration \u2500\u2500\u2500\u2500\u2500\u2500\u25ba trial-scoped ExperienceGrant\n                                          \u2502\nproposal starts                           \u2514\u2500 search/open/compare tools\n</code></pre> <p>Both are limited to prior committed logical time. Neither provides evaluator, budget, storage-write, or promotion authority.</p>"},{"location":"learn/notebooks/04-push-pull/#pushed-context-step-by-step","title":"Pushed context step by step","text":"<p>The <code>FailedAncestors</code> policy selects failed evaluations from the current parent's ancestry. Its output over three proposals is:</p> <pre><code>push_run = results[\"push-only\"][0]\npush_projection = push_run._projection()\n\nfor index, selected in enumerate(push_projection.context_selections, start=1):\n    records = selected.bundle.records\n    labels = tuple(\n        f\"{record.ref.kind}@{record.logical_step}\" for record in records\n    )\n    print(\n        f\"trial={index}\",\n        f\"selected={labels}\",\n        f\"chars={selected.bundle.usage.chars}\",\n    )\n</code></pre> Output<pre><code>trial=1 selected=() chars=0\ntrial=2 selected=('evaluation@1',) chars=1098\ntrial=3 selected=('evaluation@1', 'evaluation@2') chars=2197\n</code></pre> <p>Why the sequence grows:</p> <ol> <li>Before trial 1, no successor has failed.</li> <li>Trial 1 evaluates candidate <code>1</code> and fails.</li> <li>Before trial 2, that one failure is an ancestor observation.</li> <li>Trial 2 evaluates candidate <code>2</code> and fails.</li> <li>Before trial 3, both failures are available.</li> </ol> <p>The final selection exposes the exact bundle metadata:</p> <pre><code>bundle = push_projection.context_selections[-1].bundle\nprint(\"reason:\", bundle.reason)\nprint(\"truncated:\", bundle.truncated)\nprint(\"usage:\", bundle.usage)\nprint(\"refs:\", tuple(\n    f\"{record.ref.kind}@{record.logical_step}\" for record in bundle.records\n))\n</code></pre> Output<pre><code>reason: failed ancestor evaluations\ntruncated: False\nusage: ContextUsage(records=2, chars=2197)\nrefs: ('evaluation@1', 'evaluation@2')\n</code></pre> <p>Structured proposers can inspect <code>records</code>; a model adapter can use the bounded rendered form. Recording both prevents the rendered prompt from becoming the only evidence of what was supplied.</p>"},{"location":"learn/notebooks/04-push-pull/#why-a-bundle-is-a-snapshot","title":"Why a bundle is a snapshot","text":"<p>Once selected, a bundle cannot observe newly committed work. The proposal gets stable input, replay can validate exact references and bytes, and concurrent workers cannot silently see different \u201clatest\u201d histories. Replay reads the snapshot rather than rerunning <code>FailedAncestors</code> under changed code or data.</p>"},{"location":"learn/notebooks/04-push-pull/#pull-grants-step-by-step","title":"Pull grants step by step","text":"<p>Each successor receives a fresh grant whose <code>as_of</code> equals the last committed logical step:</p> <pre><code>pull_run = results[\"pull-only\"][0]\npull_projection = pull_run._projection()\n\nfor issued in pull_projection.experience_grants:\n    operations = pull_projection.operations_for(issued.grant.trial_id)\n    print(f\"grant as_of={issued.grant.as_of}\")\n    for attempt, resolution in operations:\n        result = getattr(resolution, \"result\", None)\n        count = len(result.references) if result is not None else 0\n        print(\n            f\"  {attempt.request.operation:&lt;7}\",\n            f\"{type(resolution).__name__:&lt;28}\",\n            f\"refs={count}\",\n        )\n</code></pre> Output<pre><code>grant as_of=0\n  search  ExperienceOperationSucceeded refs=0\ngrant as_of=1\n  search  ExperienceOperationSucceeded refs=1\n  open    ExperienceOperationSucceeded refs=1\ngrant as_of=2\n  search  ExperienceOperationSucceeded refs=2\n  open    ExperienceOperationSucceeded refs=1\n  open    ExperienceOperationSucceeded refs=1\n  compare ExperienceOperationSucceeded refs=2\n</code></pre> <p>An operation has two durable phases:</p> <pre><code>ExperienceOperationAttempted\n          \u2502\n          \u251c\u2500\u25ba ExperienceOperationSucceeded(result references + bounded data)\n          \u251c\u2500\u25ba ExperienceOperationDenied(opaque reason)\n          \u2514\u2500\u25ba ExperienceOperationExhausted(which cap was reached)\n</code></pre> <p>Recording the attempt separately matters for crash recovery: if a process dies after the attempt commits but before resolution, resume can recognize and roll forward that incomplete operation.</p>"},{"location":"learn/notebooks/04-push-pull/#the-four-useful-pull-operations","title":"The four useful pull operations","text":"<p>Search returns bounded matching references; open resolves an authorized reference; traverse follows causal relationships; and compare returns structured views of named occurrences. All remain inside the temporal grant and consume operation, result, record, and character caps.</p>"},{"location":"learn/notebooks/04-push-pull/#the-canonical-observation-ledger","title":"The canonical observation ledger","text":"<p>Derivation validation needs one deterministic order across both channels. The ledger is built as:</p> <pre><code>1. pushed bundle references, in bundle order\n2. successful pull result references, in operation order\n3. structured references contained in those results\n4. duplicates removed at their first occurrence\n</code></pre> <p>For the combined arm's final proposal, inspect the ledger and declaration together:</p> <pre><code>combined_run = results[\"combined\"][0]\ncombined = combined_run._projection()\nfinal = combined.completed_trials[-1].trial.id\nproposal = combined.proposal_for(final)\nledger = observation_ledger(combined, final)\n\nprint(\"ledger kinds:\", tuple(reference.kind for reference in ledger))\nprint(\"declared kinds:\", tuple(ref.kind for ref in proposal.derived_from))\npositions = tuple(ledger.index(ref) for ref in proposal.derived_from)\nprint(\"declared positions:\", positions)\nprint(\"ordered subsequence:\", positions == tuple(sorted(positions)))\n</code></pre> Output<pre><code>ledger kinds: ('evaluation', 'evaluation', 'trial', 'trial', 'decision', 'decision', 'decision_explanation', 'decision_explanation', 'proposal', 'proposal', 'artifact', 'artifact', 'outcome', 'outcome')\ndeclared kinds: ('evaluation', 'evaluation', 'trial', 'trial')\ndeclared positions: (0, 1, 2, 3)\nordered subsequence: True\n</code></pre> <p>Those positions are <code>(0, 1, 2, 3)</code>, a valid ordered subsequence. The proposer may omit observed records it did not use, but it may not reorder or invent them.</p>"},{"location":"learn/notebooks/04-push-pull/#choosing-push-pull-or-both","title":"Choosing push, pull, or both","text":"Choose When it fits Push Every proposal needs a stable compact briefing and predictable size Pull Relevant history depends on what the proposer discovers Both A required briefing should coexist with selective investigation <p>Combined access may expose the same fact twice, so measure duplicated content.</p>"},{"location":"learn/notebooks/04-push-pull/#usage-accounting","title":"Usage accounting","text":"<p>The public result API reports the committed totals:</p> <pre><code>for name, (result, _) in results.items():\n    usage = result.experience_usage()\n    print(\n        f\"{name:&lt;10}\",\n        f\"context=({usage.context.records}, {usage.context.chars})\",\n        f\"pull=({usage.pull.operations}, {usage.pull.results},\",\n        f\"{usage.pull.records}, {usage.pull.chars})\",\n    )\n</code></pre> Output<pre><code>no-memory  context=(0, 0) pull=(0, 0, 0, 0)\npush-only  context=(3, 3295) pull=(0, 0, 0, 0)\npull-only  context=(0, 0) pull=(7, 8, 20, 20559)\ncombined   context=(3, 3295) pull=(7, 8, 20, 20855)\n</code></pre> <p>Counts are cumulative over the run. <code>results</code> counts returned result objects; <code>records</code> counts structured records contained in them. They need not match.</p> <p>Memory usage is not evaluation/trial usage. For a live model, however, rendered characters often become provider tokens, so the evaluator or proposer should also report actual token and wall usage.</p>"},{"location":"learn/notebooks/04-push-pull/#failure-and-denial-semantics","title":"Failure and denial semantics","text":"<p>Exhaustion and denial are not empty search results:</p> <ul> <li>empty success means the query was authorized and matched nothing;</li> <li>denial means policy refused the operation without leaking protected detail;</li> <li>exhaustion means an explicit grant cap was reached;</li> <li>infrastructure failure means the reader could not reliably answer.</li> </ul> <p>Adapters must preserve these differences. A silent fallback from pull to \u201cno memory\u201d changes the experiment and hides a failure.</p>"},{"location":"learn/notebooks/04-push-pull/#what-to-notice","title":"What to notice","text":"<ul> <li>Push records the experimenter's selection; pull records the agent's choices.</li> <li>Grants are trial-scoped and strictly prior.</li> <li>A search reference and an opened record are separately auditable.</li> <li>The ledger is push-then-pull and deduplicated deterministically.</li> <li>Memory access has its own cumulative accounting.</li> <li>Neither channel can modify the durable graph.</li> </ul>"},{"location":"learn/notebooks/04-push-pull/#interpretation-questions","title":"Interpretation questions","text":"<ol> <li>Why not give the proposer a mutable graph object directly?</li> <li>What does an empty successful search prove?</li> <li>Why are push records ordered before pull results in the ledger?</li> <li>When is pull worth its larger usage footprint?</li> </ol> Answers  1. It would create ambient, time-varying authority and make replay impossible    to validate exactly. 2. The operation was authorized and no visible prior record matched; it does    not mean the reader failed. 3. Push is observed before proposal execution; pull observations occur later    in committed operation order. 4. When selective investigation can find relevant evidence that a stable small    pushed bundle cannot anticipate, and the added cost is measured."},{"location":"learn/notebooks/04-push-pull/#change-and-predict","title":"Change and predict","text":"<p>Set <code>PullAccess.max_operations=3</code>.</p> <p>The final proposer needs four operations\u2014search, open, open, compare. Predict that the fourth attempt exhausts the grant, pull usage stops at the cap, and a proposal that requires the comparison reference cannot validly cite it. The coordinator must record exhaustion rather than silently omit the operation.</p> <p>Next: trace declared derivation and comparison.</p>"},{"location":"learn/notebooks/05-derivation-comparison/","title":"Trace derivation and compare memory strategies","text":"<p>Status: Shipped.</p> <p>Seeing a record and using it are different facts. This chapter shows how a proposal makes a causal-use claim, how the coordinator validates it, and how runs with different memory declarations can still be compared honestly.</p> <p>Open the canonical executed notebook only for executable inspection.</p>"},{"location":"learn/notebooks/05-derivation-comparison/#what-it-teaches","title":"What it teaches","text":"<p><code>ProposalResult.derived_from</code> is not a free-form citation. It is an ordered set of typed references to prior records the proposer was authorized to observe.</p> <pre><code>available history \u2500\u2500 push/pull policy \u2500\u2500\u25ba observation ledger\n                                              \u2502\nproposal declares derived_from \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                              \u2502 validate\n                                              \u25bc\n                                     committed causal edges\n</code></pre> <p>The edge survives regardless of whether the proposed artifact later scores, fails evaluation, or loses search selection.</p>"},{"location":"learn/notebooks/05-derivation-comparison/#follow-the-constructed-run","title":"Follow the constructed run","text":"<p>All four variants spend the same task usage:</p> <pre><code>for name, (result, _) in results.items():\n    outcomes = tuple(\n        trial.failure.kind\n        if trial.failure is not None\n        else trial.metrics[\"score\"]\n        for trial in result.trials()\n    )\n    print(\n        f\"{name:&lt;10}\",\n        f\"outcomes={outcomes}\",\n        f\"task_usage={result.summary().usage}\",\n    )\n</code></pre> Output<pre><code>no-memory  outcomes=(0, 'evaluator_failure', 'evaluator_failure', 'evaluator_failure') task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\npush-only  outcomes=(0, 'evaluator_failure', 'evaluator_failure', 10) task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\npull-only  outcomes=(0, 'evaluator_failure', 'evaluator_failure', 10) task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\ncombined   outcomes=(0, 'evaluator_failure', 'evaluator_failure', 10) task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\n</code></pre> <p>The no-memory arm produces three evaluator failures after its seed. Push, pull, and combined arms observe the first two failures and produce <code>10</code> on the third proposal.</p> <p>This separates three stages:</p> <ol> <li>observation may influence proposal;</li> <li>the proposal commits an artifact and causal claim;</li> <li>independent evaluation decides whether that artifact is rankable.</li> </ol>"},{"location":"learn/notebooks/05-derivation-comparison/#exact-committed-edges","title":"Exact committed edges","text":"<p>Read each committed proposal's derivation in logical-step order:</p> <pre><code>for name in (\"push-only\", \"pull-only\", \"combined\"):\n    result, _ = results[name]\n    projection = result._projection()\n    print(name)\n    for submitted in projection.proposals:\n        trial = projection.completed_trial(submitted.proposal.trial_id).trial\n        kinds = tuple(ref.kind for ref in submitted.proposal.derived_from)\n        print(f\"  step={trial.logical_step} derived_from={kinds}\")\n</code></pre> Output<pre><code>push-only\n  step=1 derived_from=()\n  step=2 derived_from=('evaluation',)\n  step=3 derived_from=('evaluation', 'evaluation')\npull-only\n  step=1 derived_from=()\n  step=2 derived_from=('evaluation',)\n  step=3 derived_from=('evaluation', 'evaluation', 'trial', 'trial')\ncombined\n  step=1 derived_from=()\n  step=2 derived_from=('evaluation',)\n  step=3 derived_from=('evaluation', 'evaluation', 'trial', 'trial')\n</code></pre> <p>Why trial references matter: an evaluation says how an artifact was measured; a trial occurrence carries the causal attempt in which it appeared. The artifacts could even have equal content while their trial identities differ.</p>"},{"location":"learn/notebooks/05-derivation-comparison/#a-failed-evaluation-does-not-erase-derivation","title":"A failed evaluation does not erase derivation","text":"<p>Suppose step 2 legitimately uses the step-1 failure, proposes candidate <code>2</code>, and then candidate <code>2</code> also fails evaluation. The causal statement remains true: the earlier failure influenced the proposal. Removing the edge would confuse \u201cthe idea did not work\u201d with \u201cthe idea was never used.\u201d</p> <p>Typed outcome and causal lineage are orthogonal:</p> Proposal valid? Evaluation valid? Artifact committed? Derivation kept? Rankable? Yes Yes Yes Yes Yes Yes Typed evaluator failure Yes Yes No Typed proposer failure Not run No Valid refs may be recorded on failure evidence No Invalid derivation Not run No No No"},{"location":"learn/notebooks/05-derivation-comparison/#validation-rules","title":"Validation rules","text":"<p>A derivation declaration must be:</p> <ul> <li>made of typed <code>ExperienceRef</code> values;</li> <li>unique;</li> <li>strictly prior to the proposal;</li> <li>authorized by its push/pull observation ledger;</li> <li>in the same order as first observation.</li> </ul> <p>The coordinator, not the proposer, enforces those rules. An unauthorized edge becomes <code>PolicyViolation</code> and no candidate is committed.</p> <p>Failure precedence is deliberate:</p> <pre><code>resource exhaustion\n  outranks invalid derivation\n    outranks proposer-reported failure\n</code></pre> <p>This reports the strongest coordinator-owned fact. A proposer cannot hide an invalid causal claim by also returning a convenient failure.</p>"},{"location":"learn/notebooks/05-derivation-comparison/#replay-preserves-the-graph","title":"Replay preserves the graph","text":"<p>Committed proposals contain the derivation references. Replaying the retained event stream rebuilds identical graph edges:</p> <pre><code>result, storage = results[\"combined\"]\nevents = storage.events.read(result.id)\nleft = replay_experience_graph(result.id, events)\nright = replay_experience_graph(result.id, tuple(events))\nquery = ExperienceQuery(task_id=left.task_id, run_id=result.id)\n\nleft_edges = tuple(node.derived_from for node in left.state_at(query).nodes)\nright_edges = tuple(node.derived_from for node in right.state_at(query).nodes)\nprint(\"replayed derivation equal:\", left_edges == right_edges)\nprint(\"edge sizes:\", tuple(len(edge) for edge in left_edges))\n</code></pre> Output<pre><code>replayed derivation equal: True\nedge sizes: (0, 0, 1, 4)\n</code></pre> <p>Replay does not call the context selector, reader, filtering logic, or renderer. It validates the facts that were actually committed at that logical time.</p> <p>Empty derivation is omitted from canonical serialization for compatibility with older proposal bytes. Non-empty derivation is encoded and round-trips exactly. An explicitly encoded empty default is rejected as non-canonical so one logical value has one byte representation.</p> <pre><code>projection = result._projection()\nbootstrap = projection.bootstrap.proposal\nderived = projection.proposals[-1].proposal\nbootstrap_bytes = serialization.dumps(bootstrap)\nderived_bytes = serialization.dumps(derived)\n\nprint(\"empty derivation omitted:\", \"derived_from\" not in bootstrap_bytes)\nprint(\"non-empty derivation encoded:\", \"derived_from\" in derived_bytes)\nprint(\"round trip exact:\", serialization.loads(derived_bytes) == derived)\n</code></pre> Output<pre><code>empty derivation omitted: True\nnon-empty derivation encoded: True\nround trip exact: True\n</code></pre>"},{"location":"learn/notebooks/05-derivation-comparison/#fair-comparison-when-memory-differs","title":"Fair comparison when memory differs","text":"<p>The experiment is specifically asking whether memory access changes outcomes, so context and pull declarations are allowed to differ. Compatibility still requires equal:</p> <ul> <li>evaluator and proposer declarations;</li> <li>objectives and artifact contract;</li> <li>task and run budgets;</li> <li>initial artifact digest;</li> <li>other behavior-affecting task conditions.</li> </ul> <p>The comparison reports the intended differences, including a true self-tie:</p> <pre><code>baseline = results[\"no-memory\"][0]\nfor name, (other, _) in results.items():\n    comparison = baseline.compare(other)\n    winner = comparison.winner.primary_score if comparison.winner else \"tie\"\n    print(\n        f\"no-memory vs {name:&lt;10}\",\n        f\"winner={winner}\",\n        f\"same_seed={comparison.same_random_seed}\",\n        f\"same_context={comparison.same_context_declaration}\",\n        f\"same_pull={comparison.same_experience_declaration}\",\n    )\n</code></pre> Output<pre><code>no-memory vs no-memory  winner=tie same_seed=True same_context=True same_pull=True\nno-memory vs push-only  winner=10 same_seed=True same_context=False same_pull=True\nno-memory vs pull-only  winner=10 same_seed=True same_context=True same_pull=False\nno-memory vs combined   winner=10 same_seed=True same_context=False same_pull=False\n</code></pre> <p>That is more informative than either rejecting all memory experiments or pretending their declarations are equal.</p>"},{"location":"learn/notebooks/05-derivation-comparison/#what-a-winner-means-here","title":"What a winner means here","text":"<p>The primary score is <code>10</code> versus <code>0</code>, so the memory-aware run wins inside this task. The comparison does not say:</p> <ul> <li>which particular retrieved record caused the numerical gain beyond the   proposer's declared claim;</li> <li>that memory improves other tasks;</li> <li>that larger context is cost-effective for a model;</li> <li>that the winning artifact should be deployed.</li> </ul> <p>Those require additional tasks, ablations, cost objectives, and external promotion policy.</p>"},{"location":"learn/notebooks/05-derivation-comparison/#what-to-notice","title":"What to notice","text":"<ul> <li>Derivation belongs to proposal causality, not evaluation success.</li> <li>Every edge must come from an authorized ordered observation ledger.</li> <li>Replay rebuilds edges without rerunning retrieval.</li> <li>Comparison may intentionally vary memory strategy while holding the task   contract fixed.</li> <li>Compatibility flags make those independent-variable differences visible.</li> <li>Promotion remains outside both search and comparison.</li> </ul>"},{"location":"learn/notebooks/05-derivation-comparison/#interpretation-questions","title":"Interpretation questions","text":"<ol> <li>Why retain derivation for a candidate that fails evaluation?</li> <li>Can a proposer cite a record returned after the proposal completed?</li> <li>Why does comparison permit different memory declarations here?</li> <li>Does <code>derived_from</code> prove the referenced information truly caused a model's    internal reasoning?</li> </ol> Answers  1. The reference can still have caused the attempted candidate; outcome and    causality are separate facts. 2. No. It was not in the strictly-prior committed observation ledger. 3. Memory strategy is the experiment's intended independent variable; the    comparison reports rather than conceals that difference. 4. No. It is the proposer's explicit, validated usage claim. Stronger causal    evidence requires controlled ablations or interventions."},{"location":"learn/notebooks/05-derivation-comparison/#change-and-predict","title":"Change and predict","text":"<p>Modify the proposer to cite the same evaluation reference twice.</p> <p>Predict: <code>ProposalResult</code> rejects duplicate derivation before a candidate can be treated as successful. No artifact from that proposal becomes rankable, and the invalid edge never enters the replayed graph.</p> <p>Next: recover a durable run after interruption.</p>"},{"location":"learn/notebooks/06-crash-recovery/","title":"Recover a durable run after a crash","text":"<p>Status: Shipped.</p> <p>A useful long-running experiment must survive process death without repeating committed work or inventing a different history. This chapter explains how <code>durable-local</code> storage, replay, registered components, and <code>meta.resume</code> compose to provide that guarantee.</p> <p>Use the executed notebook to rerun the fault matrix; this page contains the full explanation.</p>"},{"location":"learn/notebooks/06-crash-recovery/#what-it-teaches","title":"What it teaches","text":"<p>Crash recovery is not \u201cstart a similar run again.\u201d It is:</p> <pre><code>reopen one exact run id\n  \u2192 validate its committed event prefix\n  \u2192 reconstruct the last complete logical state\n  \u2192 rebuild verified components\n  \u2192 continue only the missing authorized work\n  \u2192 reach the same final event stream as uninterrupted execution\n</code></pre> <p>The guarantee depends on deterministic, retry-safe components and atomic commits. A live, non-reconstructable callback cannot honestly provide it.</p>"},{"location":"learn/notebooks/06-crash-recovery/#the-running-problem","title":"The running problem","text":"<p>The example climbs from <code>0</code> to target <code>6</code> by proposing <code>parent + 1</code>:</p> <pre><code>def propose(parent, context):\n    return parent + 1\n\ndef evaluate(candidate):\n    return -abs(candidate - 6)\n</code></pre> <p>Reaching <code>6</code> requires six successor trials and seven evaluations including the seed. The task permits eight evaluations and Greedy permits eight successor trials, leaving slack so the run stops because it satisfies the objective rather than because budget expires.</p>"},{"location":"learn/notebooks/06-crash-recovery/#why-ordinary-local-callables-are-insufficient","title":"Why ordinary local callables are insufficient","text":"<p>An in-memory function object cannot be reconstructed after the Python process dies. Durable mode therefore requires registered module-level components:</p> <pre><code>registry = meta.Registry()\nproposer_ref = registry.register_proposer(\n    \"example:propose\", \"1\", propose,\n    deterministic=True,\n    retry_safe=True,\n)\nevaluator_ref = registry.register_evaluator(\n    \"example:evaluate\", \"1\", evaluate,\n    deterministic=True,\n    retry_safe=True,\n    configuration={\"target\": 6},\n)\n</code></pre> <p>The durable event stream records typed component references and manifests. On resume, the supplied registry must resolve compatible implementations whose source and configuration match those manifests.</p> <p>The target belongs in evaluator configuration because changing it changes behavior even if the Python function name remains the same.</p>"},{"location":"learn/notebooks/06-crash-recovery/#starting-a-durable-run","title":"Starting a durable run","text":"<pre><code>experiment = meta.Experiment(\n    task=build_task(evaluator_ref),\n    seed=0,\n    proposer=proposer_ref,\n    search=meta.Greedy(max_trials=8),\n    registry=registry,\n)\nresult = meta.run(\n    experiment, storage=meta.Storage.durable(path), mode=\"durable-local\"\n)\n</code></pre> <p>Run identity derives from the declarations, not from the storage directory. Starting the same task, seed, component references, policy, and budgets on two paths produces the same run ID.</p>"},{"location":"learn/notebooks/06-crash-recovery/#atomic-batch-commits","title":"Atomic batch commits","text":"<p>The coordinator groups each state transition into an all-or-none storage commit. A simplified run looks like:</p> <pre><code>batch 1  run + seed records\nbatch 2  search decision\nbatch 3  proposal/trial outcome\nbatch 4  evaluation\nbatch 5  next decision\n...\n</code></pre> <p>If power fails between batches, every earlier batch is durable and the next is absent. If it fails while a batch is being written, the storage transaction commits all of it or none of it. Recovery never has to interpret half an evaluation.</p> <p>The notebook's <code>CrashAfter</code> wrapper is only a deterministic test instrument. It permits the first <code>n</code> real commits and raises before the next. Production users do not install it or touch the internal commit strategy; real operating system or hardware failure supplies the interruption.</p>"},{"location":"learn/notebooks/06-crash-recovery/#the-three-recovery-frontiers","title":"The three recovery frontiers","text":"<p>Replay classifies the committed prefix:</p> Frontier Last durable fact What resume does next <code>DECISION_READY</code> Previous work complete Ask search for the next decision <code>TRIAL_DECIDED</code> Trial authorization committed Run the authorized proposer work <code>EVALUATION_PENDING</code> Candidate outcome committed Evaluate that committed artifact <p>These states prevent duplicate work. For example, a committed candidate with no evaluation is evaluated; the proposer is not called again to create a possibly different candidate.</p> <p>Pull experience operations add a related boundary: an attempted but unresolved read is rolled forward using the same committed operation ID.</p> <p>The sweep classifies every interior boundary:</p> <pre><code>phases = {}\ntotal_a = boundaries + 1\nfor k in range(1, total_a):\n    p = adir / f'phase{k}'\n    s = meta.Storage.durable(p)  # install + run on one handle (see sweep note)\n    install_crash(s, n=k)\n    try:\n        main.start_run(s, main.build_components())\n    except CRASH:\n        pass\n    events = meta.Storage.durable(p).events.read(rid_a)\n    phase = classify_frontier(events, replay_run_projection(rid_a, events)).name\n    phases.setdefault(phase, []).append(k)\nfor name, ks in phases.items():\n    print(f'  {name:&lt;18} at boundaries {ks}')\n</code></pre> Output<pre><code>  DECISION_READY     at boundaries [1, 4, 7, 10, 13, 16, 19, 22]\n  TRIAL_DECIDED      at boundaries [2, 5, 8, 11, 14, 17, 20]\n  EVALUATION_PENDING at boundaries [3, 6, 9, 12, 15, 18, 21]\n</code></pre>"},{"location":"learn/notebooks/06-crash-recovery/#the-demonstrated-lifecycle","title":"The demonstrated lifecycle","text":"<p>The uninterrupted reference reaches best <code>6</code> in 49 events over 23 committed batches. Then the notebook interrupts the same logical run shape:</p> <pre><code>crash_after = max(1, committed_batches // 2)\nassert 1 &lt;= crash_after &lt; committed_batches\npath_b = tmp / 'path-B'\ncrash_storage = meta.Storage.durable(path_b)\ncrasher = install_crash(crash_storage, n=crash_after)\ncrashed = False\ntry:\n    main.start_run(crash_storage, components)\nexcept SimulatedCrash as e:\n    crashed = True\n    print('crashed:', e)\nassert crashed and crasher.commits == crash_after\ncrashed_len = len(meta.Storage.durable(path_b).events.read(run_id))\nprint(f'events on disk  : {crashed_len}  (a genuine PARTIAL stream vs {complete_len} complete)')\n</code></pre> Output<pre><code>crashed: simulated process crash after 11 committed steps\nevents on disk  : 25  (a genuine PARTIAL stream vs 49 complete)\n</code></pre> <p>After <code>meta.resume(...)</code> fills the missing work, compare the interrupted and uninterrupted outcomes:</p> <pre><code>assert resumed.id == run_id\nassert resumed_best == reference_best\nok = resumed_best == reference_best\nprint(f'interrupted == uninterrupted: {ok}  ({resumed_best} == {reference_best})')\nprint(f'partial stream {crashed_len} events -&gt; recovered {resumed_len} events -&gt; best {resumed_best}')\n</code></pre> Output<pre><code>interrupted == uninterrupted: True  (6 == 6)\npartial stream 25 events -&gt; recovered 49 events -&gt; best 6\n</code></pre> <p>The advanced sweep repeats this after all 22 interior boundaries. Separate sweeps cover pushed context and pull experience:</p> <pre><code>_, _, eb, eok, ebest, rollfwd = sweep(advanced.experience_run, advanced.build_components, Path(tempfile.mkdtemp()))\nprint(f'experience run: {eb} boundaries, all equivalent = {eok}, best = {ebest}')\nprint(f'  mid-operation crash boundaries rolled forward on resume: {rollfwd}')\n</code></pre> Output<pre><code>experience run: 43 boundaries, all equivalent = True, best = 6\n  mid-operation crash boundaries rolled forward on resume: 7\n</code></pre>"},{"location":"learn/notebooks/06-crash-recovery/#reopen-is-not-resume","title":"Reopen is not resume","text":"<p>These operations answer different questions:</p> <pre><code>cold = meta.Run(rid_a, meta.Storage.durable(adir / 'ref'))\nprint('best() from a cold store :', cold.best().value)\nprint('winning lineage steps    :', [t.logical_step for t in cold.lineage()])\n</code></pre> Output<pre><code>best() from a cold store : 6\nwinning lineage steps    : [0, 1, 2, 3, 4, 5, 6]\n</code></pre> <p><code>Run(...)</code> rebuilds a read-only result from committed facts. It performs no new work and can inspect a completed or partial history where queries are defined.</p> <pre><code>continued = meta.resume(\n    run_id,\n    storage=reopened_storage,\n    registry=registry,\n)\n</code></pre> <p><code>resume</code> may invoke search, proposer, and evaluator to finish missing work. It therefore requires reconstruction authority and verified components.</p> <p>Resuming an already completed run is a no-op. Concurrent resume attempts use optimistic append control: one commits, while a stale writer receives <code>EventStreamConflict</code> rather than duplicating a transition.</p>"},{"location":"learn/notebooks/06-crash-recovery/#what-cannot-resume","title":"What cannot resume","text":"<p>A run may retain durable history without being resumable. Local patch callbacks, command evaluator objects, provider sessions, or closures can be inspectable after reopening while lacking a portable reconstruction contract.</p> <p>Meta-Evolve raises <code>CapabilityError</code> instead of silently substituting a different component. Durable storage is evidence retention; resumability is an additional capability.</p>"},{"location":"learn/notebooks/06-crash-recovery/#what-to-notice","title":"What to notice","text":"<ul> <li>The event log is append-only; crash simulation never truncates it.</li> <li>Atomicity prevents partial logical batches.</li> <li>Replay determines exactly which work is missing.</li> <li>Component manifests protect behavior across process restart.</li> <li>Run identity is logical and independent of the storage path.</li> <li>Resume does not mean arbitrary live-model output becomes reproducible.</li> </ul>"},{"location":"learn/notebooks/06-crash-recovery/#interpretation-questions","title":"Interpretation questions","text":"<ol> <li>Why does a committed candidate with no evaluation not run its proposer again?</li> <li>Why must the evaluator's target be in its manifest configuration?</li> <li>What is the difference between reopening and resuming?</li> <li>Can a non-deterministic remote provider promise a byte-identical resumed    result merely because events are durable?</li> </ol> Answers  1. The proposal result already exists; rerunning could create a second outcome    for one authorized trial. Resume continues at `EVALUATION_PENDING`. 2. The target changes scoring behavior while the function's name and source may    stay constant. 3. Reopen reads committed history; resume is authorized to reconstruct    components and continue missing work. 4. No. Durable observations can be retained, but equivalent future provider    output requires stronger provider-specific guarantees."},{"location":"learn/notebooks/06-crash-recovery/#change-and-predict","title":"Change and predict","text":"<p>Move the simulated crash from batch <code>11</code> to batch <code>12</code>.</p> <p>Predict which frontier the new prefix reaches, whether the event count grows, and which operation resume performs first. The final best and complete event stream must remain identical to the reference.</p> <p>Next: evolve immutable source with an external grader.</p>"},{"location":"learn/notebooks/07-source-patching/","title":"Evolve immutable source with an external grader","text":"<p>Status: Shipped.</p> <p>So far, artifacts were small Python values. This chapter follows the same declarative loop when the artifact is a directory of source files and the evaluator is a real subprocess outside candidate authority.</p> <p>Open the canonical executed notebook only if you want to execute the cells; this page explains the full lifecycle.</p>"},{"location":"learn/notebooks/07-source-patching/#what-it-teaches","title":"What it teaches","text":"<p>The complete repair is:</p> <pre><code>load immutable UTF-8 source\n  \u2192 propose one exact file replacement in a private workspace\n  \u2192 capture a new immutable SourceTree\n  \u2192 materialize it in a fresh evaluation workspace\n  \u2192 run an external grader under a declared process protocol\n  \u2192 commit score, evidence, usage, and parent lineage\n</code></pre> <p>The original checkout is never modified. A candidate proposal cannot include grader edits because the grader is not part of the candidate artifact. Runtime host isolation is a separate concern explained below.</p>"},{"location":"learn/notebooks/07-source-patching/#the-broken-program","title":"The broken program","text":"<p>The candidate tree contains one file, <code>clamp.py</code>. Its documented job is to keep <code>value</code> between <code>low</code> and <code>high</code>. The seed implementation is wrong; the desired replacement is:</p> <pre><code>def clamp(value, low, high):\n    return max(low, min(value, high))\n</code></pre> <p>The evaluator-owned tests include values below, inside, and above the interval. The seed scores <code>0</code>; the repaired successor scores <code>1</code>.</p>"},{"location":"learn/notebooks/07-source-patching/#step-1-load-an-immutable-sourcetree","title":"Step 1: load an immutable <code>SourceTree</code>","text":"<pre><code>source = meta.load_source_tree(staged)\nprint(\"loaded immutable source:\", tuple(source) == (\"clamp.py\",))\n</code></pre> Output<pre><code>loaded immutable source: True\n</code></pre> <p>The loader recursively captures exact relative paths and UTF-8 text. It rejects:</p> <ul> <li>symlinks;</li> <li>binary or non-UTF-8 content;</li> <li>non-regular files;</li> <li>unsafe or ambiguous paths.</li> </ul> <p>There are deliberately no ignore rules. A generated <code>__pycache__</code> changes the directory being declared and is rejected rather than silently omitted. Stage only intended files into a clean directory when tools may have left debris.</p> <p><code>SourceTree</code> is immutable. A successor is new content with parent lineage, not an in-place edit of <code>source</code>.</p>"},{"location":"learn/notebooks/07-source-patching/#step-2-describe-one-exact-patch","title":"Step 2: describe one exact patch","text":"<pre><code>def repair(tree, context):\n    return meta.TextPatch(\n        path=\"clamp.py\",\n        expected=tree[\"clamp.py\"],\n        replacement=FIXED,\n    )\n</code></pre> <p><code>expected</code> is optimistic concurrency for file content. The patch applies only if the private workspace still contains the exact parent text the proposer saw. This prevents a stale patch from overwriting an unexpected revision.</p> <p><code>PatchProposer</code> owns the workspace lifecycle:</p> <pre><code>materialize parent \u2192 invoke patch callback \u2192 validate path/expected/replacement\n\u2192 apply exact edit \u2192 capture SourceTree \u2192 dispose workspace\n</code></pre> <p>Invalid path, conflict, unchanged replacement, malformed patch, and cleanup failure remain typed proposal outcomes. They are not low task scores.</p>"},{"location":"learn/notebooks/07-source-patching/#step-3-keep-the-grader-external","title":"Step 3: keep the grader external","text":"<p>The evaluator declaration is:</p> <pre><code>evaluator = meta.CommandEvaluator(\n    command=meta.ProcessSpec(\n        argv=(python, \"run_grader.py\", \"grader.py\"),\n        environment={\"PYTHONDONTWRITEBYTECODE\": \"1\"},\n    ),\n    environment=meta.EvaluationEnvironment(\n        name=\"python-clamp-tests\",\n        version=\"1\",\n    ),\n)\n</code></pre> <p>For each candidate, <code>CommandEvaluator</code> creates a fresh workspace, materializes only the candidate source, runs the fixed command, captures bounded output, and disposes the workspace.</p> <p><code>grader.py</code> and <code>run_grader.py</code> live outside the <code>SourceTree</code>. The candidate cannot propose changes to them because they are not in its artifact or proposal workspace.</p>"},{"location":"learn/notebooks/07-source-patching/#the-exit-protocol","title":"The exit protocol","text":"<p>The fixed runner translates process behavior into domain outcomes:</p> Exit Meaning Outcome Rankable? <code>0</code> Candidate passes <code>score=1</code> Yes <code>1</code> Candidate fails tests or cannot load <code>score=0</code> Yes Other Grader/protocol broke <code>EvaluatorFailure</code> No Timeout Process exceeded wall limit <code>TimeoutFailure</code> No <p>Candidate syntax error is rankable failure because the task is to supply working source. Missing grader is not candidate performance; scoring it <code>0</code> would reward infrastructure reliability as though it measured code quality.</p> <pre><code>fault = broken_evaluator(source)\nprocess = next(item for item in fault.evidence if item.kind == \"process-result\")\nprint(\"grader fault:\", type(fault.failure).__name__)\nprint(\"grader fault rankable:\", bool(fault.metrics))\nprint(\"grader fault process exit:\", process.data[\"exit_code\"])\nprint(\"grader fault evidence kinds:\", [item.kind for item in fault.evidence])\n</code></pre> Output<pre><code>grader fault: EvaluatorFailure\ngrader fault rankable: False\ngrader fault process exit: 2\ngrader fault evidence kinds: ['evaluation-environment', 'process-result']\n</code></pre>"},{"location":"learn/notebooks/07-source-patching/#step-4-budget-seed-and-successor","title":"Step 4: budget seed and successor","text":"<pre><code>budget = meta.Budget(evaluations=2, trials=1)\ntask = meta.Task(\n    evaluator=evaluator,\n    artifact=meta.SourceTree,\n    budget=budget,\n)\n</code></pre> <p>Two evaluations cover the broken seed and one proposed successor. One trial authorizes the patch. The task owns this coordinator ceiling.</p>"},{"location":"learn/notebooks/07-source-patching/#step-5-run-the-ordinary-loop","title":"Step 5: run the ordinary loop","text":"<pre><code>experiment = meta.Experiment(\n    task=task,\n    seed=source,\n    proposer=meta.PatchProposer(repair),\n    search=meta.Greedy(max_trials=1),\n)\nresult = meta.run(experiment, storage=storage, mode=\"local\")\n</code></pre> <p>No coordinator branch knows this is Python repair. The artifact codec handles <code>SourceTree</code>; the proposer handles patches; the evaluator handles process execution; Greedy sees only rankable outcomes.</p>"},{"location":"learn/notebooks/07-source-patching/#step-6-read-immutable-evidence","title":"Step 6: read immutable evidence","text":"<p>The saved run prints the evaluator-owned evidence and causal result:</p> <pre><code>print(\"source digests:\", digests)\nprint(\"scores:\", scores)\nprint(\"process exits:\", exits)\nprint(\"failures:\", failures)\nprint(\"evidence kinds:\", kinds)\nprint(\"repaired best:\", best.value[\"clamp.py\"] == FIXED)\nprint(\"parent unchanged:\", source[\"clamp.py\"] != FIXED)\nprint(\"lineage length:\", len(lineage))\n</code></pre> Output<pre><code>source digests: ['659bfe4a9f389018a5059f9045d6db37592c017591629d6baf5a7ab52792ca53', '08abe248cf356c0ee0322aa9329598221640460d6cf0202228097d7f4c4c21c0']\nscores: [0.0, 1.0]\nprocess exits: [1, 0]\nfailures: [None, None]\nevidence kinds: [['evaluation-environment', 'process-result'], ['evaluation-environment', 'process-result']]\nrepaired best: True\nparent unchanged: True\nlineage length: 2\n</code></pre> <p>Each lineage entry includes a content digest. The repaired tree has a different digest and names the seed artifact as its parent. Reopening durable storage can read the same best artifact and lineage without touching the checkout.</p> <p>Process wall time is occurrence evidence, not deterministic identity. Content, declarations, scores, and semantic outcome are the stable parts of this local example.</p>"},{"location":"learn/notebooks/07-source-patching/#durable-history-is-not-cold-resume","title":"Durable history is not cold resume","text":"<p>The run uses durable storage but <code>mode=\"local\"</code>. Its live patch callback, command evaluator, and workspace providers have no portable reconstruction manifest. Therefore:</p> <pre><code>with meta.Storage.durable(history) as reopened:\n    durable = meta.Run(result.id, reopened)\n    print(\"reopened best:\", durable.best() == best)\n    print(\"reopened lineage:\", durable.lineage() == lineage)\n    try:\n        meta.resume(result.id, storage=reopened, registry=meta.Registry())\n    except meta.CapabilityError as error:\n        if \"not resumable\" not in str(error):\n            raise\n        print(\"cold resume: CapabilityError (local run is not resumable)\")\n    else:\n        raise RuntimeError(\"local workspace run unexpectedly resumed\")\n</code></pre> Output<pre><code>reopened best: True\nreopened lineage: True\ncold resume: CapabilityError (local run is not resumable)\n</code></pre> <p>Failing closed is important. Substituting a different grader or patcher after a restart would create one run ID with two behaviors.</p>"},{"location":"learn/notebooks/07-source-patching/#the-subprocess-boundary-is-not-hostile-isolation","title":"The subprocess boundary is not hostile isolation","text":"<p><code>DevelopmentSubprocessSandbox</code> controls working directory, environment, timeout, process-group termination, and retained stdout/stderr. It does not isolate network, CPU, memory, syscalls, or arbitrary host filesystem access.</p> <p>Use a hardened container or other isolation provider for untrusted code. Do not describe this development boundary as a security sandbox.</p>"},{"location":"learn/notebooks/07-source-patching/#what-to-notice","title":"What to notice","text":"<ul> <li>Candidate authority is exactly the files inside the immutable tree.</li> <li>Exact patches prevent stale overwrite and preserve the parent.</li> <li>Proposal and evaluation use separate fresh workspaces.</li> <li>The external grader remains outside candidate lineage.</li> <li>Candidate-caused failure is rankable; evaluator failure is not.</li> <li>Durable inspection does not imply resumable execution.</li> </ul>"},{"location":"learn/notebooks/07-source-patching/#interpretation-questions","title":"Interpretation questions","text":"<ol> <li>Why include <code>expected</code> text in a patch?</li> <li>Why is a candidate syntax error score <code>0</code> while a missing grader has no    metric?</li> <li>Does <code>SourceTree</code> prevent candidate code from accessing the host at runtime?</li> <li>Why can the run reopen but not resume?</li> </ol> Answers  1. It ensures the patch applies to the exact parent revision the proposer saw. 2. Syntax validity is task performance; missing evaluator infrastructure means    the task was never measured. 3. No. It bounds the versioned artifact and workspace capture, not hostile-code    runtime authority. 4. Stored artifacts/events are portable, but the local live callbacks and    providers are not reconstructable from a registry manifest."},{"location":"learn/notebooks/07-source-patching/#change-and-predict","title":"Change and predict","text":"<p>Change the replacement to invalid Python.</p> <p>Predict: the patch itself is structurally valid and commits a new source tree; the external runner exits <code>1</code>; evaluation returns rankable <code>score=0</code>; Greedy keeps the original seed as best; the failed candidate and its process evidence remain in <code>run.trials()</code> but not in the winning lineage.</p> <p>You now have the complete basic path: declare a task, choose search, compare it fairly, reuse experience causally, recover durable work, and evolve real source under an external evaluator.</p>"},{"location":"learn/research-labs/01-feedback-descent/","title":"Feedback Descent: improve a prompt from textual feedback","text":"<p>Status: Experimental/live-only.</p> <p>Optional executable companion: open the canonical notebook. This chapter contains the full explanation; the notebook is not required.</p>"},{"location":"learn/research-labs/01-feedback-descent/#the-concrete-problem","title":"The concrete problem","text":"<p>Who holds the all-time record at the Grammys for the most wins in the album of the year category?</p> <p>The solver receives this question and a noisy document bundle. It does not receive the evaluator's answer, gold URL, gold-document label, or the fact that this is the Feedback Descent development row.</p> <p>Two prompt candidates start from the same evidence:</p> <pre><code>candidate A\nAnswer the question from the supplied documents. Return only the answer.\n\ncandidate B\nFind the document that directly resolves the question, ignore conflicting\ndistractors, and return only the answer.\n</code></pre> <p>The artifact being improved is the prompt itself, represented as <code>meta.Text</code>. The question, documents, exact-answer grader, and budget remain fixed.</p>"},{"location":"learn/research-labs/01-feedback-descent/#the-idea","title":"The idea","text":"<p>Ordinary optimization says only \u201cA scored 0 and B scored 1.\u201d Feedback Descent adds an inspectable explanation of why one candidate is preferred, then asks an editor to derive a successor from that explanation.</p> <pre><code>prompt A \u2500\u2500\u25ba answer A \u2500\u2500\u2510\n                         \u251c\u2500\u25ba pairwise preference + textual feedback\nprompt B \u2500\u2500\u25ba answer B \u2500\u2500\u2518                 \u2502\n                                           \u25bc\n                              edit preferred prompt\n                                           \u2502\n                                           \u25bc\n                                  evaluate edited prompt\n</code></pre> <p>This lab isolates that mechanism. It does not implement the paper's full search strategy or claim to reproduce its results.</p>"},{"location":"learn/research-labs/01-feedback-descent/#from-meta-evolve-principles-to-this-algorithm","title":"From Meta-Evolve principles to this algorithm","text":"<p>Feedback Descent sounds specialized, but the teaching loop is assembled from ordinary evaluated-improvement roles:</p> Role Concrete component Why it is separate Mutable artifact Two seed <code>meta.Text</code> prompts, then one edited prompt The thing being optimized is explicit and immutable Task input Candidate-visible <code>case.md</code> Every solver receives the same question and noisy documents Proposal evidence Structured preference plus textual feedback The edit can name exactly what observation caused it Proposer Editor session constrained by <code>EDIT_SCHEMA</code> It may rewrite a prompt but cannot score it Evaluator Pairwise comparison session plus local exact grading Hidden answer and ranking stay outside prompt authority Search shape Compare two seeds, edit the preferred one once This is a fixed teaching schedule, not an unbounded optimizer Budget Five individually bounded sessions One bad step cannot trigger retries until improvement appears <p>There is no numeric gradient. \u201cDescent\u201d means turning an evaluator's natural language diagnosis into the direction of the next artifact change:</p> <pre><code>observed answer difference\n  \u2192 evaluator says which behavior was better and why\n  \u2192 editor converts that reason into a general prompt instruction\n  \u2192 unchanged task measures the successor\n</code></pre> <p>The algorithm is simple because Meta-Evolve already supplies immutable text, bounded provider sessions, structured evidence, typed failures, usage, and identity. The lab only has to specify who sees which files and how those five sessions connect.</p>"},{"location":"learn/research-labs/01-feedback-descent/#how-the-implementation-moves-data","title":"How the implementation moves data","text":"<p><code>run()</code> loads one full <code>EvaluatorCase</code>, but immediately renders only <code>case.public</code> for solver workspaces. <code>_solve()</code> combines one prompt with that public file and asks for the closed <code>ANSWER_SCHEMA</code>.</p> <pre><code>PROMPTS[name] + public case\n  \u2500\u2500 _solve \u2500\u2500\u25ba SessionRecord(answer or failure)\n  \u2500\u2500 grade_record(expected answer, evaluator side) \u2500\u2500\u25ba Measurement\n</code></pre> <p>Only <code>_comparison_request()</code> receives <code>case.answer</code>. It writes a private <code>comparison.md</code> containing the reference and both observed answers, then asks for <code>COMPARISON_SCHEMA</code>. That response is evaluator evidence, not a prompt artifact.</p> <p>The editor gets a new <code>SourceTree</code> with exactly:</p> <pre><code>prompt.md    preferred seed prompt\nfeedback.md  comparator's textual feedback\n</code></pre> <p>It cannot recover the hidden answer from those files. After schema validation, the code lifts <code>value[\"prompt\"]</code> into a new <code>meta.Text</code>, rejects an empty or unchanged edit, records <code>edit_derived_from=\"feedback.comparison\"</code>, and calls the same <code>_solve()</code> and <code>grade_record()</code> path used for the seeds.</p> <p>This reuse is what makes the final comparison interpretable: the artifact is different, while public case, model, schema, grader, and bounds remain fixed.</p>"},{"location":"learn/research-labs/01-feedback-descent/#walkthrough-one-improvement-cycle-in-four-phases","title":"Walkthrough: one improvement cycle in four phases","text":"<p>This walkthrough uses phase for an outer algorithm step. The five provider sessions inside the phases are five independent inner <code>meta.run</code> calls with <code>Greedy(max_trials=1)</code>; they are not five search iterations.</p> Phase Meta-Evolve components State entering the phase State leaving the phase 1. Measure seeds Two immutable <code>meta.Text</code> artifacts; two solver <code>SessionRequest</code>s; evaluator-owned <code>grade_record()</code> Prompt A, prompt B, and the same public case Two <code>SessionRecord</code>s and two exact measurements 2. Derive feedback Comparator request with <code>COMPARISON_SCHEMA</code> Both observed answers plus the evaluator-only reference Preferred seed name and textual feedback, or a typed failure 3. Propose successor Editor request over a two-file <code>SourceTree</code> Preferred <code>prompt.md</code> and <code>feedback.md</code>; no hidden answer New <code>meta.Text</code> prompt with lineage to <code>feedback.comparison</code> 4. Measure and retain Reused solver path and exact grader; <code>LabResult</code> Successor prompt and the unchanged public case Edited measurement, all session evidence, artifacts, lineage, and usage <p>The authority boundary stays visible at every handoff. Solvers can answer but cannot grade; the comparator can read the reference but cannot edit the artifact; the editor can edit but sees only feedback; and outer Python decides whether a structured result is a measurement, a successor, or an unrankable failure.</p> <p>For a concrete state trace, suppose candidate B is preferred. No live outcome is being claimed here\u2014the names show how records would flow:</p> <pre><code>meta.Text(prompt B)\n  + SessionRecord(candidate B answer)\n  + SessionRecord(comparison feedback)\n  \u2500\u2500 editor request \u2500\u2500\u25ba meta.Text(edited prompt)\n  \u2500\u2500 unchanged solver/evaluator \u2500\u2500\u25ba Measurement(edited)\n  \u2500\u2500 retain \u2500\u2500\u25ba LabResult\n</code></pre>"},{"location":"learn/research-labs/01-feedback-descent/#the-five-sessions","title":"The five sessions","text":"# Session Observes Produces 1 <code>candidate_a</code> solver Prompt A, public question and documents Structured answer A 2 <code>candidate_b</code> solver Prompt B, identical public question and documents Structured answer B 3 Pairwise evaluator Both answers and evaluator-only reference answer Preferred candidate and textual feedback 4 Prompt editor Preferred prompt and textual feedback only Rewritten prompt 5 Edited-prompt solver Rewritten prompt, original public question and documents Structured answer for exact grading <p>The editor never sees the reference answer or gold URL. Its input is exactly the preferred prompt plus the feedback, so the successor can record:</p> <pre><code>edited prompt derived_from feedback.comparison\n</code></pre> <p>If the comparison or editor fails, later dependent sessions do not run. Five is the ceiling for one complete path, not a promise to spend five calls after a typed failure makes the remaining comparison meaningless.</p>"},{"location":"learn/research-labs/01-feedback-descent/#structured-evidence","title":"Structured evidence","text":"<p>The evaluator must return this closed-world shape:</p> <pre><code>{\n  \"preferred_candidate\": \"candidate_a\",\n  \"textual_feedback\": \"A general explanation of what the prompt should change.\"\n}\n</code></pre> <p><code>preferred_candidate</code> may be only <code>candidate_a</code> or <code>candidate_b</code>. Feedback must be non-empty, and extra fields are rejected. The independent validator runs for both live wrapper output and injected test output.</p> <p>The editor separately returns:</p> <pre><code>{\"prompt\": \"The complete rewritten solver prompt.\"}\n</code></pre> <p>An empty or unchanged prompt is a typed <code>ProposerFailure</code>; it is not evaluated as if it were a new candidate.</p>"},{"location":"learn/research-labs/01-feedback-descent/#minimal-runnable-mechanism","title":"Minimal runnable mechanism","text":"<p>The real implementation is deliberately small:</p> <pre><code>from sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.feedback_descent import run\n\nresult = run(UserSdkSessionRunner(agent_factory=build_agent))\n\nprint(\"preferred:\", result.artifacts.get(\"preferred\"))\nprint(\"feedback:\", result.artifacts.get(\"feedback\"))\nprint(\"edited:\", result.artifacts.get(\"edited\"))\nprint(\"derived from:\", result.artifacts.get(\"edit_derived_from\"))\nprint(\"edited measurement:\", result.measurements.get(\"edited\"))\nprint(\"usage:\", result.usage)\n</code></pre> Illustrative shape \u2014 live run, nothing captured<pre><code>preferred: &lt;candidate_a-or-candidate_b&gt;\nfeedback: &lt;structured evaluator rationale&gt;\nedited: &lt;complete rewritten prompt&gt;\nderived from: feedback.comparison\nedited measurement: &lt;exact metric or typed failure&gt;\nusage: &lt;aggregated provider usage&gt;\n</code></pre> <p>Run it from a user-owned driver after authenticating your SDK:</p> <pre><code>META_EVOLVE_LIVE_SDK=1 uv run python your_sealqa_driver.py feedback_descent\n</code></pre> <p>The five sessions all use the pinned Haiku model, equal 50-turn ceilings, a 1,800-second adapter timeout, and a 1,860-second inner wall budget.</p>"},{"location":"learn/research-labs/01-feedback-descent/#how-to-read-the-result","title":"How to read the result","text":"<p>Inspect these fields in order:</p> <ol> <li><code>sessions</code>: did every requested session succeed, and what did it cost?</li> <li><code>measurements.candidate_a</code> and <code>candidate_b</code>: were the initial answers exact?</li> <li><code>artifacts.preferred</code>: which prompt did the evaluator select?</li> <li><code>artifacts.feedback</code>: is the explanation general, actionable, and grounded    in the observed difference?</li> <li><code>artifacts.edited</code>: did the editor actually change the prompt?</li> <li><code>artifacts.edit_derived_from</code>: does lineage name the comparison evidence?</li> <li><code>measurements.edited</code>: did the one edited evaluation improve, tie, or regress?</li> </ol> <p>Possible outcomes are intentionally broader than \u201csuccess\u201d:</p> Outcome Honest interpretation Edited score improves The edit helped this one development case. Tie Feedback and editing bought no measured gain here. Regression The edit damaged this case; preserve the negative result. Comparison invalid There is no preference to rank or edit from. Editor unchanged No successor exists. Provider/timeout failure The attempt is unrankable, not a zero score. <p>One development row cannot establish general prompt quality. A robust claim would require a separately declared held-out suite and repeated compatible comparisons.</p>"},{"location":"learn/research-labs/01-feedback-descent/#leakage-and-validity-checklist","title":"Leakage and validity checklist","text":"<ul> <li>Only the pairwise evaluator may read the hidden reference answer.</li> <li>Neither solver request contains source URLs, split roles, or gold labels.</li> <li>The editor workspace contains only <code>prompt.md</code> and <code>feedback.md</code>.</li> <li>The edited solver sees the same public case as both initial solvers.</li> <li>All solver arms use the same model and resource declarations.</li> <li>A favorable edit is reported once; it is not selected from repeated reruns.</li> </ul>"},{"location":"learn/research-labs/01-feedback-descent/#change-and-predict","title":"Change and predict","text":"<p>Remove \u201cignore conflicting distractors\u201d from candidate B. Before running, predict:</p> <ol> <li>which prompt the evaluator will prefer;</li> <li>what general feedback it will produce;</li> <li>whether the edited prompt will improve, tie, regress, or fail;</li> <li>which evidence and lineage fields should stay unchanged.</li> </ol> <p>The interesting result is not merely the final score. It is whether the recorded feedback caused an inspectable prompt change under a valid comparison.</p>"},{"location":"learn/research-labs/02-autodata/","title":"AutoData: improve a generation recipe with solver behavior","text":"<p>Status: Experimental/live-only.</p> <p>Optional executable companion: open the canonical notebook. Everything needed to understand the lab is explained below.</p>"},{"location":"learn/research-labs/02-autodata/#the-concrete-problem","title":"The concrete problem","text":"<p>What is the current age of the oldest person to sail solo across the Pacific Ocean?</p> <p>This frozen row supplies the grounding documents from which a generator creates one new question-answer item. The artifact being improved is not the answer and not the generated row; it is the <code>meta.Text</code> generation recipe.</p> <p>The seed recipe says:</p> <pre><code>Generate one unambiguous fact-seeking question answerable from the evidence.\nReturn its exact short answer and the document IDs used.\n</code></pre> <p>The lab asks whether solver behavior can identify a useful generated item and provide evidence for rewriting that recipe.</p>"},{"location":"learn/research-labs/02-autodata/#the-idea","title":"The idea","text":"<p>AutoData-style generation uses downstream behavior as a quality signal:</p> <pre><code>recipe \u2500\u2500\u25ba generated QA item\n                    \u2502\n          \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n          \u25bc                   \u25bc\n restricted solver      informed solver\n question only          question + evidence\n          \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                    \u25bc\n       deterministic acceptance checks\n                    \u2502\n                    \u25bc\n              rewrite recipe\n</code></pre> <p>The paper discusses weak and strong models. This lab deliberately uses the same pinned model with different context. The code roles are therefore named <code>restricted_solver</code> and <code>informed_solver</code>. Calling them weak and strong would hide the fact that context\u2014not model capability\u2014is the changed variable. That setup is a teaching simplification and a confounder.</p>"},{"location":"learn/research-labs/02-autodata/#from-meta-evolve-principles-to-this-algorithm","title":"From Meta-Evolve principles to this algorithm","text":"<p>The important move is to optimize the recipe, not to keep whichever single question looks nice. Each component has one authority:</p> Role Concrete component What it contributes Mutable artifact <code>meta.Text</code> recipe A versioned instruction that can have a successor Generator Schema-constrained session Applies a recipe to public evidence and proposes one QA item Restricted probe Same model with <code>question.md</code> only Tests whether evidence is unnecessary or the answer leaks Informed probe Same model with selected <code>evidence.md</code> Tests whether cited evidence supports recovery Evaluator <code>quality_checks()</code> in local Python Converts observations into explicit acceptance reasons Proposer Recipe-rewriter session Turns the report into a generalized successor recipe Search shape Seed evaluation \u2192 one rewrite \u2192 successor evaluation Makes the causal comparison small and inspectable <p>The generated QA item is an evaluation episode, not the durable thing being optimized. A seed recipe and successor recipe may each generate different questions; comparison asks whether the recipe produces an item that passes the same quality protocol.</p> <pre><code>recipe artifact\n  \u2192 generator applies it\n  \u2192 two controlled solver contexts probe the item\n  \u2192 deterministic evaluator produces accepted + reasons\n  \u2192 rewriter uses that evidence to propose the next recipe artifact\n</code></pre> <p>This is why the method is easy to build with Meta-Evolve: artifact lineage, bounded sessions, evidence, schema failure, and usage already exist. AutoData's domain logic is the small <code>quality_checks()</code> function and the choice of what the two solver arms may see.</p>"},{"location":"learn/research-labs/02-autodata/#how-_evaluate_recipe-assembles-one-evaluation","title":"How <code>_evaluate_recipe()</code> assembles one evaluation","text":"<p>The function first writes the public SealQA bundle to <code>evidence.md</code> and asks the generator for <code>ITEM_SCHEMA</code>. If generation fails validation, it returns one session plus a rejection report and spends no solver calls.</p> <p>For a valid item it builds two fresh workspaces:</p> <pre><code>restricted_solver/\n  question.md\n\ninformed_solver/\n  question.md\n  evidence.md   only documents named by evidence_doc_ids\n</code></pre> <p>The code resolves local document IDs against the available public bundle. An unknown ID therefore cannot smuggle in external evidence; it becomes the deterministic <code>unknown_evidence_document</code> reason.</p> <p>Both <code>_solver()</code> calls use the same answer instructions, <code>ANSWER_SCHEMA</code>, SDK wrapper declaration, limits, and wall budget. Only the informed workspace has evidence. <code>quality_checks()</code> then reads the generator item and both <code>SessionRecord</code>s. It never calls a model:</p> <pre><code>schema valid?\nIDs known?\nanswer text absent from question?\nboth solver sessions healthy?\nrestricted answer differs from reference?\ninformed answer exactly matches reference?\n</code></pre> <p>The complete dictionary\u2014generated item, solver outputs, <code>accepted</code>, and every reason\u2014is written into <code>LabResult.artifacts[\"evidence\"]</code>. The rewrite workspace contains the seed recipe and this report. The successor is lifted back into <code>meta.Text</code> and sent through the identical <code>_evaluate_recipe()</code> function.</p>"},{"location":"learn/research-labs/02-autodata/#walkthrough-one-improvement-cycle-in-three-phases","title":"Walkthrough: one improvement cycle in three phases","text":"<p>Each phase below is outer Python orchestration. Its generator, solver, and rewriter sessions are separate inner <code>meta.run</code> calls with one trial each; the lab does not run three greedy search iterations.</p> Phase Meta-Evolve components What the candidate can observe Evaluator-owned result 1. Evaluate seed recipe Seed <code>meta.Text</code>; generator request; restricted and informed solver requests Generator sees the public bundle; restricted solver sees only the generated question; informed solver sees only selected evidence <code>quality_checks()</code> records accepted/rejected reasons and a seed measurement 2. Propose successor recipe Rewriter request; successor <code>meta.Text</code> Seed recipe, generated item, solver records, and the complete deterministic report Schema and non-identity checks decide whether a successor exists 3. Evaluate and retain successor The same <code>_evaluate_recipe()</code> protocol; <code>LabResult</code> A fresh generator applies only the successor recipe; solver visibility remains split Successor acceptance measurement plus both recipes, reports, sessions, lineage, and usage <p>The state transition is therefore:</p> <pre><code>meta.Text(seed recipe)\n  \u2500\u2500 generator + two probes \u2500\u2500\u25ba seed acceptance report\n  \u2500\u2500 rewriter SessionRequest \u2500\u2500\u25ba meta.Text(successor recipe)\n  \u2500\u2500 same generator + two probes \u2500\u2500\u25ba successor acceptance report\n  \u2500\u2500 retain \u2500\u2500\u25ba LabResult\n</code></pre> <p>The generated QA items are temporary evaluation episodes. They appear in the evidence record, but the lineage edge connects recipe to recipe. The local acceptance gate, not the generator or rewriter, decides whether each episode meets the declared behavior.</p>"},{"location":"learn/research-labs/02-autodata/#the-seven-sessions","title":"The seven sessions","text":"<p>One recipe evaluation uses three SDK sessions:</p> <ol> <li>generate a structured QA item from the frozen document bundle;</li> <li>ask the restricted solver with no documents;</li> <li>ask the informed solver with the generated item's selected documents.</li> </ol> <p>The complete path is:</p> # Session Purpose 1 Seed generator Apply the seed recipe. 2 Seed restricted solver Test whether the question leaks or is already known. 3 Seed informed solver Test whether selected evidence supports the reference answer. 4 Recipe rewriter Generalize from the seed acceptance report. 5 Successor generator Apply the rewritten recipe. 6 Successor restricted solver Repeat the restricted check. 7 Successor informed solver Repeat the grounded check. <p>The local checks between sessions are deterministic and make no provider calls:</p> <pre><code>3 seed sessions + 1 rewrite + 3 successor sessions = 7 maximum\n</code></pre>"},{"location":"learn/research-labs/02-autodata/#the-generated-item-contract","title":"The generated-item contract","text":"<p>The generator must return:</p> <pre><code>{\n  \"question\": \"One generated fact-seeking question\",\n  \"reference_answer\": \"One exact short answer\",\n  \"evidence_doc_ids\": [\"doc-03\", \"doc-09\"]\n}\n</code></pre> <p>The schema requires all three fields, non-empty strings, and no extra fields. Document IDs must exist in the candidate-visible bundle. They are stable local IDs\u2014not upstream URLs or labels that reveal which source is gold.</p> <p>The <code>reference_answer</code> here belongs to the synthetic item produced by the generator. It is not the evaluator-only answer of the frozen SealQA row.</p>"},{"location":"learn/research-labs/02-autodata/#deterministic-quality-checks","title":"Deterministic quality checks","text":"<p>An item is accepted only when every check passes:</p> Check Rejection reason Meaning Schema structured-output failure Required fields are missing or malformed. Evidence IDs <code>unknown_evidence_document</code> The recipe cited a document it never received. Leakage <code>reference_answer_leaked_in_question</code> The question gives away its own answer. Restricted exact answer <code>restricted_solver_already_knows_answer</code> The item may be trivial, memorized, or leaking. Informed exact answer <code>informed_solver_cannot_recover_answer</code> Selected evidence does not establish the answer. Session health <code>solver_failure</code> At least one solver produced no rankable answer. <p>In compact form, the desired behavior is:</p> <pre><code>restricted answer != reference answer\ninformed answer    == reference answer\n</code></pre> <p>This is a proxy for grounded usefulness, not proof. The restricted model may guess incorrectly despite already knowing the fact, and the informed model may match for the wrong reason.</p>"},{"location":"learn/research-labs/02-autodata/#recipe-rewriting-and-lineage","title":"Recipe rewriting and lineage","text":"<p>The rewriter sees:</p> <ul> <li>the seed recipe;</li> <li>the complete generated item;</li> <li>both solver answers or failures;</li> <li>the acceptance decision and rejection reasons.</li> </ul> <p>It is told to generalize rather than copy the generated question or answer. Its structured output is simply:</p> <pre><code>{\"recipe\": \"The complete successor generation recipe.\"}\n</code></pre> <p>The result retains both recipes, both generated examples, both solver pairs, both acceptance reports, evaluator identity, typed failures, and aggregate usage. That makes the causal story inspectable:</p> <pre><code>seed recipe + seed rejection evidence \u2500\u2500\u25ba successor recipe\n</code></pre>"},{"location":"learn/research-labs/02-autodata/#minimal-runnable-mechanism","title":"Minimal runnable mechanism","text":"<pre><code>from sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.autodata import run\n\nresult = run(UserSdkSessionRunner(agent_factory=build_agent))\n\nprint(\"seed recipe:\", result.artifacts[\"seed_recipe\"])\nprint(\"successor recipe:\", result.artifacts.get(\"successor_recipe\"))\nprint(\"evidence:\", result.artifacts[\"evidence\"])\nprint(\"measurements:\", result.measurements)\nprint(\"usage:\", result.usage)\n</code></pre> Illustrative shape \u2014 live run, nothing captured<pre><code>seed recipe: &lt;original generation instructions&gt;\nsuccessor recipe: &lt;evidence-guided rewritten instructions&gt;\nevidence: &lt;generated items, solver answers, decisions, and rejection reasons&gt;\nmeasurements: &lt;seed and successor acceptance metrics or typed failures&gt;\nusage: &lt;aggregated provider usage&gt;\n</code></pre> <p>Run it with:</p> <pre><code>META_EVOLVE_LIVE_SDK=1 uv run python your_sealqa_driver.py autodata\n</code></pre>"},{"location":"learn/research-labs/02-autodata/#how-to-read-the-result","title":"How to read the result","text":"<p>Compare the two acceptance reports rather than looking only at the final flag:</p> Seed Successor Interpretation rejected accepted The rewrite fixed the observed gate on this one attempt. rejected rejected Inspect whether the reason changed; the rewrite may still have altered behavior. accepted accepted There may have been no useful error signal for rewriting. accepted rejected The rewrite regressed the recipe. failure any The failed arm is unrankable and should not be treated as rejection. <p>One generated item per recipe is a mechanism demonstration. It does not estimate a stable acceptance rate, diversity, or downstream training value.</p>"},{"location":"learn/research-labs/02-autodata/#leakage-and-comparison-checklist","title":"Leakage and comparison checklist","text":"<ul> <li>The restricted workspace contains only <code>question.md</code>.</li> <li>The informed workspace adds <code>evidence.md</code>; model and bounds stay identical.</li> <li>Neither solver receives the frozen row's hidden answer or URLs.</li> <li>The generator may see public document text but no gold label.</li> <li>The rewriter sees generated evidence, not the held-out behavior of future   generated items.</li> <li>Same-model/different-context results are not described as weak-vs-strong   model evidence.</li> </ul>"},{"location":"learn/research-labs/02-autodata/#change-and-predict","title":"Change and predict","text":"<p>Modify the recipe to require two independent evidence document IDs. Predict:</p> <ol> <li>whether unknown-document rejection becomes more common;</li> <li>whether the informed solver becomes more reliable;</li> <li>whether leakage changes at all;</li> <li>what a rejected successor would teach you about the recipe rather than the    model.</li> </ol>"},{"location":"learn/research-labs/03-meta-harness-precursor/","title":"Meta-Harness precursor: improve a bounded local harness","text":"<p>Status: Experimental/live-only.</p> <p>Optional executable companion: open the canonical notebook. The notebook is not required to follow this chapter.</p> <p>Superseded teaching precursor</p> <p>This historical local lab is not the M8 API. Its claims remain scoped to the precursor that ran them. For typed harness artifacts, trusted mutation validation, portable episodes, comparison, and export, use the M8 harness guide.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#the-concrete-problems","title":"The concrete problems","text":"<p>The diagnostic row asks:</p> <p>How many NBA players have scored 60 or more points in a regular season game since 2023?</p> <p>The mutator may inspect the baseline's attempt on that row. It does not see the unseen validation row, whose question asks how many current and former Real Madrid players appear in the latest Forbes top-ten highest-paid-athletes list.</p> <p>The experiment asks whether changing a planner and context policy after one diagnostic failure helps on that separate validation case.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#what-harness-means-here","title":"What \u201charness\u201d means here","text":"<p>The local harness is an ordinary shipped <code>meta.SourceTree</code> containing exactly two files:</p> <pre><code>planner.md\ncontext_policy.json\n</code></pre> <p>The baseline is conceptually:</p> <pre><code>planner.md\n  Answer from supplied evidence. Prefer recent documents.\n  Return only the short answer.\n\ncontext_policy.json\n  {\"max_documents\": 5, \"order\": \"given\"}\n</code></pre> <p><code>planner.md</code> controls how the solver reasons and formats its reply. <code>context_policy.json</code> controls how many documents enter the solver workspace. The order is fixed to <code>given</code> in this precursor.</p> <p>This is intentionally smaller than a production harness. There are no new runtime types, portable manifests, automatic replay, or promotion records.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#from-meta-evolve-principles-to-this-algorithm","title":"From Meta-Evolve principles to this algorithm","text":"<p>This lab widens the artifact from one prompt to two coordinated files while keeping every control outside that tree:</p> Role Concrete component Boundary it demonstrates Mutable artifact Ordinary immutable <code>meta.SourceTree</code> Planner and context policy can change together Diagnostic evaluator Baseline solver plus local exact grade Produces evidence about current harness behavior Proposer Mutator session over read-only prior source/evidence Suggests file replacements but cannot apply arbitrary paths Mutation guard <code>apply_mutation()</code> Enforces allowlist and JSON semantics before evaluation Validation evaluator Same unseen row for baseline and candidate Measures the intended harness difference fairly Comparison <code>compare()</code> over two <code>Measurement</code>s Reports improvement, tie, regression, or unrankable failure Promotion Constant <code>False</code> outside the candidate A better score does not deploy anything <p>The research algorithm is therefore only this composition:</p> <pre><code>observe baseline on development evidence\n  \u2192 propose a bounded source change\n  \u2192 reject changes outside authority\n  \u2192 evaluate parent and successor on untouched validation\n  \u2192 report comparison without promotion\n</code></pre> <p>Meta-Evolve already provides the versioned tree, session failures, evidence, usage, and evaluator identity. The precursor adds no coordinator behavior. Its domain code is a two-path allowlist, one JSON validator, document slicing, and an equal-arm comparison.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#how-the-two-files-affect-solver-execution","title":"How the two files affect solver execution","text":"<p><code>_solve()</code> is the only function that interprets the harness. It parses <code>context_policy.json</code>, takes the first <code>max_documents</code> in fixed <code>given</code> order, renders them into <code>validation.md</code>, and uses <code>planner.md</code> as the solver instructions:</p> <pre><code>SourceTree.context_policy.json \u2500\u2500\u25ba number of visible documents\nSourceTree.planner.md          \u2500\u2500\u25ba reasoning and answer instructions\npublic SolverCase             \u2500\u2500\u25ba validation.md\n</code></pre> <p>This means a mutation has two understandable levers: change how the model is instructed, change how much evidence it receives, or change both. It cannot add a tool, swap a model, reorder documents, alter the grader, or spend more sessions because none of those controls are interpreted from the artifact.</p> <p><code>_mutation_request()</code> materializes prior files under <code>prior/</code>, diagnostic measurement/answer/trace under <code>evidence/</code>, and the lifecycle warning under <code>controls/</code>. The paths are read-only evidence in the mutator's workspace. The returned <code>files</code> object is not trusted as a patch until <code>apply_mutation()</code> checks paths, UTF-8 strings, non-identity, and policy JSON.</p> <p>Only after that guard returns a new immutable tree are the two validation sessions authorized. Invalid mutation therefore cannot consume held-out evaluation or leave a partial successor.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#walkthrough-one-improvement-cycle-in-four-phases","title":"Walkthrough: one improvement cycle in four phases","text":"<p>These are outer orchestration phases, not repeated search iterations. Each solver or mutator session is an independent, one-trial inner <code>meta.run</code>.</p> Phase Meta-Evolve components State entering the phase State leaving the phase 1. Diagnose parent Baseline <code>SourceTree</code>; diagnostic solver request; local exact grader Baseline planner/policy and the public diagnostic case Baseline <code>SessionRecord</code>, diagnostic measurement, answer, and trace kinds 2. Propose and guard mutation Mutator request; <code>apply_mutation()</code> authority check Read-only prior source and diagnostic evidence; no validation row Candidate <code>SourceTree</code>, or a typed failure with no successor 3. Validate equal arms Two solver requests using the same unseen public row Baseline and candidate trees under identical model, schema, grader, and budgets Baseline and candidate validation measurements 4. Compare and retain Evaluator-owned <code>compare()</code>; <code>LabResult</code> Both rankable measurements or a typed failure Improvement/tie/regression/failure label, artifacts, evidence, usage, and <code>automatic_promotion=False</code> <p>The bounded state transition is:</p> <pre><code>SourceTree(baseline)\n  \u2500\u2500 diagnostic evaluator \u2500\u2500\u25ba measurement + trace evidence\n  \u2500\u2500 mutator SessionRequest \u2500\u2500\u25ba proposed file replacements\n  \u2500\u2500 apply_mutation allowlist \u2500\u2500\u25ba SourceTree(candidate)\n  \u2500\u2500 equal unseen validation \u2500\u2500\u25ba comparison\n  \u2500\u2500 retain without promotion \u2500\u2500\u25ba LabResult\n</code></pre> <p>The mutation proposal is not itself an artifact. Outer Python first validates its paths and contents, then creates an immutable successor tree. This is the point where Meta-Evolve's proposal/promotion separation is concrete: the model suggests files, the experiment admits or rejects them, the evaluator compares them, and no component deploys the result.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#the-four-sessions","title":"The four sessions","text":"<pre><code>baseline harness \u2500\u2500\u25ba diagnostic attempt\n       \u2502                    \u2502\n       \u2502                    \u25bc\n       \u2502            source + score + trace\n       \u2502                    \u2502\n       \u2502                    \u25bc\n       \u2502              mutation session\n       \u2502                    \u2502\n       \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     \u25bc\n       \u25bc              \u25bc  candidate harness\nbaseline validation  candidate validation\n       \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                      \u25bc\n          equal-declaration comparison\n</code></pre> # Session Candidate-visible inputs Output 1 Baseline diagnostic Baseline harness and diagnostic public case Structured answer, trace, usage or failure 2 Harness mutator Complete prior source and diagnostic record Proposed file edits 3 Baseline validation Baseline harness and unseen public validation case Structured answer 4 Candidate validation Candidate harness and the same validation case Structured answer <p>The mutator's read-only workspace materializes:</p> <ul> <li>the complete prior <code>planner.md</code> and <code>context_policy.json</code>;</li> <li>the diagnostic exact score;</li> <li>the baseline's candidate answer or typed failure;</li> <li>recorded tool-trace kinds;</li> <li>the lifecycle and authority warning.</li> </ul> <p>It does not contain the validation case, evaluator answer, permission state, budget controls, audit history, or promotion decision.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#mutation-authority","title":"Mutation authority","text":"<p>The structured mutation shape is:</p> <pre><code>{\n  \"files\": {\n    \"planner.md\": \"Complete replacement planner text\",\n    \"context_policy.json\": \"{\\\"max_documents\\\": 10, \\\"order\\\": \\\"given\\\"}\"\n  }\n}\n</code></pre> <p>The local validator applies an exact allowlist:</p> <pre><code>allowed\n  planner.md\n  context_policy.json\n\nprotected examples\n  evaluation_data.json\n  permissions.json\n  budgets.json\n  audit_history.jsonl\n  evidence.jsonl\n  promotion.json\n</code></pre> <p>Any path outside the two-file allowlist is a typed <code>PolicyViolation</code>. Protected controls are named in its evidence, and no successor <code>SourceTree</code> is created. Invalid UTF-8 values or malformed context-policy JSON are typed <code>InvalidOutput</code>. Empty or unchanged edits are typed <code>ProposerFailure</code>.</p> <p>The policy JSON must contain a positive integer <code>max_documents</code> and <code>\"order\": \"given\"</code>. This validates the candidate before any held-out provider session is spent.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#equal-validation","title":"Equal validation","text":"<p>Baseline and candidate are evaluated on the same unseen row with:</p> <ul> <li>the same declared user-owned SDK wrapper;</li> <li>the same answer schema and normalized exact grader;</li> <li>the same turn, timeout, and wall declarations;</li> <li>the same public document order;</li> <li>the same fixture-row checksum in evaluator identity.</li> </ul> <p>The harness artifact is the intended difference. A comparison result is one of:</p> Result Meaning <code>improvement</code> Candidate exact score is higher. <code>tie</code> Both valid arms have the same exact score. <code>regression</code> Candidate exact score is lower. <code>unrankable_typed_failure</code> At least one arm produced no valid metric. <p>The result also states <code>automatic_promotion = False</code>. Search or comparison may identify a better local artifact; neither the candidate nor this evaluator may deploy it.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#minimal-runnable-mechanism","title":"Minimal runnable mechanism","text":"<pre><code>from sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.meta_harness import run\n\nresult = run(UserSdkSessionRunner(agent_factory=build_agent))\n\nprint(\"baseline:\", result.artifacts[\"baseline\"])\nprint(\"candidate:\", result.artifacts.get(\"candidate\"))\nprint(\"comparison:\", result.artifacts.get(\"comparison\"))\nprint(\"automatic promotion:\", result.artifacts.get(\"automatic_promotion\", False))\nprint(\"measurements:\", result.measurements)\nprint(\"usage:\", result.usage)\n</code></pre> Illustrative shape \u2014 live run, nothing captured<pre><code>baseline: &lt;planner and context-policy SourceTree&gt;\ncandidate: &lt;allowlisted successor SourceTree or None&gt;\ncomparison: &lt;improvement, tie, regression, or unrankable typed failure&gt;\nautomatic promotion: False\nmeasurements: &lt;equal-declaration validation measurements&gt;\nusage: &lt;aggregated provider usage&gt;\n</code></pre> <p>Run it with:</p> <pre><code>META_EVOLVE_LIVE_SDK=1 uv run python your_sealqa_driver.py meta_harness\n</code></pre> <p>If mutation is invalid, only two sessions are needed: the diagnostic and mutator. The baseline/candidate validation pair is skipped because there is no admissible successor to compare.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#how-to-interpret-a-result","title":"How to interpret a result","text":"<p>An improvement means only that one diagnostic-informed mutation performed better on one unseen row. It does not establish that:</p> <ul> <li>the planner change rather than the context-size change caused the gain;</li> <li>the candidate generalizes to a broader repository task distribution;</li> <li>the extra context is cost-effective;</li> <li>the harness should be promoted;</li> <li>the experiment can be durably replayed.</li> </ul> <p>A tie can still reveal that the mutation changed cost, document visibility, or failure behavior without changing exact accuracy. A regression is valuable evidence that the diagnostic adaptation overfit or removed useful context.</p>"},{"location":"learn/research-labs/03-meta-harness-precursor/#authority-and-validity-checklist","title":"Authority and validity checklist","text":"<ul> <li>Did the candidate change only the two permitted paths?</li> <li>Was a forbidden-control proposal rejected without a successor?</li> <li>Did the mutator see complete diagnostic evidence but no validation row?</li> <li>Did both validation arms use identical declarations and document order?</li> <li>Are failures reported by kind rather than converted to zero?</li> <li>Is promotion still explicitly false?</li> <li>Is this chapter still labeled a superseded precursor rather than current API?</li> </ul>"},{"location":"learn/research-labs/03-meta-harness-precursor/#change-and-predict","title":"Change and predict","text":"<p>Lower <code>max_documents</code> from 5 to 2 without changing the planner. Before running, predict:</p> <ol> <li>whether the useful document remains visible;</li> <li>whether cost or usage should change;</li> <li>whether the candidate will improve, tie, regress, or fail;</li> <li>why even an improvement would not authorize promotion.</li> </ol>"},{"location":"learn/research-labs/04-evoskill/","title":"EvoSkill: induce a reusable skill from recorded failures","text":"<p>Status: Experimental/live-only.</p> <p>Optional executable companion: open the canonical notebook. This chapter is complete without it.</p>"},{"location":"learn/research-labs/04-evoskill/#the-concrete-problem","title":"The concrete problem","text":"<p>One held-out question asks:</p> <p>How many airlines are members of IATA, the organization that supports airline activity and helps formulate industry policy and standards?</p> <p>The skill builder never sees that row. It receives experience from two separate induction rows, then the unchanged baseline and skill-augmented agents are compared on this and one other held-out row.</p> <p>The artifact being improved is a shipped <code>meta.SkillSet</code>: an immutable mapping from a path such as <code>skills/sealqa.md</code> to one inspectable Markdown document.</p>"},{"location":"learn/research-labs/04-evoskill/#the-idea","title":"The idea","text":"<p>An EvoSkill-style loop turns concrete agent failures into reusable procedures:</p> <pre><code>induction case 1 \u2500\u2500\u25ba recorded failure \u2500\u2500\u2510\n                                        \u251c\u2500\u25ba failure analysis \u2500\u2500\u25ba Markdown skill\ninduction case 2 \u2500\u2500\u25ba recorded failure \u2500\u2500\u2518                           \u2502\n                                                                    \u25bc\nheld-out case 1 \u2500\u2500\u25ba baseline vs skill-augmented comparison          \u2502\nheld-out case 2 \u2500\u2500\u25ba baseline vs skill-augmented comparison \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>This lab separates three questions that are often blurred together:</p> <ol> <li>Did the baseline agent fail on induction cases?</li> <li>Did an analyzer actually read and generalize from those failures?</li> <li>Did the resulting skill help unchanged agents on held-out cases?</li> </ol>"},{"location":"learn/research-labs/04-evoskill/#from-meta-evolve-principles-to-this-algorithm","title":"From Meta-Evolve principles to this algorithm","text":"<p>EvoSkill becomes a short composition once failure collection, proposal, and held-out evaluation are kept as different components:</p> Role Concrete component Why it exists Episode runner <code>_solve()</code> using the common bounded session adapter Produces comparable answers or typed failures Evaluator Local <code>grade_record()</code> Keeps expected answers outside agent authority Experience Two <code>EvidenceDraft</code> induction records Makes prior failure observable without leaking gold answers Analyzer Pull-enabled session Chooses which prior records to inspect and generalizes patterns Proposer/builder Separate schema-constrained session Converts analysis into an inspectable skill artifact Mutable artifact Shipped immutable <code>meta.SkillSet</code> Versions the reusable procedure as ordinary Markdown Validation Two baseline/skill held-out pairs Tests transfer without reusing induction cases <p>The \u201calgorithm\u201d is not hidden inside one giant prompt:</p> <pre><code>run baseline episodes\n  \u2192 grade externally\n  \u2192 expose bounded failure evidence\n  \u2192 analyze through audited experience pull\n  \u2192 build a typed skill artifact\n  \u2192 inject only that artifact into fresh held-out agents\n  \u2192 compare each pair externally\n</code></pre> <p>This is simple to build because Meta-Evolve already supplies <code>SkillSet</code>, experience grants, derivation, session accounting, typed failure, and immutable artifacts. The lab-specific code decides what a safe failure record contains and how the Markdown is inserted into the held-out instruction block.</p>"},{"location":"learn/research-labs/04-evoskill/#how-records-become-a-skill-without-leaking-evaluation","title":"How records become a skill without leaking evaluation","text":"<p>For each induction row, <code>_solve()</code> receives only its public case. Local grading then creates a <code>Measurement</code>. <code>_failure_evidence()</code> deliberately projects that full evaluator state into a smaller candidate-visible record:</p> <pre><code>included: case_id, prediction, exact-if-valid, failure_kind\nomitted:  expected answer, URLs, gold document, held-out content\n</code></pre> <p>Those two records become <code>seed_evidence</code> in the analyzer's inner run. Because the analyzer also receives <code>PullAccess</code>, it must actively search/open them; the run records whether it did. <code>ANALYSIS_SCHEMA</code> forces separate <code>failure_patterns</code> and <code>procedures</code>, so the next step receives structured generalization rather than raw hidden evaluator state.</p> <p>The builder is a new session with only <code>analysis.json</code>. It cannot query the induction experience or inspect held-out rows. Its validated Markdown is lifted into:</p> <pre><code>meta.SkillSet({\"skills/sealqa.md\": skill_markdown})\n</code></pre> <p>For held-out evaluation, <code>_solve()</code> is reused unchanged. The skill arm differs only by appending the rendered skill to <code>BASE_INSTRUCTIONS</code>. This makes the skill artifact\u2014not a model, case, schema, or budget change\u2014the intended cause of any measured difference.</p>"},{"location":"learn/research-labs/04-evoskill/#walkthrough-one-improvement-cycle-in-four-phases","title":"Walkthrough: one improvement cycle in four phases","text":"<p>The four phases describe the outer lab algorithm. The eight SDK sessions inside them are independent inner <code>meta.run</code> calls with one trial each, not eight optimizer iterations.</p> Phase Meta-Evolve components Candidate-visible observation Retained evaluator result 1. Record induction behavior Baseline solver requests; local grader; two <code>EvidenceDraft</code>s Each solver sees only its public induction case Predictions, exact-if-valid, and failure kinds; expected answers stay private 2. Analyze bounded experience Pull-enabled analyzer request with <code>meta.PullAccess</code> Only the two projected failure records, read through audited pull operations Failure patterns, procedures, pull counters, or a typed failure 3. Build successor skill Builder request; immutable <code>meta.SkillSet</code> <code>analysis.json</code> only; no raw experience or held-out rows One validated Markdown skill at <code>skills/sealqa.md</code> 4. Compare on held-out rows Four fresh solver requests; reused grader; per-row comparison; <code>LabResult</code> Baseline sees base instructions; skill arm adds only the rendered skill Two independent comparison labels plus sessions, measurements, lineage, experience usage, and provider usage <p>The artifact and evidence flow is:</p> <pre><code>baseline induction SessionRecords\n  \u2500\u2500 evaluator projection \u2500\u2500\u25ba two EvidenceDraft failure records\n  \u2500\u2500 PullAccess analyzer \u2500\u2500\u25ba structured patterns and procedures\n  \u2500\u2500 builder SessionRequest \u2500\u2500\u25ba SkillSet(skills/sealqa.md)\n  \u2500\u2500 two equal held-out pairs \u2500\u2500\u25ba two external comparisons\n  \u2500\u2500 retain \u2500\u2500\u25ba LabResult\n</code></pre> <p>There is no mutable skill before phase 3. The analyzer proposes an explanation, the builder proposes Markdown, schema checks lift that Markdown into the versioned artifact, and only fresh held-out evaluations measure it. If analysis or building fails, the lab retains the typed failure and does not spend the held-out sessions.</p>"},{"location":"learn/research-labs/04-evoskill/#the-eight-sessions","title":"The eight sessions","text":"# Session Purpose 1 Induction episode 1 Run the unchanged baseline on frozen row 5. 2 Induction episode 2 Run the unchanged baseline on frozen row 6. 3 Failure analyzer Pull both recorded failure records and propose patterns/procedures. 4 Skill builder Convert the analysis into one Markdown skill with checks. 5 Held-out 1 baseline Run unchanged instructions on frozen row 7. 6 Held-out 1 skill Run identical instructions plus the skill on row 7. 7 Held-out 2 baseline Run unchanged instructions on frozen row 8. 8 Held-out 2 skill Run identical instructions plus the skill on row 8. <p>There is no skill evaluation on induction rows and no skill construction from held-out rows. That isolation prevents the builder from simply encoding the answers on which the skill is judged.</p>"},{"location":"learn/research-labs/04-evoskill/#what-an-induction-failure-contains","title":"What an induction failure contains","text":"<p>Each completed induction episode is graded locally. The experience record contains:</p> <pre><code>{\n  \"case_id\": \"longseal-004\",\n  \"prediction\": \"the agent's answer, if any\",\n  \"exact\": 0.0,\n  \"failure_kind\": null\n}\n</code></pre> <p>If the provider or adapter fails, <code>exact</code> is absent and <code>failure_kind</code> names the typed outcome. The record deliberately omits:</p> <ul> <li>the evaluator's expected answer;</li> <li>source or gold URLs;</li> <li>the gold-document label;</li> <li>held-out row contents.</li> </ul> <p>This makes the failure complete as an observation of what the agent did without turning evaluator secrets into proposer-visible experience.</p>"},{"location":"learn/research-labs/04-evoskill/#shipped-experience-pull-seam","title":"Shipped experience-pull seam","text":"<p>The analyzer is not handed a manually concatenated failure report. The two failure records are committed as seed evaluation evidence in an ordinary inner run, and the analyzer receives bounded <code>meta.PullAccess</code>:</p> <pre><code>meta.PullAccess(\n    max_operations=20,\n    max_results=20,\n    max_records=100,\n    max_chars=32_000,\n)\n</code></pre> <p>A user-owned SDK custom tool can search and open those strictly prior records. The proposal must declare which experience it derived from. Pull usage is retained separately from provider token/wall usage.</p> <p>That distinction yields two different null results:</p> <pre><code>skill did not help, analyzer used experience\nskill did not help, analyzer never read the offered experience\n</code></pre> <p>The second says nothing about whether failure-driven induction helps; it says the configured agent did not exercise the mechanism.</p>"},{"location":"learn/research-labs/04-evoskill/#analyzer-and-skill-contracts","title":"Analyzer and skill contracts","text":"<p>The analyzer returns:</p> <pre><code>{\n  \"failure_patterns\": [\"General pattern observed across failures\"],\n  \"procedures\": [\"Reusable action an agent should take\"]\n}\n</code></pre> <p>The separate builder sees this analysis\u2014not the held-out cases\u2014and returns:</p> <pre><code>{\n  \"skill_markdown\": \"# Skill title\\n\\nProcedure...\\n\\n## Verification\\n- Check...\"\n}\n</code></pre> <p>The emitted <code>meta.SkillSet</code> contains one path:</p> <pre><code>skills/sealqa.md\n</code></pre> <p>Keeping it as Markdown makes the learned procedure directly inspectable. The builder instruction requires explicit verification checks so the skill is more than generic advice such as \u201cbe careful.\u201d</p>"},{"location":"learn/research-labs/04-evoskill/#held-out-comparison","title":"Held-out comparison","text":"<p>For each held-out row, two fresh sessions receive the same public question, documents, model, answer schema, turn/timeout/wall bounds, and exact grader. Only the instruction block differs:</p> <pre><code>baseline arm\n  base solver instructions\n\nskill arm\n  base solver instructions\n  + rendered skills/sealqa.md\n</code></pre> <p>Each pair is classified independently:</p> Result Meaning <code>improvement</code> Skill exact score is higher. <code>tie</code> Both valid arms have the same score. <code>regression</code> Skill exact score is lower. <code>unrankable_typed_failure</code> At least one arm has no metric. <p>The report retains both results. It does not average a provider failure into a low score or hide a regression behind the other row's improvement.</p>"},{"location":"learn/research-labs/04-evoskill/#minimal-runnable-mechanism","title":"Minimal runnable mechanism","text":"<pre><code>from sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.evoskill import run\n\nresult = run(UserSdkSessionRunner(agent_factory=build_agent))\n\nprint(\"analysis:\", result.artifacts.get(\"analysis\"))\nprint(\"skill set:\", result.artifacts.get(\"skill_set\"))\nprint(\"experience usage:\", result.artifacts.get(\"experience_usage\"))\nprint(\"held-out comparisons:\", result.artifacts.get(\"held_out_comparisons\"))\nprint(\"measurements:\", result.measurements)\nprint(\"usage:\", result.usage)\n</code></pre> Illustrative shape \u2014 live run, nothing captured<pre><code>analysis: &lt;failure patterns and procedures&gt;\nskill set: &lt;one inspectable Markdown skill&gt;\nexperience usage: &lt;operations, records, characters, and unused evidence&gt;\nheld-out comparisons: &lt;two improvement, tie, regression, or failure labels&gt;\nmeasurements: &lt;induction and held-out measurements&gt;\nusage: &lt;aggregated provider usage&gt;\n</code></pre> <p>Run it with:</p> <pre><code>META_EVOLVE_LIVE_SDK=1 uv run python your_sealqa_driver.py evoskill\n</code></pre> <p>If analysis or building fails, held-out sessions are skipped: there is no valid skill artifact to compare. Eight is the maximum complete path.</p>"},{"location":"learn/research-labs/04-evoskill/#how-to-interpret-the-result","title":"How to interpret the result","text":"<p>Read these facts separately:</p> <ol> <li>Were the induction episodes valid wrong answers or typed failures?</li> <li>How many experience operations, records, and characters did the analyzer use?</li> <li>Are the reported failure patterns supported by both episodes?</li> <li>Does the Markdown skill contain concrete procedures and verification checks?</li> <li>What happened on each held-out pair\u2014improvement, tie, regression, or failure?</li> <li>Did skill augmentation change provider usage as well as correctness?</li> </ol> <p>Two held-out examples demonstrate the comparison mechanism; they do not support a general claim that the skill transfers across SealQA or other task families.</p>"},{"location":"learn/research-labs/04-evoskill/#isolation-and-validity-checklist","title":"Isolation and validity checklist","text":"<ul> <li>Rows 5\u20136 appear only in induction and failure evidence.</li> <li>Rows 7\u20138 appear only in the four held-out sessions.</li> <li>Failure evidence omits reference answers, URLs, and split labels.</li> <li>Analyzer and builder are separate provider sessions.</li> <li>Pull counters show whether offered experience was actually used.</li> <li>Baseline and skill arms otherwise share compatible declarations.</li> <li>Ties, regressions, failures, and unused experience remain visible.</li> </ul>"},{"location":"learn/research-labs/04-evoskill/#change-and-predict","title":"Change and predict","text":"<p>Remove the requirement for verification checks from the skill-builder instruction. Before running, predict:</p> <ol> <li>whether the skill becomes shorter but less actionable;</li> <li>whether analyzer pull usage should change;</li> <li>which held-out arm is most likely to tie or regress;</li> <li>what evidence would distinguish a bad skill from unused experience.</li> </ol>"},{"location":"notebooks/02_policy_comparison/milestone_2_tour/","title":"Milestone 2 tour: one loop, replaceable search","text":"In\u00a0[1]: Copied! <pre>import meta_evolve as meta\nfrom meta_evolve import domain\n\nTARGET = 12\nMOVES = (-2, 1, 5)\nBUDGET = meta.Budget(evaluations=9, trials=8)\n\ndef propose(parent, context):\n    return parent + context.rng.choice(MOVES)\n\ndef evaluate(candidate):\n    return -abs(candidate - TARGET)\n\ntask = meta.Task(\n    evaluator=evaluate,\n    objectives=(meta.Maximize(\"score\", satisfy=0),),\n    budget=BUDGET,\n)\npolicies = {\n    \"Greedy\": meta.Greedy(max_trials=8),\n    \"BestOfN\": meta.BestOfN(max_trials=8),\n    \"Population\": meta.Population(size=3, tournament_size=2, max_trials=8),\n}\nstores = {name: meta.Storage.in_memory() for name in policies}\nruns = {\n    name: meta.run(\n        meta.Experiment(task=task, seed=0, proposer=propose, search=policy,\n                        random_seed=1), storage=stores[name]\n    )\n    for name, policy in policies.items()\n}\nprint(\"runs ready:\", tuple(runs))\n</pre> import meta_evolve as meta from meta_evolve import domain  TARGET = 12 MOVES = (-2, 1, 5) BUDGET = meta.Budget(evaluations=9, trials=8)  def propose(parent, context):     return parent + context.rng.choice(MOVES)  def evaluate(candidate):     return -abs(candidate - TARGET)  task = meta.Task(     evaluator=evaluate,     objectives=(meta.Maximize(\"score\", satisfy=0),),     budget=BUDGET, ) policies = {     \"Greedy\": meta.Greedy(max_trials=8),     \"BestOfN\": meta.BestOfN(max_trials=8),     \"Population\": meta.Population(size=3, tournament_size=2, max_trials=8), } stores = {name: meta.Storage.in_memory() for name in policies} runs = {     name: meta.run(         meta.Experiment(task=task, seed=0, proposer=propose, search=policy,                         random_seed=1), storage=stores[name]     )     for name, policy in policies.items() } print(\"runs ready:\", tuple(runs)) <pre>runs ready: ('Greedy', 'BestOfN', 'Population')\n</pre> In\u00a0[2]: Copied! <pre>for name, result in runs.items():\n    summary = result.summary()\n    print(\n        name,\n        f\"score={summary.primary_score}\",\n        f\"usage={summary.usage}\",\n        f\"lineage={list(summary.winning_lineage)}\",\n    )\n</pre> for name, result in runs.items():     summary = result.summary()     print(         name,         f\"score={summary.primary_score}\",         f\"usage={summary.usage}\",         f\"lineage={list(summary.winning_lineage)}\",     ) <pre>Greedy score=0 usage=Usage(evaluations=6, trials=5, tokens=0, wall_seconds=0.0) lineage=[0, 5, 10, 11, 12]\nBestOfN score=-7 usage=Usage(evaluations=9, trials=8, tokens=0, wall_seconds=0.0) lineage=[0, 5]\nPopulation score=-2 usage=Usage(evaluations=9, trials=8, tokens=0, wall_seconds=0.0) lineage=[0, 1, 2, 7, 8, 9, 14]\n</pre> In\u00a0[3]: Copied! <pre>result = runs[\"Greedy\"]\nsummary = result.summary()\nprint(\"best:\", result.best())\nprint(\"primary score:\", summary.primary_score)\nprint(\"winning lineage:\", list(summary.winning_lineage))\nprint(\"seed metrics:\", dict(result.trials()[0].metrics))\n</pre> result = runs[\"Greedy\"] summary = result.summary() print(\"best:\", result.best()) print(\"primary score:\", summary.primary_score) print(\"winning lineage:\", list(summary.winning_lineage)) print(\"seed metrics:\", dict(result.trials()[0].metrics)) <pre>best: 12\nprimary score: 0\nwinning lineage: [0, 5, 10, 11, 12]\nseed metrics: {'score': -12}\n</pre> <p>Greedy expands the current best artifact, producing a chain. BestOfN always expands the initial artifact, producing independent spokes of a star. Population retains a rank-ordered top-k archive and expands deterministic tournament winners, producing a tree. These policies change parent selection and retention, not the coordinator or evaluator.</p> In\u00a0[4]: Copied! <pre>for other_name in (\"BestOfN\", \"Population\"):\n    comparison = runs[\"Greedy\"].compare(runs[other_name])\n    winner = comparison.winner.policy.name if comparison.winner else \"tie\"\n    print(\n        f\"Greedy vs {other_name}: winner={winner}, \"\n        f\"same_random_seed={comparison.same_random_seed}\",\n    )\n</pre> for other_name in (\"BestOfN\", \"Population\"):     comparison = runs[\"Greedy\"].compare(runs[other_name])     winner = comparison.winner.policy.name if comparison.winner else \"tie\"     print(         f\"Greedy vs {other_name}: winner={winner}, \"         f\"same_random_seed={comparison.same_random_seed}\",     ) <pre>Greedy vs BestOfN: winner=greedy, same_random_seed=True\nGreedy vs Population: winner=greedy, same_random_seed=True\n</pre> In\u00a0[5]: Copied! <pre>from collections import Counter\n\nfor name, result in runs.items():\n    projection = result._projection()\n    kinds = Counter(type(item.decision).__name__ for item in projection.decisions)\n    declaration = projection.start.search.policy\n    print(\n        name,\n        f\"policy={declaration.name}@{declaration.version}\",\n        f\"decisions={dict(kinds)}\",\n        f\"explanations={len(projection.explanations)}\",\n    )\n</pre> from collections import Counter  for name, result in runs.items():     projection = result._projection()     kinds = Counter(type(item.decision).__name__ for item in projection.decisions)     declaration = projection.start.search.policy     print(         name,         f\"policy={declaration.name}@{declaration.version}\",         f\"decisions={dict(kinds)}\",         f\"explanations={len(projection.explanations)}\",     ) <pre>Greedy policy=greedy@1 decisions={'TrialSpec': 5, 'Stop': 1} explanations=6\nBestOfN policy=best-of-n@1 decisions={'TrialSpec': 8, 'Stop': 1} explanations=9\nPopulation policy=population@1 decisions={'ArchiveUpdate': 9, 'TrialSpec': 8, 'Stop': 1} explanations=18\n</pre> In\u00a0[6]: Copied! <pre>population = runs[\"Population\"]._projection()\nprint(\"Population archive members:\", len(population.search_state.archive_members))\nprint(\"First three explanations:\")\nfor explanation in population.explanations[:3]:\n    print(\"-\", explanation.rationale)\n</pre> population = runs[\"Population\"]._projection() print(\"Population archive members:\", len(population.search_state.archive_members)) print(\"First three explanations:\") for explanation in population.explanations[:3]:     print(\"-\", explanation.rationale) <pre>Population archive members: 3\nFirst three explanations:\n- admit candidate at population rank 1 of 1\n- expand tournament winner at population rank 1 from 1 sampled members\n- admit candidate at population rank 1 of 2\n</pre> In\u00a0[7]: Copied! <pre>from meta_evolve.application import replay_run_projection\n\nresult = runs[\"Population\"]\nevents = stores[\"Population\"].events.read(result.id)\nrebuilt = replay_run_projection(result.id, events)\nprint(\"retained events:\", len(events))\nprint(\"projection reproduced:\", rebuilt == result._projection())\nprint(\"usage reproduced:\", rebuilt.search_state.usage)\n</pre> from meta_evolve.application import replay_run_projection  result = runs[\"Population\"] events = stores[\"Population\"].events.read(result.id) rebuilt = replay_run_projection(result.id, events) print(\"retained events:\", len(events)) print(\"projection reproduced:\", rebuilt == result._projection()) print(\"usage reproduced:\", rebuilt.search_state.usage) <pre>retained events: 73\nprojection reproduced: True\nusage reproduced: Usage(evaluations=9, trials=8, tokens=0, wall_seconds=0.0)\n</pre>"},{"location":"notebooks/02_policy_comparison/milestone_2_tour/#milestone-2-tour-one-loop-replaceable-search","title":"Milestone 2 tour: one loop, replaceable search\u00b6","text":"<p>Milestone 2 keeps task execution fixed while making search replaceable and results fairly comparable. M2.1 introduced replayable archive decisions, linked decision explanations, and a shared policy conformance contract. M2.2 added independent BestOfN. M2.3 added a retained Population with deterministic tournament selection. M2.4 added immutable run summaries, strict comparison, and the canonical policy benchmark.</p>"},{"location":"notebooks/02_policy_comparison/milestone_2_tour/#1-keep-the-task-fixed-and-swap-only-search","title":"1. Keep the task fixed and swap only search\u00b6","text":"<p>The seed is evaluated once. <code>max_trials</code> counts successor proposals, while the task budget counts all evaluations including that seed.</p>"},{"location":"notebooks/02_policy_comparison/milestone_2_tour/#2-compare-only-compatible-runs","title":"2. Compare only compatible runs\u00b6","text":"<p><code>Run.compare()</code> rejects mismatched evaluator/proposer declarations, objectives, budgets, or starting artifacts. Different policies, policy configurations, and root seeds are allowed. Exact score equality is a tie; usage never breaks one.</p>"},{"location":"notebooks/02_policy_comparison/milestone_2_tour/#3-optional-kernel-inspection-decisions-explain-themselves","title":"3. Optional kernel inspection: decisions explain themselves\u00b6","text":"<p>The public callable path ends above. The underscore projection below is an internal diagnostic used here to show the M2 record model: every policy decision has a linked explanation; Population additionally records one <code>ArchiveUpdate</code> for every completed evaluation.</p>"},{"location":"notebooks/02_policy_comparison/milestone_2_tour/#4-rebuild-the-same-projection-from-retained-facts","title":"4. Rebuild the same projection from retained facts\u00b6","text":"<p>M2 replay means folding the retained in-memory event tuple and reproducing historical policy decisions. It does not yet mean process restart, persistent storage, or resume; those remain later milestones.</p>"},{"location":"notebooks/02_policy_comparison/milestone_2_tour/#where-to-go-next","title":"Where to go next\u00b6","text":"<p>Run <code>main.py</code> for the concise 40-seed report, then open <code>policy_comparison.ipynb</code> for complete distributions and representative star/chain/tree lineages. Those results are a small deterministic illustration in the locked project environment, not broad scientific evidence or a cross-version CPython guarantee. Local callable equality is declaration-based (<code>module:qualname</code>) and does not prove behavioral equivalence.</p>"},{"location":"notebooks/02_policy_comparison/policy_comparison/","title":"Fair comparison of three search policies","text":"In\u00a0[1]: Copied! <pre>from pathlib import Path\nimport runpy\n\npath = Path(\"benchmark.py\")\nif not path.exists():\n    path = Path(\"examples/learn/policy_comparison/benchmark.py\")\nbenchmark = runpy.run_path(path, run_name=\"__main__\")\nreports = benchmark[\"REPORTS\"]\n</pre> from pathlib import Path import runpy  path = Path(\"benchmark.py\") if not path.exists():     path = Path(\"examples/learn/policy_comparison/benchmark.py\") benchmark = runpy.run_path(path, run_name=\"__main__\") reports = benchmark[\"REPORTS\"] <pre>policy comparison: target=12 moves=(-2, 1, 5) seeds=0..39 budget=(evaluations=9, trials=8)\nBestOfN: solved=0/40 scores={-7: 40} evaluations={9: 40} usage={(9, 8, 0, 0.0): 40} representative=(seed=0, lineage=[0, 5])\nGreedy: solved=15/40 scores={-9: 1, -8: 1, -5: 3, -4: 2, -3: 3, -2: 3, -1: 12, 0: 15} evaluations={5: 1, 6: 7, 7: 4, 9: 28} usage={(5, 4, 0, 0.0): 1, (6, 5, 0, 0.0): 7, (7, 6, 0, 0.0): 4, (9, 8, 0, 0.0): 28} representative=(seed=1, lineage=[0, 5, 10, 11, 12])\nPopulation: solved=14/40 scores={-10: 1, -9: 1, -8: 2, -6: 2, -5: 1, -4: 3, -3: 1, -2: 5, -1: 10, 0: 14} evaluations={5: 1, 6: 2, 7: 2, 8: 6, 9: 29} usage={(5, 4, 0, 0.0): 1, (6, 5, 0, 0.0): 2, (7, 6, 0, 0.0): 2, (8, 7, 0, 0.0): 6, (9, 8, 0, 0.0): 29} representative=(seed=0, lineage=[0, 5, 10, 11, 12])\n</pre> In\u00a0[2]: Copied! <pre>topologies = {\n    \"BestOfN\": \"star spoke\",\n    \"Greedy\": \"chain\",\n    \"Population\": \"winning tree branch\",\n}\nfor report in reports:\n    print(\n        f\"{report.policy}: {topologies[report.policy]} \"\n        f\"{list(report.representative_lineage)} \"\n        f\"(seed {report.representative_seed})\"\n    )\n</pre> topologies = {     \"BestOfN\": \"star spoke\",     \"Greedy\": \"chain\",     \"Population\": \"winning tree branch\", } for report in reports:     print(         f\"{report.policy}: {topologies[report.policy]} \"         f\"{list(report.representative_lineage)} \"         f\"(seed {report.representative_seed})\"     ) <pre>BestOfN: star spoke [0, 5] (seed 0)\nGreedy: chain [0, 5, 10, 11, 12] (seed 1)\nPopulation: winning tree branch [0, 5, 10, 11, 12] (seed 0)\n</pre> In\u00a0[3]: Copied! <pre>import json\n\nevidence = {}\nfor report in reports:\n    evidence[report.policy] = {\n        \"solve_rate\": [report.solve_count, report.run_count],\n        \"scores\": list(report.score_distribution),\n        \"evaluations\": list(report.evaluation_distribution),\n        \"usage\": [[list(key), count] for key, count in report.usage_distribution],\n        \"representative_seed\": report.representative_seed,\n        \"lineage\": list(report.representative_lineage),\n    }\nprint(json.dumps(evidence, sort_keys=True))\n</pre> import json  evidence = {} for report in reports:     evidence[report.policy] = {         \"solve_rate\": [report.solve_count, report.run_count],         \"scores\": list(report.score_distribution),         \"evaluations\": list(report.evaluation_distribution),         \"usage\": [[list(key), count] for key, count in report.usage_distribution],         \"representative_seed\": report.representative_seed,         \"lineage\": list(report.representative_lineage),     } print(json.dumps(evidence, sort_keys=True)) <pre>{\"BestOfN\": {\"evaluations\": [[9, 40]], \"lineage\": [0, 5], \"representative_seed\": 0, \"scores\": [[-7, 40]], \"solve_rate\": [0, 40], \"usage\": [[[9, 8, 0, 0.0], 40]]}, \"Greedy\": {\"evaluations\": [[5, 1], [6, 7], [7, 4], [9, 28]], \"lineage\": [0, 5, 10, 11, 12], \"representative_seed\": 1, \"scores\": [[-9, 1], [-8, 1], [-5, 3], [-4, 2], [-3, 3], [-2, 3], [-1, 12], [0, 15]], \"solve_rate\": [15, 40], \"usage\": [[[5, 4, 0, 0.0], 1], [[6, 5, 0, 0.0], 7], [[7, 6, 0, 0.0], 4], [[9, 8, 0, 0.0], 28]]}, \"Population\": {\"evaluations\": [[5, 1], [6, 2], [7, 2], [8, 6], [9, 29]], \"lineage\": [0, 5, 10, 11, 12], \"representative_seed\": 0, \"scores\": [[-10, 1], [-9, 1], [-8, 2], [-6, 2], [-5, 1], [-4, 3], [-3, 1], [-2, 5], [-1, 10], [0, 14]], \"solve_rate\": [14, 40], \"usage\": [[[5, 4, 0, 0.0], 1], [[6, 5, 0, 0.0], 2], [[7, 6, 0, 0.0], 2], [[8, 7, 0, 0.0], 6], [[9, 8, 0, 0.0], 29]]}}\n</pre> In\u00a0[4]: Copied! <pre>greedy = next(report for report in reports if report.policy == \"Greedy\")\nprint(f\"Greedy solved: {greedy.solve_count}/{greedy.run_count}\")\nprint(\"Greedy scores:\", dict(greedy.score_distribution))\n</pre> greedy = next(report for report in reports if report.policy == \"Greedy\") print(f\"Greedy solved: {greedy.solve_count}/{greedy.run_count}\") print(\"Greedy scores:\", dict(greedy.score_distribution)) <pre>Greedy solved: 15/40\nGreedy scores: {-9: 1, -8: 1, -5: 3, -4: 2, -3: 3, -2: 3, -1: 12, 0: 15}\n</pre>"},{"location":"notebooks/02_policy_comparison/policy_comparison/#fair-comparison-of-three-search-policies","title":"Fair comparison of three search policies\u00b6","text":"<p>This canonical Milestone 2 sweep compares BestOfN, Greedy, and Population on the same target <code>12</code>, moves <code>(-2, 1, 5)</code>, initial artifact <code>0</code>, primary score <code>-abs(value - 12)</code>, nine-evaluation/eight-trial budget, and root seeds <code>0</code> through <code>39</code>. Population has size <code>3</code> and tournament size <code>2</code>.</p>"},{"location":"notebooks/02_policy_comparison/policy_comparison/#what-the-lineages-mean","title":"What the lineages mean\u00b6","text":"<p>BestOfN always expands zero, so <code>[0, 5]</code> is one spoke from a star. Its result is also a proof: independent one-step successors are limited to <code>{-2, 1, 5}</code>, making <code>5</code> and score <code>-7</code> the best possible. Greedy's representative is the accepted chain <code>[0, 5, 10, 11, 12]</code>. Population creates a tree through tournament expansion and retention; <code>[0, 5, 10, 11, 12]</code> is the winning branch of that larger tree.</p>"},{"location":"notebooks/02_policy_comparison/policy_comparison/#evidence-boundary","title":"Evidence boundary\u00b6","text":"<p>This is a small deterministic illustration in the locked project environment, not broad scientific evidence and not a cross-version CPython guarantee. The user proposer continues to use <code>random.Random.choice</code> through <code>context.rng</code>, matching the existing accepted example contract. Local callable comparison uses <code>module:qualname</code> declaration identity; it does not prove behavioral equivalence.</p>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/","title":"Derivation, replay, and fair memory comparison","text":"In\u00a0[1]: Copied! <pre>from pathlib import Path\nimport runpy\n\nfrom meta_evolve import serialization\nfrom meta_evolve import ExperienceQuery\nfrom meta_evolve.application import replay_experience_graph\n\npath = Path(\"main.py\")\nif not path.exists():\n    path = Path(\"examples/learn/experience_reuse/main.py\")\ndemo = runpy.run_path(path)\nresults = demo[\"run_experiment\"]()\nprint(\"comparison lab ready\")\n</pre> from pathlib import Path import runpy  from meta_evolve import serialization from meta_evolve import ExperienceQuery from meta_evolve.application import replay_experience_graph  path = Path(\"main.py\") if not path.exists():     path = Path(\"examples/learn/experience_reuse/main.py\") demo = runpy.run_path(path) results = demo[\"run_experiment\"]() print(\"comparison lab ready\") <pre>comparison lab ready\n</pre> In\u00a0[2]: Copied! <pre>for name, (result, _) in results.items():\n    outcomes = tuple(\n        trial.failure.kind\n        if trial.failure is not None\n        else trial.metrics[\"score\"]\n        for trial in result.trials()\n    )\n    print(\n        f\"{name:&lt;10}\",\n        f\"outcomes={outcomes}\",\n        f\"task_usage={result.summary().usage}\",\n    )\n</pre> for name, (result, _) in results.items():     outcomes = tuple(         trial.failure.kind         if trial.failure is not None         else trial.metrics[\"score\"]         for trial in result.trials()     )     print(         f\"{name:&lt;10}\",         f\"outcomes={outcomes}\",         f\"task_usage={result.summary().usage}\",     ) <pre>no-memory  outcomes=(0, 'evaluator_failure', 'evaluator_failure', 'evaluator_failure') task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\npush-only  outcomes=(0, 'evaluator_failure', 'evaluator_failure', 10) task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\npull-only  outcomes=(0, 'evaluator_failure', 'evaluator_failure', 10) task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\ncombined   outcomes=(0, 'evaluator_failure', 'evaluator_failure', 10) task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0)\n</pre> In\u00a0[3]: Copied! <pre>for name in (\"push-only\", \"pull-only\", \"combined\"):\n    result, _ = results[name]\n    projection = result._projection()\n    print(name)\n    for submitted in projection.proposals:\n        trial = projection.completed_trial(submitted.proposal.trial_id).trial\n        kinds = tuple(ref.kind for ref in submitted.proposal.derived_from)\n        print(f\"  step={trial.logical_step} derived_from={kinds}\")\n</pre> for name in (\"push-only\", \"pull-only\", \"combined\"):     result, _ = results[name]     projection = result._projection()     print(name)     for submitted in projection.proposals:         trial = projection.completed_trial(submitted.proposal.trial_id).trial         kinds = tuple(ref.kind for ref in submitted.proposal.derived_from)         print(f\"  step={trial.logical_step} derived_from={kinds}\") <pre>push-only\n  step=1 derived_from=()\n  step=2 derived_from=('evaluation',)\n  step=3 derived_from=('evaluation', 'evaluation')\npull-only\n  step=1 derived_from=()\n  step=2 derived_from=('evaluation',)\n  step=3 derived_from=('evaluation', 'evaluation', 'trial', 'trial')\ncombined\n  step=1 derived_from=()\n  step=2 derived_from=('evaluation',)\n  step=3 derived_from=('evaluation', 'evaluation', 'trial', 'trial')\n</pre> <p>Coordinator failure precedence is deliberate: resource exhaustion wins over invalid derivation, invalid derivation wins over a reported proposer failure, and either coordinator-imposed failure clears the edge and commits no candidate. A legitimate reported failure may retain valid derivation.</p> In\u00a0[4]: Copied! <pre>result, storage = results[\"combined\"]\nevents = storage.events.read(result.id)\nleft = replay_experience_graph(result.id, events)\nright = replay_experience_graph(result.id, tuple(events))\nquery = ExperienceQuery(task_id=left.task_id, run_id=result.id)\n\nleft_edges = tuple(node.derived_from for node in left.state_at(query).nodes)\nright_edges = tuple(node.derived_from for node in right.state_at(query).nodes)\nprint(\"replayed derivation equal:\", left_edges == right_edges)\nprint(\"edge sizes:\", tuple(len(edge) for edge in left_edges))\n</pre> result, storage = results[\"combined\"] events = storage.events.read(result.id) left = replay_experience_graph(result.id, events) right = replay_experience_graph(result.id, tuple(events)) query = ExperienceQuery(task_id=left.task_id, run_id=result.id)  left_edges = tuple(node.derived_from for node in left.state_at(query).nodes) right_edges = tuple(node.derived_from for node in right.state_at(query).nodes) print(\"replayed derivation equal:\", left_edges == right_edges) print(\"edge sizes:\", tuple(len(edge) for edge in left_edges)) <pre>replayed derivation equal: True\nedge sizes: (0, 0, 1, 4)\n</pre> In\u00a0[5]: Copied! <pre>projection = result._projection()\nbootstrap = projection.bootstrap.proposal\nderived = projection.proposals[-1].proposal\nbootstrap_bytes = serialization.dumps(bootstrap)\nderived_bytes = serialization.dumps(derived)\n\nprint(\"empty derivation omitted:\", \"derived_from\" not in bootstrap_bytes)\nprint(\"non-empty derivation encoded:\", \"derived_from\" in derived_bytes)\nprint(\"round trip exact:\", serialization.loads(derived_bytes) == derived)\n</pre> projection = result._projection() bootstrap = projection.bootstrap.proposal derived = projection.proposals[-1].proposal bootstrap_bytes = serialization.dumps(bootstrap) derived_bytes = serialization.dumps(derived)  print(\"empty derivation omitted:\", \"derived_from\" not in bootstrap_bytes) print(\"non-empty derivation encoded:\", \"derived_from\" in derived_bytes) print(\"round trip exact:\", serialization.loads(derived_bytes) == derived) <pre>empty derivation omitted: True\nnon-empty derivation encoded: True\nround trip exact: True\n</pre> In\u00a0[6]: Copied! <pre>baseline = results[\"no-memory\"][0]\nfor name, (other, _) in results.items():\n    comparison = baseline.compare(other)\n    winner = comparison.winner.primary_score if comparison.winner else \"tie\"\n    print(\n        f\"no-memory vs {name:&lt;10}\",\n        f\"winner={winner}\",\n        f\"same_seed={comparison.same_random_seed}\",\n        f\"same_context={comparison.same_context_declaration}\",\n        f\"same_pull={comparison.same_experience_declaration}\",\n    )\n</pre> baseline = results[\"no-memory\"][0] for name, (other, _) in results.items():     comparison = baseline.compare(other)     winner = comparison.winner.primary_score if comparison.winner else \"tie\"     print(         f\"no-memory vs {name:&lt;10}\",         f\"winner={winner}\",         f\"same_seed={comparison.same_random_seed}\",         f\"same_context={comparison.same_context_declaration}\",         f\"same_pull={comparison.same_experience_declaration}\",     ) <pre>no-memory vs no-memory  winner=tie same_seed=True same_context=True same_pull=True\nno-memory vs push-only  winner=10 same_seed=True same_context=False same_pull=True\nno-memory vs pull-only  winner=10 same_seed=True same_context=True same_pull=False\nno-memory vs combined   winner=10 same_seed=True same_context=False same_pull=False\n</pre>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/#derivation-replay-and-fair-memory-comparison","title":"Derivation, replay, and fair memory comparison\u00b6","text":"<p>This lab focuses on the M3.4 distinction between experience observed and experience declared as causal. It then checks that derivation survives replay and that runs with different memory strategies remain comparable.</p>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/#1-see-where-reuse-changes-the-outcome","title":"1. See where reuse changes the outcome\u00b6","text":"<p>All variants spend the same task budget. Only the three memory-aware runs can turn the two failed evaluations into the successful value <code>10</code>.</p>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/#2-inspect-exact-committed-derivation","title":"2. Inspect exact committed derivation\u00b6","text":"<p>A failed evaluation may still carry useful derivation. The second ordinary proposal cites one failure; the third cites two failures and, for pull variants, the compared trial occurrences.</p>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/#3-replay-the-same-edge-graph","title":"3. Replay the same edge graph\u00b6","text":"<p>Replay validates the event stream and rebuilds derivation from durable proposals. It does not call the live context selector, reader, filtering code, or renderer.</p>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/#4-see-the-serialization-compatibility-rule","title":"4. See the serialization compatibility rule\u00b6","text":"<p>Empty derivation is omitted, so old proposal and event bytes remain unchanged. Non-empty derivation is encoded normally. An explicitly encoded empty default is rejected as non-canonical.</p>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/#5-compare-different-memory-strategies-fairly","title":"5. Compare different memory strategies fairly\u00b6","text":"<p>Memory declarations may differ. Compatibility still requires equal evaluator, proposer, objective, task/run budgets, artifact declaration, and initial digest. The comparison reports memory-declaration equality rather than rejecting the pair.</p>"},{"location":"notebooks/03_experience_reuse/derivation_comparison_lab/#takeaway","title":"Takeaway\u00b6","text":"<p>M3.4 does not claim that every observed record caused a result. It preserves the proposer's explicit usage intent while the coordinator independently enforces visibility, ordering, budgets, failure semantics, and evaluation authority.</p>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/","title":"Milestone 3 tour: experience that remains causal","text":"In\u00a0[1]: Copied! <pre>from pathlib import Path\nimport runpy\n\nfrom meta_evolve import ExperienceQuery\nfrom meta_evolve.application import replay_experience_graph\n\npath = Path(\"main.py\")\nif not path.exists():\n    path = Path(\"examples/learn/experience_reuse/main.py\")\ndemo = runpy.run_path(path)\nresults = demo[\"run_experiment\"]()\nprint(\"variants:\", tuple(results))\n</pre> from pathlib import Path import runpy  from meta_evolve import ExperienceQuery from meta_evolve.application import replay_experience_graph  path = Path(\"main.py\") if not path.exists():     path = Path(\"examples/learn/experience_reuse/main.py\") demo = runpy.run_path(path) results = demo[\"run_experiment\"]() print(\"variants:\", tuple(results)) <pre>variants: ('no-memory', 'push-only', 'pull-only', 'combined')\n</pre> In\u00a0[2]: Copied! <pre>for name, (result, _) in results.items():\n    summary = result.summary()\n    print(\n        name,\n        f\"score={summary.primary_score}\",\n        f\"task_usage={summary.usage}\",\n        f\"memory_usage={summary.experience_usage}\",\n    )\n</pre> for name, (result, _) in results.items():     summary = result.summary()     print(         name,         f\"score={summary.primary_score}\",         f\"task_usage={summary.usage}\",         f\"memory_usage={summary.experience_usage}\",     ) <pre>no-memory score=0 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=0, chars=0), pull=ExperienceUsage(operations=0, results=0, records=0, chars=0))\npush-only score=10 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=3, chars=3295), pull=ExperienceUsage(operations=0, results=0, records=0, chars=0))\npull-only score=10 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=0, chars=0), pull=ExperienceUsage(operations=7, results=8, records=20, chars=20559))\ncombined score=10 task_usage=Usage(evaluations=4, trials=3, tokens=0, wall_seconds=0.0) memory_usage=RunExperienceUsage(context=ContextUsage(records=3, chars=3295), pull=ExperienceUsage(operations=7, results=8, records=20, chars=20855))\n</pre> In\u00a0[3]: Copied! <pre>combined, storage = results[\"combined\"]\nevents = storage.events.read(combined.id)\ngraph = replay_experience_graph(combined.id, events)\nquery = ExperienceQuery(task_id=graph.task_id, run_id=combined.id)\nstate = graph.state_at(query)\n\nfor node in state.nodes:\n    kinds = tuple(record.kind for record in node.records)\n    print(f\"step={node.logical_step} records={kinds}\")\nprint(\"M3.1 frontier:\", [node.logical_step for node in graph.frontier(query)])\n</pre> combined, storage = results[\"combined\"] events = storage.events.read(combined.id) graph = replay_experience_graph(combined.id, events) query = ExperienceQuery(task_id=graph.task_id, run_id=combined.id) state = graph.state_at(query)  for node in state.nodes:     kinds = tuple(record.kind for record in node.records)     print(f\"step={node.logical_step} records={kinds}\") print(\"M3.1 frontier:\", [node.logical_step for node in graph.frontier(query)]) <pre>step=0 records=('trial', 'proposal', 'artifact', 'outcome', 'evaluation')\nstep=1 records=('decision', 'decision_explanation', 'context_selection', 'experience_grant', 'experience_attempt', 'experience_success', 'proposal', 'artifact', 'trial', 'outcome', 'evaluation')\nstep=2 records=('decision', 'decision_explanation', 'context_selection', 'experience_grant', 'experience_attempt', 'experience_success', 'experience_attempt', 'experience_success', 'proposal', 'artifact', 'trial', 'outcome', 'evaluation')\nstep=3 records=('decision', 'decision_explanation', 'context_selection', 'experience_grant', 'experience_attempt', 'experience_success', 'experience_attempt', 'experience_success', 'experience_attempt', 'experience_success', 'experience_attempt', 'experience_success', 'proposal', 'artifact', 'trial', 'outcome', 'evaluation')\nM3.1 frontier: [3]\n</pre> In\u00a0[4]: Copied! <pre>historical = graph.state_at(ExperienceQuery(\n    task_id=graph.task_id,\n    run_id=combined.id,\n    as_of=2,\n))\nprint(\"visible steps as_of=2:\", [node.logical_step for node in historical.nodes])\nprint(\"frontier steps:\", [node.logical_step for node in state.nodes])\n</pre> historical = graph.state_at(ExperienceQuery(     task_id=graph.task_id,     run_id=combined.id,     as_of=2, )) print(\"visible steps as_of=2:\", [node.logical_step for node in historical.nodes]) print(\"frontier steps:\", [node.logical_step for node in state.nodes]) <pre>visible steps as_of=2: [0, 1, 2]\nfrontier steps: [0, 1, 2, 3]\n</pre> In\u00a0[5]: Copied! <pre>projection = combined._projection()\nfor selected in projection.context_selections:\n    refs = tuple(record.ref.kind for record in selected.bundle.records)\n    print(\n        f\"step={selected.bundle.records[-1].logical_step if refs else '-'}\",\n        f\"reason={selected.bundle.reason!r}\",\n        f\"refs={refs}\",\n        f\"usage={selected.bundle.usage}\",\n    )\nprint(\"M3.2 declaration:\", projection.context_declaration.spec)\n</pre> projection = combined._projection() for selected in projection.context_selections:     refs = tuple(record.ref.kind for record in selected.bundle.records)     print(         f\"step={selected.bundle.records[-1].logical_step if refs else '-'}\",         f\"reason={selected.bundle.reason!r}\",         f\"refs={refs}\",         f\"usage={selected.bundle.usage}\",     ) print(\"M3.2 declaration:\", projection.context_declaration.spec) <pre>step=- reason='failed ancestor evaluations' refs=() usage=ContextUsage(records=0, chars=0)\nstep=1 reason='failed ancestor evaluations' refs=('evaluation',) usage=ContextUsage(records=1, chars=1098)\nstep=2 reason='failed ancestor evaluations' refs=('evaluation', 'evaluation') usage=ContextUsage(records=2, chars=2197)\nM3.2 declaration: ContextSpec(policy=PolicyRef(name='failed-ancestors', version='1', origin='local'), max_records=20, max_chars=8000)\n</pre> In\u00a0[6]: Copied! <pre>for issued in projection.experience_grants:\n    operations = projection.operations_for(issued.grant.trial_id)\n    print(f\"grant as_of={issued.grant.as_of} operations={len(operations)}\")\n    for attempt, resolution in operations:\n        print(\n            \" \",\n            attempt.request.operation,\n            type(resolution).__name__,\n            projection.experience_usage_for(attempt.trial_id),\n        )\nprint(\"M3.3 declaration:\", projection.experience_declaration.spec)\n</pre> for issued in projection.experience_grants:     operations = projection.operations_for(issued.grant.trial_id)     print(f\"grant as_of={issued.grant.as_of} operations={len(operations)}\")     for attempt, resolution in operations:         print(             \" \",             attempt.request.operation,             type(resolution).__name__,             projection.experience_usage_for(attempt.trial_id),         ) print(\"M3.3 declaration:\", projection.experience_declaration.spec) <pre>grant as_of=0 operations=1\n  search ExperienceOperationSucceeded ExperienceUsage(operations=1, results=0, records=0, chars=0)\ngrant as_of=1 operations=2\n  search ExperienceOperationSucceeded ExperienceUsage(operations=2, results=2, records=2, chars=2196)\n  open ExperienceOperationSucceeded ExperienceUsage(operations=2, results=2, records=2, chars=2196)\ngrant as_of=2 operations=4\n  search ExperienceOperationSucceeded ExperienceUsage(operations=4, results=6, records=18, chars=18659)\n  open ExperienceOperationSucceeded ExperienceUsage(operations=4, results=6, records=18, chars=18659)\n  open ExperienceOperationSucceeded ExperienceUsage(operations=4, results=6, records=18, chars=18659)\n  compare ExperienceOperationSucceeded ExperienceUsage(operations=4, results=6, records=18, chars=18659)\nM3.3 declaration: ExperienceSpec(max_operations=20, max_results=20, max_records=100, max_chars=32000)\n</pre> In\u00a0[7]: Copied! <pre>for node in state.nodes[1:]:\n    print(\n        f\"step={node.logical_step}\",\n        \"derived_from=\",\n        tuple(reference.kind for reference in node.derived_from),\n    )\n\nbaseline = results[\"no-memory\"][0]\ncomparison = baseline.compare(combined)\nprint(\"M3.4 winner score:\", comparison.winner.primary_score)\nprint(\"same context:\", comparison.same_context_declaration)\nprint(\"same pull:\", comparison.same_experience_declaration)\n</pre> for node in state.nodes[1:]:     print(         f\"step={node.logical_step}\",         \"derived_from=\",         tuple(reference.kind for reference in node.derived_from),     )  baseline = results[\"no-memory\"][0] comparison = baseline.compare(combined) print(\"M3.4 winner score:\", comparison.winner.primary_score) print(\"same context:\", comparison.same_context_declaration) print(\"same pull:\", comparison.same_experience_declaration) <pre>step=1 derived_from= ()\nstep=2 derived_from= ('evaluation',)\nstep=3 derived_from= ('evaluation', 'evaluation', 'trial', 'trial')\nM3.4 winner score: 10\nsame context: False\nsame pull: False\n</pre>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/#milestone-3-tour-experience-that-remains-causal","title":"Milestone 3 tour: experience that remains causal\u00b6","text":"<p>Milestone 3 makes earlier work useful to later proposers without turning memory into ambient authority. This tour follows the four slices in order: M3.1 temporal graph, M3.2 pushed context, M3.3 audited pull access, and M3.4 declared derivation and comparison.</p> <p>The deterministic experiment deliberately produces two failed evaluations. Memory-aware variants can then reuse those failures to reach <code>10</code>; the no-memory variant cannot.</p>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/#1-run-the-same-experiment-four-ways","title":"1. Run the same experiment four ways\u00b6","text":"<p>Task, seed, proposer, evaluator, chain search, random seed, and task/run budgets stay fixed. Only the memory declaration changes.</p>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/#2-m31-the-temporal-experience-graph","title":"2. M3.1 \u2014 the temporal experience graph\u00b6","text":"<p>The graph is a read model over committed trials and records. Nodes are causal occurrences, not deduplicated payloads. An <code>as_of</code> boundary can reveal the past without leaking future trials.</p>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/#3-m32-policy-pushed-context","title":"3. M3.2 \u2014 policy-pushed context\u00b6","text":"<p>A context policy chooses a bounded, exact bundle before the proposer runs. The bundle stores structured records, a canonical rendering, a reason, truncation, and usage. It is replayed rather than selected again.</p>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/#4-m33-agent-directed-pull-access","title":"4. M3.3 \u2014 agent-directed pull access\u00b6","text":"<p>The reader lets a proposer search, open, traverse, and compare within a strictly-prior grant. Every attempt is charged and resolves to success, opaque denial, or content-free exhaustion. Usage is cumulative inside each grant.</p>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/#5-m34-declare-what-actually-caused-the-proposal","title":"5. M3.4 \u2014 declare what actually caused the proposal\u00b6","text":"<p>Observation and use are distinct. <code>derived_from</code> must be a unique ordered subsequence of the committed push-then-pull ledger. Valid edges survive failed evaluations and typed proposer failures; unauthorized declarations become policy failures.</p>"},{"location":"notebooks/03_experience_reuse/milestone_3_tour/#what-to-keep-in-your-head","title":"What to keep in your head\u00b6","text":"<ul> <li>Graph: what committed experience existed at a logical time.</li> <li>Push: what a policy placed in front of the proposer.</li> <li>Pull: what the proposer chose to inspect through a bounded capability.</li> <li>Derivation: which observed references the proposer says it actually used.</li> <li>Evaluation: still independent of proposer authority.</li> </ul> <p>The example is a small deterministic illustration, not broad scientific evidence. Its purpose is to make the authority and replay contracts tangible.</p>"},{"location":"notebooks/03_experience_reuse/push_pull_lab/","title":"Push and pull lab: two routes to the same experience","text":"In\u00a0[1]: Copied! <pre>from pathlib import Path\nimport runpy\n\nfrom meta_evolve.application.derivation import observation_ledger\n\npath = Path(\"main.py\")\nif not path.exists():\n    path = Path(\"examples/learn/experience_reuse/main.py\")\ndemo = runpy.run_path(path)\nresults = demo[\"run_experiment\"]()\nprint(\"lab ready\")\n</pre> from pathlib import Path import runpy  from meta_evolve.application.derivation import observation_ledger  path = Path(\"main.py\") if not path.exists():     path = Path(\"examples/learn/experience_reuse/main.py\") demo = runpy.run_path(path) results = demo[\"run_experiment\"]() print(\"lab ready\") <pre>lab ready\n</pre> In\u00a0[2]: Copied! <pre>push_run = results[\"push-only\"][0]\npush_projection = push_run._projection()\n\nfor index, selected in enumerate(push_projection.context_selections, start=1):\n    records = selected.bundle.records\n    labels = tuple(\n        f\"{record.ref.kind}@{record.logical_step}\" for record in records\n    )\n    print(\n        f\"trial={index}\",\n        f\"selected={labels}\",\n        f\"chars={selected.bundle.usage.chars}\",\n    )\n</pre> push_run = results[\"push-only\"][0] push_projection = push_run._projection()  for index, selected in enumerate(push_projection.context_selections, start=1):     records = selected.bundle.records     labels = tuple(         f\"{record.ref.kind}@{record.logical_step}\" for record in records     )     print(         f\"trial={index}\",         f\"selected={labels}\",         f\"chars={selected.bundle.usage.chars}\",     ) <pre>trial=1 selected=() chars=0\ntrial=2 selected=('evaluation@1',) chars=1098\ntrial=3 selected=('evaluation@1', 'evaluation@2') chars=2197\n</pre> In\u00a0[3]: Copied! <pre>bundle = push_projection.context_selections[-1].bundle\nprint(\"reason:\", bundle.reason)\nprint(\"truncated:\", bundle.truncated)\nprint(\"usage:\", bundle.usage)\nprint(\"refs:\", tuple(\n    f\"{record.ref.kind}@{record.logical_step}\" for record in bundle.records\n))\n</pre> bundle = push_projection.context_selections[-1].bundle print(\"reason:\", bundle.reason) print(\"truncated:\", bundle.truncated) print(\"usage:\", bundle.usage) print(\"refs:\", tuple(     f\"{record.ref.kind}@{record.logical_step}\" for record in bundle.records )) <pre>reason: failed ancestor evaluations\ntruncated: False\nusage: ContextUsage(records=2, chars=2197)\nrefs: ('evaluation@1', 'evaluation@2')\n</pre> <p>A bundle is a snapshot, not a live graph handle. Replay validates its references and exact values against prior committed records without invoking the policy again.</p> In\u00a0[4]: Copied! <pre>pull_run = results[\"pull-only\"][0]\npull_projection = pull_run._projection()\n\nfor issued in pull_projection.experience_grants:\n    operations = pull_projection.operations_for(issued.grant.trial_id)\n    print(f\"grant as_of={issued.grant.as_of}\")\n    for attempt, resolution in operations:\n        result = getattr(resolution, \"result\", None)\n        count = len(result.references) if result is not None else 0\n        print(\n            f\"  {attempt.request.operation:&lt;7}\",\n            f\"{type(resolution).__name__:&lt;28}\",\n            f\"refs={count}\",\n        )\n</pre> pull_run = results[\"pull-only\"][0] pull_projection = pull_run._projection()  for issued in pull_projection.experience_grants:     operations = pull_projection.operations_for(issued.grant.trial_id)     print(f\"grant as_of={issued.grant.as_of}\")     for attempt, resolution in operations:         result = getattr(resolution, \"result\", None)         count = len(result.references) if result is not None else 0         print(             f\"  {attempt.request.operation:&lt;7}\",             f\"{type(resolution).__name__:&lt;28}\",             f\"refs={count}\",         ) <pre>grant as_of=0\n  search  ExperienceOperationSucceeded refs=0\ngrant as_of=1\n  search  ExperienceOperationSucceeded refs=1\n  open    ExperienceOperationSucceeded refs=1\ngrant as_of=2\n  search  ExperienceOperationSucceeded refs=2\n  open    ExperienceOperationSucceeded refs=1\n  open    ExperienceOperationSucceeded refs=1\n  compare ExperienceOperationSucceeded refs=2\n</pre> In\u00a0[5]: Copied! <pre>combined_run = results[\"combined\"][0]\ncombined = combined_run._projection()\nfinal = combined.completed_trials[-1].trial.id\nproposal = combined.proposal_for(final)\nledger = observation_ledger(combined, final)\n\nprint(\"ledger kinds:\", tuple(reference.kind for reference in ledger))\nprint(\"declared kinds:\", tuple(ref.kind for ref in proposal.derived_from))\npositions = tuple(ledger.index(ref) for ref in proposal.derived_from)\nprint(\"declared positions:\", positions)\nprint(\"ordered subsequence:\", positions == tuple(sorted(positions)))\n</pre> combined_run = results[\"combined\"][0] combined = combined_run._projection() final = combined.completed_trials[-1].trial.id proposal = combined.proposal_for(final) ledger = observation_ledger(combined, final)  print(\"ledger kinds:\", tuple(reference.kind for reference in ledger)) print(\"declared kinds:\", tuple(ref.kind for ref in proposal.derived_from)) positions = tuple(ledger.index(ref) for ref in proposal.derived_from) print(\"declared positions:\", positions) print(\"ordered subsequence:\", positions == tuple(sorted(positions))) <pre>ledger kinds: ('evaluation', 'evaluation', 'trial', 'trial', 'decision', 'decision', 'decision_explanation', 'decision_explanation', 'proposal', 'proposal', 'artifact', 'artifact', 'outcome', 'outcome')\ndeclared kinds: ('evaluation', 'evaluation', 'trial', 'trial')\ndeclared positions: (0, 1, 2, 3)\nordered subsequence: True\n</pre> In\u00a0[6]: Copied! <pre>for name, (result, _) in results.items():\n    usage = result.experience_usage()\n    print(\n        f\"{name:&lt;10}\",\n        f\"context=({usage.context.records}, {usage.context.chars})\",\n        f\"pull=({usage.pull.operations}, {usage.pull.results},\",\n        f\"{usage.pull.records}, {usage.pull.chars})\",\n    )\n</pre> for name, (result, _) in results.items():     usage = result.experience_usage()     print(         f\"{name:&lt;10}\",         f\"context=({usage.context.records}, {usage.context.chars})\",         f\"pull=({usage.pull.operations}, {usage.pull.results},\",         f\"{usage.pull.records}, {usage.pull.chars})\",     ) <pre>no-memory  context=(0, 0) pull=(0, 0, 0, 0)\npush-only  context=(3, 3295) pull=(0, 0, 0, 0)\npull-only  context=(0, 0) pull=(7, 8, 20, 20559)\ncombined   context=(3, 3295) pull=(7, 8, 20, 20855)\n</pre>"},{"location":"notebooks/03_experience_reuse/push_pull_lab/#push-and-pull-lab-two-routes-to-the-same-experience","title":"Push and pull lab: two routes to the same experience\u00b6","text":"<p>Push and pull share committed records but express different decisions. Push records a policy's selection before execution. Pull records the proposer's own bounded investigation during execution.</p> <p>This lab inspects both channels and then reconstructs the canonical observation ledger used by M3.4.</p>"},{"location":"notebooks/03_experience_reuse/push_pull_lab/#1-inspect-pushed-bundles","title":"1. Inspect pushed bundles\u00b6","text":"<p>The demo's <code>FailedAncestors</code> policy pushes only failed ancestor evaluations. Notice how the number of selected failures grows as the chain advances.</p>"},{"location":"notebooks/03_experience_reuse/push_pull_lab/#2-inspect-pull-grants-and-operations","title":"2. Inspect pull grants and operations\u00b6","text":"<p>Each successor gets a new grant with <code>as_of = logical_step - 1</code>. The final proposer searches two failed evaluations, opens both, and compares their trials.</p>"},{"location":"notebooks/03_experience_reuse/push_pull_lab/#3-reconstruct-the-push-then-pull-ledger","title":"3. Reconstruct the push-then-pull ledger\u00b6","text":"<p>The ledger is ordered by committed observation: bundle references first, then successful pull results in operation order. References and structured record references are deduplicated by first occurrence.</p>"},{"location":"notebooks/03_experience_reuse/push_pull_lab/#4-compare-channel-specific-usage","title":"4. Compare channel-specific usage\u00b6","text":"<p>Memory access is measured but does not consume task or run budgets. Context usage counts selected records and rendered characters. Pull usage additionally counts attempts and returned references.</p>"},{"location":"notebooks/03_experience_reuse/push_pull_lab/#try-next","title":"Try next\u00b6","text":"<p>Change <code>FailedAncestors</code> to push only one failure, lower <code>PullAccess.max_operations</code>, or remove the comparison call. Predict the changed ledger and usage before rerunning. The interesting question is not merely whether memory helps, but which access decision helped at what cost.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/","title":"Crash recovery for durable runs ()","text":"In\u00a0[1]: Copied! <pre>import inspect\nimport sys\nfrom pathlib import Path\n\nimport meta_evolve as meta\n\n# main.py lives next to this notebook; add its dir so its module-level\n# (source-digestible) proposer/evaluator/helpers can be imported.\nsys.path.insert(0, str(Path('examples/operations/crash_recovery').resolve()))\nimport main  # the runnable example this notebook mirrors\n\nTARGET = main.TARGET\nprint(inspect.getsource(main.propose))\nprint(inspect.getsource(main.evaluate))\n</pre> import inspect import sys from pathlib import Path  import meta_evolve as meta  # main.py lives next to this notebook; add its dir so its module-level # (source-digestible) proposer/evaluator/helpers can be imported. sys.path.insert(0, str(Path('examples/operations/crash_recovery').resolve())) import main  # the runnable example this notebook mirrors  TARGET = main.TARGET print(inspect.getsource(main.propose)) print(inspect.getsource(main.evaluate)) <pre>def propose(parent: object, context: object) -&gt; object:\n    \"\"\"Climb toward the target by one each step. Deterministic and retry-safe:\n    the same parent always yields the same child, so replaying it during\n    recovery reproduces the identical candidate.\n\n    ``context`` is deliberately ignored: this is a fixed +1 climb with no\n    randomness. (Sibling example 01 instead draws from ``context.rng`` -- that\n    is where the per-step context earns its keep.)\"\"\"\n\n    return parent + 1\n\ndef evaluate(candidate: object) -&gt; float:\n    \"\"\"Higher is better; the search maximizes this score. Closer to the target\n    scores higher, and the target itself is the ceiling.\"\"\"\n\n    return -abs(float(candidate) - TARGET)\n\n</pre> In\u00a0[2]: Copied! <pre># build_components / build_task / start_run are the example's own helpers.\n# start_run is the single run shape; the run id derives from its inputs, so the\n# same inputs on two durable paths produce the SAME run id (how resume addresses it).\nprint(inspect.getsource(main.build_components))\nprint(inspect.getsource(main.start_run))\n\ncomponents = main.build_components()\n</pre> # build_components / build_task / start_run are the example's own helpers. # start_run is the single run shape; the run id derives from its inputs, so the # same inputs on two durable paths produce the SAME run id (how resume addresses it). print(inspect.getsource(main.build_components)) print(inspect.getsource(main.start_run))  components = main.build_components() <pre>def build_components() -&gt; Components:\n    \"\"\"Register the example callables and retain their typed references.\"\"\"\n    registry = Registry()\n    proposer = registry.register_proposer(\n        \"example:propose\", \"1\", propose, deterministic=True, retry_safe=True\n    )\n    evaluator = registry.register_evaluator(\n        \"example:evaluate\",\n        \"1\",\n        evaluate,\n        deterministic=True,\n        retry_safe=True,\n        configuration={\"target\": TARGET},\n    )\n    return Components(registry, proposer, evaluator)\n\ndef start_run(storage: meta.Storage, components: Components) -&gt; meta.Run:\n    \"\"\"The one run shape used everywhere in this example. Because the run id is\n    derived from these inputs, calling ``start_run`` with the same components on\n    two different durable paths produces runs with the SAME id.\"\"\"\n\n    experiment = meta.Experiment(\n        task=build_task(components.evaluator),\n        seed=0,\n        proposer=components.proposer,\n        registry=components.registry,\n        search=meta.Greedy(max_trials=8),\n    )\n    return meta.run(experiment, storage=storage, mode=\"durable-local\")\n\n</pre> In\u00a0[3]: Copied! <pre>import tempfile\n\n# The example already defines the crash simulation; we reuse it (and show it).\nSimulatedCrash = main.SimulatedCrash\ninstall_crash = main._install_crash\nprint(inspect.getsource(main.CrashAfter))\n\ntmp = Path(tempfile.mkdtemp())\nprint('workspace:', tmp)\n</pre> import tempfile  # The example already defines the crash simulation; we reuse it (and show it). SimulatedCrash = main.SimulatedCrash install_crash = main._install_crash print(inspect.getsource(main.CrashAfter))  tmp = Path(tempfile.mkdtemp()) print('workspace:', tmp) <pre>class CrashAfter:\n    \"\"\"Commit-strategy wrapper: perform the first ``n`` commits for real, then\n    raise ``SimulatedCrash`` on the next one. Never touches what was committed.\"\"\"\n\n    def __init__(self, inner: object, *, n: int) -&gt; None:\n        self._inner = inner\n        self._n = n\n        self.commits = 0\n\n    def commit(\n        self,\n        run_id: RunId,\n        expected_version: int,\n        *,\n        events: Iterable[EventEnvelope],\n        occurrences: Iterable[Artifact],\n    ) -&gt; int:\n        if self.commits &gt;= self._n:\n            raise SimulatedCrash(f\"simulated process crash after {self._n} committed steps\")\n        version = self._inner.commit(\n            run_id, expected_version, events=events, occurrences=occurrences\n        )\n        self.commits += 1\n        return version\n\nworkspace: /var/folders/fn/fg595s6s7wd8d9z3jhr5vsbw0000gn/T/tmp5ituhb2o\n</pre> In\u00a0[4]: Copied! <pre>path_a = tmp / 'path-A'\nref_storage = meta.Storage.durable(path_a)\ncounter = install_crash(ref_storage, n=10_000)  # never fires; only counts\nreference = main.start_run(ref_storage, components)\nrun_id = reference.id\nreference_best = reference.best().value\ncommitted_batches = counter.commits\ncomplete_len = len(meta.Storage.durable(path_a).events.read(run_id))\nprint('run id          :', run_id)\nprint('best()          :', reference_best)\nprint('committed batches:', committed_batches)\nprint('events on disk  :', complete_len)\n</pre> path_a = tmp / 'path-A' ref_storage = meta.Storage.durable(path_a) counter = install_crash(ref_storage, n=10_000)  # never fires; only counts reference = main.start_run(ref_storage, components) run_id = reference.id reference_best = reference.best().value committed_batches = counter.commits complete_len = len(meta.Storage.durable(path_a).events.read(run_id)) print('run id          :', run_id) print('best()          :', reference_best) print('committed batches:', committed_batches) print('events on disk  :', complete_len) <pre>run id          : run-72c1e35d855eae5221fd9084ed59dd6027923a87c246dc9c46bde07e1a8a9e99\nbest()          : 6\ncommitted batches: 23\nevents on disk  : 49\n</pre> In\u00a0[5]: Copied! <pre>crash_after = max(1, committed_batches // 2)\nassert 1 &lt;= crash_after &lt; committed_batches\npath_b = tmp / 'path-B'\ncrash_storage = meta.Storage.durable(path_b)\ncrasher = install_crash(crash_storage, n=crash_after)\ncrashed = False\ntry:\n    main.start_run(crash_storage, components)\nexcept SimulatedCrash as e:\n    crashed = True\n    print('crashed:', e)\nassert crashed and crasher.commits == crash_after\ncrashed_len = len(meta.Storage.durable(path_b).events.read(run_id))\nprint(f'events on disk  : {crashed_len}  (a genuine PARTIAL stream vs {complete_len} complete)')\n</pre> crash_after = max(1, committed_batches // 2) assert 1 &lt;= crash_after &lt; committed_batches path_b = tmp / 'path-B' crash_storage = meta.Storage.durable(path_b) crasher = install_crash(crash_storage, n=crash_after) crashed = False try:     main.start_run(crash_storage, components) except SimulatedCrash as e:     crashed = True     print('crashed:', e) assert crashed and crasher.commits == crash_after crashed_len = len(meta.Storage.durable(path_b).events.read(run_id)) print(f'events on disk  : {crashed_len}  (a genuine PARTIAL stream vs {complete_len} complete)') <pre>crashed: simulated process crash after 11 committed steps\nevents on disk  : 25  (a genuine PARTIAL stream vs 49 complete)\n</pre> In\u00a0[6]: Copied! <pre>resumed = meta.resume(run_id, storage=meta.Storage.durable(path_b), registry=components.registry)\nresumed_best = resumed.best().value\nresumed_len = len(meta.Storage.durable(path_b).events.read(run_id))\nprint(f'events on disk  : {resumed_len}  (recovered to the full run)')\nprint('best()          :', resumed_best)\n</pre> resumed = meta.resume(run_id, storage=meta.Storage.durable(path_b), registry=components.registry) resumed_best = resumed.best().value resumed_len = len(meta.Storage.durable(path_b).events.read(run_id)) print(f'events on disk  : {resumed_len}  (recovered to the full run)') print('best()          :', resumed_best) <pre>events on disk  : 49  (recovered to the full run)\nbest()          : 6\n</pre> In\u00a0[7]: Copied! <pre>assert resumed.id == run_id\nassert resumed_best == reference_best\nok = resumed_best == reference_best\nprint(f'interrupted == uninterrupted: {ok}  ({resumed_best} == {reference_best})')\nprint(f'partial stream {crashed_len} events -&gt; recovered {resumed_len} events -&gt; best {resumed_best}')\n</pre> assert resumed.id == run_id assert resumed_best == reference_best ok = resumed_best == reference_best print(f'interrupted == uninterrupted: {ok}  ({resumed_best} == {reference_best})') print(f'partial stream {crashed_len} events -&gt; recovered {resumed_len} events -&gt; best {resumed_best}') <pre>interrupted == uninterrupted: True  (6 == 6)\npartial stream 25 events -&gt; recovered 49 events -&gt; best 6\n</pre> <p>What this proves. The crash left a genuine partial durable stream; <code>resume</code> replayed the committed prefix, found the recovery frontier, re-drove the deterministic search from exactly there, and reached the identical <code>best()</code> \u2014 with a byte-identical final event stream. That is the M4 durable-runs guarantee.</p> <p>The same guarantee holds for runs that use M3 policy-pushed context and pull experience (see <code>tests/test_resume_context.py</code> / <code>tests/test_resume_experience_faults.py</code>).</p> In\u00a0[8]: Copied! <pre>from meta_evolve.application.recovery import classify_frontier\nfrom meta_evolve.application.projections import replay_run_projection\nfrom meta_evolve.domain import ExperienceOperationAttempted\nfrom meta_evolve.errors import ExperienceStoreUnavailable\nimport advanced  # context/experience components next to main.py\n# Include both coordinator and audited-reader crash boundaries.\nCRASH = (SimulatedCrash, ExperienceStoreUnavailable)\ndef n_batches(shape, make_components):\n    s = meta.Storage.durable(Path(tempfile.mkdtemp()) / 'c')\n    counter = install_crash(s, n=10_000)  # unreachable: only counts\n    shape(s, make_components())\n    return counter.commits\ndef sweep(shape, make_components, adir):\n    \"\"\"Crash after EVERY committed boundary K; resume each; return (#boundaries,\n    all-equivalent?, best, #mid-attempt-rollforwards).\"\"\"\n    ref_components = make_components()\n    ref = shape(meta.Storage.durable(adir / 'ref'), ref_components)\n    rid, best = ref.id, ref.best().value\n    ev = meta.Storage.durable(adir / 'ref').events.read(rid)\n    total = n_batches(shape, make_components)\n    all_ok, rollfwd = True, 0\n    for k in range(1, total):\n        p = adir / f'k{k}'\n        # Install and run on one handle; resume from a freshly opened handle.\n        s = meta.Storage.durable(p)\n        install_crash(s, n=k)\n        try:\n            shape(s, make_components())\n        except CRASH:\n            pass\n        crashed = meta.Storage.durable(p).events.read(rid)\n        if crashed and isinstance(crashed[-1].body, ExperienceOperationAttempted):\n            if replay_run_projection(rid, crashed).resolution_for(crashed[-1].body.operation_id) is None:\n                rollfwd += 1\n        registry = make_components().registry\n        r = meta.resume(rid, storage=meta.Storage.durable(p), registry=registry)\n        if not (r.best().value == best and meta.Storage.durable(p).events.read(rid) == ev):\n            all_ok = False\n    return rid, ev, total - 1, all_ok, best, rollfwd\n</pre> from meta_evolve.application.recovery import classify_frontier from meta_evolve.application.projections import replay_run_projection from meta_evolve.domain import ExperienceOperationAttempted from meta_evolve.errors import ExperienceStoreUnavailable import advanced  # context/experience components next to main.py # Include both coordinator and audited-reader crash boundaries. CRASH = (SimulatedCrash, ExperienceStoreUnavailable) def n_batches(shape, make_components):     s = meta.Storage.durable(Path(tempfile.mkdtemp()) / 'c')     counter = install_crash(s, n=10_000)  # unreachable: only counts     shape(s, make_components())     return counter.commits def sweep(shape, make_components, adir):     \"\"\"Crash after EVERY committed boundary K; resume each; return (#boundaries,     all-equivalent?, best, #mid-attempt-rollforwards).\"\"\"     ref_components = make_components()     ref = shape(meta.Storage.durable(adir / 'ref'), ref_components)     rid, best = ref.id, ref.best().value     ev = meta.Storage.durable(adir / 'ref').events.read(rid)     total = n_batches(shape, make_components)     all_ok, rollfwd = True, 0     for k in range(1, total):         p = adir / f'k{k}'         # Install and run on one handle; resume from a freshly opened handle.         s = meta.Storage.durable(p)         install_crash(s, n=k)         try:             shape(s, make_components())         except CRASH:             pass         crashed = meta.Storage.durable(p).events.read(rid)         if crashed and isinstance(crashed[-1].body, ExperienceOperationAttempted):             if replay_run_projection(rid, crashed).resolution_for(crashed[-1].body.operation_id) is None:                 rollfwd += 1         registry = make_components().registry         r = meta.resume(rid, storage=meta.Storage.durable(p), registry=registry)         if not (r.best().value == best and meta.Storage.durable(p).events.read(rid) == ev):             all_ok = False     return rid, ev, total - 1, all_ok, best, rollfwd In\u00a0[9]: Copied! <pre>adir = Path(tempfile.mkdtemp())\nrid_a, ev_a, boundaries, all_ok, best_a, _ = sweep(main.start_run, main.build_components, adir)\nprint(f'plain run: {boundaries} interior boundaries, all equivalent = {all_ok}, best = {best_a}')\n</pre> adir = Path(tempfile.mkdtemp()) rid_a, ev_a, boundaries, all_ok, best_a, _ = sweep(main.start_run, main.build_components, adir) print(f'plain run: {boundaries} interior boundaries, all equivalent = {all_ok}, best = {best_a}') <pre>plain run: 22 interior boundaries, all equivalent = True, best = 6\n</pre> In\u00a0[10]: Copied! <pre>phases = {}\ntotal_a = boundaries + 1\nfor k in range(1, total_a):\n    p = adir / f'phase{k}'\n    s = meta.Storage.durable(p)  # install + run on one handle (see sweep note)\n    install_crash(s, n=k)\n    try:\n        main.start_run(s, main.build_components())\n    except CRASH:\n        pass\n    events = meta.Storage.durable(p).events.read(rid_a)\n    phase = classify_frontier(events, replay_run_projection(rid_a, events)).name\n    phases.setdefault(phase, []).append(k)\nfor name, ks in phases.items():\n    print(f'  {name:&lt;18} at boundaries {ks}')\n</pre> phases = {} total_a = boundaries + 1 for k in range(1, total_a):     p = adir / f'phase{k}'     s = meta.Storage.durable(p)  # install + run on one handle (see sweep note)     install_crash(s, n=k)     try:         main.start_run(s, main.build_components())     except CRASH:         pass     events = meta.Storage.durable(p).events.read(rid_a)     phase = classify_frontier(events, replay_run_projection(rid_a, events)).name     phases.setdefault(phase, []).append(k) for name, ks in phases.items():     print(f'  {name:&lt;18} at boundaries {ks}') <pre>  DECISION_READY     at boundaries [1, 4, 7, 10, 13, 16, 19, 22]\n  TRIAL_DECIDED      at boundaries [2, 5, 8, 11, 14, 17, 20]\n  EVALUATION_PENDING at boundaries [3, 6, 9, 12, 15, 18, 21]\n</pre> In\u00a0[11]: Copied! <pre>cold = meta.Run(rid_a, meta.Storage.durable(adir / 'ref'))\nprint('best() from a cold store :', cold.best().value)\nprint('winning lineage steps    :', [t.logical_step for t in cold.lineage()])\n</pre> cold = meta.Run(rid_a, meta.Storage.durable(adir / 'ref')) print('best() from a cold store :', cold.best().value) print('winning lineage steps    :', [t.logical_step for t in cold.lineage()]) <pre>best() from a cold store : 6\nwinning lineage steps    : [0, 1, 2, 3, 4, 5, 6]\n</pre> In\u00a0[12]: Copied! <pre>_, _, cb, cok, cbest, _ = sweep(advanced.context_run, advanced.build_components, Path(tempfile.mkdtemp()))\nprint(f'context run: {cb} boundaries, all equivalent = {cok}, best = {cbest}')\n</pre> _, _, cb, cok, cbest, _ = sweep(advanced.context_run, advanced.build_components, Path(tempfile.mkdtemp())) print(f'context run: {cb} boundaries, all equivalent = {cok}, best = {cbest}') <pre>context run: 29 boundaries, all equivalent = True, best = 6\n</pre> In\u00a0[13]: Copied! <pre>_, _, eb, eok, ebest, rollfwd = sweep(advanced.experience_run, advanced.build_components, Path(tempfile.mkdtemp()))\nprint(f'experience run: {eb} boundaries, all equivalent = {eok}, best = {ebest}')\nprint(f'  mid-operation crash boundaries rolled forward on resume: {rollfwd}')\n</pre> _, _, eb, eok, ebest, rollfwd = sweep(advanced.experience_run, advanced.build_components, Path(tempfile.mkdtemp())) print(f'experience run: {eb} boundaries, all equivalent = {eok}, best = {ebest}') print(f'  mid-operation crash boundaries rolled forward on resume: {rollfwd}') <pre>experience run: 43 boundaries, all equivalent = True, best = 6\n  mid-operation crash boundaries rolled forward on resume: 7\n</pre> In\u00a0[14]: Copied! <pre>again_components = main.build_components()\nagain = meta.resume(rid_a, storage=meta.Storage.durable(adir / 'ref'), registry=again_components.registry)\nprint('re-resume best()   :', again.best().value)\nprint('stream unchanged   :', meta.Storage.durable(adir / 'ref').events.read(rid_a) == ev_a)\n</pre> again_components = main.build_components() again = meta.resume(rid_a, storage=meta.Storage.durable(adir / 'ref'), registry=again_components.registry) print('re-resume best()   :', again.best().value) print('stream unchanged   :', meta.Storage.durable(adir / 'ref').events.read(rid_a) == ev_a) <pre>re-resume best()   : 6\nstream unchanged   : True\n</pre>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#crash-recovery-for-durable-runs-metaresume","title":"Crash recovery for durable runs (<code>meta.resume</code>)\u00b6","text":"<p>The central promise of a durable Meta-Evolve run: a process that dies partway through loses no committed work, and once <code>resume</code>-d reaches the exact result an uninterrupted run would have reached.</p> <p>Interrupted is logically equivalent to uninterrupted.</p> <p>This notebook walks the lifecycle end to end:</p> <ol> <li>Run a durable-local run to completion, uninterrupted, on path A \u2014 its <code>best()</code> is the reference.</li> <li>Start the same run on path B, but simulate a process crash after a few committed batches.</li> <li>Reopen path B and <code>resume</code> the run.</li> <li>Assert the resumed <code>best()</code> equals the uninterrupted reference.</li> </ol> <p>Key concepts: atomic batch commits (a crash leaves only whole batches, never a half-written one), the recovery frontier (<code>resume</code> replays the committed prefix and continues deterministically from the last decided step), and deterministic run identity (the run id is derived from the inputs, so the run on path B shares path A's id and <code>resume</code> can address it).</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#components-imported-from-the-example-module","title":"Components \u2014 imported from the example module\u00b6","text":"<p><code>durable-local</code> mode requires registered, source-digestible, module-level functions so <code>resume</code> can verify the components it replays are byte-for-byte the ones recorded. Functions defined inside a notebook cell have no resolvable source file, so durable-local would reject them (<code>CapabilityError</code>). We therefore import the real components from <code>examples/operations/crash_recovery/main.py</code> (an importable module) and show their source below \u2014 the notebook orchestrates the lifecycle with the example's own building blocks.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#simulating-a-crash-you-never-write-this","title":"Simulating a crash \u2014 you never write this\u00b6","text":"<p>A real crash (power loss, <code>kill -9</code>, OOM) needs no code; the OS/hardware supplies it. <code>CrashAfter</code> exists only so a deterministic notebook can stand in for one \u2014 in production you never author it or touch <code>storage._commit_strategy</code>.</p> <p>A faithful crash never truncates the append-only store: it just stops committing after some whole batch <code>n</code>, so the <code>n</code> committed batches on disk are exactly what an uninterrupted run would have written, with no partial batch.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#1-uninterrupted-reference-run-on-path-a","title":"(1) Uninterrupted reference run on path A\u00b6","text":"<p>A counting wrapper with an unreachable threshold rides along to tally path A's own committed batches.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#2-start-the-same-run-on-path-b-then-crash-mid-way","title":"(2) Start the same run on path B \u2014 then crash mid-way\u00b6","text":"<p>The run id matches path A's by construction, so we can <code>resume</code> it by id.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#3-reopen-path-b-and-resumerun_id","title":"(3) Reopen path B and <code>resume(run_id, ...)</code>\u00b6","text":"<p>No crash wrapper this time \u2014 a fresh durable handle on the same path, exactly as a restarted process would.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#4-the-payoff-interrupted-uninterrupted","title":"(4) The payoff \u2014 interrupted == uninterrupted\u00b6","text":""},{"location":"notebooks/04_crash_recovery/crash_recovery/#advanced-examples","title":"Advanced examples\u00b6","text":"<p>The basic lifecycle resumed from a single mid-run crash. These show the guarantee is total \u2014 resume works from every committed boundary \u2014 and extends to M3 policy-pushed context and pull experience. All components come from <code>main.py</code> / <code>advanced.py</code> (importable + source-digestible, as durable-local requires).</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#a-fault-matrix-resume-from-every-committed-boundary","title":"A. Fault matrix \u2014 resume from every committed boundary\u00b6","text":"<p>Crash after each batch K in turn; each resume reaches the identical <code>best()</code> and a byte-identical final stream.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#b-the-recovery-frontier","title":"B. The recovery frontier\u00b6","text":"<p><code>resume</code> classifies where the crash left the run (via <code>classify_frontier</code>) before continuing deterministically. The phases seen across boundaries:</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#c-cold-rebuild-from-empty-tables","title":"C. Cold rebuild from empty tables\u00b6","text":"<p>A fresh process reopens the durable store; the read model is rebuilt purely from the committed event log \u2014 no in-memory state carried over.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#d-resumable-with-m3-policy-pushed-context-ancestorsonly","title":"D. Resumable with M3 policy-pushed context (<code>AncestorsOnly</code>)\u00b6","text":"<p>The run records a context-policy manifest, its declaration, and each per-trial selection. <code>resume</code> rebuilds the context policy from the registry and reuses the committed selections \u2014 equivalent at every boundary.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#e-resumable-with-m3-pull-experience-pullaccess","title":"E. Resumable with M3 pull experience (<code>PullAccess</code>)\u00b6","text":"<p>Here the proposer reads experience each step, audited as an <code>attempt</code> then a <code>resolution</code> (two atomic batches). A crash between them leaves a trailing unresolved attempt; <code>resume</code> rolls it forward \u2014 recomputing the resolution and reusing the committed operation id \u2014 to a byte-identical stream.</p>"},{"location":"notebooks/04_crash_recovery/crash_recovery/#f-resume-is-safe-to-re-run","title":"F. Resume is safe to re-run\u00b6","text":"<p>Resuming an already-complete run is a no-op returning the same result. (Two processes racing to resume one crashed run is also safe: exactly one commits and the other gets <code>EventStreamConflict</code> \u2014 never a double-commit or partial write. See <code>tests/test_resume_faults.py</code>.)</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/","title":"Milestone 5 tour: immutable source, external evidence","text":"In\u00a0[1]: Copied! <pre>from pathlib import Path\nfrom shutil import copyfile\nfrom tempfile import TemporaryDirectory\nimport sys\n\nimport meta_evolve as meta\n\nexample = Path.cwd()\nif not (example / \"candidate\").is_dir():\n    example = example / \"examples\" / \"05_python_function\"\ntemporary = TemporaryDirectory()\nevidence = Path(temporary.name)\nstaged = evidence / \"candidate\"\nstaged.mkdir()\ncopyfile(example / \"candidate\" / \"clamp.py\", staged / \"clamp.py\")\nsource = meta.load_source_tree(staged)\nprint(\"loaded immutable source:\", tuple(source) == (\"clamp.py\",))\n</pre> from pathlib import Path from shutil import copyfile from tempfile import TemporaryDirectory import sys  import meta_evolve as meta  example = Path.cwd() if not (example / \"candidate\").is_dir():     example = example / \"examples\" / \"05_python_function\" temporary = TemporaryDirectory() evidence = Path(temporary.name) staged = evidence / \"candidate\" staged.mkdir() copyfile(example / \"candidate\" / \"clamp.py\", staged / \"clamp.py\") source = meta.load_source_tree(staged) print(\"loaded immutable source:\", tuple(source) == (\"clamp.py\",))  <pre>loaded immutable source: True\n</pre> In\u00a0[2]: Copied! <pre>FIXED = \"\"\"\\\ndef clamp(value, low, high):\n    return max(low, min(value, high))\n\"\"\"\n\ndef repair(tree, context):\n    return meta.TextPatch(\n        path=\"clamp.py\",\n        expected=tree[\"clamp.py\"],\n        replacement=FIXED,\n    )\n\npatch = repair(source, None)\nprint(\"repair path:\", patch.path)\nprint(\"repair matches parent:\", patch.expected == source[\"clamp.py\"])\nprint(\"repair changes content:\", patch.replacement != patch.expected)\n</pre> FIXED = \"\"\"\\ def clamp(value, low, high):     return max(low, min(value, high)) \"\"\"  def repair(tree, context):     return meta.TextPatch(         path=\"clamp.py\",         expected=tree[\"clamp.py\"],         replacement=FIXED,     )  patch = repair(source, None) print(\"repair path:\", patch.path) print(\"repair matches parent:\", patch.expected == source[\"clamp.py\"]) print(\"repair changes content:\", patch.replacement != patch.expected)  <pre>repair path: clamp.py\nrepair matches parent: True\nrepair changes content: True\n</pre> In\u00a0[3]: Copied! <pre>evaluator = meta.CommandEvaluator(\n    command=meta.ProcessSpec(\n        argv=(sys.executable, str(example / \"run_grader.py\"),\n              str(example / \"grader.py\")),\n        environment={\"PYTHONDONTWRITEBYTECODE\": \"1\"},\n    ),\n    environment=meta.EvaluationEnvironment(\n        name=\"python-clamp-tests\",\n        version=\"1\",\n    ),\n)\nprint(\"evaluator:\", type(evaluator).__name__)\nprint(\"environment: python-clamp-tests@1\")\n</pre> evaluator = meta.CommandEvaluator(     command=meta.ProcessSpec(         argv=(sys.executable, str(example / \"run_grader.py\"),               str(example / \"grader.py\")),         environment={\"PYTHONDONTWRITEBYTECODE\": \"1\"},     ),     environment=meta.EvaluationEnvironment(         name=\"python-clamp-tests\",         version=\"1\",     ), ) print(\"evaluator:\", type(evaluator).__name__) print(\"environment: python-clamp-tests@1\")  <pre>evaluator: CommandEvaluator\nenvironment: python-clamp-tests@1\n</pre> In\u00a0[4]: Copied! <pre>budget = meta.Budget(evaluations=2, trials=1)\ntask = meta.Task(\n    evaluator=evaluator,\n    artifact=meta.SourceTree,\n    budget=budget,\n)\nprint(f\"budget: evaluations={budget.evaluations} trials={budget.trials}\")\n</pre> budget = meta.Budget(evaluations=2, trials=1) task = meta.Task(     evaluator=evaluator,     artifact=meta.SourceTree,     budget=budget, ) print(f\"budget: evaluations={budget.evaluations} trials={budget.trials}\") <pre>budget: evaluations=2 trials=1\n</pre> In\u00a0[5]: Copied! <pre>history = evidence / \"history\"\nwith meta.Storage.durable(history) as storage:\n    experiment = meta.Experiment(\n        task=task,\n        seed=source,\n        proposer=meta.PatchProposer(repair),\n        search=meta.Greedy(max_trials=1),\n    )\n    result = meta.run(experiment, storage=storage, mode=\"local\")\n    best = result.best()\n    lineage = result.lineage()\nprint(\"local run complete:\", len(lineage) == 2)\n</pre> history = evidence / \"history\" with meta.Storage.durable(history) as storage:     experiment = meta.Experiment(         task=task,         seed=source,         proposer=meta.PatchProposer(repair),         search=meta.Greedy(max_trials=1),     )     result = meta.run(experiment, storage=storage, mode=\"local\")     best = result.best()     lineage = result.lineage() print(\"local run complete:\", len(lineage) == 2) <pre>local run complete: True\n</pre> In\u00a0[6]: Copied! <pre>digests = [step.artifact.digest for step in lineage]\nscores = [step.metrics[\"score\"] for step in lineage]\nexits = [\n    next(item.data[\"exit_code\"] for item in step.evidence\n         if item.kind == \"process-result\")\n    for step in lineage\n]\nfailures = [type(step.failure).__name__ if step.failure else None for step in lineage]\nkinds = [[item.kind for item in step.evidence] for step in lineage]\nprint(\"source digests:\", digests)\nprint(\"scores:\", scores)\nprint(\"process exits:\", exits)\nprint(\"failures:\", failures)\nprint(\"evidence kinds:\", kinds)\nprint(\"repaired best:\", best.value[\"clamp.py\"] == FIXED)\nprint(\"parent unchanged:\", source[\"clamp.py\"] != FIXED)\nprint(\"lineage length:\", len(lineage))\n</pre> digests = [step.artifact.digest for step in lineage] scores = [step.metrics[\"score\"] for step in lineage] exits = [     next(item.data[\"exit_code\"] for item in step.evidence          if item.kind == \"process-result\")     for step in lineage ] failures = [type(step.failure).__name__ if step.failure else None for step in lineage] kinds = [[item.kind for item in step.evidence] for step in lineage] print(\"source digests:\", digests) print(\"scores:\", scores) print(\"process exits:\", exits) print(\"failures:\", failures) print(\"evidence kinds:\", kinds) print(\"repaired best:\", best.value[\"clamp.py\"] == FIXED) print(\"parent unchanged:\", source[\"clamp.py\"] != FIXED) print(\"lineage length:\", len(lineage))  <pre>source digests: ['659bfe4a9f389018a5059f9045d6db37592c017591629d6baf5a7ab52792ca53', '08abe248cf356c0ee0322aa9329598221640460d6cf0202228097d7f4c4c21c0']\nscores: [0.0, 1.0]\nprocess exits: [1, 0]\nfailures: [None, None]\nevidence kinds: [['evaluation-environment', 'process-result'], ['evaluation-environment', 'process-result']]\nrepaired best: True\nparent unchanged: True\nlineage length: 2\n</pre> In\u00a0[7]: Copied! <pre>with meta.Storage.durable(history) as reopened:\n    durable = meta.Run(result.id, reopened)\n    print(\"reopened best:\", durable.best() == best)\n    print(\"reopened lineage:\", durable.lineage() == lineage)\n    try:\n        meta.resume(result.id, storage=reopened, registry=meta.Registry())\n    except meta.CapabilityError as error:\n        if \"not resumable\" not in str(error):\n            raise\n        print(\"cold resume: CapabilityError (local run is not resumable)\")\n    else:\n        raise RuntimeError(\"local workspace run unexpectedly resumed\")\n</pre> with meta.Storage.durable(history) as reopened:     durable = meta.Run(result.id, reopened)     print(\"reopened best:\", durable.best() == best)     print(\"reopened lineage:\", durable.lineage() == lineage)     try:         meta.resume(result.id, storage=reopened, registry=meta.Registry())     except meta.CapabilityError as error:         if \"not resumable\" not in str(error):             raise         print(\"cold resume: CapabilityError (local run is not resumable)\")     else:         raise RuntimeError(\"local workspace run unexpectedly resumed\") <pre>reopened best: True\nreopened lineage: True\ncold resume: CapabilityError (local run is not resumable)\n</pre> In\u00a0[8]: Copied! <pre>broken_evaluator = meta.CommandEvaluator(\n    command=meta.ProcessSpec(\n        argv=(sys.executable, str(example / \"run_grader.py\"),\n              str(example / \"missing-grader.py\")),\n        environment={\"PYTHONDONTWRITEBYTECODE\": \"1\"},\n    ),\n    environment=meta.EvaluationEnvironment(name=\"python-clamp-tests\", version=\"1\"),\n)\nfault = broken_evaluator(source)\nprocess = next(item for item in fault.evidence if item.kind == \"process-result\")\nprint(\"grader fault:\", type(fault.failure).__name__)\nprint(\"grader fault rankable:\", bool(fault.metrics))\nprint(\"grader fault process exit:\", process.data[\"exit_code\"])\nprint(\"grader fault evidence kinds:\", [item.kind for item in fault.evidence])\ntemporary.cleanup()\nprint(\"temporary history disposed:\", True)\n</pre> broken_evaluator = meta.CommandEvaluator(     command=meta.ProcessSpec(         argv=(sys.executable, str(example / \"run_grader.py\"),               str(example / \"missing-grader.py\")),         environment={\"PYTHONDONTWRITEBYTECODE\": \"1\"},     ),     environment=meta.EvaluationEnvironment(name=\"python-clamp-tests\", version=\"1\"), ) fault = broken_evaluator(source) process = next(item for item in fault.evidence if item.kind == \"process-result\") print(\"grader fault:\", type(fault.failure).__name__) print(\"grader fault rankable:\", bool(fault.metrics)) print(\"grader fault process exit:\", process.data[\"exit_code\"]) print(\"grader fault evidence kinds:\", [item.kind for item in fault.evidence]) temporary.cleanup() print(\"temporary history disposed:\", True)  <pre>grader fault: EvaluatorFailure\ngrader fault rankable: False\ngrader fault process exit: 2\ngrader fault evidence kinds: ['evaluation-environment', 'process-result']\ntemporary history disposed: True\n</pre>"},{"location":"notebooks/05_python_function/milestone_5_tour/#milestone-5-tour-immutable-source-external-evidence","title":"Milestone 5 tour: immutable source, external evidence\u00b6","text":"<p>This tour follows the canonical clamp repair in six small steps. The declaration path stays at source, patch, evaluator, budget, and search vocabulary. Returned lineage entries provide optional inspection after the run.</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/#1-load-immutable-source","title":"1. Load immutable source\u00b6","text":"<p>The source tree contains exact UTF-8 text. The tour stages only the declared tracked <code>candidate/clamp.py</code> bytes into its temporary evidence directory before loading. This isolates the tour from checkout-generated <code>__pycache__</code> without adding ignore rules: production <code>load_source_tree</code> still rejects every undeclared file.</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/#2-declare-the-exact-file-repair","title":"2. Declare the exact-file repair\u00b6","text":"<p>The patch names one file, commits the parent text it expects, and supplies its replacement. A conflict, invalid path, or unchanged replacement is a typed proposal outcome.</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/#3-declare-the-independent-evaluator","title":"3. Declare the independent evaluator\u00b6","text":"<p>The grader remains outside the candidate tree, and the fixed <code>run_grader.py</code> owns the exit protocol: exit <code>0</code> passes, exit <code>1</code> is a rankable task failure, and any other exit is a typed evaluator failure.</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/#4-declare-one-budget","title":"4. Declare one budget\u00b6","text":"<p>Two evaluations cover the broken seed and one successor. The same budget is passed to the task and run.</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/#5-run-one-local-greedy-step","title":"5. Run one local Greedy step\u00b6","text":"<p>Proposal and evaluation each receive a fresh workspace. Durable storage retains the committed history, while the live local callbacks remain non-portable.</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/#6-inspect-the-result-and-durable-boundary","title":"6. Inspect the result and durable boundary\u00b6","text":"<p>The public lineage query is progressive disclosure: it returns immutable lineage entries for inspection without putting kernel construction types into the declaration story. Content digests and semantic outcomes are deterministic; observed process wall time is intentionally omitted.</p>"},{"location":"notebooks/05_python_function/milestone_5_tour/#failure-boundary-a-broken-grader-is-not-a-low-score","title":"Failure boundary: a broken grader is not a low score\u00b6","text":"<p>Candidate-caused breakage remains the rankable score <code>0</code> seen above. This separate probe removes the grader itself: runner exit <code>2</code> becomes <code>EvaluatorFailure</code>, with no metric to rank.</p>"},{"location":"notebooks/18_sealqa_research_labs/01_feedback_descent/","title":"Feedback Descent lab","text":"<p>Terminology. A candidate is a <code>meta.Text</code> solver prompt. A pairwise evaluator chooses between two answers and emits textual feedback; an editor derives one successor prompt from that evidence. This is a small teaching implementation inspired by Feedback Descent, not a reproduction of its results or an official SealQA evaluation.</p> In\u00a0[\u00a0]: Copied! <pre>from pathlib import Path\nimport os, sys\n\nroot = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists())\nlab = root / \"examples/research/sealqa_labs\"\nif str(lab) not in sys.path:\n    sys.path.insert(0, str(lab))\n\nfrom sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.config import *\n\nprint({\n    \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,\n    \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,\n    \"opt_in\": f\"{LIVE_OPT_IN}=1\",\n})\n</pre> from pathlib import Path import os, sys  root = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists()) lab = root / \"examples/research/sealqa_labs\" if str(lab) not in sys.path:     sys.path.insert(0, str(lab))  from sealqa_labs import UserSdkSessionRunner from sealqa_labs.config import *  print({     \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,     \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,     \"opt_in\": f\"{LIVE_OPT_IN}=1\", })  In\u00a0[\u00a0]: Copied! <pre>if os.getenv(LIVE_OPT_IN) != \"1\":\n    raise RuntimeError(\n        f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"\n    )\n\nfrom sealqa_labs.feedback_descent import execute\n\nraise RuntimeError(\n    \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\"\n)\nprint(\"sessions executed:\", len(result.sessions))\nprint(\"usage:\", result.usage)\n</pre> if os.getenv(LIVE_OPT_IN) != \"1\":     raise RuntimeError(         f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"     )  from sealqa_labs.feedback_descent import execute  raise RuntimeError(     \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\" ) print(\"sessions executed:\", len(result.sessions)) print(\"usage:\", result.usage)  In\u00a0[\u00a0]: Copied! <pre>for session in result.sessions:\n    print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\"))\nprint(\"artifacts:\", result.artifacts)\nprint(\"measurements:\", result.measurements)\n</pre> for session in result.sessions:     print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\")) print(\"artifacts:\", result.artifacts) print(\"measurements:\", result.measurements)"},{"location":"notebooks/18_sealqa_research_labs/01_feedback_descent/#feedback-descent-lab","title":"Feedback Descent lab\u00b6","text":"<p>SealQA question: Who holds the all-time record at the Grammys for the most wins in the album of the year category?</p>"},{"location":"notebooks/18_sealqa_research_labs/01_feedback_descent/#what-changes","title":"What changes\u00b6","text":"<p>Only the solver prompt changes. The frozen question and documents, hidden exact answer, declarations, and evaluator remain outside candidate authority. The path is two initial solves, one structured comparison, one feedback-derived edit, and one edited-prompt solve: 5 live sessions.</p>"},{"location":"notebooks/18_sealqa_research_labs/01_feedback_descent/#live-declaration","title":"Live declaration\u00b6","text":"<p>Bring your own user-owned SDK wrapper and identify its behavior-affecting declaration. Set <code>META_EVOLVE_LIVE_SDK=1</code> only after authenticating it and reviewing its session, tool, permission, and spending limits. Every inner run declares <code>INNER_BUDGET.wall_seconds = 1860</code>; the wrapper must honor that bound or refuse preflight. These calls may incur cost.</p>"},{"location":"notebooks/18_sealqa_research_labs/01_feedback_descent/#visible-record","title":"Visible record\u00b6","text":"<p>The next cell exposes prompt artifacts, the preferred candidate, textual feedback, edit lineage, typed failures, exact-answer measurements, and aggregate usage. Malformed comparison output, a missing edit, timeout, or provider failure has no metric.</p>"},{"location":"notebooks/18_sealqa_research_labs/01_feedback_descent/#live-observation","title":"Live observation\u00b6","text":"<p>No official provider capture is pending. A user may perform one authorized run with a declared wrapper. Any published capture must keep the single outcome\u2014including a tie, regression, or typed failure\u2014and sanitize credentials without rerunning for improvement.</p>"},{"location":"notebooks/18_sealqa_research_labs/01_feedback_descent/#audit-and-exercise","title":"Audit and exercise\u00b6","text":"<ul> <li>Did the editor see only the preferred prompt and textual feedback?</li> <li>Did the evaluator-only answer enter any solver or editor request?</li> <li>Is one edited evaluation enough to claim a robust improvement?</li> </ul> <p>Change and predict: remove the instruction to ignore distractors from candidate B. Predict the preference, feedback, and final exact score before rerunning.</p>"},{"location":"notebooks/18_sealqa_research_labs/02_autodata/","title":"AutoData lab","text":"<p>Terminology. The evolving <code>meta.Text</code> is a data-generation recipe. A <code>restricted_solver</code> sees only the generated question; an <code>informed_solver</code> sees the same question plus selected frozen evidence. The paper describes weak/strong models; this lab uses one model under different contexts. Inspired by AutoData.</p> In\u00a0[\u00a0]: Copied! <pre>from pathlib import Path\nimport os, sys\n\nroot = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists())\nlab = root / \"examples/research/sealqa_labs\"\nif str(lab) not in sys.path:\n    sys.path.insert(0, str(lab))\n\nfrom sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.config import *\n\nprint({\n    \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,\n    \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,\n    \"opt_in\": f\"{LIVE_OPT_IN}=1\",\n})\n</pre> from pathlib import Path import os, sys  root = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists()) lab = root / \"examples/research/sealqa_labs\" if str(lab) not in sys.path:     sys.path.insert(0, str(lab))  from sealqa_labs import UserSdkSessionRunner from sealqa_labs.config import *  print({     \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,     \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,     \"opt_in\": f\"{LIVE_OPT_IN}=1\", })  In\u00a0[\u00a0]: Copied! <pre>if os.getenv(LIVE_OPT_IN) != \"1\":\n    raise RuntimeError(\n        f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"\n    )\n\nfrom sealqa_labs.autodata import execute\n\nraise RuntimeError(\n    \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\"\n)\nprint(\"sessions executed:\", len(result.sessions))\nprint(\"usage:\", result.usage)\n</pre> if os.getenv(LIVE_OPT_IN) != \"1\":     raise RuntimeError(         f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"     )  from sealqa_labs.autodata import execute  raise RuntimeError(     \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\" ) print(\"sessions executed:\", len(result.sessions)) print(\"usage:\", result.usage)  In\u00a0[\u00a0]: Copied! <pre>for session in result.sessions:\n    print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\"))\nprint(\"artifacts:\", result.artifacts)\nprint(\"measurements:\", result.measurements)\n</pre> for session in result.sessions:     print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\")) print(\"artifacts:\", result.artifacts) print(\"measurements:\", result.measurements)"},{"location":"notebooks/18_sealqa_research_labs/02_autodata/#autodata-lab","title":"AutoData lab\u00b6","text":"<p>SealQA question: What is the current age of the oldest person to sail solo across the Pacific Ocean?</p>"},{"location":"notebooks/18_sealqa_research_labs/02_autodata/#what-changes","title":"What changes\u00b6","text":"<p>Only the recipe changes. Each recipe uses a generator and two solver sessions, followed by local schema, answerability, leakage, and normalized exact-answer checks. One rewrite separates the two evaluations: 3 + 1 + 3 = 7 sessions. Same-model/different-context is a teaching simplification and a confounder.</p>"},{"location":"notebooks/18_sealqa_research_labs/02_autodata/#live-declaration","title":"Live declaration\u00b6","text":"<p>Bring your own user-owned SDK wrapper and identify its behavior-affecting declaration. Set <code>META_EVOLVE_LIVE_SDK=1</code> only after authenticating it and reviewing its session, tool, permission, and spending limits. Every inner run declares <code>INNER_BUDGET.wall_seconds = 1860</code>; the wrapper must honor that bound or refuse preflight. These calls may incur cost.</p>"},{"location":"notebooks/18_sealqa_research_labs/02_autodata/#visible-record","title":"Visible record\u00b6","text":"<p>Generated items, both solver answers, acceptance decisions, rejection reasons, recipe lineage, typed failures, and usage remain inspectable. A session failure is unrankable; it is never converted to a rejected example with score zero.</p>"},{"location":"notebooks/18_sealqa_research_labs/02_autodata/#live-observation","title":"Live observation\u00b6","text":"<p>No official provider capture is pending. A user may perform one authorized run with a declared wrapper. Preserve rejection or a successor that is no better; do not rerun until a generated item happens to pass.</p>"},{"location":"notebooks/18_sealqa_research_labs/02_autodata/#audit-and-exercise","title":"Audit and exercise\u00b6","text":"<ul> <li>Does the restricted solver workspace contain evidence?</li> <li>Can the informed solver's context advantage be confused with model strength?</li> <li>Does the generated reference answer leak verbatim into its question?</li> </ul> <p>Change and predict: require two evidence document IDs. Predict which deterministic check changes and whether acceptance becomes easier or harder.</p>"},{"location":"notebooks/18_sealqa_research_labs/03_meta_harness_precursor/","title":"Meta-Harness precursor lab","text":"<p>Terminology. A local harness is a shipped <code>meta.SourceTree</code> containing only <code>planner.md</code> and <code>context_policy.json</code>. This is a superseded pre-M8 teaching precursor, not the M8 API. Its historical claims remain scoped to this local experiment; use the M8 harness guide for the current typed boundary.</p> In\u00a0[\u00a0]: Copied! <pre>from pathlib import Path\nimport os, sys\n\nroot = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists())\nlab = root / \"examples/research/sealqa_labs\"\nif str(lab) not in sys.path:\n    sys.path.insert(0, str(lab))\n\nfrom sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.config import *\n\nprint({\n    \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,\n    \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,\n    \"opt_in\": f\"{LIVE_OPT_IN}=1\",\n})\n</pre> from pathlib import Path import os, sys  root = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists()) lab = root / \"examples/research/sealqa_labs\" if str(lab) not in sys.path:     sys.path.insert(0, str(lab))  from sealqa_labs import UserSdkSessionRunner from sealqa_labs.config import *  print({     \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,     \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,     \"opt_in\": f\"{LIVE_OPT_IN}=1\", })  In\u00a0[\u00a0]: Copied! <pre>if os.getenv(LIVE_OPT_IN) != \"1\":\n    raise RuntimeError(\n        f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"\n    )\n\nfrom sealqa_labs.meta_harness import execute\n\nraise RuntimeError(\n    \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\"\n)\nprint(\"sessions executed:\", len(result.sessions))\nprint(\"usage:\", result.usage)\n</pre> if os.getenv(LIVE_OPT_IN) != \"1\":     raise RuntimeError(         f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"     )  from sealqa_labs.meta_harness import execute  raise RuntimeError(     \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\" ) print(\"sessions executed:\", len(result.sessions)) print(\"usage:\", result.usage)  In\u00a0[\u00a0]: Copied! <pre>for session in result.sessions:\n    print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\"))\nprint(\"artifacts:\", result.artifacts)\nprint(\"measurements:\", result.measurements)\n</pre> for session in result.sessions:     print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\")) print(\"artifacts:\", result.artifacts) print(\"measurements:\", result.measurements)"},{"location":"notebooks/18_sealqa_research_labs/03_meta_harness_precursor/#meta-harness-precursor-lab","title":"Meta-Harness precursor lab\u00b6","text":"<p>SealQA question: How many NBA players have scored 60 or more points in a regular season game since 2023?</p>"},{"location":"notebooks/18_sealqa_research_labs/03_meta_harness_precursor/#lifecycle-warning","title":"Lifecycle warning\u00b6","text":"<p>This superseded precursor is local-only. It performs one baseline diagnostic, one mutation, and equal baseline/candidate runs on one unseen validation case: 4 sessions. It never promotes a candidate automatically.</p>"},{"location":"notebooks/18_sealqa_research_labs/03_meta_harness_precursor/#what-changes","title":"What changes\u00b6","text":"<p>Only the planner and context policy may change. Evaluation data, permissions, budgets, audit history, evidence, and promotion controls are rejected with a typed policy violation and no successor. Prior source, score, traces, and failures are materialized as read-only diagnostic evidence.</p>"},{"location":"notebooks/18_sealqa_research_labs/03_meta_harness_precursor/#live-declaration","title":"Live declaration\u00b6","text":"<p>Bring your own user-owned SDK wrapper and identify its behavior-affecting declaration. Set <code>META_EVOLVE_LIVE_SDK=1</code> only after authenticating it and reviewing its session, tool, permission, and spending limits. Every inner run declares <code>INNER_BUDGET.wall_seconds = 1860</code>; the wrapper must honor that bound or refuse preflight. These calls may incur cost.</p>"},{"location":"notebooks/18_sealqa_research_labs/03_meta_harness_precursor/#live-observation","title":"Live observation\u00b6","text":"<p>No official provider capture is pending. A user may perform one authorized run with a declared wrapper; keep a tie, regression, typed failure, or improvement as-is. One unseen row is a mechanism demonstration, not promotion evidence.</p>"},{"location":"notebooks/18_sealqa_research_labs/03_meta_harness_precursor/#audit-and-exercise","title":"Audit and exercise\u00b6","text":"<ul> <li>Do both validation arms share the same case and declarations?</li> <li>Did the mutator receive any validation answer or promotion authority?</li> <li>Would a planner change and context-size change be causally distinguishable?</li> </ul> <p>Change and predict: lower <code>max_documents</code> to 2. Predict the baseline/candidate relation before rerunning, then inspect whether the gold evidence was excluded.</p>"},{"location":"notebooks/18_sealqa_research_labs/04_evoskill/","title":"EvoSkill lab","text":"<p>Terminology. Two induction episodes create recorded failures. A failure analyzer reads them through shipped <code>PullAccess</code>; a separate builder emits one Markdown document inside shipped <code>meta.SkillSet</code>. Two untouched rows are held out. Inspired by EvoSkill.</p> In\u00a0[\u00a0]: Copied! <pre>from pathlib import Path\nimport os, sys\n\nroot = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists())\nlab = root / \"examples/research/sealqa_labs\"\nif str(lab) not in sys.path:\n    sys.path.insert(0, str(lab))\n\nfrom sealqa_labs import UserSdkSessionRunner\nfrom sealqa_labs.config import *\n\nprint({\n    \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,\n    \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,\n    \"opt_in\": f\"{LIVE_OPT_IN}=1\",\n})\n</pre> from pathlib import Path import os, sys  root = next(p for p in (Path.cwd(), *Path.cwd().parents) if (p / \"pyproject.toml\").exists()) lab = root / \"examples/research/sealqa_labs\" if str(lab) not in sys.path:     sys.path.insert(0, str(lab))  from sealqa_labs import UserSdkSessionRunner from sealqa_labs.config import *  print({     \"wrapper\": WRAPPER_DECLARATION, \"sessions\": TOTAL_SESSIONS,     \"inner_wall_seconds\": INNER_BUDGET.wall_seconds,     \"opt_in\": f\"{LIVE_OPT_IN}=1\", })  In\u00a0[\u00a0]: Copied! <pre>if os.getenv(LIVE_OPT_IN) != \"1\":\n    raise RuntimeError(\n        f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"\n    )\n\nfrom sealqa_labs.evoskill import execute\n\nraise RuntimeError(\n    \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\"\n)\nprint(\"sessions executed:\", len(result.sessions))\nprint(\"usage:\", result.usage)\n</pre> if os.getenv(LIVE_OPT_IN) != \"1\":     raise RuntimeError(         f\"Live-only lab: set {LIVE_OPT_IN}=1 after authenticating your SDK.\"     )  from sealqa_labs.evoskill import execute  raise RuntimeError(     \"Define build_agent(request), then run(UserSdkSessionRunner(agent_factory=build_agent)).\" ) print(\"sessions executed:\", len(result.sessions)) print(\"usage:\", result.usage)  In\u00a0[\u00a0]: Copied! <pre>for session in result.sessions:\n    print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\"))\nprint(\"artifacts:\", result.artifacts)\nprint(\"measurements:\", result.measurements)\n</pre> for session in result.sessions:     print(session.label, \"failure=\" + (session.failure.kind if session.failure else \"none\")) print(\"artifacts:\", result.artifacts) print(\"measurements:\", result.measurements)"},{"location":"notebooks/18_sealqa_research_labs/04_evoskill/#evoskill-lab","title":"EvoSkill lab\u00b6","text":"<p>SealQA question: How many airlines are members of IATA, the organization that supports airline activity and helps formulate industry policy and standards?</p>"},{"location":"notebooks/18_sealqa_research_labs/04_evoskill/#what-changes","title":"What changes\u00b6","text":"<p>Only the skill augmentation changes. The baseline instructions, model, cases, evaluator, and bounds remain fixed. The path is 2 induction sessions, 1 failure analysis, 1 skill build, and 2 \u00d7 2 held-out comparisons: 8 sessions.</p>"},{"location":"notebooks/18_sealqa_research_labs/04_evoskill/#live-declaration","title":"Live declaration\u00b6","text":"<p>Bring your own user-owned SDK wrapper and identify its behavior-affecting declaration. Set <code>META_EVOLVE_LIVE_SDK=1</code> only after authenticating it and reviewing its session, tool, permission, and spending limits. Every inner run declares <code>INNER_BUDGET.wall_seconds = 1860</code>; the wrapper must honor that bound or refuse preflight. These calls may incur cost.</p>"},{"location":"notebooks/18_sealqa_research_labs/04_evoskill/#visible-record","title":"Visible record\u00b6","text":"<p>The complete recorded induction failures are seed evaluation evidence. Pull-operation counters distinguish offered from used experience. The Markdown skill, procedures, checks, held-out ties/regressions/failures, lineage, and usage remain visible.</p>"},{"location":"notebooks/18_sealqa_research_labs/04_evoskill/#live-observation","title":"Live observation\u00b6","text":"<p>No official provider capture is pending. A user may perform one authorized run with a declared wrapper; preserve the result and state when the analyzer never read the offered failures; unused experience is a result, not a silent fallback.</p>"},{"location":"notebooks/18_sealqa_research_labs/04_evoskill/#audit-and-exercise","title":"Audit and exercise\u00b6","text":"<ul> <li>Are rows 7\u20138 absent from induction and skill building?</li> <li>Do failure records omit evaluator-only answers and source URLs?</li> <li>Are baseline and augmented held-out sessions otherwise compatible?</li> </ul> <p>Change and predict: remove verification checks from the skill-builder instruction. Predict held-out ties, regressions, and pull usage before rerunning.</p>"},{"location":"notebooks/19_autoresearch/autoresearch/","title":"Autoresearch, in miniature","text":"In\u00a0[1]: Copied! <pre>from pathlib import Path\nimport runpy\n\nroot = next(\n    path for path in (Path.cwd(), *Path.cwd().parents)\n    if (path / \"pyproject.toml\").exists()\n)\nlab = runpy.run_path(root / \"examples/research/autoresearch/main.py\")\nprint(\"lab loaded:\", \"run_outer\" in lab)\n</pre> from pathlib import Path import runpy  root = next(     path for path in (Path.cwd(), *Path.cwd().parents)     if (path / \"pyproject.toml\").exists() ) lab = runpy.run_path(root / \"examples/research/autoresearch/main.py\") print(\"lab loaded:\", \"run_outer\" in lab) <pre>lab loaded: True\n</pre> In\u00a0[2]: Copied! <pre>result = lab[\"run_outer\"]()\nprint(lab[\"report\"](result))\n</pre> result = lab[\"run_outer\"]() print(lab[\"report\"](result)) <pre>autoresearch teaching run: outer=(evaluations=3,trials=2) inner=(evaluations=5,trials=4)\nrecipe=(1,) public={'public-large': 0.14285714285714285, 'public-small': 0.3333333333333333} private_quality=0.180556\nrecipe=(1, 2) public={'public-large': 0.2, 'public-small': 1.0} private_quality=0.321429\nrecipe=(1, 2, 4) public={'public-large': 0.3333333333333333, 'public-small': 0.5} private_quality=0.600000\nselected=(1, 2, 4)\nheld_out_baseline={'held-out-small': 0.16666666666666666, 'held-out-large': 0.09090909090909091}\nheld_out_selected={'held-out-small': 0.5, 'held-out-large': 0.14285714285714285}\npromotion=none\n</pre> In\u00a0[3]: Copied! <pre>[\n    (str(trial.artifact.value), tuple(item.kind for item in trial.evidence))\n    for trial in result.trials()\n    if trial.artifact is not None\n]\n</pre> [     (str(trial.artifact.value), tuple(item.kind for item in trial.evidence))     for trial in result.trials()     if trial.artifact is not None ] Out[3]: <pre>[('{\"moves\":[1]}', ('autoresearch-public-report',)),\n ('{\"moves\":[1,2]}', ('autoresearch-public-report',)),\n ('{\"moves\":[1,2,4]}', ('autoresearch-public-report',))]</pre> In\u00a0[4]: Copied! <pre>import meta_evolve as meta\n\noutcome = lab[\"evaluate_researcher\"](meta.Text('{\"moves\":[0]}'))\nprint(\"failure:\", type(outcome.failure).__name__)\nprint(\"metrics:\", dict(outcome.metrics))\n</pre> import meta_evolve as meta  outcome = lab[\"evaluate_researcher\"](meta.Text('{\"moves\":[0]}')) print(\"failure:\", type(outcome.failure).__name__) print(\"metrics:\", dict(outcome.metrics)) <pre>failure: InvalidOutput\nmetrics: {}\n</pre>"},{"location":"notebooks/19_autoresearch/autoresearch/#autoresearch-in-miniature","title":"Autoresearch, in miniature\u00b6","text":"<p>Can an outer Meta-Evolve run improve the recipe used by an inner researcher while private tasks, budgets, and promotion remain outside candidate authority? This notebook executes the deterministic teaching example. The standalone chapter contains the complete explanation.</p>"},{"location":"notebooks/19_autoresearch/autoresearch/#run-the-outer-loop","title":"Run the outer loop\u00b6","text":"<p>The seed and two successors each receive the same four inner benchmark tasks and the same five-evaluation inner budget. Public task scores are evidence; private quality is the selection metric.</p>"},{"location":"notebooks/19_autoresearch/autoresearch/#read-the-evidence-boundary","title":"Read the evidence boundary\u00b6","text":"<p>The proposer derives each edit from a public-report evidence record. Private task details never enter that evidence. Held-out scores are calculated only after <code>result.best()</code> has fixed the selected recipe, and <code>promotion=none</code> is explicit.</p>"},{"location":"notebooks/19_autoresearch/autoresearch/#change-and-predict","title":"Change and predict\u00b6","text":"<p>Reduce <code>INNER_TRIALS</code> from four to two and update <code>INNER_BUDGET</code>. Predict the selected recipe first. If it changes, did the researcher become worse, or did the evaluation choose a different researcher for a different resource regime?</p>"},{"location":"reference/api/","title":"Shipped API reference","text":"<p>Status: Shipped. Every entry on this page is generated from the installed <code>meta_evolve</code> package. The four tiers organize the root imports without changing or hiding them.</p>"},{"location":"reference/api/#beginner-and-expanded-runs","title":"Beginner and expanded runs","text":"<ul> <li><code>improve</code></li> <li><code>Task</code></li> <li><code>Experiment</code></li> <li><code>run</code></li> <li><code>Maximize</code></li> <li><code>Minimize</code></li> <li><code>Greedy</code></li> <li><code>BestOfN</code></li> <li><code>Population</code></li> <li><code>search</code></li> <li><code>Run</code></li> <li><code>TrialView</code></li> <li><code>RunSummary</code></li> <li><code>RunComparison</code></li> <li><code>Usage</code></li> </ul>"},{"location":"reference/api/#artifact-domains-and-external-evaluation","title":"Artifact domains and external evaluation","text":"<ul> <li><code>Text</code></li> <li><code>SourceTree</code></li> <li><code>SkillSet</code></li> <li><code>load_source_tree</code></li> <li><code>ProposalResult</code></li> <li><code>ProposerContext</code></li> <li><code>EvaluationResult</code></li> <li><code>EvidenceDraft</code></li> <li><code>CommandEvaluator</code></li> <li><code>EvaluationEnvironment</code></li> <li><code>ProcessSpec</code></li> <li><code>PatchProposer</code></li> <li><code>PatchResult</code></li> <li><code>TextPatch</code></li> </ul>"},{"location":"reference/api/#experience","title":"Experience","text":"<ul> <li><code>AncestorsOnly</code></li> <li><code>BestSibling</code></li> <li><code>NoMemory</code></li> <li><code>PullAccess</code></li> <li><code>ContextPolicy</code></li> <li><code>ExperienceAccess</code></li> <li><code>ExperienceGraph</code></li> <li><code>ExperienceQuery</code></li> <li><code>ExperienceNode</code></li> <li><code>ExperienceRecord</code></li> <li><code>ExperienceReader</code></li> <li><code>ExperienceState</code></li> <li><code>RunExperienceUsage</code></li> </ul>"},{"location":"reference/api/#inspection-persistence-extensions-and-errors","title":"Inspection, persistence, extensions, and errors","text":"<ul> <li><code>ArtifactView</code></li> <li><code>DecisionView</code></li> <li><code>TrialDecisionView</code></li> <li><code>ArchiveDecisionView</code></li> <li><code>StopDecisionView</code></li> <li><code>EvidenceView</code></li> <li><code>RunInspection</code></li> <li><code>RunOverview</code></li> <li><code>RunDeclaration</code></li> <li><code>Storage</code></li> <li><code>Registry</code></li> <li><code>Session</code></li> <li><code>resume</code></li> <li><code>Budget</code></li> <li><code>ArtifactPresentation</code></li> <li><code>ArtifactDiff</code></li> <li><code>present_artifact</code></li> <li><code>diff_artifacts</code></li> <li><code>artifact_payload_bytes</code></li> <li><code>compare_harnesses</code></li> <li><code>compare_policies</code></li> <li><code>export_harness</code></li> <li><code>load_harness</code></li> <li><code>BudgetUnavailable</code></li> <li><code>CapabilityError</code></li> <li><code>DurableCorruption</code></li> <li><code>DurableStoreError</code></li> <li><code>ExperienceAccessDenied</code></li> <li><code>ExperienceBudgetExhausted</code></li> <li><code>InvalidDecision</code></li> <li><code>NoSuccessfulEvaluation</code></li> <li><code>RunError</code></li> <li><code>RunNotFound</code></li> <li><code>RunsNotComparable</code></li> <li><code>SchemaVersionRejected</code></li> <li><code>UnboundSearchProgram</code></li> </ul>"},{"location":"reference/api/#presentation-records","title":"Presentation records","text":"<p>Only <code>ArtifactPresentation</code> and <code>ArtifactDiff</code> are root-exported records. Their minimal immutable supporting records live in <code>meta_evolve.presentation</code>: <code>PresentationSection</code> groups labelled <code>PresentationField</code> values and <code>PresentationDocument</code> text; each document has an optional path and explicit truncation state. <code>FileDiff</code> carries its path, added/removed/modified/unchanged status, typed <code>DiffLine</code> values with optional old/new line numbers, and an optional refusal or truncation notice. These records contain inert data only, not HTML, SVG, URLs, or trusted markup.</p>"},{"location":"reference/api/#optional-local-browser","title":"Optional local browser","text":"<p>The browser adds no root exports beyond the dependency-free presentation contract above. Install <code>meta-evolve[browser]</code> (or use <code>uv sync --extra browser</code>) and start <code>meta-evolve ui --storage PATH</code>. Its server-rendered routes are documented in the local browser guide.</p>"},{"location":"reference/api/#generated-entries","title":"Generated entries","text":"<p>The root package keeps the simple callable path small: define a task, bound it with a budget, and state what to maximize or minimize. The full durable vocabulary for policy authors and kernel code lives in <code>meta_evolve.domain</code>, the read-only graph query surface used by context policies is root-exported, and canonical serialization lives in <code>meta_evolve.serialization</code>.</p>"},{"location":"reference/api/#meta_evolve.DecisionView","title":"<code>DecisionView = TrialDecisionView | ArchiveDecisionView | StopDecisionView</code>  <code>module-attribute</code>","text":""},{"location":"reference/api/#meta_evolve.ArchiveDecisionView","title":"<code>ArchiveDecisionView</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.ArtifactView","title":"<code>ArtifactView</code>  <code>dataclass</code>","text":"<p>One artifact occurrence with decoded and inert payload projections.</p> <p><code>value</code> retains the established author-facing decoded object.  Safe presentation consumes only the private frozen payload captured beside it. The payload is deliberately absent from repr and equality so adding it does not change the existing public view semantics.</p>"},{"location":"reference/api/#meta_evolve.ArtifactPresentation","title":"<code>ArtifactPresentation</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.ArtifactPresentation.documents","title":"<code>documents</code>  <code>property</code>","text":"<p>All retained documents in presentation order.</p>"},{"location":"reference/api/#meta_evolve.ArtifactDiff","title":"<code>ArtifactDiff</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.AncestorsOnly","title":"<code>AncestorsOnly</code>  <code>dataclass</code>","text":"<p>Push records from accessible ancestors in graph and commit order.</p>"},{"location":"reference/api/#meta_evolve.BestOfN","title":"<code>BestOfN</code>  <code>dataclass</code>","text":"<p>Independently expand the seed for at most <code>max_trials</code> attempts.</p>"},{"location":"reference/api/#meta_evolve.BestOfN.bind","title":"<code>bind(proposer, budget)</code>","text":"<p>Authorize one proposer and derive the unit trial allocation.</p>"},{"location":"reference/api/#meta_evolve.BestSibling","title":"<code>BestSibling</code>  <code>dataclass</code>","text":"<p>Push the best successful prior sibling under the primary objective.</p>"},{"location":"reference/api/#meta_evolve.Budget","title":"<code>Budget</code>  <code>dataclass</code>","text":"<p>Optional ceilings; <code>None</code> means that resource is unbounded.</p>"},{"location":"reference/api/#meta_evolve.BudgetUnavailable","title":"<code>BudgetUnavailable</code>","text":"<p>               Bases: <code>RunError</code></p> <p>The requested action cannot fit within its effective budget.</p>"},{"location":"reference/api/#meta_evolve.CapabilityError","title":"<code>CapabilityError</code>","text":"<p>               Bases: <code>RunError</code></p> <p>A run's declared or requested execution profile is not honored.</p> <p>Raised for unverifiable component implementations (lambdas, closures, <code>functools.partial</code>, decorated wrappers, or callables with no resolvable module source), unregistered <code>(role, name, version)</code> lookups, and durable-local configuration that lacks a registry, durable storage, or a registered evaluator/proposer/policy. Also raised when storage is asked for an optional capability its components do not declare, such as enumerating runs through a non-<code>RunIndex</code> event store.</p>"},{"location":"reference/api/#meta_evolve.ContextPolicy","title":"<code>ContextPolicy</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Select the exact bounded context bundle pushed to one proposer call.</p> <p>Only records committed strictly before the candidate trial's logical step are eligible; context facts themselves are never eligible. The kernel rejects a bundle that selects anything else or exceeds the declared bounds.</p>"},{"location":"reference/api/#meta_evolve.CommandEvaluator","title":"<code>CommandEvaluator</code>  <code>dataclass</code>","text":"<p>Materialize a source tree and trust a fixed 0/1/other exit protocol.</p>"},{"location":"reference/api/#meta_evolve.DurableCorruption","title":"<code>DurableCorruption</code>","text":"<p>               Bases: <code>DurableStoreError</code></p> <p>Durable data is internally inconsistent or references missing content.</p>"},{"location":"reference/api/#meta_evolve.DurableStoreError","title":"<code>DurableStoreError</code>","text":"<p>               Bases: <code>RunError</code></p> <p>Base class for durable-format and durable-store failures.</p>"},{"location":"reference/api/#meta_evolve.ExperienceAccess","title":"<code>ExperienceAccess</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>A run-level declaration that normalizes to one durable access spec.</p>"},{"location":"reference/api/#meta_evolve.ExperienceAccessDenied","title":"<code>ExperienceAccessDenied</code>","text":"<p>               Bases: <code>LookupError</code></p> <p>An opaque denial that discloses no protected content or topology.</p>"},{"location":"reference/api/#meta_evolve.ExperienceBudgetExhausted","title":"<code>ExperienceBudgetExhausted</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>A content-free signal that a grant's operation ceiling was exceeded.</p>"},{"location":"reference/api/#meta_evolve.ExperienceGraph","title":"<code>ExperienceGraph</code>  <code>dataclass</code>","text":"<p>Immutable graph projection; synthetic access sets are test-only seams.</p>"},{"location":"reference/api/#meta_evolve.ExperienceNode","title":"<code>ExperienceNode</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.ExperienceQuery","title":"<code>ExperienceQuery</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.ExperienceRecord","title":"<code>ExperienceRecord</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.ExperienceReader","title":"<code>ExperienceReader</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>The bounded read capability supplied only to successor proposers.</p>"},{"location":"reference/api/#meta_evolve.ExperienceState","title":"<code>ExperienceState</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.EvaluationResult","title":"<code>EvaluationResult</code>  <code>dataclass</code>","text":"<p>Normalized independent metrics or a typed evaluator failure.</p>"},{"location":"reference/api/#meta_evolve.EvaluationEnvironment","title":"<code>EvaluationEnvironment</code>  <code>dataclass</code>","text":"<p>Versioned identity for the environment in which a grader runs.</p>"},{"location":"reference/api/#meta_evolve.EvidenceView","title":"<code>EvidenceView</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.Experiment","title":"<code>Experiment</code>  <code>dataclass</code>","text":"<p>An immutable evaluated-search declaration.</p> <p>Equality is ordinary dataclass equality over these eight fields. In particular, no structural equality is promised for live proposer or registry objects.</p> <p>Parameters:</p> Name Type Description Default <code>task</code> <code>Task</code> <p>Evaluation contract, artifact type, objectives, and optional budget ceiling.</p> required <code>seed</code> <code>object</code> <p>Initial value accepted by the task's artifact codec.</p> required <code>proposer</code> <code>ProposerLike</code> <p>Callable, adapter, or registered proposer component.</p> required <code>search</code> <code>SearchProgram | None</code> <p>Search policy; <code>None</code> means <code>Greedy(max_trials=20)</code> at execution time.</p> <code>None</code> <code>context</code> <code>ContextPolicy | None</code> <p>Optional policy-pushed experience selector.</p> <code>None</code> <code>experience</code> <code>ExperienceAccess | None</code> <p>Optional bounded agent-pulled experience access.</p> <code>None</code> <code>random_seed</code> <code>int</code> <p>Integer seed for recorded deterministic search choices; defaults to <code>0</code>.</p> <code>0</code> <code>registry</code> <code>Registry | None</code> <p>Extra artifact codecs and registered durable components.</p> <code>None</code>"},{"location":"reference/api/#meta_evolve.EvidenceDraft","title":"<code>EvidenceDraft</code>  <code>dataclass</code>","text":"<p>Evidence content before the coordinator assigns durable identity.</p>"},{"location":"reference/api/#meta_evolve.Greedy","title":"<code>Greedy</code>  <code>dataclass</code>","text":"<p>Expand the best evaluated artifact for at most <code>max_trials</code> attempts.</p>"},{"location":"reference/api/#meta_evolve.Greedy.bind","title":"<code>bind(proposer, budget)</code>","text":"<p>Authorize one proposer and derive the unit trial allocation from the run budget.</p>"},{"location":"reference/api/#meta_evolve.InvalidDecision","title":"<code>InvalidDecision</code>","text":"<p>               Bases: <code>RunError</code></p> <p>A search decision is stale, malformed, or exceeds its authority.</p>"},{"location":"reference/api/#meta_evolve.Maximize","title":"<code>Maximize</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_Objective</code></p>"},{"location":"reference/api/#meta_evolve.Minimize","title":"<code>Minimize</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_Objective</code></p>"},{"location":"reference/api/#meta_evolve.NoSuccessfulEvaluation","title":"<code>NoSuccessfulEvaluation</code>","text":"<p>               Bases: <code>RunError</code></p> <p>A result query has no successful evaluation to return.</p>"},{"location":"reference/api/#meta_evolve.NoMemory","title":"<code>NoMemory</code>  <code>dataclass</code>","text":"<p>Explicit spelling of the default, normalized completely away by <code>run</code>.</p>"},{"location":"reference/api/#meta_evolve.Population","title":"<code>Population</code>  <code>dataclass</code>","text":"<p>Retain the top <code>size</code> candidates and expand tournament winners.</p>"},{"location":"reference/api/#meta_evolve.Population.bind","title":"<code>bind(proposer, budget)</code>","text":"<p>Authorize one proposer and derive the unit trial allocation.</p>"},{"location":"reference/api/#meta_evolve.ProcessSpec","title":"<code>ProcessSpec</code>  <code>dataclass</code>","text":"<p>A value-valid process request whose authority is decided by a provider.</p>"},{"location":"reference/api/#meta_evolve.ProposalResult","title":"<code>ProposalResult</code>  <code>dataclass</code>","text":"<p>A normalized candidate or typed proposer failure.</p> <p><code>candidate</code> is a value in the task's artifact type, not a durable payload: the registered codec is the one authority for that translation, and it runs where the task's kind and representation are known.</p>"},{"location":"reference/api/#meta_evolve.ProposerContext","title":"<code>ProposerContext</code>  <code>dataclass</code>","text":"<p>The exact candidate-facing state authorized for one proposal.</p>"},{"location":"reference/api/#meta_evolve.PatchProposer","title":"<code>PatchProposer</code>","text":"<p>Apply a deterministic patch command through a private workspace.</p>"},{"location":"reference/api/#meta_evolve.PatchResult","title":"<code>PatchResult</code>  <code>dataclass</code>","text":"<p>A patch command or typed proposer failure with proposal observations.</p>"},{"location":"reference/api/#meta_evolve.PullAccess","title":"<code>PullAccess</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.Registry","title":"<code>Registry</code>","text":"<p>(role, name, version)-keyed components and policy factories.</p>"},{"location":"reference/api/#meta_evolve.Registry.register_context_policy","title":"<code>register_context_policy(name, version, factory, *, deterministic, retry_safe, dependencies=())</code>","text":"<p>Register a module-level factory that rebuilds a context policy.</p>"},{"location":"reference/api/#meta_evolve.Registry.resolve_context_policy","title":"<code>resolve_context_policy(spec)</code>","text":"<p>Rebuild a context policy and confirm its declaration round-trips.</p>"},{"location":"reference/api/#meta_evolve.Registry.register_artifact_type","title":"<code>register_artifact_type(codec)</code>","text":"<p>Register one artifact codec keyed by (kind, representation).</p> <p>Built-in keys are non-overridable \u2014 every codec offered for one is rejected, the built-in object included: a new decode under an old key would change the meaning of every artifact already committed under it, so different behavior needs a new key.</p>"},{"location":"reference/api/#meta_evolve.Registry.resolve_policy","title":"<code>resolve_policy(spec, *, budget)</code>","text":"<p>Rebuild a bound program from <code>spec</code> and confirm it round-trips.</p> <p>Constructs the unbound preset from the registered factory and <code>spec.configuration</code>, binds it against <code>proposer</code>/<code>budget</code>, and asserts the rebuilt <code>SearchSpec</code> equals <code>spec</code> exactly \u2014 the same check M4.4's <code>resume</code> relies on (there, <code>budget</code> is the recorded <code>RunStarted.run_budget</code>).</p>"},{"location":"reference/api/#meta_evolve.Run","title":"<code>Run</code>","text":"<p>Read-only queries over one committed local run.</p>"},{"location":"reference/api/#meta_evolve.Run.inspect","title":"<code>inspect()</code>","text":"<p>Return one immutable aggregate built from one validated replay.</p>"},{"location":"reference/api/#meta_evolve.Run.trials","title":"<code>trials()</code>","text":"<p>Every committed attempt in commit order, seed trial first.</p> <p>The winner-shaped queries answer \"what came out of this run\"; this one answers \"what happened in it\", so it reports losers, failed proposals, and trials the budget never authorised for evaluation, and it is the one query that never raises <code>NoSuccessfulEvaluation</code>. Read <code>TrialView.state</code> to tell those apart.</p>"},{"location":"reference/api/#meta_evolve.RunComparison","title":"<code>RunComparison</code>  <code>dataclass</code>","text":"<p>The exact-score result for two compatible run summaries.</p>"},{"location":"reference/api/#meta_evolve.RunDeclaration","title":"<code>RunDeclaration</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.RunExperienceUsage","title":"<code>RunExperienceUsage</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.RunError","title":"<code>RunError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Base class for application-level run failures.</p>"},{"location":"reference/api/#meta_evolve.RunNotFound","title":"<code>RunNotFound</code>","text":"<p>               Bases: <code>RunError</code></p> <p>No durable run exists under the requested id (an empty event stream).</p>"},{"location":"reference/api/#meta_evolve.RunInspection","title":"<code>RunInspection</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.RunOverview","title":"<code>RunOverview</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.RunSummary","title":"<code>RunSummary</code>  <code>dataclass</code>","text":"<p>An immutable, storage-independent view of one successful run.</p>"},{"location":"reference/api/#meta_evolve.RunsNotComparable","title":"<code>RunsNotComparable</code>","text":"<p>               Bases: <code>RunError</code></p> <p>Two runs disagree on a declaration their comparison is defined over.</p> <p><code>compare_runs</code> ranks two runs only when they answer the same question: the same task, objective, budgets, initial artifact, and authorized components. <code>declaration</code> names the one that differed, so a caller can branch on it without parsing the message.</p>"},{"location":"reference/api/#meta_evolve.SchemaVersionRejected","title":"<code>SchemaVersionRejected</code>","text":"<p>               Bases: <code>DurableStoreError</code></p> <p>A durable schema version is unknown, future, or unmigratable.</p>"},{"location":"reference/api/#meta_evolve.Session","title":"<code>Session</code>","text":"<p>One synchronous handle for paced, uninterrupted, or resumed execution.</p>"},{"location":"reference/api/#meta_evolve.Session.run","title":"<code>run</code>  <code>property</code>","text":"<p>The stable inspectable Run associated with this handle.</p>"},{"location":"reference/api/#meta_evolve.Session.stopped","title":"<code>stopped</code>  <code>property</code>","text":"<p>Whether a terminal stop decision has been committed.</p>"},{"location":"reference/api/#meta_evolve.Session.start","title":"<code>start(experiment, /, *, storage=None, budget=None, mode='local')</code>  <code>classmethod</code>","text":"<p>Validate and bootstrap an experiment before returning its handle.</p>"},{"location":"reference/api/#meta_evolve.Session.resume","title":"<code>resume(run_id, /, *, storage, registry)</code>  <code>classmethod</code>","text":"<p>Reconstruct a durable frontier without completing pending work.</p>"},{"location":"reference/api/#meta_evolve.Session.step","title":"<code>step()</code>","text":"<p>Complete exactly one owed or newly committed policy decision.</p>"},{"location":"reference/api/#meta_evolve.SkillSet","title":"<code>SkillSet</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Mapping[str, str]</code></p> <p>An immutable mapping from <code>skills/&lt;name&gt;.md</code> to exact UTF-8 text.</p>"},{"location":"reference/api/#meta_evolve.Storage","title":"<code>Storage</code>  <code>dataclass</code>","text":"<p>Paired artifact and event storage, plus one all-or-none <code>commit</code>.</p> <p><code>is_durable</code> and <code>is_read_only</code> are independent capabilities. Durability says committed facts live on durable media; read-only says this handle has no commit authority. A writable durable handle therefore reports <code>True, False</code> while :meth:<code>read_only</code> reports <code>True, True</code>.</p>"},{"location":"reference/api/#meta_evolve.Storage.durable","title":"<code>durable(path)</code>  <code>classmethod</code>","text":"<p>Wire durable storage into <code>path</code> (created if missing): one SQLite connection (events + artifact occurrence rows) shared with a content-addressed filesystem blob store under <code>path/artifacts</code>.</p>"},{"location":"reference/api/#meta_evolve.Storage.read_only","title":"<code>read_only(path)</code>  <code>classmethod</code>","text":"<p>Open an existing durable directory with inspection authority only.</p> <p>The directory and current schema must already exist. This path never creates, stamps, migrates, or otherwise repairs durable state.</p>"},{"location":"reference/api/#meta_evolve.Storage.compose","title":"<code>compose(artifacts, events, *, commit_strategy, close=None)</code>  <code>classmethod</code>","text":"<p>Compose independently implemented stores behind one atomic commit.</p> <p>The strategy declares whether its commit survives process restart; callers cannot separately forge <code>is_durable</code>. Composed storage is always writable: <code>Storage</code> cannot verify that third-party stores genuinely refuse writes, so read-only inspection handles come only from :meth:<code>read_only</code>, which pairs the declaration with stores built to fail closed.</p>"},{"location":"reference/api/#meta_evolve.Storage.commit","title":"<code>commit(run_id, expected_version, *, events, occurrences)</code>","text":"<p>Commit <code>events</code> and <code>occurrences</code> as one all-or-none unit.</p> <p><code>EventStreamConflict</code> on a stale <code>expected_version</code> propagates unchanged, and nothing is written. See <code>unit_of_work.py</code> for the per-backend guarantee (durable: one SQLite transaction; in-memory: a locked copy-on-write swap; hand-built pair: local-only fallback).</p>"},{"location":"reference/api/#meta_evolve.Storage.runs","title":"<code>runs()</code>","text":"<p>Every run this storage holds events for, sorted by id.</p> <p>Identities, not <code>Run</code> objects: opening a run validates and replays its whole stream, and detail is already <code>Run.summary()</code>'s job. A listing exists so those queries stay reachable once the ids have scrolled out of a terminal, so it returns exactly what reopening one needs. The order is lexicographic by id and identical across backends; run ids are content-derived, so it is not chronological.</p> <p>Enumeration is an optional store capability (<code>ports.RunIndex</code>), declared by implementing it. A paired event store that does not is refused explicitly here \u2014 never reported as an empty store, which would be a silent capability downgrade.</p>"},{"location":"reference/api/#meta_evolve.Storage.run_version","title":"<code>run_version(run_id)</code>","text":"<p>Maximum committed sequence, or <code>0</code> for an unseen run id.</p> <p>This optional high-water-mark query is intended for polling. Opening a required run remains authoritative and raises <code>RunNotFound</code> when no events exist.</p>"},{"location":"reference/api/#meta_evolve.StopDecisionView","title":"<code>StopDecisionView</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.SourceTree","title":"<code>SourceTree</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Mapping[str, str]</code></p> <p>An immutable mapping from portable paths to exact UTF-8 text.</p>"},{"location":"reference/api/#meta_evolve.Task","title":"<code>Task</code>  <code>dataclass</code>","text":"<p>The evaluator, objectives, budget, and accepted artifact declaration.</p>"},{"location":"reference/api/#meta_evolve.Text","title":"<code>Text</code>","text":"<p>               Bases: <code>str</code></p> <p>An immutable text artifact value; a plain <code>str</code> in every other way.</p>"},{"location":"reference/api/#meta_evolve.TrialView","title":"<code>TrialView</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.TrialDecisionView","title":"<code>TrialDecisionView</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.TextPatch","title":"<code>TextPatch</code>  <code>dataclass</code>","text":"<p>Replace one complete file only when its expected text still matches.</p>"},{"location":"reference/api/#meta_evolve.UnboundSearchProgram","title":"<code>UnboundSearchProgram</code>","text":"<p>               Bases: <code>RunError</code></p> <p>A public search preset was used without run-level binding.</p>"},{"location":"reference/api/#meta_evolve.Usage","title":"<code>Usage</code>  <code>dataclass</code>","text":""},{"location":"reference/api/#meta_evolve.present_artifact","title":"<code>present_artifact(artifact)</code>","text":"<p>Return a bounded inert presentation of one frozen artifact payload.</p>"},{"location":"reference/api/#meta_evolve.diff_artifacts","title":"<code>diff_artifacts(parent, child)</code>","text":"<p>Return a bounded direct-parent diff for a supported artifact family.</p>"},{"location":"reference/api/#meta_evolve.artifact_payload_bytes","title":"<code>artifact_payload_bytes(artifact)</code>","text":"<p>Return exact <code>canonical-payload-v1</code> bytes used for content digesting.</p>"},{"location":"reference/api/#meta_evolve.improve","title":"<code>improve(*, seed, proposer, evaluator, trials=20, storage=None, random_seed=0)</code>","text":"<p>Run a bounded greedy improvement loop with a callable evaluator.</p> <p>This is declaration sugar for :class:<code>~meta_evolve.Task</code>, :class:<code>~meta_evolve.Experiment</code>, :class:<code>~meta_evolve.Greedy</code>, and :func:<code>~meta_evolve.run</code>. Use that expanded path when you need custom artifacts, objectives, budgets, search, context, experience, execution mode, or registry components.</p> <p>Parameters:</p> Name Type Description Default <code>seed</code> <p>Initial artifact value.</p> required <code>proposer</code> <p>Callable or local proposer adapter.</p> required <code>evaluator</code> <p>Callable that independently measures an artifact.</p> required <code>trials</code> <code>int</code> <p>Maximum number of successor proposals; defaults to <code>20</code>.</p> <code>20</code> <code>storage</code> <p>Artifact and event storage; defaults to isolated memory.</p> <code>None</code> <code>random_seed</code> <code>int</code> <p>Integer seed for recorded deterministic choices.</p> <code>0</code> <p>Returns:</p> Type Description <code>Run</code> <p>The same inspectable :class:<code>~meta_evolve.Run</code> as :func:<code>~meta_evolve.run</code>.</p>"},{"location":"reference/api/#meta_evolve.search","title":"<code>search(*, max_trials, **configuration)</code>","text":"<p>Decorate one bounded incumbent-refinement decision function.</p> <p>The callback receives opaque <code>seed</code> and optional <code>incumbent</code> handles, immutable <code>configuration</code>, and only <code>expand(parent)</code> / <code>stop(reason)</code> actions. Declare every behavior-affecting value on this decorator; closure capture is rejected so the complete configuration remains identity-visible.</p>"},{"location":"reference/api/#meta_evolve.resume","title":"<code>resume(run_id, *, storage, registry)</code>","text":"<p>Exhaust one validated durable session and return its stable Run.</p>"},{"location":"reference/api/#meta_evolve.run","title":"<code>run(experiment=_UNSET, /, *, task=_UNSET, seed=_UNSET, proposer=_UNSET, search=_UNSET, context=_UNSET, experience=_UNSET, storage=None, budget=None, random_seed=_UNSET, mode='local', registry=_UNSET)</code>","text":"<pre><code>run(experiment: Experiment, /, *, storage: Storage | None = None, budget: Budget | None = None, mode: ExecutionMode = 'local') -&gt; Run\n</code></pre><pre><code>run(*, task: Task, seed: object, proposer: ProposerLike, search: SearchProgram | None = None, context: ContextPolicy | None = None, experience: ExperienceAccess | None = None, storage: Storage | None = None, budget: Budget | None = None, random_seed: int = 0, mode: ExecutionMode = 'local', registry: Registry | None = None) -&gt; Run\n</code></pre> <p>Run one evaluated improvement search and return its inspectable history.</p> <p>Call either <code>run(experiment, ...)</code> with one positional-only :class:<code>~meta_evolve.Experiment</code>, or use its declaration fields as keywords. The keyword form retains the effective defaults <code>search=None</code>, <code>context=None</code>, <code>experience=None</code>, <code>random_seed=0</code>, and <code>registry=None</code>. The default search therefore remains <code>Greedy(max_trials=20)</code>. Both forms prepare and exhaust the same :class:<code>~meta_evolve.Session</code> used for paced and resumed execution.</p> <p>Parameters:</p> Name Type Description Default <code>experiment</code> <code>Experiment | object</code> <p>Typed declaration. It cannot be combined with declaration keywords.</p> <code>_UNSET</code> <code>task</code> <code>Task | object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <code>seed</code> <code>object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <code>proposer</code> <code>ProposerLike | object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <code>search</code> <code>SearchProgram | None | object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <code>context</code> <code>ContextPolicy | None | object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <code>experience</code> <code>ExperienceAccess | None | object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <code>storage</code> <code>Storage | None</code> <p>Artifact and event storage; defaults to isolated memory storage.</p> <code>None</code> <code>budget</code> <code>Budget | None</code> <p>Run budget, which may narrow but never widen the task budget.</p> <code>None</code> <code>random_seed</code> <code>int | object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <code>mode</code> <code>ExecutionMode</code> <p><code>\"local\"</code> or <code>\"durable-local\"</code> execution.</p> <code>'local'</code> <code>registry</code> <code>Registry | None | object</code> <p>See :class:<code>~meta_evolve.Experiment</code>.</p> <code>_UNSET</code> <p>Returns:</p> Type Description <code>Run</code> <p>A :class:<code>Run</code> for result, trial, lineage, usage, and comparison queries.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If a declared public input has the wrong type.</p> <code>ValueError</code> <p>If declarations conflict or the task budget is widened.</p> <code>CapabilityError</code> <p>If durable-local requirements cannot be satisfied.</p> <p>Invalid proposals and ordinary proposer or evaluator failures are recorded as typed trial outcomes; they are not converted to exceptions or low scores.</p>"},{"location":"reference/api/#meta_evolve.load_source_tree","title":"<code>load_source_tree(path)</code>","text":"<p>Load a directory without following links or normalizing file content.</p>"},{"location":"reference/api/#meta_evolve.compare_harnesses","title":"<code>compare_harnesses(baseline_run, candidate_run)</code>","text":"<p>Compare two compatible held-out seed evaluations under equal controls.</p>"},{"location":"reference/api/#meta_evolve.compare_policies","title":"<code>compare_policies(baseline_run, candidate_run)</code>","text":"<p>Compare held-out policy evidence under exactly equal external controls.</p>"},{"location":"reference/api/#meta_evolve.export_harness","title":"<code>export_harness(artifact, destination, overwrite=False)</code>","text":"<p>Export one selected harness view with validation and atomic placement.</p>"},{"location":"reference/api/#meta_evolve.load_harness","title":"<code>load_harness(destination)</code>","text":"<p>Load and strictly validate a prior Meta-Evolve harness export.</p>"},{"location":"reference/api/#harness-namespace","title":"Harness namespace","text":"<p>The constructible M8 family is namespaced rather than added to the ordinary root path.</p> <p>Trusted harness artifacts, mutation surfaces, episodes, and egress views.</p>"},{"location":"reference/api/#meta_evolve.harness.HarnessArtifact","title":"<code>HarnessArtifact</code>  <code>dataclass</code>","text":"<p>Schema-v1 harness manifest with exactly two mutable parameters.</p>"},{"location":"reference/api/#meta_evolve.harness.HarnessArtifact.apply","title":"<code>apply(mutation)</code>","text":"<p>Construct an intended successor through a declared surface.</p> <p>This is wrapper convenience. The artifact codec independently repeats the authority check inside proposal completion before commit.</p>"},{"location":"reference/api/#meta_evolve.harness.HarnessMutation","title":"<code>HarnessMutation</code>  <code>dataclass</code>","text":"<p>One structured request to change a manifest-declared surface.</p>"},{"location":"reference/api/#meta_evolve.harness.MutationSurface","title":"<code>MutationSurface</code>","text":"<p>               Bases: <code>Protocol[MutationT, SurfaceT]</code></p> <p>A declared value that can construct one intended local mutation.</p>"},{"location":"reference/api/#meta_evolve.harness.TextParam","title":"<code>TextParam</code>  <code>dataclass</code>","text":"<p>A named mutable text value with immutable component authority.</p>"},{"location":"reference/api/#meta_evolve.harness.PolicyParam","title":"<code>PolicyParam</code>  <code>dataclass</code>","text":"<p>A named mutable policy configuration with immutable authority.</p>"},{"location":"reference/api/#meta_evolve.harness.MutationApplication","title":"<code>MutationApplication</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[SurfaceT]</code></p> <p>Exactly one changed surface or typed failure from wrapper validation.</p>"},{"location":"reference/api/#meta_evolve.harness.RepositoryEpisode","title":"<code>RepositoryEpisode</code>  <code>dataclass</code>","text":"<p>Public task seed plus evaluator-only tests and bounded execution.</p>"},{"location":"reference/api/#meta_evolve.harness.EpisodeSuite","title":"<code>EpisodeSuite</code>  <code>dataclass</code>","text":"<p>Explicit, non-overlapping development and held-out episode splits.</p>"},{"location":"reference/api/#mutation-and-policy-namespaces","title":"Mutation and policy namespaces","text":"<p>The neutral mutation result/protocol and constructible M9 policy family stay namespaced. Only strict comparison is a root convenience.</p> <p>Neutral contracts for typed, authority-bounded mutation surfaces.</p> <p>Trusted policy artifacts, executable interpretation, and evaluation views.</p>"},{"location":"reference/api/#meta_evolve.mutation.MutationApplication","title":"<code>MutationApplication</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[SurfaceT]</code></p> <p>Exactly one changed surface or typed failure from wrapper validation.</p>"},{"location":"reference/api/#meta_evolve.mutation.MutationSurface","title":"<code>MutationSurface</code>","text":"<p>               Bases: <code>Protocol[MutationT, SurfaceT]</code></p> <p>A declared value that can construct one intended local mutation.</p>"},{"location":"reference/api/#meta_evolve.policy.PolicyArtifact","title":"<code>PolicyArtifact</code>  <code>dataclass</code>","text":"<p>Schema-v1 data interpreted only by the trusted candidate policy.</p>"},{"location":"reference/api/#meta_evolve.policy.PolicyMutation","title":"<code>PolicyMutation</code>  <code>dataclass</code>","text":"<p>One request to change exactly one policy-artifact surface.</p>"},{"location":"reference/api/#meta_evolve.policy.ParentSelection","title":"<code>ParentSelection</code>  <code>dataclass</code>","text":"<p>Whether each trial expands the original seed or current incumbent.</p>"},{"location":"reference/api/#meta_evolve.policy.TrialLimit","title":"<code>TrialLimit</code>  <code>dataclass</code>","text":"<p>A voluntary stopping limit, always subordinate to the run budget.</p>"},{"location":"reference/api/#meta_evolve.policy.CandidatePolicy","title":"<code>CandidatePolicy</code>  <code>dataclass</code>","text":"<p>Built-in search preset configured by a trusted policy manifest.</p>"},{"location":"reference/api/#meta_evolve.policy.PolicyEpisode","title":"<code>PolicyEpisode</code>  <code>dataclass</code>","text":"<p>One independently runnable inner search declaration.</p>"},{"location":"reference/api/#meta_evolve.policy.PolicyEpisode.effective_task","title":"<code>effective_task</code>  <code>property</code>","text":"<p>Task identity with evaluator-owned environment folded into it.</p>"},{"location":"reference/api/#meta_evolve.policy.PolicySuite","title":"<code>PolicySuite</code>  <code>dataclass</code>","text":"<p>Non-empty, explicitly separated development and held-out episodes.</p>"},{"location":"reference/api/#meta_evolve.policy.EpisodeOutcome","title":"<code>EpisodeOutcome</code>  <code>dataclass</code>","text":"<p>One isolated inner run and its evaluator-owned cache disposition.</p>"},{"location":"reference/api/#meta_evolve.policy.PolicyEvaluation","title":"<code>PolicyEvaluation</code>  <code>dataclass</code>","text":"<p>Every episode outcome plus deterministic aggregate evidence.</p>"},{"location":"reference/api/#meta_evolve.policy.PolicyComparison","title":"<code>PolicyComparison</code>  <code>dataclass</code>","text":"<p>Evidence of eligibility; never promotion or deployment authority.</p>"},{"location":"reference/experience-provenance/","title":"Advanced experience provenance","text":"<p>Status: Shipped. This reference follows the outcome-first experience lesson.</p> <p>Pushed and pulled observations enter one ordered ledger. Each entry records the run, occurrence kind and ID, logical step, event sequence, operation, result, and exact usage. A proposal's optional <code>derived_from</code> tuple must be an ordered subsequence of references that this proposer actually observed.</p> <p>Validation rejects a reference that is missing, duplicated contrary to its ledger position, out of scope, from the future, content-inconsistent, or ordered differently from observation. Rejection is typed invalid output and creates no successor artifact.</p> <p>Committed context selections and pull audits are facts. Replay rebuilds the experience projection and validates derivation without calling the selector, reader, renderer, proposer, or provider. Logical time\u2014not wall-clock completion\u2014 decides what was prior.</p> <p>This strict layer preserves causal claims while leaving everyday proposals free to declare no derivation. Experience remains bounded, explicit, and unable to change evaluation, budgets, permissions, or promotion.</p>"}]}